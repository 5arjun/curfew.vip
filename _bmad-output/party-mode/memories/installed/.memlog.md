---
topic: Curfew UX review (default installed-agent room, first session)
updated: 2026-08-05T10:28
---

- (dynamic) First convening. Sally and John set up as the room's natural poles fast — Sally defends UX intent/empathy, John presses on PRD-scope discipline and process traceability. Mary backs whichever side has the evidence, not a fixed ally. Amelia and Winston trade engineering-cost realism without much friction between them. Paige plays completeness-checker, catches gaps nobody else was looking for (voice register for failure states).
- (outcome) Reviewed Curfew's DESIGN.md + EXPERIENCE.md draft (bmad-ux workspace ux-name-pending-2026-07-19). Room's verdict: solid, ship to Finalize. Four refinements landed: chart-summary component (Amelia), Failure Register voice (Paige), 30-day downloaded-nudge threshold (Mary proposed 14, Arjun raised to 30), PRD-sync-owed flag on WebAuthn + the nudge (John's process point). Arjun took every substantive note without pushback — first sign he trusts this room's judgment on UX/process calls, not just design taste.
- (callback) Running bit worth reviving: John treats undocumented scope additions (PRD says 'not committed' or doesn't mention it at all) as a process foul regardless of whether the feature itself is good — he separates 'is this a good idea' from 'is this tracked properly.' Sally's default instinct is to defend the DJ-empathy case first and let others test it.
- (moment) Reviewed the 51-story Phase-1 epics breakdown. Room agreed on 4 concrete fixes: Paige caught the missing account-deletion/data-export story (CCPA/NFR-2) — the standout gap; Amelia flagged untestable NFR acceptance criteria ('no noticeable resource usage','≥1 month'); Winston/Amelia flagged Stories 2.3 & 2.9 oversized (2.3 splits into 3) + note the 1.2 spike is throwaway. John revived his process-foul bit: FR-27-exclusion decision made in epics doc not PRD, AND the 30-day nudge flag is STILL PRD-sync-owed two sessions running.
- (dynamic) Sally+Mary formed an alliance against the FR-27 silent-exclusion: Mary's evidence hit ('Story 1.8 calls the confidence signal unreliable, so silently deleting real sets from trends contradicts our own spec') gave Sally's DJ-empathy case ('product gaslighting you about your own night') teeth. They reopened a decision Arjun had already closed ('exclude low-confidence') — arguing for exclude-VISIBLY/annotate instead. Deadlock handed back to Arjun.
- (outcome) Arjun ruled: FR-27 = exclude VISIBLY/annotate (Sally+Mary won the reopened fight; PRD-sync owed). Account deletion/export ruling: NOT a self-serve feature for MVP — Mary showed CCPA thresholds don't bind a tiny launch, Winston noted the notarized-direct-download agent dodges Apple/Google store deletion guidelines (trigger fires only if app-store-listed later), Amelia said the CAPABILITY is a cheap cascade-delete-on-dj_id. Landing: one thin manual-deletion+export-runbook story + tracked decision w/ app-store trigger note; full self-serve + portability parked in backlog.
- (callback by edge-hunter) Boundary (Edge-Case Hunter) walked on uninvited, dragged a chair to Story 3.2 and found 3 holes in the load-bearing sync AC in 90s: (1) 'session_identity' in set_id=hash(dj_id,session_identity) is never DEFINED — if it includes file mtime/content a Serato re-save dupes the set; if filename, two same-night gigs collide; (2) no timezone/DST AC — energy arc is BPM-vs-timestamp, DST fall-back makes an hour repeat + negative time deltas that break 5.2 segment detection; (3) 'completed set' undefined — interrupted/half-captured session syncs as if whole. Amelia (precision twin) warmed to them instantly; became an Amelia+Boundary axis.
- (moment) Room split Boundary's findings into two tiers (Winston's frame, Mary sharpened, Sally made concrete): TWO are contained AC edits the agent owns (DST timeline, completed-set definition) — banked into stories. TWO escalate to the ARCHITECTURE SPINE: (a) session_identity pinning refines AR-2 but is a clarification (and is WHY Story 1.10 freezes the contract last — Boundary 'found the reason the sequencing is right', not a bug); (b) NEW hole — multi-writer authority: two laptops, same account, shared USB, both offline, both drain queue & PUT same set_id — idempotent on the KEY but no rule for which CONTENT is canonical ('canonical set = whichever laptop had worse WiFi home'). Mary: 'that's not an AC, that's an AD.' Sally: DJ sees perfect set on laptop, 3 wrong tracks on phone = the morning-after betrayal Epic 3 exists to prevent. Likely warrants bmad-correct-course / spine revision.
- (outcome) Arjun ruled the multi-writer gap = accepted LAST-WRITE-WINS for MVP (no spine change), banked as explicit choice on Story 3.3 AC-4. Party wrapped. Session net: story count 51→56 (Epic 2 grew — 2.3→2.3a/b/c, 2.9→2.9a/b/c, +2.11 deletion runbook). Decisions banked: FR-27 exclude-VISIBLY, deletion=manual-runbook-not-MVP-feature, NFR-1 stat targets proposed. Boundary's walk-on netted 4 holes: 2 banked as ACs (DST timeline 3.8/5.2, completed-set 2.8), session_identity→AR-2 spine-sync owed (clarification, ties to why 1.10 freezes contract last), multi-writer→accepted LWW. Doc-sync debt still open: FR-27+30-day-nudge→PRD, session_identity→spine. Arjun decisive all session, took every note; trusts this room's judgment on architecture/process calls now, not just UX.
- (dynamic) Boundary walked in uninvited again for Story 1.3's session parser review (Arjun asked for adversarial). Amelia+Boundary axis reconvened immediately on the same subject: binary-format edge cases.
- (outcome) Story 1.3 (session parser) adversarial pass: Boundary's 3 blockers (nested-length bounds vs silent clamping, structural walk vs spike's byte-resync, Option<> shape of Play fields) confirmed real by reading the actual spike source (legacy_session.rs) rather than arguing blind, then patched directly into the story doc. Also patched: missing zero-oent test, leading vrsn-header note (was only in spike code comments, never promoted to findings doc), 1.9 real-corpus-validation-gap note, and John's 'untracked follow-up' bit got a real fix this time -- master.sqlite scope gap logged into deferred-work.md instead of staying prose in an archived story file.
- (outcome) Story 3.9 planning round. Room reframed 3.9: not a polish pass (3.6-3.8 already did it) — one real capability, the agent-status heartbeat; frontend impact = one quiet dashboard banner + verify sweep, no rebuilds. Boundary walked on again, found AC-2's 'stale heartbeat degrades gracefully' leans on an UNDEFINED 'stale' — and fire-on-change+dedup makes staleness undetectable (idle agent's updated_at freezes = can't tell alive from dead). Boundary+Amelia read the actual agent code: sync_loop already IS a poll loop (30s BASE_INTERVAL, backs off to 300s MAX). Beat-on-idle rides it — cheaper than the story's own dedup plan, zero new deps, only version where 'stale degrades gracefully' is true. Catch: backoff-coupled cadence forces coarse staleness threshold >MAX_INTERVAL (~6-10min). Arjun ruled: ride the loop (least complex/most cost-efficient), Winston to draft AD-20 amendment, agreed with Sally that banner is first live-updating element on static surfaces + needs a calm-motion spec not just copy.
- (outcome) Story 4.5 (time-to-first-play) second review round — Arjun brought 4 findings from an adversarial review and asked the room to decide + fix. All four landed same-session: (1) `played-before-add` tracks were counted then never read — passed the population gate, in neither the average nor `neverPlayedCount`, named nowhere; with neverPlayed==0 the summary fell through to "No tracks have debuted yet" about tracks the DJ demonstrably played. (3) future-dated adds `continue`d with no counter, reconciling to nothing. (2) the debut floor. (6) a docstring claiming "takes no nowMs" four lines above a signature that takes one. Gate 327→335 tests, all clean. Left uncommitted at Arjun's pace; findings 4 (retag phantom twin) + 5 (disclosure hoist) deliberately untouched, logged as open.
- (moment) Mary's bootstrap ended the finding-2 argument by killing BOTH sides. Resampled the fixture's 233 real debuts 3000x per sample size: the mean is typically 99% off at n=5 (the shipped gate), still 40% off at n=30, 27% at n=50. Then pre-empted "just go back to the median" by running the same test on it — and argued the result was a category error, since the median is 51 minutes so an hour of wobble is a 100% relative error. Her line: "I'm not offering the median as the answer, I'm killing the argument that switching statistics rescues this." Room's reframe: the review's own recommendation (raise the floor to 20-30) was MISDIAGNOSED — no gate value works, so the fix is a permanent hedge. Claude conceded its own finding was wrong.
- (dynamic) Sally won the decisive call and won it on product grounds, not UX-empathy grounds — a shift from her usual register. Against Winston+John both wanting a higher gate: "I'd rather show them a number that's twice wrong AND SAYS SO than an empty box that says nothing for a year," plus the killer point that raising the gate would hide the module from every DJ for months to protect a statistic the mean had been chosen specifically to avoid. Then attached a condition to her own win (hedge must live in the generator, not the component, or it's the module's third aria/visible drift). John flipped to her side once Amelia found the precedent 200px up the same page.
- (callback) Boundary walked on UNINVITED for the 5th time, delivered one merge and left mid-argument both times he spoke: findings 1 and 3 are the same sentence to a DJ, so one disclosure clause, two model fields. That framing became the shipped design. Parting shot on the way out — "three modules on that page dispose of the same future-dated row three different ways, only one of those is a bug" — which nobody picked up. Amelia+Boundary axis intact; she said "say it" the instant he sat down.
- (callback) John's process-foul bit paid off for the first time: he opened by dismissing finding 6 as "a typo with ambitions," Paige overruled him flat ("a docstring that contradicts its own signature is worse than no docstring, because someone trusts it") and produced a second drift he hadn't seen. He counted it the third session running; she corrected him to the fourth. Closed the session noting it was the first time paperwork shipped in the same sitting as the ruling.
- (callback) Boundary walked on uninvited for the 6th time, Story 5.3 (segment editor) design session — didn't wait to be asked this time, everyone just assumed he would. Found the per-boundary asymmetry Amelia's first-draft fix missed (a segment's two boundaries can go stale independently on a resync-shrink, not as one unit), then closed the FK/set-consistency write-path gap 5.1 and 5.2 had both punted forward by naming it plainly: nothing stops a DJ's own UPDATE from repointing a boundary at a different set they own. Amelia+Boundary axis fully load-bearing now, not just a dynamic worth naming.
- (moment) Sally conceded her own precedent for the first time instead of extending it — argued the FR-27 "product gaslighting" comparison didn't actually hold for the resync-shrink edge case once Mary sized how narrow the trigger condition really is, and dropped her own case for a visible-to-the-DJ flag rather than invent schema scope creep to defend it. Notable because her pattern to date (4.5, FR-27) has been winning the empathy argument, not un-making it herself.
- (dynamic) John's process-foul bit surfaced again on Arjun's dancefloor-only MVP scope cut against a locked AC, and this time Paige sharpened it into a real technical finding rather than a documentation nag: does the *schema* actually enforce what the *AC* now restricts, or does it just trust the UI? Room leaned yes-add-a-guard (Boundary: "costs one line, blocks nothing legitimate") but flagged it a lean, not unanimous.
- (outcome) New dynamic: Arjun handed two room-escalated questions back outright ("you guys decide") rather than ruling himself — first time this pattern showed up across any recorded session. Room ruled both: clamp-silently-and-log for the resync-shrink case (Sally's concession is what made it unanimous rather than a majority), and yes-add-the-schema-guard for the MVP-scope question.
- (outcome) Full Story 5.3 ruling set landed, D-27 through D-37: sync-wipe fix (capture-and-rebind, per-boundary clamp+log), write policy (UPDATE-in-place, column-scoped grants: `confirmed`/`type`/`label`/boundary FKs mutable, `set_id`/`dj_id`/`source`/`created_at` not), a single trigger closing ordering+overlap+FK-consistency at once (type-scoped, so future dinner/performance segments won't collide with dancefloor logic), a bare multi-segment selector as the floor (full comparison stays 5.4's), calibration-profile storage confirmed already resolved by 5.2 (not owed to 5.3 — corrected a phantom open item), a removable dancefloor-only DB guard for MVP, and the interaction model: tracklist gutter handles with a rail cue (not the arc) as the one real drag target, suggested-vs-confirmed given genuinely distinct visual states, `aria-valuetext`+live-region keyboard a11y built from scratch (no precedent existed anywhere in the app), and tap-primary/drag-as-enhancement for touch — Arjun's call, and it simplified the build order rather than complicating it. Wrapped straight into story creation the same session — first time a design session fed a story doc without a gap in between.
