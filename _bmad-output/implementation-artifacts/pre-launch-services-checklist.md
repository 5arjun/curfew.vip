# Pre-launch / production-services checklist

**As of:** 2026-08-16 (full re-verification pass — every open row below was checked against the live system, not carried forward on trust; six rows were stale and are corrected inline)
**Owner:** Arjun (all items below are account-level/manual actions, not coding-agent tasks)
**Purpose:** single place tracking every external account/service Curfew needs between "runs entirely against the local Supabase stack" and "real production launch." Consolidates supabase/PROVISIONING.md, PRD §11, Architecture Spine Open Questions, and epics.md cross-cutting notes — none of which individually owned this list.

Update this file directly when an item's status changes; it isn't generated by a workflow.

---

## 1. No code blocker — do whenever convenient

| Item | Status | Notes |
| --- | --- | --- |
| **Vercel deploy** | **Resolved 2026-07-28 — Story 2.4, Task 3.1/3.2. Custom domain since resolved too: `https://curfew.vip` is the live production origin as of 2026-08-14** (verified 2026-08-16: `/`, `/login`, `/privacy`, `/terms` all 200). | ⚠️ **The old `https://name-pending-gamma.vercel.app` origin now returns 404** — it is dead, not merely superseded. Anything in this doc or in configuration still naming that host is stale by definition; two such entries were found and are noted in the redirect-allow-list row in §4. Original note, kept for history: real production deploy first went live at that `*.vercel.app` origin (bare, no custom domain at the time — see the domain row below). Vercel project `5arjuns-projects/name-pending`, `rootDirectory` set to `web` (monorepo — set via a direct Management API call, not exposed by `vercel project update`'s CLI flags), build/install auto-detected once that was set (pnpm workspace resolves correctly from repo root). Both dependencies flagged below are also resolved: prod Supabase's Auth Site URL was still `localhost` and silently broke OAuth/email-confirmation redirects for any real user — fixed via Authentication → URL Configuration (Site URL → the deploy URL above; Redirect URLs → `/auth/callback` and `/auth/confirm`). `NEXT_PUBLIC_APPLE_SIGNIN_AVAILABLE=true` is set on the Vercel project's production environment — confirmed live (the deployed `/login` renders "Sign in with Apple" without "(coming soon)"). **Known minor cleanup, not blocking:** an empty, unused Vercel project named `web` was created and abandoned mid-session before the `rootDirectory` approach was found — harmless (no deployments), delete via the Vercel dashboard whenever convenient. |
| **Production domain name** | **Resolved 2026-07-27 — `curfew.vip` purchased.** | Unblocks the dependent rows below: Vercel custom domain, production Google/Apple OAuth redirect URIs, Apple's Sign In with Apple Services ID (requires a verified domain), and the email provider's sending-domain verification (SPF/DKIM/DMARC) — see §4 below, now partially unblocked too. **Renewal: 2027-07-27, $16/yr via Cloudflare** (confirmed by Arjun 2026-07-27, written back here 2026-07-28) — not on auto-renew tracking anywhere else, so this row is the source of truth; revisit before that date. |

## 2. In progress

| Item | Status | Notes |
| --- | --- | --- |
| **Apple Developer Program enrollment** ($99/yr) | **Resolved 2026-07-28** — enrolled and paid. | Covers two stories: Story 2.3b's Sign In with Apple and Story 2.9a's macOS code-signing/notarization — one membership, confirmed shared. **Renewal: 2027-07-28, $99/yr** (confirmed by Arjun) — not on auto-renew tracking anywhere else, so this row is the source of truth alongside the domain renewal row above; revisit before that date. |

## 3. Tied to a specific story/epic — provision when that story starts

| Item | Forcing function | Notes |
| --- | --- | --- |
| **Supabase cloud project** (org + prod project; billing/branching deferred) | **Resolved 2026-07-27** — provisioned ahead of its Story 2.10/3.2 forcing function, as a dependency of Story 2.3d's email wiring instead. | Runbook: `supabase/PROVISIONING.md`, steps 1–3 all done. Project named `prod` (`jmitbnrofacxwsbwuxzs`), **on the free tier** — no PITR, no preview branching until upgraded to Pro ($25/mo base + metered branching + $100/mo PITR add-on, confirmed against supabase.com/pricing 2026-07-27). Free project pauses after 7 days idle — **mitigated 2026-07-28** via `.github/workflows/supabase-keepalive.yml` (scheduled ping every 3 days) instead of upgrading tier pre-launch (Arjun's ruling). ~~Action needed: add the `SUPABASE_PROD_PUBLISHABLE_KEY` secret in GitHub repo settings~~ — **done, verified 2026-08-16**: the secret exists (created 2026-07-28, i.e. it was added the day after this row was written and the row was never updated). GitHub connected; branching still deferred pending Pro upgrade. |
| **Stripe** | Epic 7 (Stories 7.1–7.5) | Cleanly scoped already, nothing to do before Epic 7 starts. **One sub-item not covered by the epic's code scope**: Stripe's own account activation (legal entity/sole-proprietor details, bank account, identity verification) has its own review lead time, similar in kind to the code-signing certs below — worth starting that activation in parallel with, not after, Epic 7's first story if launch timing is tight. |
| **Windows code-signing** | **Closed 2026-08-16 by ruling: Windows ships UNSIGNED.** No longer a launch blocker and no longer a purchase. | **Arjun's decision 2026-08-16, after costing the alternatives.** Windows has no equivalent of the $99/yr Apple membership that already covers macOS signing *and* notarization — the certificate is the entire product there. The two real options were Azure Artifact Signing (~$120/yr; $9.99/mo Basic on a paid, non-trial Azure subscription) or a traditional CA cert (~$200–400/yr plus HSM hardware). Neither was judged worth buying before real signups reveal whether Curfew's DJs use Windows at all, given Serato skews heavily Mac. **The eligibility question that had been open since 2026-07-30 is moot, but for the record it was very likely a non-issue**: Azure's individual/sole-proprietor Public Trust tier is US/Canada-only, and Arjun's account phone is a `+1 267` (Philadelphia) number. **What this costs the user:** SmartScreen shows a full-screen "Windows protected your PC" block on first run; the installer is only reachable via "More info" → "Run anyway". **The download page should warn about this before the user meets it — that copy does not exist yet and is the one piece of follow-up this ruling creates.** **What it does not cost:** update integrity — `TAURI_SIGNING_PRIVATE_KEY` is the minisign updater keypair, unrelated to Authenticode, so update payloads stay verified. **Implementation:** the Azure path was *removed* rather than left dormant (the `artifact-signing-cli` install step, all five `AZURE_*` env vars, and `signCommand` in `tauri.windows-release.conf.json`), so nothing in the repo reads as provisioned when it is not; a placeholder-check step that could no longer fail was deleted for the same reason. Reversible by restoring the block from git history — the workflow was fully written and only ever lacked provisioning. Superseded detail from 2026-07-30, kept for whoever reverses this: `release-windows.yml` is built around **Azure Artifact Signing** (Microsoft's own HSM-backed signing service, renamed from "Trusted Signing" in January 2026) rather than a traditional CA-purchased OV/EV cert + physical HSM token — since June 1, 2023, CA/Browser Forum rules require the private key for any publicly-trusted code-signing cert to live on certified hardware and be non-exportable, so there's no `.p12`-style flat file to hand CI the way Apple's cert works. **(a) Eligibility — open, needs your confirmation:** Public Trust individual/sole-proprietor eligibility (no incorporated-business or 3-year-history requirement) is gated to the **United States and Canada**. Confirm you're provisioning from an eligible region before starting; if not, fall back to a traditional CA-issued OV cert instead (individual-eligible per Tauri's own docs, at the cost of no immediate SmartScreen reputation). **(b) Cost:** $9.99/mo Basic tier (up to 5,000 signatures), requires a **paid, non-trial** Azure subscription — free/trial/sponsored subscriptions are explicitly not supported. **(c) Identity verification:** still applies; this story's research did not independently confirm a faster timeline than epics.md's 1–3 week estimate for a traditional cert — assume a similar wall-clock wait, not something these numbers guarantee is shorter. The epics already accept a macOS-first launch fallback (Story 2.9a shipped, 2.9b trails) if this drags. **(d) Also needed before the workflow can run a real release:** once the Artifact Signing account + certificate profile exist, its regional signing endpoint (no env-var equivalent in `artifact-signing-cli`) must be filled into `agent/src-tauri/tauri.windows-release.conf.json`'s `signCommand`, replacing the `<AZURE_ARTIFACT_SIGNING_ENDPOINT>` placeholder currently there. |
| **Tauri updater signing keypair** | **Resolved 2026-07-30 — Epic 2 retrospective.** Keypair generated; one manual step remains (GitHub secret). | Generated via `pnpm dlx @tauri-apps/cli@latest signer generate --ci -p "" -w ~/.tauri/curfew-agent-updater.key` (no password — simplest for CI, since GitHub Actions reads `TAURI_SIGNING_PRIVATE_KEY` directly with no `_PASSWORD` secret needed). Private key lives only at `~/.tauri/curfew-agent-updater.key` (outside the repo, never committed). Public key's content already wired into `agent/src-tauri/tauri.conf.json`'s `plugins.updater.pubkey`, replacing the old placeholder. ~~Still Arjun's manual action: add the private key file's content as the `TAURI_SIGNING_PRIVATE_KEY` GitHub repo secret~~ — **done 2026-08-16**, set via `gh secret set` and confirmed present in `gh secret list`. **This row is now fully closed.** Two things verified at the same time, both of which would have failed silently at update time rather than at build time: the public key baked into `agent/src-tauri/tauri.conf.json:47` is byte-identical to `~/.tauri/curfew-agent-updater.key.pub` (a mismatch means every auto-update fails signature verification on the user's machine), and the updater endpoint's repo `5arjun/curfew.vip` exists and is public. **Do not add `TAURI_SIGNING_PRIVATE_KEY_PASSWORD`** — the keypair was generated with an empty password and both workflows already pass that optional var through empty; creating it with any value breaks signing. |
| **Production OAuth app credentials** (Google Cloud Console + Apple Services ID, for the real domain) | **Both resolved 2026-07-28** — enabled and saved in the prod Supabase Dashboard; real sign-ins verified against prod for both. *(Corrected 2026-07-28 code review: this row previously said "Google still outstanding," contradicting the story's own Change Log — Arjun confirmed Google is in fact enabled.)* | Story 2.3b's original credentials are dev/local-only (registered against `127.0.0.1:54321`, Supabase's local Auth callback). **Apple** has its own production registration: Services ID `app.curfew.web.signin` (App ID `app.curfew.web`), Return URL `https://jmitbnrofacxwsbwuxzs.supabase.co/auth/v1/callback`, enabled in the prod Supabase Dashboard. **Recurring task — Apple `client_secret` JWT expires 2027-01-24** (ES256 JWT, ~180-day lifetime, no auto-refresh): regenerate via `supabase/generate-apple-client-secret.mjs --key <path-to-.p8> --team-id <team-id> --key-id <key-id> --client-id app.curfew.web.signin` (Team ID/Key ID intentionally omitted here — see Apple Developer Portal → Certificates, IDs & Profiles → Keys — consistent with the generator script's own shell-history/scrollback caution) before that date and re-save in the prod Dashboard, alongside the Apple Developer Program renewal above. **Google — dev/prod credential split completed 2026-08-16.** Prod now runs on its own Google Cloud OAuth client, `827894280695-i8r4v75h5n99bnjngnp263tpsaa3kdg6.apps.googleusercontent.com` ("Curfew — production"), created in the *same* Google Cloud project as the dev client (project number `827894280695`) — deliberately not a new project, since the OAuth consent screen is project-level and a separate project would mean re-branding and re-publishing it from scratch for no isolation benefit. Authorized JavaScript origin `https://curfew.vip`; sole Authorized redirect URI `https://jmitbnrofacxwsbwuxzs.supabase.co/auth/v1/callback`. Swapped into the prod Supabase Dashboard and **verified end-to-end by Arjun 2026-08-16** (real Google sign-up → dashboard; consent-screen URL's `client_id` confirmed as the new suffix `i8r4v75h5n99…`, not dev's `d3akrinu…`). The prod callback URI has been **removed from the dev client**, which now carries only `http://127.0.0.1:54321/auth/v1/callback` — this reverses the 2026-07-28 "load-bearing, keep it" ruling, which was correct only while prod shared the dev client's credentials. `supabase/config.toml:375` still pins the **dev** client ID (`827894280695-d3akrinuaugi2eigkqr54ang9sdalpmf`) for local — correct and unchanged. *Verification gotcha worth remembering: the `827894280695-` prefix is the shared Google Cloud project number, not part of the client identity — only the suffix after the dash distinguishes clients, and misreading the prefix as a match will make a failed swap look successful.* Consent-screen check resolved 2026-07-28: publishing mode is **"In production"** (not "Testing"), so prod Google sign-in is not silently capped to allow-listed accounts. Earlier cleanup resolved 2026-07-28 (2.3b review follow-through): the `localhost:3000/auth/callback` entry in prod's GoTrue redirect allow-list — added purely for manual testing — has been removed by Arjun. **Still open — consent-screen branding (see the row below).** |
| **Agent release-build config** (`SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY`, `CURFEW_WEB_URL` baked into the signed agent binary) | Story 2.10 (agent secure token storage) — **resolved 2026-08-16**: all three values now wired into both release workflows | ✅ **Fixed 2026-08-16.** `SUPABASE_URL` (inlined), `SUPABASE_PUBLISHABLE_KEY` (from the existing `SUPABASE_PROD_PUBLISHABLE_KEY` secret) and `CURFEW_WEB_URL` (inlined) were added to the `env:` block of both `release-macos.yml` and `release-windows.yml`; both files re-parsed as valid YAML afterwards. The two URLs are hardcoded rather than made secrets — neither is secret, and inlining keeps a release reproducible from the commit alone. **Not yet exercised by a real signed release** (that is still blocked on the Apple/Azure/Tauri signing secrets in the row below), so this is "correctly wired" rather than "proven in a build". Original finding, kept for history: 🚨 **Hard release blocker, found by direct inspection 2026-08-16.** Not secrets (publishable-safe, same values `web/`'s `NEXT_PUBLIC_` env vars already carry) but still unset in CI — `agent/src-tauri/build.rs` reads them from `agent/src-tauri/.env.local` (gitignored, local-dev-only) at build time via `cargo:rustc-env`; an unset var compiles to an **empty string** rather than failing the build. Verified: the `env:` blocks of `release-macos.yml` (line 129) and `release-windows.yml` (line 151) pass **`SENTRY_DSN` and nothing else** — `SUPABASE_URL`, `SUPABASE_PUBLISHABLE_KEY` and `CURFEW_WEB_URL` appear nowhere in either workflow. **A signed release cut today therefore ships an agent that cannot reach Supabase at all (empty URL and key) and whose "Link Account" tray item opens `/link-agent` on an empty origin.** It builds green and fails only in the user's hands. **All three values are now known**, so this is a code fix, not a provisioning wait: `SUPABASE_URL` = `https://jmitbnrofacxwsbwuxzs.supabase.co` (confirmed live), `SUPABASE_PUBLISHABLE_KEY` = the already-existing `SUPABASE_PROD_PUBLISHABLE_KEY` repo secret, and **`CURFEW_WEB_URL` = `https://curfew.vip` — previously recorded here as "genuinely unresolved," which went stale when the domain went live 2026-08-14.** `agent/src-tauri/src/config.rs`'s doc comments for `CURFEW_WEB_URL` and `SENTRY_DSN` said the same stale things and were corrected 2026-08-16. |
| **Account deletion + export runbook forward-hooks** (`supabase/ACCOUNT-DELETION-EXPORT-RUNBOOK.md`) | Story 2.11 (account deletion + export, manual runbook) — Story 3.1 forward-hook closed 2026-07-30; one forward-hook remains | The runbook is accurate and verified against **today's** schema, which as of Story 3.1 is `public.djs` + `sessions`/`sets`/`plays`. **Resolved 2026-07-30 (Story 3.1 dev-story):** deletion cascade re-verified for real against local Postgres with a full row set across all four tables (`auth.users` delete → `djs` → `sessions` → `sets`/`plays`, all cascade, no manual step needed); export procedure (§3) now runs one query per table (`djs`, `sessions`, `sets`, `plays`) scoped to `dj_id`. **Still open, not this story's to close:** once any Epic 7 story lands `stripe_customer_id` on `djs`, that story must add a Stripe-customer-deletion step before the `auth.users` delete (runbook §2 already has this forward-hook TODO inline). Also tracked (no action needed pre-launch): AC-4's App Store/Google Play listing trigger, which would require converting this from a manual runbook into a self-serve in-app feature — not relevant until a mobile/store listing is planned. |
| **GitHub Actions CI secrets** (`SUPABASE_ACCESS_TOKEN`, `SUPABASE_PROJECT_REF`, code-signing certs, Tauri updater signing keypair) | Same trigger as the Supabase cloud project / Story 2.9a-c | Already flagged in PROVISIONING.md §4 and AR-14 — listed here only so this doc is the one complete picture. Do not wire these into CI early; each has an explicit "not yet" in its owning doc. **Story 2.9a (`release-macos.yml`) — ✅ all six Apple secrets set 2026-08-16, and both halves validated against Apple rather than merely populated.** `APPLE_SIGNING_IDENTITY` = `Developer ID Application: Arjun Patel (8JK54C3MS9)`; `APPLE_API_KEY` = `BK5PNU9862`; `APPLE_API_ISSUER` = `24172cfe-…`. **Validation performed (do this again after any cert/key rotation):** `xcrun notarytool history` authenticated successfully with the issuer/key-id/`.p8` triple, proving the notarization credentials work together; and the `APPLE_CERTIFICATE` base64 was decoded into a throwaway keychain exactly as CI does, yielding **1 valid codesigning identity**. Four traps hit on the way, all of which will recur for anyone repeating this: **(1)** importing the `.cer` failed with **-25294** because Keychain Access had **iCloud** selected — certificates only import into **login**. **(2)** Once imported the identity showed `CSSMERR_TP_NOT_TRUSTED` and `find-identity -v` listed **nothing**, because the machine had **zero Apple intermediate CAs**; installing Apple Root CA + Developer ID G2 fixed it. Note `find-identity -v` hides invalid identities entirely, so "0 valid identities" looks identical to "no certificate at all" — use `security find-identity` without `-v` to see the real reason. **(3)** `security export -t identities` exports **every** identity in the keychain: the first `.p12` contained the Developer ID cert *plus* an unrelated T-Mobile internal-systems cert and a `localhost` cert, **including their private keys**, which would have shipped third-party credentials into a CI secret. The stored secret is a rebuilt single-identity `.p12` (key matched to cert by public-key comparison — the key bag is labelled `Curfew`, not the cert name) bundled with the G2 intermediate. **(4)** Both a Sign In with Apple key and an App Store Connect API key are named `AuthKey_XXXXXXXXXX.p8` with 10-char IDs and are **indistinguishable by filename**; the pre-existing sign-in key (`67QM89GQC7`, Story 2.3b) was briefly and wrongly uploaded as the notarization key before being removed. Notarization needs a **Team Key** created under App Store Connect → Users and Access → Integrations, and **the Issuer ID only appears on that page once at least one key exists.** Original list, kept for reference — `APPLE_CERTIFICATE` (base64 `.p12` Developer ID Application cert), `APPLE_CERTIFICATE_PASSWORD` (the `.p12` export password), `APPLE_SIGNING_IDENTITY` (e.g. `"Developer ID Application: <Name> (<TEAMID>)"`), `APPLE_API_ISSUER`, `APPLE_API_KEY`, `APPLE_API_KEY_CONTENT` (notarization API-key method — `APPLE_API_KEY_CONTENT` holds the full contents of the downloaded `.p8` file itself, not a path; the workflow writes it to a file on the runner and points `APPLE_API_KEY_PATH` at that path internally, since a path string from Arjun's machine wouldn't exist on GitHub's ephemeral runner). Apple Developer Program enrollment (row above) is done, but that alone doesn't generate a Developer ID cert, export a `.p12`, or create notarization API-key credentials — those are separate manual Apple Developer Portal steps only Arjun can perform; the workflow will fail at the signing step until they're added. ~~**Story 2.9b (`release-windows.yml`): five `AZURE_*` secrets**~~ — **no longer required as of 2026-08-16.** Windows ships unsigned (see the Windows code-signing row above), and the Azure env block was removed from the workflow, so these secrets are not needed and should not be created. `release-windows.yml` now requires only `TAURI_SIGNING_PRIVATE_KEY`, which is already set — meaning **both release workflows are fully provisioned and neither is blocked on any outstanding purchase or account.** **Story 2.9c (both `release-macos.yml` and `release-windows.yml`) — ✅ resolved 2026-08-16:** `TAURI_SIGNING_PRIVATE_KEY` is set. `TAURI_SIGNING_PRIVATE_KEY_PASSWORD` is intentionally **absent** and must stay absent (empty-password keypair). One shared updater keypair signs both OS's release artifacts. **Full secret status as of 2026-08-16** — present: `TAURI_SIGNING_PRIVATE_KEY`, `SENTRY_DSN`, `SUPABASE_PROD_PUBLISHABLE_KEY`. Missing: all 6 Apple and all 5 Azure secrets above. So the updater half of signing is done for both platforms and **platform code-signing is now the only thing standing between this repo and a real release** — macOS needs certificate/notarization work that is unblocked today (enrollment is done), Windows still has the open US/Canada eligibility question. |

## 4. Real gap — not owned by any story today

| Item | Status |
| --- | --- |
| **`support@curfew.vip` inbox** | **Resolved 2026-08-16 — Cloudflare Email Routing, confirmed by Arjun.** DNS independently verified the same day: `curfew.vip` publishes Cloudflare's three routing MX records (`route1/2/3.mx.cloudflare.net`) and an SPF record (`v=spf1 include:_spf.mx.cloudflare.net ~all`), and Arjun confirmed the `support@` route itself is created. Previously read "the address does not exist… mailto links into a void" — stale. The mailto surface is wider than the two affordances originally flagged: `support@curfew.vip` is referenced **9 times across 4 pages** — Settings (export request, About→Support), Privacy (×2), Terms (×2), and the Contact page (×2). All are now live rather than dead. **End-to-end test send performed and confirmed working by Arjun 2026-08-16** — mail actually lands, so this row is closed on delivery, not just on DNS. |
| **Google consent screen shows `jmitbnrofacxwsbwuxzs.supabase.co`, not "Curfew"** | **Open (flagged 2026-08-16).** Google's account chooser reads "to continue to **jmitbnrofacxwsbwuxzs.supabase.co**" — the raw prod Supabase project ref, shown to every DJ who signs in with Google. Not a credentials problem and **not fixed by the dev/prod client split above** (that was separately worth doing, but branding is project-level, not client-level). **Root cause, confirmed from the live consent URL's own `app_domain=https%3A%2F%2Fjmitbnrofacxwsbwuxzs.supabase.co` parameter:** Google derives the displayed identity from the **redirect URI's host**, and substitutes your app name only when that host sits under a domain you've verified. The callback lives on `supabase.co`, which Arjun cannot own or verify — so consent-screen branding for `curfew.vip` (App name "Curfew", authorized domain `curfew.vip`, Search Console-verified, all completed 2026-08-16) never reaches it. **Two fixes, both with a real cost.** *(a)* **Supabase custom auth domain** (`auth.curfew.vip`) moves the callback onto an owned domain and is the deterministic fix — requires **Pro, $25/mo**, which would also close the free-tier pause risk (retiring `supabase-keepalive.yml`) and the no-PITR gap in the §3 Supabase row, so the $25 buys three things, not one. Note it is not a pure dashboard toggle: changing the callback host means re-adding the redirect URI on the prod Google client **and** re-doing Apple's Return URL plus regenerating its `client_secret` JWT. *(b)* **Google brand verification** — logo + Privacy Policy + ToS URLs, few-business-days review. No longer blocked on the legal pages (both shipped 2026-08-15 and live: `https://curfew.vip/privacy`, `https://curfew.vip/terms`, both 200 — see the corrected §5 row), but given the `app_domain` mechanism above there is **genuine doubt it changes this display at all**, since the host it objects to still isn't one Google can attribute to Curfew. Treat (b) as the cheap experiment and (a) as the thing that actually works. |
| **Sentry project + DSN** | **Half resolved 2026-08-16 — agent done, `web/` still open.** *(Previously "Open… `config.rs` documents no production DSN exists" — stale on the agent half.)* **Agent:** a Sentry project exists (platform Rust, currently 0 errors / 0 transactions — i.e. provisioned but never yet exercised by a real build), the `SENTRY_DSN` repo secret was set 2026-08-01, and both release workflows already pass it into the build. `config.rs`'s comment claiming otherwise was corrected 2026-08-16. **`web/` — wired 2026-08-16, one manual step outstanding.** Sentry project `web` created under org `curfew` (DSN ends `…/4511921727340544`) and `@sentry/nextjs@10.70.0` installed (its peer range `^16.0.0-0` covers this app's Next 16.2.10 — checked before installing). Files added: `web/lib/sentry-shared.ts` (single source for DSN + options, so the three runtimes can't drift), `web/instrumentation.ts` (server/edge init **plus `onRequestError`** — without that export, Server Component crashes never reach Sentry while client errors arrive normally, i.e. a convincing half-working state), `web/sentry.server.config.ts`, `web/sentry.edge.config.ts`, and `web/app/global-error.tsx` (the app had **no error boundary of any kind**; this is the only one that catches a root-layout crash, previously both the worst failure and the one Sentry couldn't see). `instrumentation-client.ts` was extended by hand rather than by Sentry's wizard, which would have overwritten the existing BotID init. Verified: clean build, tunnel rewrite `/monitoring` present *alongside* BotID's rewrites, DSN in the client bundle, 929/929 web tests and the `no-hardcoded-colors` guard green. Reporting is gated to `NODE_ENV === "production"` and `sendDefaultPii` is **false** — the latter is what keeps the privacy page's collection claims true, so the two must move together. **Privacy page updated in the same change**: its processor list said "Sentry — crash reports from the agent", which this work would have made false. **⏳ Still owed by Arjun: create a Sentry org auth token (https://sentry.io/settings/curfew/auth-tokens/, scopes `project:releases` + `org:read`) and add it as `SENTRY_AUTH_TOKEN` to the Vercel project's Production + Preview envs.** Until then the build skips source-map upload and every web stack trace arrives minified — it fails silently, not loudly, which is why it is called out here. Cosmetic, also outstanding: rename the agent's Sentry project from its platform-default slug `rust` to `agent`. |
| **Supabase "Manual linking" toggle (prod + local)** | **Resolved 2026-08-16 — both halves.** Settings' provider row calls `supabase.auth.linkIdentity()` for Google/Apple, which requires **Manual linking enabled** on the Supabase project. **Local:** `enable_manual_linking = true` at `supabase/config.toml:208` — was already true when this row still read "not enabled anywhere today," i.e. that wording was stale on the local half. **Prod:** Arjun confirmed 2026-08-16 that the toggle (Authentication → Sign In / Providers → "Allow manual linking") was **off**, and turned it on. So the original flag was correct about prod and this was a genuine live gap, not a stale row — the "+ Link" affordance really would have errored for every DJ until today. **Not yet exercised:** nobody has actually clicked "+ Link" against prod since the toggle flipped; worth folding into the same manual pass as the passkey re-test below. |
| **Prod DB migrated BEFORE any agent/web release that depends on a new migration** | **Original instance resolved; generalised into a standing rule 2026-08-16.** The specific 2026-08-05 flag — migration `20260806090100` (`add_agent_status_agent_version`) absent from prod, making every `set_agent_status` heartbeat 404 and the agent read dead product-wide — **is closed: `20260806090100` is applied to prod** (verified against the live migration list 2026-08-16). But the row was only ever written about that one migration, which is the wrong shape: the hazard recurs on every migration. **Second instance, found and closed the same day:** `20260816170000_normalize_djs_phone_e164` was merged to `main` and **deployed to production while still unapplied to prod's database** — the exact ordering this rule forbids. It was benign only by luck (the CHECK-violation error copy in `3cf3ed3` is inert without the constraint, and both prod rows already happened to be E.164, so the backfill was a no-op). **Applied to prod 2026-08-16**; constraint `djs_phone_e164` verified present with the repo's exact definition and both rows verified conforming. One gotcha for whoever runs the next one: the `apply_migration` call returned `42710: constraint already exists` **after** having succeeded, so the error was from a retry, not a failure — check `list_migrations` and `pg_constraint` before re-running or assuming it didn't land. **Standing rule: before any release — agent or web — diff the repo's `supabase/migrations/` against prod's applied list and apply the difference first.** Ruled as a release gate rather than permanent fallback code in the agent. |
| **Prod redirect allow-list: `/auth/reset`** | **Resolved 2026-08-16 — verified against the live dashboard.** The password-reset recovery email redirects to `{origin}/auth/reset`; `https://curfew.vip/auth/reset` **is present** in Authentication → URL Configuration → Redirect URLs. Site URL is `https://curfew.vip`. The original instruction here — add the entry under `name-pending-gamma.vercel.app` — was superseded by the domain move and should not be followed. ~~One cleanup left: two entries pointing at the dead `name-pending-gamma.vercel.app` host~~ — **done 2026-08-16**, Arjun removed both. The allow-list now holds exactly the three `curfew.vip` entries (`/auth/callback`, `/auth/confirm`, `/auth/reset`). Reason for removing rather than ignoring: that origin 404s so the entries could never succeed, but an allow-list entry for a hostname you no longer control becomes a real problem if the Vercel subdomain is ever reassigned to someone else. |
| **Passkey sign-in not provisioned on prod** | **Resolved 2026-07-28 — Story 2.4, Task 3.3/3.4. Re-pointed at `curfew.vip` and re-verified 2026-08-16.** Current live config: passkey auth enabled, Relying Party Display Name "Curfew", Relying Party ID **`curfew.vip`**, Relying Party Origins **`https://curfew.vip,https://www.curfew.vip`**. **Re-proven end-to-end against `curfew.vip` by Arjun 2026-08-16**: real passkey registration from Settings ("Add a passkey" → "Passkey added.") *and* a real passkey sign-in on a fresh session, both confirmed. So this row is proven working, not merely configured. Two things worth keeping from the interim caveat, since both could recur on any future domain move: **(a)** WebAuthn scopes a credential to its RP ID, so the original 2026-07-28 verification (performed under RP ID `name-pending-gamma.vercel.app`) proved nothing about the current config — a domain move silently invalidates every existing passkey and every prior proof. **(b)** Had any old credential survived the move it would have been actively harmful, not merely dead: `web/app/components/settings/ProvidersRow.tsx:164` renders "Add a passkey" only when `!passkeyAttached`, so a stale server-side credential would make Settings display "Passkey ✓ attached" while hiding the button, leaving the user unable to register a working passkey and told they already had one. Confirmed non-issue this time only because `auth.webauthn_credentials` was verified empty (0 rows) beforehand — **check that table before, not after, any future RP ID change.** Original note: human-verified end-to-end by Arjun against the live deploy — real passkey **register** (via the restyled `EnablePasskeyPrompt`) and real passkey **sign-in** (via the restyled login-form `BiometricAnchor`), both confirmed working. See `deferred-work.md` (entry closed) and the story file's Dev Agent Record for the full session narrative. |
| **Email sending provider** (Resend) | **Fully resolved and verified 2026-07-28.** Domain verified (sending from the `updates.curfew.vip` subdomain of `curfew.vip`), production Supabase project provisioned (`prod`, see §3 above), credentials wired via Resend's native Supabase integration, and a **real production send confirmed end-to-end 2026-07-28**: signed up against `prod`'s live API with a real external inbox, confirmation email arrived, SPF/DKIM/DMARC all independently confirmed passing in the raw headers (not just Resend's bundled domain badge). `supabase/config.toml`'s `[auth.email.smtp]` block remains commented out (local-only `local_smtp` testing inbox stays active) — production credentials live on the Supabase project itself, never in this repo. **Known separate gap, not this row's concern:** the confirmation link itself redirects to `localhost` since `prod`'s Auth Site URL isn't configured — tracked under §1's Vercel deploy row, resolves once `web/` has a real public URL to point Site URL/Redirect URLs at. See "Scoping the email-provider work" below for the original correct-course reasoning that spawned this story. |

## 5. Compliance / legal — open PRD line items, not attached to any epic

| Item | Status | Notes |
| --- | --- | --- |
| **CCPA formal privacy review** | Outstanding pre-launch checklist item (PRD §11 item 4, Architecture Spine Open Questions #6) | Both docs already conclude US-only-at-launch makes a CCPA-level posture (not full GDPR) sufficient, and both already downgrade this from "open question" to "pre-launch checklist item" — but neither attaches it to an epic or a date. Needs a decision: self-review checklist vs. paid legal review, and when in the timeline it happens (before or gating public launch). |
| **Terms of Service / Privacy Policy documents** | **Written and live — corrected 2026-08-16.** *(This row previously read "Not found anywhere in the docs… neither page exists anywhere in the app," re-confirmed 2026-08-06 against Story 3.10. That went stale when the pages shipped and nothing updated it — verified 2026-08-16 by fetching prod, both 200.)* Pre-launch **legal review** is still owed; the drafting is not. | `web/app/(marketing)/privacy/page.tsx` (211 lines) and `web/app/(marketing)/terms/page.tsx` (191 lines), both marked "Effective August 15, 2026", live at `https://curfew.vip/privacy` and `https://curfew.vip/terms`. The privacy page's own header comment states its processor list, cookie names and rights section were each grounded in what is actually wired (`EMAIL-PROVISIONING.md`, `agent/src-tauri`, `ACCOUNT-DELETION-EXPORT-RUNBOOK.md`) — and it flags "Pre-launch legal review still owed" inline, which is the part that remains open and is what the CCPA row above needs to review against. **Unblocks** Google brand verification (§4's consent-screen row), which requires both URLs. |

## 6. Worth a line, not urgent

| Item | Status |
| --- | --- |
| **Reverse-geocoding provider** (Google Places / Apple Maps / OSM Nominatim, for FR-18 venue suggestion) | Unchosen — PRD Open Question #7, deferred to Epic 5 (post-validation track, "may trail," doesn't block SM-1/SM-2). No action needed now; listed so it doesn't get forgotten when Epic 5 starts. |

---

## Scoping the email-provider work

Recommendation: **`bmad-correct-course`, not `bmad-create-story`.**

Reasoning: `bmad-create-story` drafts a dev-ready story file from a description that already exists in `epics.md` (that's exactly what happened for 2.3a and 2.3b — both were already stubbed out in the Epic 2 story list before context-engineering). The email-sending provider has **no existing entry anywhere in epics.md** — it's net-new scope discovered mid-epic, not a story waiting to be detailed. `bmad-correct-course` is the tool built for exactly this: a significant gap found during sprint execution that needs the epic/story backlog itself updated (new story inserted, possibly a one-line architecture-spine note added since production SMTP config is an AD-10/Supabase-Auth-adjacent decision) before a story file gets written against it.

Likely shape once correct-course runs: a new story in Epic 2 (natural fit — it directly unblocks Story 2.3a's confirmation email in production, and Epic 2 already owns the Supabase Auth configuration surface), scoped to (a) standing up a Resend-or-similar account + verified sending domain (manual, Arjun's call, same pattern as PROVISIONING.md), and (b) the coding half: uncommenting/filling `[auth.email.smtp]` in `config.toml` with env-var-sourced secrets, confirming it applies to both local and the eventual cloud project, and documenting the runbook the same way Story 2.1 documented Supabase provisioning.

Ready to run `bmad-correct-course` for this now if you want to proceed.
