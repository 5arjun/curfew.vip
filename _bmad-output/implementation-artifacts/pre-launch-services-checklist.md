# Pre-launch / production-services checklist

**As of:** 2026-07-27
**Owner:** Arjun (all items below are account-level/manual actions, not coding-agent tasks)
**Purpose:** single place tracking every external account/service Curfew needs between "runs entirely against the local Supabase stack" and "real production launch." Consolidates supabase/PROVISIONING.md, PRD §11, Architecture Spine Open Questions, and epics.md cross-cutting notes — none of which individually owned this list.

Update this file directly when an item's status changes; it isn't generated by a workflow.

---

## 1. No code blocker — do whenever convenient

| Item | Status | Notes |
| --- | --- | --- |
| **Vercel deploy** | **Resolved 2026-07-28 — Story 2.4, Task 3.1/3.2.** | Real production deploy live at **https://name-pending-gamma.vercel.app** (bare `*.vercel.app` origin, no custom domain yet — that's still a separate, larger item, see the domain row below). Vercel project `5arjuns-projects/name-pending`, `rootDirectory` set to `web` (monorepo — set via a direct Management API call, not exposed by `vercel project update`'s CLI flags), build/install auto-detected once that was set (pnpm workspace resolves correctly from repo root). Both dependencies flagged below are also resolved: prod Supabase's Auth Site URL was still `localhost` and silently broke OAuth/email-confirmation redirects for any real user — fixed via Authentication → URL Configuration (Site URL → the deploy URL above; Redirect URLs → `/auth/callback` and `/auth/confirm`). `NEXT_PUBLIC_APPLE_SIGNIN_AVAILABLE=true` is set on the Vercel project's production environment — confirmed live (the deployed `/login` renders "Sign in with Apple" without "(coming soon)"). **Known minor cleanup, not blocking:** an empty, unused Vercel project named `web` was created and abandoned mid-session before the `rootDirectory` approach was found — harmless (no deployments), delete via the Vercel dashboard whenever convenient. |
| **Production domain name** | **Resolved 2026-07-27 — `curfew.vip` purchased.** | Unblocks the dependent rows below: Vercel custom domain, production Google/Apple OAuth redirect URIs, Apple's Sign In with Apple Services ID (requires a verified domain), and the email provider's sending-domain verification (SPF/DKIM/DMARC) — see §4 below, now partially unblocked too. **Renewal: 2027-07-27, $16/yr via Cloudflare** (confirmed by Arjun 2026-07-27, written back here 2026-07-28) — not on auto-renew tracking anywhere else, so this row is the source of truth; revisit before that date. |

## 2. In progress

| Item | Status | Notes |
| --- | --- | --- |
| **Apple Developer Program enrollment** ($99/yr) | **Resolved 2026-07-28** — enrolled and paid. | Covers two stories: Story 2.3b's Sign In with Apple and Story 2.9a's macOS code-signing/notarization — one membership, confirmed shared. **Renewal: 2027-07-28, $99/yr** (confirmed by Arjun) — not on auto-renew tracking anywhere else, so this row is the source of truth alongside the domain renewal row above; revisit before that date. |

## 3. Tied to a specific story/epic — provision when that story starts

| Item | Forcing function | Notes |
| --- | --- | --- |
| **Supabase cloud project** (org + prod project; billing/branching deferred) | **Resolved 2026-07-27** — provisioned ahead of its Story 2.10/3.2 forcing function, as a dependency of Story 2.3d's email wiring instead. | Runbook: `supabase/PROVISIONING.md`, steps 1–3 all done. Project named `prod` (`jmitbnrofacxwsbwuxzs`), **on the free tier** — no PITR, no preview branching until upgraded to Pro ($25/mo base + metered branching + $100/mo PITR add-on, confirmed against supabase.com/pricing 2026-07-27). Free project pauses after 7 days idle — **mitigated 2026-07-28** via `.github/workflows/supabase-keepalive.yml` (scheduled ping every 3 days) instead of upgrading tier pre-launch (Arjun's ruling). **Action needed before this is fully live: add the `SUPABASE_PROD_PUBLISHABLE_KEY` secret in GitHub repo settings** (Settings → Secrets and variables → Actions), value from the prod project's Settings → API Keys — the workflow will fail its health check until this exists. GitHub connected; branching still deferred pending Pro upgrade. |
| **Stripe** | Epic 7 (Stories 7.1–7.5) | Cleanly scoped already, nothing to do before Epic 7 starts. **One sub-item not covered by the epic's code scope**: Stripe's own account activation (legal entity/sole-proprietor details, bank account, identity verification) has its own review lead time, similar in kind to the code-signing certs below — worth starting that activation in parallel with, not after, Epic 7's first story if launch timing is tight. |
| **Windows code-signing (Azure Artifact Signing)** | Story 2.9b (Windows signed build) — workflow code shipped 2026-07-30, not yet provisioned | **Concrete path chosen 2026-07-30, provisioning not started**: `release-windows.yml` is built around **Azure Artifact Signing** (Microsoft's own HSM-backed signing service, renamed from "Trusted Signing" in January 2026) rather than a traditional CA-purchased OV/EV cert + physical HSM token — since June 1, 2023, CA/Browser Forum rules require the private key for any publicly-trusted code-signing cert to live on certified hardware and be non-exportable, so there's no `.p12`-style flat file to hand CI the way Apple's cert works. **(a) Eligibility — open, needs your confirmation:** Public Trust individual/sole-proprietor eligibility (no incorporated-business or 3-year-history requirement) is gated to the **United States and Canada**. Confirm you're provisioning from an eligible region before starting; if not, fall back to a traditional CA-issued OV cert instead (individual-eligible per Tauri's own docs, at the cost of no immediate SmartScreen reputation). **(b) Cost:** $9.99/mo Basic tier (up to 5,000 signatures), requires a **paid, non-trial** Azure subscription — free/trial/sponsored subscriptions are explicitly not supported. **(c) Identity verification:** still applies; this story's research did not independently confirm a faster timeline than epics.md's 1–3 week estimate for a traditional cert — assume a similar wall-clock wait, not something these numbers guarantee is shorter. The epics already accept a macOS-first launch fallback (Story 2.9a shipped, 2.9b trails) if this drags. **(d) Also needed before the workflow can run a real release:** once the Artifact Signing account + certificate profile exist, its regional signing endpoint (no env-var equivalent in `artifact-signing-cli`) must be filled into `agent/src-tauri/tauri.windows-release.conf.json`'s `signCommand`, replacing the `<AZURE_ARTIFACT_SIGNING_ENDPOINT>` placeholder currently there. |
| **Production OAuth app credentials** (Google Cloud Console + Apple Services ID, for the real domain) | **Both resolved 2026-07-28** — enabled and saved in the prod Supabase Dashboard; real sign-ins verified against prod for both. *(Corrected 2026-07-28 code review: this row previously said "Google still outstanding," contradicting the story's own Change Log — Arjun confirmed Google is in fact enabled.)* | Story 2.3b's original credentials are dev/local-only (registered against `127.0.0.1:54321`, Supabase's local Auth callback). **Apple** has its own production registration: Services ID `app.curfew.web.signin` (App ID `app.curfew.web`), Return URL `https://jmitbnrofacxwsbwuxzs.supabase.co/auth/v1/callback`, enabled in the prod Supabase Dashboard. **Recurring task — Apple `client_secret` JWT expires 2027-01-24** (ES256 JWT, ~180-day lifetime, no auto-refresh): regenerate via `supabase/generate-apple-client-secret.mjs --key <path-to-.p8> --team-id <team-id> --key-id <key-id> --client-id app.curfew.web.signin` (Team ID/Key ID intentionally omitted here — see Apple Developer Portal → Certificates, IDs & Profiles → Keys — consistent with the generator script's own shell-history/scrollback caution) before that date and re-save in the prod Dashboard, alongside the Apple Developer Program renewal above. **Google** reused the existing dev Google Cloud OAuth client (no second client) with `https://jmitbnrofacxwsbwuxzs.supabase.co/auth/v1/callback` added as an additional Authorized redirect URI, and is enabled in the prod Supabase Dashboard with the same Client ID/Secret. **Cleanup resolved 2026-07-28 (2.3b review follow-through): the `localhost:3000/auth/callback` entry in prod's GoTrue redirect allow-list — added purely for manual testing — has been removed by Arjun. The prod callback stays on the *dev* Google client by decision: prod Supabase's Google provider uses that same client's ID/secret, so that redirect URI is load-bearing, not scaffolding (Arjun's ruling: keep it, removing it would break prod Google sign-in). Consent-screen check resolved 2026-07-28: Arjun confirmed the Google client's OAuth consent screen is in **"In production"** publishing mode (not "Testing"), so prod Google sign-in is not silently capped to allow-listed accounts. One follow-up still tracked as pre-launch hardening (no code, not blocking pre-launch): create a dedicated prod Google OAuth client so dev/prod credentials are split before launch, and move the prod callback to it.** |
| **GitHub Actions CI secrets** (`SUPABASE_ACCESS_TOKEN`, `SUPABASE_PROJECT_REF`, code-signing certs, Tauri updater signing keypair) | Same trigger as the Supabase cloud project / Story 2.9a-c | Already flagged in PROVISIONING.md §4 and AR-14 — listed here only so this doc is the one complete picture. Do not wire these into CI early; each has an explicit "not yet" in its owning doc. **Story 2.9a (`release-macos.yml`) added 2026-07-29 — not yet added:** `APPLE_CERTIFICATE` (base64 `.p12` Developer ID Application cert), `APPLE_CERTIFICATE_PASSWORD` (the `.p12` export password), `APPLE_SIGNING_IDENTITY` (e.g. `"Developer ID Application: <Name> (<TEAMID>)"`), `APPLE_API_ISSUER`, `APPLE_API_KEY`, `APPLE_API_KEY_CONTENT` (notarization API-key method — `APPLE_API_KEY_CONTENT` holds the full contents of the downloaded `.p8` file itself, not a path; the workflow writes it to a file on the runner and points `APPLE_API_KEY_PATH` at that path internally, since a path string from Arjun's machine wouldn't exist on GitHub's ephemeral runner). Apple Developer Program enrollment (row above) is done, but that alone doesn't generate a Developer ID cert, export a `.p12`, or create notarization API-key credentials — those are separate manual Apple Developer Portal steps only Arjun can perform; the workflow will fail at the signing step until they're added. **Story 2.9b (`release-windows.yml`) added 2026-07-30 — not yet added:** `AZURE_CLIENT_ID`, `AZURE_CLIENT_SECRET`, `AZURE_TENANT_ID` (an Azure App Registration's credentials), `AZURE_ARTIFACT_SIGNING_ACCOUNT`, `AZURE_ARTIFACT_SIGNING_CERTIFICATE_PROFILE` (identify which Artifact Signing account/cert profile to sign with). Provisioning the Azure App Registration, Artifact Signing account, and certificate profile is Arjun's manual action in the Azure Portal (see the Windows code-signing row above) — not achievable from this codebase; the workflow will fail at the signing step until they're added, same as the Apple secrets above. |

## 4. Real gap — not owned by any story today

| Item | Status |
| --- | --- |
| **Passkey sign-in not provisioned on prod** | **Resolved 2026-07-28 — Story 2.4, Task 3.3/3.4.** `[auth.passkey]`/`[auth.webauthn]` enabled on the prod Supabase Dashboard: Relying Party Display Name "Curfew", Relying Party ID `name-pending-gamma.vercel.app`, Relying Party Origins `https://name-pending-gamma.vercel.app`. Human-verified end-to-end by Arjun against the live deploy: real passkey **register** (via the restyled `EnablePasskeyPrompt`) and real passkey **sign-in** (via the restyled login-form `BiometricAnchor`), both confirmed working. See `deferred-work.md` (entry closed) and the story file's Dev Agent Record for the full session narrative. |
| **Email sending provider** (Resend) | **Fully resolved and verified 2026-07-28.** Domain verified (sending from the `updates.curfew.vip` subdomain of `curfew.vip`), production Supabase project provisioned (`prod`, see §3 above), credentials wired via Resend's native Supabase integration, and a **real production send confirmed end-to-end 2026-07-28**: signed up against `prod`'s live API with a real external inbox, confirmation email arrived, SPF/DKIM/DMARC all independently confirmed passing in the raw headers (not just Resend's bundled domain badge). `supabase/config.toml`'s `[auth.email.smtp]` block remains commented out (local-only `local_smtp` testing inbox stays active) — production credentials live on the Supabase project itself, never in this repo. **Known separate gap, not this row's concern:** the confirmation link itself redirects to `localhost` since `prod`'s Auth Site URL isn't configured — tracked under §1's Vercel deploy row, resolves once `web/` has a real public URL to point Site URL/Redirect URLs at. See "Scoping the email-provider work" below for the original correct-course reasoning that spawned this story. |

## 5. Compliance / legal — open PRD line items, not attached to any epic

| Item | Status | Notes |
| --- | --- | --- |
| **CCPA formal privacy review** | Outstanding pre-launch checklist item (PRD §11 item 4, Architecture Spine Open Questions #6) | Both docs already conclude US-only-at-launch makes a CCPA-level posture (not full GDPR) sufficient, and both already downgrade this from "open question" to "pre-launch checklist item" — but neither attaches it to an epic or a date. Needs a decision: self-review checklist vs. paid legal review, and when in the timeline it happens (before or gating public launch). |
| **Terms of Service / Privacy Policy documents** | Not found anywhere in the docs — flagging since it's a prerequisite most people miss | A CCPA review needs an actual privacy policy to review against. No mention of either document existing yet in PRD, epics, or UX specs. Likely bundled into Epic 6 (Marketing & Entry Surfaces) as a page, but epics.md's Epic 6 story list doesn't currently list one. |

## 6. Worth a line, not urgent

| Item | Status |
| --- | --- |
| **Reverse-geocoding provider** (Google Places / Apple Maps / OSM Nominatim, for FR-18 venue suggestion) | Unchosen — PRD Open Question #7, deferred to Epic 5 (post-validation track, "may trail," doesn't block SM-1/SM-2). No action needed now; listed so it doesn't get forgotten when Epic 5 starts. |

---

## Scoping the email-provider work

Recommendation: **`bmad-correct-course`, not `bmad-create-story`.**

Reasoning: `bmad-create-story` drafts a dev-ready story file from a description that already exists in `epics.md` (that's exactly what happened for 2.3a and 2.3b — both were already stubbed out in the Epic 2 story list before context-engineering). The email-sending provider has **no existing entry anywhere in epics.md** — it's net-new scope discovered mid-epic, not a story waiting to be detailed. `bmad-correct-course` is the tool built for exactly this: a significant gap found during sprint execution that needs the epic/story backlog itself updated (new story inserted, possibly a one-line architecture-spine note added since production SMTP config is an AD-10/Supabase-Auth-adjacent decision) before a story file gets written against it.

Likely shape once correct-course runs: a new story in Epic 2 (natural fit — it directly unblocks Story 2.3a's confirmation email in production, and Epic 2 already owns the Supabase Auth configuration surface), scoped to (a) standing up a Resend-or-similar account + verified sending domain (manual, Arjun's call, same pattern as PROVISIONING.md), and (b) the coding half: uncommenting/filling `[auth.email.smtp]` in `config.toml` with env-var-sourced secrets, confirming it applies to both local and the eventual cloud project, and documenting the runbook the same way Story 2.1 documented Supabase provisioning.

Ready to run `bmad-correct-course` for this now if you want to proceed.
