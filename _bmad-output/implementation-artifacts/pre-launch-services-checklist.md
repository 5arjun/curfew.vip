# Pre-launch / production-services checklist

**As of:** 2026-07-27
**Owner:** Arjun (all items below are account-level/manual actions, not coding-agent tasks)
**Purpose:** single place tracking every external account/service Curfew needs between "runs entirely against the local Supabase stack" and "real production launch." Consolidates supabase/PROVISIONING.md, PRD §11, Architecture Spine Open Questions, and epics.md cross-cutting notes — none of which individually owned this list.

Update this file directly when an item's status changes; it isn't generated by a workflow.

---

## 1. No code blocker — do whenever convenient

| Item | Status | Notes |
| --- | --- | --- |
| **Vercel deploy** | Not started | No dedicated story; Architecture Spine names Vercel as the web host (Stack table, Deployment table) but nothing gates on it existing yet. Low friction — `vercel link` + connect the GitHub repo whenever you want a live preview/prod URL. |
| **Production domain name** | Not started — **not mentioned anywhere in PRD/Architecture/epics** | Nothing in the docs registers or even names a domain. This is a hidden dependency for several other rows below: Vercel custom domain, production Google/Apple OAuth redirect URIs, Apple's Sign In with Apple Services ID (requires a verified domain), and the email provider's sending-domain verification (SPF/DKIM/DMARC). Worth picking and registering early since domain verification (email, Apple) has its own lag. |

## 2. In progress

| Item | Status | Notes |
| --- | --- | --- |
| **Apple Developer Program enrollment** ($99/yr) | **In progress as of 2026-07-27** | Covers two stories: Story 2.3b's Sign In with Apple and Story 2.9a's macOS code-signing/notarization — one membership, confirmed shared. **Caveat surfaced while context-engineering 2.3b**: enrollment alone isn't sufficient for Sign In with Apple — it additionally needs a **Services ID with Sign In with Apple enabled**, a separate configuration step inside the developer account, which itself needs a verified/registered domain (ties to the domain gap above). Confirm this second step once enrollment clears. |

## 3. Tied to a specific story/epic — provision when that story starts

| Item | Forcing function | Notes |
| --- | --- | --- |
| **Supabase cloud project** (org + billing + prod project + GitHub preview branching) | Story 2.10 (agent token storage) or 3.2 (sync), whichever lands first | Runbook already written: `supabase/PROVISIONING.md`. Explicitly not gated to any epic — can run anytime, optional until a real consumer exists. Paid tier required (PITR + preview branching). |
| **Stripe** | Epic 7 (Stories 7.1–7.5) | Cleanly scoped already, nothing to do before Epic 7 starts. **One sub-item not covered by the epic's code scope**: Stripe's own account activation (legal entity/sole-proprietor details, bank account, identity verification) has its own review lead time, similar in kind to the code-signing certs below — worth starting that activation in parallel with, not after, Epic 7's first story if launch timing is tight. |
| **Windows OV/EV code-signing certificate** | Story 2.9b (Windows signed build) | **Missing from your original list and worth calling out explicitly**: this is a *separate* procurement from Apple Developer Program enrollment — different vendor, different cost, and per epics.md's own cross-cutting notes, the EV cert's identity verification "can take 1–3 weeks of wall-clock time with no code involved." The epics already accept a macOS-first launch fallback (Story 2.9a ships, 2.9b trails) if this drags — but if you want Windows at launch, this needs to start now, in parallel with Apple's, not after. Confirm whether you've started this; nothing in the docs suggests you have. |
| **Production OAuth app credentials** (Google Cloud Console + Apple Services ID, for the real domain) | Before flipping OAuth on for real users — logically after the domain + prod Supabase project exist | Story 2.3b's credentials are dev/local-only (registered against `127.0.0.1:54321`, Supabase's local Auth callback). Production needs its own registration pass once a real domain and real Supabase project exist. Also worth flagging as an **ongoing** dependency, not one-time: Apple's `client_secret` is a hand-generated ES256 JWT that **expires every 6 months** — a recurring renewal task, not a one-time setup cost. |
| **GitHub Actions CI secrets** (`SUPABASE_ACCESS_TOKEN`, `SUPABASE_PROJECT_REF`, code-signing certs, Tauri updater signing keypair) | Same trigger as the Supabase cloud project / Story 2.9a-c | Already flagged in PROVISIONING.md §4 and AR-14 — listed here only so this doc is the one complete picture. Do not wire these into CI early; each has an explicit "not yet" in its owning doc. |

## 4. Real gap — not owned by any story today

| Item | Status |
| --- | --- |
| **Email sending provider** (Resend or similar) | **Confirmed total gap** — not mentioned in PRD, epics.md, or the Architecture Spine. `supabase/config.toml`'s `[auth.email.smtp]` block is commented out (local-only `local_smtp` testing inbox is active instead). Story 2.3a's signup-confirmation email works today only because it's talking to the local Supabase stack; it has no path to sending a real email in production. See "Scoping the email-provider work" below for how this gets turned into a story. |

## 5. Compliance / legal — open PRD line items, not attached to any epic

| Item | Status | Notes |
| --- | --- | --- |
| **CCPA formal privacy review** | Outstanding pre-launch checklist item (PRD §11 item 4, Architecture Spine Open Questions #6) | Both docs already conclude US-only-at-launch makes a CCPA-level posture (not full GDPR) sufficient, and both already downgrade this from "open question" to "pre-launch checklist item" — but neither attaches it to an epic or a date. Needs a decision: self-review checklist vs. paid legal review, and when in the timeline it happens (before or gating public launch). |
| **Terms of Service / Privacy Policy documents** | Not found anywhere in the docs — flagging since it's a prerequisite most people miss | A CCPA review needs an actual privacy policy to review against. No mention of either document existing yet in PRD, epics, or UX specs. Likely bundled into Epic 6 (Marketing & Entry Surfaces) as a page, but epics.md's Epic 6 story list doesn't currently list one. |

## 6. Worth a line, not urgent

| Item | Status |
| --- | --- |
| **Reverse-geocoding provider** (Google Places / Apple Maps / OSM Nominatim, for FR-18 venue suggestion) | Unchosen — PRD Open Question #7, deferred to Epic 5 (post-validation track, "may trail," doesn't block SM-1/SM-2). No action needed now; listed so it doesn't get forgotten when Epic 5 starts. |

---

## Scoping the email-provider work

Recommendation: **`bmad-correct-course`, not `bmad-create-story`.**

Reasoning: `bmad-create-story` drafts a dev-ready story file from a description that already exists in `epics.md` (that's exactly what happened for 2.3a and 2.3b — both were already stubbed out in the Epic 2 story list before context-engineering). The email-sending provider has **no existing entry anywhere in epics.md** — it's net-new scope discovered mid-epic, not a story waiting to be detailed. `bmad-correct-course` is the tool built for exactly this: a significant gap found during sprint execution that needs the epic/story backlog itself updated (new story inserted, possibly a one-line architecture-spine note added since production SMTP config is an AD-10/Supabase-Auth-adjacent decision) before a story file gets written against it.

Likely shape once correct-course runs: a new story in Epic 2 (natural fit — it directly unblocks Story 2.3a's confirmation email in production, and Epic 2 already owns the Supabase Auth configuration surface), scoped to (a) standing up a Resend-or-similar account + verified sending domain (manual, Arjun's call, same pattern as PROVISIONING.md), and (b) the coding half: uncommenting/filling `[auth.email.smtp]` in `config.toml` with env-var-sourced secrets, confirming it applies to both local and the eventual cloud project, and documenting the runbook the same way Story 2.1 documented Supabase provisioning.

Ready to run `bmad-correct-course` for this now if you want to proceed.
