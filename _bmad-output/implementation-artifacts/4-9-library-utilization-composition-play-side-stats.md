---
baseline_commit: ec15b20f81631f310e5a95f598995f43c0a265a8
---

# Story 4.9: Library Utilization composition + play-side utilization stats

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a DJ,
I want Library Utilization to show whether I keep replaying the same tracks, which tracks I actually lean on, and which I played once and forgot,
so that the page tells me how I'm *using* what I own instead of showing me a single conversion number.

## Non-negotiables

Everything below is derived and argued later in this file. If context is lost, these ten lines are the contract.

1. `web/` only. No `agent/`, no `shared/`, no schema migration. One additive **local-dev-only** `supabase/seed.sql` is the single exception (**D-23**).
2. Low-confidence predicate = **`listModel.ts`'s `isLowConfidenceSet`** (`confidence.value < 1.0 || track_count < HERO_MIN_TRACKS`), **not** `styleEvolution.ts`'s bare `< 1.0` (**D-20** — the bare one does not exclude soundchecks; see GAP-2).
3. Track identity = normalized **`title` + `artist`**, never `track_id` (**D-18**). Do not reuse `playsByTrack` (**GAP-4**).
4. Repeat-track rate formula is fixed in **D-17**. Trailing window `REPEAT_WINDOW_SETS = 5` **predecessors**, excluding the measured set.
5. Similarity matrix capped at `SIMILARITY_MATRIX_SETS = 10` (**D-19**), cap stated in the UI, degrading to a ranked list at phone widths (**D-22**).
6. Rotation size is **fixed at trailing 60 days**, renders **outside** the `aria-label="Conversion"` landmark, states its own window (**D-21**).
7. Every module: `<section className="lu-module dz-shell">` + `dz-dots`, **every branch inside the shell**, own insufficient-history copy, accessible name that never states a figure the visible UI declined to state.
8. `no-hardcoded-colors.test.ts` bans hex, `rgba(`, `hsl(`, `oklch/lab/color-mix(`, **and the words `transparent`/`currentColor`** across all of `web/app/**`. Heatmap intensity rides `opacity` or discrete `tokens.css` steps.
9. No ranking vocabulary anywhere ("top", "best", "#1", medals, badges) — `DESIGN.md:199`.
10. Test at the pure-function layer **plus** `renderToStaticMarkup` prop-threading assertions (**D-24**). No RTL, no jsdom. Then a real-browser DOM measurement at 1440/375/320.

## Context & Authority

**Why this story exists (Arjun, 2026-08-07).** Library Utilization holds Story 4.3's meter, Story 4.7 AC-3's moved conversion trend, and Story 4.5's time-to-first-play. This story is the page's actual composition plus the batch of utilization stats that need **no agent work at all** — every one is computable from `plays` alone. It discharges the Epic-4 header note's "more components wanted… first named candidate: # of repeat tracks between sets" (`epics.md:796`, annotated `[DISCHARGED 2026-08-07]`).

**This is a `web/`-only story.** AD-3's frozen contract is untouched: every field these five metrics read (`title`, `artist`, `started_at`, `position`, `derived.confidence`) has been on `SyncPlay`/`SyncSetDerived` since the Story 1.10 freeze. AD-1's rule is what authorizes web-side aggregation — *"the line is raw-Serato derivation (edge-only) vs SQL over clean synced rows (cloud-legal)"*. AD-7 is untouched (no new table, no new policy). Say this out loud in the completion notes; it is the cheapest proof that "zero agent work" is a fact rather than an aspiration. The one deliberate exception is D-23's local-dev seed, which is not a schema change and never runs against production.

**Story 4.6 has landed, so this page reads real Supabase data.** `getRecentSets()` is bounded at `RECENT_SETS_LIMIT = 500` (`web/lib/sets/index.ts:105`), ordered newest-first server-side. Every metric here aggregates that already-bounded array — no new query, no new seam function, no unbounded scan.

### Five gaps found during story creation, not by `epics.md`

Story 4.7's Task 3 is the precedent: a gap named at story-creation time and resolved deliberately, rather than discovered mid-implementation and settled by whichever wiring was fastest to write.

**GAP-1 — AC-10 is a retrofit, not a wiring task; nothing on this page excludes low-confidence sets today.** `web/lib/sets/libraryConversion.ts` (990 lines) never reads `derived.confidence` — grep it. Story 4.3's meter, Story 4.2/4.7's trend, and Story 4.5's time-to-first-play all count soundcheck plays right now. AC-10 says "**this page** applies the same exclude-visibly contract Style Evolution does." Ruled in **D-20**.

**GAP-2 — there are THREE low-confidence predicates in this codebase, they disagree, and the obvious one is the wrong one.**
- `styleEvolution.ts:196` — module-private `isLowConfidence(set) = set.derived.confidence.value < 1.0` (Story 4.1's D-4: binary, no tier).
- `listModel.ts:86-89` — exported `isLowConfidenceSet(set) = confidence.value < 1.0 || trackCount < HERO_MIN_TRACKS` (`HERO_MIN_TRACKS === MIN_PLAYS_FOR_DETECTION === 6`, `dancefloor.ts:21`). Used by the dashboard's set list and most-played.
- `SetHeader.tsx:23` — a third, display-only `c.value <= 0.5 || c.track_count < 4`.

The trap: `agent/src-tauri/src/confidence.rs` is **symmetric by design** — its own doc comment says *"A session that's obviously 'not a set' (too few plays) and a session that's obviously a real set (long, naturally punctuated) both score `1.0`"*. `MIN_PLAYS_FOR_AMBIGUITY = 4`, and `LOW_CONFIDENCE_VALUE = 0.2` is only ever assigned to the dense-continuous ambiguous case. **So a 2–3 track soundcheck scores 1.0 and sails straight through `confidence.value < 1.0`** — the predicate AC-10's "same as Style Evolution" wording points at is precisely the one that does *not* do what AC-10 asks for. Ruled in **D-20**.

**GAP-3 — AC-7's rotation-size window is ambiguous as written, and the answer decides where the module renders.** "a trailing **60-day** window — matching Story 4.3's shipped conversion-rate default (60/30/14-day selectable scale)… so every window on this page stays consistent" reads either as *fixed at 60* or as *follows the shared dropdown*. Not cosmetic: `page.tsx:156-177` and `TimeToFirstPlay.tsx:33-39` establish a structural rule for this page — **window-governed modules render inside `LibraryUtilizationView`; window-independent modules render outside as siblings** — precisely so a control never appears to own a figure it cannot move. Ruled in **D-21**.

**GAP-4 — `playsByTrack` looks reusable and is not.** `page.tsx:74` builds one shared index and its comment warns that it used to be two and that the split caused a real bug — which invites reuse here. But `libraryConversion.ts:178-193` returns `Map<track_id, number[]>`: it `continue`s past any play missing `track_id` **and** any play with an unparseable `started_at`, and it carries **no set identity at all**. It therefore cannot answer AC-2, AC-4, AC-5 or AC-6, and its key space directly contradicts **D-18**. The one-shared-index rule is scoped to `playsByTrack`'s three conversion consumers. This story builds a **separate** set-membership index, and that is not a regression of the rule.

**GAP-5 — AC-11's real-browser pass is currently unperformable, and no prior story had this problem.** Story 4.6 retired the committed fixtures from the production read path (`web/lib/sets/index.ts:28-45`, explicit), production holds 0 sets / 0 plays (measured during Story 4.5's review), and there is **no seed file**: `supabase/config.toml:66-75` has `[db.seed] enabled = false` with `sql_paths = ["./seed.sql"]` pointing at a file that is not committed — and its own comment warns that enabling it while the file is missing breaks the migration-apply CI gate. So there is no way to get a populated page into a browser, let alone the 0/1/2/>10-set states AC-9 and AC-11 require. Story 4.5 could still swap trimmed fixtures; that route is gone. Left unaddressed, a dev agent will either fake the pass or re-point the seam at fixtures and silently revert 4.6. Ruled in **D-23**.

### Rulings — decided here so the dev agent does not have to settle them

AC-3 requires the repeat-track quantity to be "named as precisely as Story 4.1's amended AC-5 names 2^H — never left as 'a rate' for the implementation to settle," citing 4.1's D-10 lesson (a spec that described something other than what shipped). An agent asked to *rule* under time pressure picks whatever is fastest to write, which reproduces exactly that failure. So all of it is fixed here.

- **D-17 — Repeat-track rate.** For each surviving set that has at least one surviving predecessor: `(distinct tracks in that set that also appear in the up-to-5 surviving sets immediately preceding it) ÷ (distinct tracks in that set)`. The 5 are **predecessors only** — the measured set is not one of them. The surfaced figure is the **unweighted mean** of those per-set shares, with the **count of sets it averaged over stated alongside it**. The oldest surviving set has no predecessor and is excluded from the mean, never counted as 0% (D-8: a gap, never a fabricated zero). Unweighted is deliberate: the question AC-2 poses is "does a typical *night* repeat," so each night counts once regardless of length — and the small-set skew that would otherwise make this dangerous is exactly what D-20's predicate removes, which is why these two rulings must be implemented together or not at all. With exactly 2 surviving sets there is exactly 1 measurable set, which is why AC-2's gate is "≥2 captured sets."

  > **⚑ RESOLVED 2026-08-08 (Arjun): D-17 as written above is what shipped.** The single-ratio alternative was rejected and is not implemented anywhere in the code. This block is retained for the reasoning, not as an open question — it was left unannotated at implementation time while the Completion Notes said it was closed, which is the contradiction Story 4.9's code review caught. The original text follows.
  >
  > **⚑ OPEN (superseded — see above) — Arjun's ruling owed before dev starts.** A second create-story pass ran on this story concurrently (see the reconciliation entry at the top of `sprint-status.yaml`) and ruled the *rejected alternative* instead: **a single ratio** — "of the distinct tracks across your trailing 5 surviving sets, the share appearing in more than one" — rejecting the mean on Story 4.5's skew lesson. The two passes agreed on everything else. The trade: the single ratio is simpler and cannot skew, but reads only the most recent block and so cannot answer "does a *typical* night repeat," which is the question AC-2 poses; D-17 answers that and is protected from the small-set skew the alternative fears by the compound predicate **both** passes adopted in D-20. **D-17 as written above is what to build absent a ruling otherwise**; the alternative is a contained swap inside the builder plus its tests, not a re-architecture. Do not implement both and do not average them.
- **D-18 — Track identity for all five play-side metrics is normalized `title` + `artist`, NOT `track_id`.** (a) These metrics never join to `library_track_events`, which is the only thing `track_id` exists to make joinable (`shared/src/index.ts:128-144`); (b) `track_id` is `?: string | null` — optional post-freeze, `null` whenever the source carried no portable path — so keying on it would silently drop off-library tracks from a "what do I lean on" list; (c) mixing the two key spaces double-counts one track whose plays split between rows that do and don't carry a hash. This matches `rightColumn.ts:115`'s existing `JSON.stringify([title, artist ?? ""])` key exactly. **Do not "fix" this into agreeing with `libraryConversion.ts`'s `track_id` keying** — that file answers a different question. Plays with a `null` title are excluded and their count disclosed (AD-11's "never omitted, never guessed"; `rightColumn.ts:114`'s own `title != null` guard).
- **D-19 — The similarity matrix is capped at the 10 most recent surviving sets**, reusing `MOST_PLAYED_RECENT_SETS = 10` (`rightColumn.ts:83`) rather than inventing a second "recent" scale on the same product. Export it as a named constant, and **state the cap in the UI** whenever more surviving sets exist than the matrix shows — a silent top-10 truncation reads as "this is all your history."
- **D-20 — The page's low-confidence contract.** (i) It applies **page-wide** — the meter, the trend, time-to-first-play, and all five new modules read the same surviving-set population under one page-level reveal. AC-10 says "this page"; and a single visible "N sessions hidden — show them?" control sitting above modules it does not govern is the same "one control appearing to own a figure it does not move" failure `page.tsx:156-177` already rules against, made worse by the control actively misdescribing its own scope. (ii) The predicate is **`listModel.ts`'s `isLowConfidenceSet`**, not Style Evolution's — per GAP-2, the bare `< 1.0` form does not exclude the short soundchecks AC-10 exists to keep out of a repeat rate, and `rightColumn.ts:60-64` records the shipped bug (a 1-play soundcheck deciding most-played) that the compound form was added to fix. AC-10 requires the *exclude-visibly contract*; the affordance is what is shared, and the divergence in predicate is deliberate and must be written into a comment at all three definition sites. (iii) **Consequence that must not be missed: the reveal's copy currently says "N low-confidence sessions hidden," which under the compound predicate would misdescribe what it hid.** Either widen the copy (e.g. "N short or low-confidence sessions") or pass a copy override — do not ship a count whose noun is wrong.
- **D-21 — Rotation size is fixed at a trailing 60 days**, renders **outside** `LibraryUtilizationView` as a sibling of it, and **states its own 60-day window in its own copy**. This is AC-7's literal text ("across a trailing 60-day window"); following the dropdown would leave the tile violating its own AC at the 14-day selection. It also keeps the module a server component, and — decisively — `LibraryUtilizationView.tsx:39` renders `<section className="lu-conversion" aria-label="Conversion">`, so nesting a play-side stat there would announce it to assistive tech inside a landmark named "Conversion". Same placement rule as `TimeToFirstPlay`, applied in the same direction.
- **D-22 — The similarity view degrades to a ranked "most alike" list below the width at which its cells can each meet 24×24**, reusing the workhorses row markup. A 10×10 grid cannot meet 24×24 at 320px inside `.lu`'s gutters; expect the degraded form to be the phone experience rather than an edge case. This is Story 4.8 AC-11's pattern for the Camelot wheel, which is the same problem already solved once.
- **D-23 — Author `supabase/seed.sql` (local-dev only) and enable `[db.seed]`.** Generated from the retained `recent-sets.fixture.json` sample data, it is additive, is never applied to production, and closes GAP-5 for this story and every story after it. Two cautions, both from `config.toml`'s own comment: the file **must be committed** in the same change that flips `enabled = true`, or the migration-apply CI gate breaks; and confirm the additive-only migration guard (`supabase/scripts/check-additive-only-migrations.sh`) does not treat a seed as a migration. **Do not re-point `web/lib/sets/index.ts` at the fixtures** — that silently reverts Story 4.6 and is the failure this ruling exists to prevent.
- **D-24 — Adopt a `renderToStaticMarkup` assertion harness for prop-threading.** `deferred-work.md` carries this as an open recommendation with a proven prototype (7 assertions incl. a negative control, built during the 4.5 merge, deleted rather than committed because adopting `web/`'s first component-render test "deserves its own decision"). This story is that decision, and it is the story that most needs it: it threads optional disclosure/model props through several new hops, and `?: string | null` at every hop means `tsc` and `eslint` stay green while a line silently stops rendering. `react-dom` is already a dependency — no new packages. Scope it narrowly: string assertions over rendered markup, **no RTL, no jsdom**, and every new threaded prop gets a negative control that removes it and asserts the copy disappears.

## Acceptance Criteria

Reproduced verbatim from `epics.md` Story 4.9 (lines 948-958):

1. **Given** the page today (Story 4.3's meter, plus Story 4.7 AC-3's incoming conversion trend), **When** Library Utilization renders, **Then** it has a real composition — the conversion pair reading as one unit sharing a single window selection, with the utilization stats below — rather than components stacked in arrival order. *(UX-DR17)*
2. **Given** ≥2 captured sets, **Then** a **repeat-track rate** renders: how many tracks recur across nearby sets, as the "am I playing the same thing every night" counterpart to the aging shelf's neglect signal. *(Epic-4 header note, 2026-08-07; Decision B — cold-start-honest by construction, since it needs no play Curfew never saw)*
3. **Given** repeat-track rate needs a definition that cannot drift, **Then** "nearby" means a **trailing 5-set window** (ruled 2026-08-07, party mode: wide enough to be forgiving of gaps between gigs without reading as counting ancient repeats) and the surfaced quantity is named as precisely as Story 4.1's amended AC-5 names 2^H — never left as "a rate" for the implementation to settle. *(4.1's D-10 lesson: a spec that described something other than what shipped)*
4. **Given** the same overlap data, **Then** a **set-similarity** view renders it as a matrix/heatmap across recent sets — an aggregate, non-time-series display that reads off a small history, unlike the trend lines. *(Cold-start density, per Story 4.7's rationale)*
5. **Given** play history, **Then** a **workhorses** list ranks tracks by **number of sets they appeared in**, not by play count — deliberately a different question from the dashboard's most-played, and the story file must state that difference so the two surfaces are never "fixed" into agreeing. *(Distinct from Story 3.6's `rightColumn` most-played)*
6. **Given** play history, **Then** a **one-and-done** list surfaces tracks played exactly once — the actionable mirror of AC-5. *(FR-12's neglect theme, play-side)*
7. **Given** play history, **Then** a **rotation size** tile shows distinct tracks against total plays across a **trailing 60-day window** — matching Story 4.3's shipped conversion-rate default (60/30/14-day selectable scale, no 90-day option — corrected 2026-08-07 after Story 4.3's own review caught the mismatch against an earlier draft of this AC) so every window on this page stays consistent — rendering as e.g. "340 plays, 180 unique." *(New)*
8. **Given** every stat here counts only Curfew-captured plays, **Then** the page says so once, plainly, in the product's own voice — framed as the DJ's record rather than as a limitation, and **never** as elapsed subscription time. *(Decision B's binding copy rule — no "since you joined," no "in your N months")*
9. **Given** a DJ with 0 or 1 sets, **Then** each component renders its own positive-framed insufficient state (repeat rate and similarity genuinely need ≥2 sets) rather than the page blanking wholesale. *(UX-DR19; Story 4.7 AC-8's precedent)*
10. **Given** low-confidence sets, **Then** this page applies the same exclude-**visibly** contract Style Evolution does — soundchecks must not silently inflate a repeat rate. *(FR-27; Story 4.1 AC-2)*
11. **Given** 375px and 320px, **Then** the composition holds with no horizontal overflow and every target meets WCAG 2.2 AA SC 2.5.8's 24×24 minimum, **measured against the DOM in a real browser**. *(4.1's review lesson; UX-DR21, UX-DR22)*

## Tasks / Subtasks

> Order matters: Task 2 defines every metric's input population, and Task 3's seed is what makes Task 9's pass possible at all. Doing metrics first means redoing them.

- [x] **Task 1 — Read before writing** (no AC, prerequisite for all)
  - [x] `web/app/(authenticated)/library-utilization/page.tsx` in full (185 lines). Server component; fetches `getRecentSets`/`getLibraryAddEvents`/`getLibraryRoster` in one `Promise.all`; builds the shared `playsByTrack` index at line 74; renders `<LibraryUtilizationView>` (150) → `<TimeToFirstPlay>` (178) → one page-level `.lu-disclosure` (182). The render block is lines 142-184; the placement rationale comment is 156-177 and is the composition contract this story extends.
  - [x] `LibraryUtilizationView.tsx` (54 lines) — `"use client"`, owns exactly one thing: `useConversionWindowSelection()`. Its doc comment says verbatim: *"Story 4.9 is expected to add further Library Utilization components below this pair."* Line 39 is the `aria-label="Conversion"` landmark D-21 turns on.
  - [x] `TimeToFirstPlay.tsx` in full — the **module template**: `<section className="lu-module dz-shell" aria-label={summary}>` + `<span className="dz-dots" aria-hidden />` + `.lu-stat-head`/`.lu-stat-label`, with **every branch inside the shell** (its comment explains the shipped layout bug that rule exists to prevent). Copy this structure; do not invent a second module shape.
  - [x] `web/app/library-utilization.css` (203 lines). `.lu > .lu-module { max-width: 440px }` (79) is scoped to **direct children of `.lu`** — a module nested in a new grid wrapper loses the cap (that regression already shipped once; see the comment at 65-78). `.lu-module .se-empty` (90) re-sizes the shared insufficient-history block down for stat modules. `.lu` itself is `flex/column/gap:18px` with floating-nav bottom clearance and a `≥900.02px` nav-rail left inset (19-34) — both load-bearing, both recently repaired; do not disturb them.
  - [x] `styleEvolution.ts:190-198`, `listModel.ts:86-89`, `SetHeader.tsx:18-25`, and `agent/src-tauri/src/confidence.rs`'s `SessionConfidence` doc comment — all four, so GAP-2's symmetry trap is understood rather than taken on this file's word.
  - [x] `rightColumn.ts:83-132` (`mostPlayedInRecentSets`) — the dashboard surface AC-5 must stay deliberately different from: ranks by **play count**, over the **last 10 non-low-confidence sets**, scoped to each set's **detected dancefloor segment**, keyed on `[title, artist]` (115), guarded by `title != null` (114).
  - [x] `libraryConversion.ts:178-193` (`playsByTrack`) — confirm GAP-4 for yourself before writing the new index.
  - [x] `LowConfidenceReveal.tsx` and `ConversionWindowDropdown.tsx` — the reveal affordance to reuse, and the exact `useSyncExternalStore` shape any new persisted control must follow.
  - [x] `deferred-work.md` — the open items on **this page**, three of which this story touches: no `<h2>` anywhere and nested landmark regions (4.7's R-10, open); the merged three-module composition never rendered in a browser, with a named unverified defect (`TimeToFirstPlay`'s insufficient branch flattening a `dz-shell` into a pill with copy 4px from a curved edge); and the undated-add-date clause stated twice, flagged as needing one ruling for all three modules rather than two in opposite directions.
  - [x] Confirm by grep that no set-similarity, repeat-rate, workhorses, one-and-done or rotation-size computation exists in `web/lib/` today (it does not, as of story creation) — all five are new, not wiring.

- [x] **Task 2 — Implement D-20: the page's low-confidence contract** (AC: 10) — *before any metric; it defines every metric's input*
  - [x] Re-measure production read-only before retrofitting: Story 4.5's review closed an equivalent question by reading prod (0 plays / 0 sets) rather than reasoning about it, which is what makes "silently changing numbers the DJ has already seen" (`epics.md:997`) a zero-blast-radius hazard here. **If production is no longer empty, stop and flag** — the retrofit changes three shipped figures and that is Arjun's call, not the dev agent's. Record the measurement, not the inference.
  - [x] Export one shared predicate per D-20(ii) — `isLowConfidenceSet` from `listModel.ts`, or hoisted somewhere both pages can import. Leave a comment at **all three** definition sites (`styleEvolution.ts`, `listModel.ts`, `SetHeader.tsx`) naming the others and why they differ; three silently-divergent predicates for one concept is how the next one gets added.
  - [x] Precompute **both** populations (excluding / including) server-side up front and have the client swap which it renders, exactly as `styleEvolution.ts` does. No recomputation on click — the D-13 discipline `CONVERSION_WINDOWS` already follows on this page.
  - [x] Reuse `LowConfidenceReveal` (`hiddenCount`/`revealed`/`onReveal`/`onHide`, unpersisted `useState(false)` per D-4). Fix its copy per D-20(iii) so the noun matches what the compound predicate actually hid.
  - [x] The reveal is a second page-level client concern. Keep the new modules **server** components: a thin client wrapper that receives both prerendered subtrees as props is the shape to use, not dragging five modules across `"use client"` — the exact cost `page.tsx:170-174` argues against.

- [x] **Task 3 — Implement D-23: local-dev seed** (AC: 9, 11 — the enabler for both)
  - [x] Author `supabase/seed.sql` from `recent-sets.fixture.json`, committed alongside flipping `config.toml`'s `[db.seed] enabled = true`. Cover the states Task 9 must drive: a populated DJ, and enough shape to trim down to 0 / 1 / 2 / >10 sets, at least one low-confidence set, at least one short (<6 track) set, and at least one play with a `null` title.
  - [x] Verify `supabase db reset` applies migrations then the seed cleanly, and that `supabase/scripts/check-additive-only-migrations.sh` and the pgTAP gate are unaffected.
  - [x] Do **not** touch `web/lib/sets/index.ts`. If the browser pass seems to need it, that is the signal the seed is wrong, not the seam.

- [x] **Task 4 — The five pure metrics** (AC: 2, 3, 4, 5, 6, 7)
  - [x] Create **`web/lib/sets/libraryUtilization.ts`** — a new file, not additions to `libraryConversion.ts` (990 lines, scoped to the add-event/library join; these metrics never touch add-events). Mirror its house shape: exported constants, exported interfaces, one `build*` per model, one `*Summary(model): string` generator per module for the accessible name.
  - [x] Implement **D-18's identity helper first** and key all five through it. One helper, not five inline `JSON.stringify` calls. Build a **new** set-membership index per GAP-4 — `Map<trackKey, Set<setId>>` plus per-set distinct-track sets — never `playsByTrack`.
  - [x] **Undated sets** (`started_at: null`) must be handled explicitly, not by accident — they decide "the 10 most recent," the 60-day window, and D-17's ordering. Follow the two existing precedents: `rightColumn.ts:87-91`'s `startMs` (sorts undated last via `-Infinity`, never `NaN` into a comparator) and `styleEvolution`'s `undatedCount` disclosure. Silence here is a silent drop.
  - [x] **Repeat-track rate** per **D-17**, constant `REPEAT_WINDOW_SETS = 5`. Compute the pairwise overlap once and let AC-4's matrix read the same data.
  - [x] **Set similarity**: pairwise Jaccard (`|A ∩ B| ÷ |A ∪ B|`) over distinct tracks, across `SIMILARITY_MATRIX_SETS = 10` (**D-19**), returning the matrix plus the total surviving-set count so the UI can state the cap. Axis labels come from `session_label` via `formatSessionLabel` — **never `external_id`**, which post-4.6 is a uuid (`web/lib/sets/types.ts` documents the `SET 872d5614-…` regression this already caused once).
  - [x] **Workhorses**: rank by **count of distinct sets a track appears in**, ties broken by total plays then first-seen order — a total order, per `getRecentSets`' own comment on why a comparator returning 0 is a defect. Whole sets, **not** dancefloor-segment-scoped: that segmentation belongs to the dashboard, and inheriting it would add a second unintended difference on top of AC-5's one intended difference.
  - [x] **One-and-done**: tracks whose total play count across the surviving population is exactly 1 (a track played twice within a single set does not qualify). Ordered most-recently-played first — actionable, not alphabetical.
  - [x] **Rotation size**: distinct tracks and total plays over a fixed trailing 60 days (**D-21**), returning both figures plus the window it used.
  - [x] Export named constants for the visible row caps on workhorses and one-and-done — do not leave them as inline literals; D-19 pins the matrix's cap and these deserve the same treatment.
  - [x] Every model returns explicit `null`/absent for "not enough data," never a fabricated `0` — D-8's discipline, and the class of bug Story 4.7's R-2 caught (an excluded-count reported as 0 precisely when 100% of the data was excluded).
  - [x] New `web/lib/sets/libraryUtilization.test.ts` in the existing vitest style. Cover at minimum: D-17's mean with the oldest set correctly excluded; the exactly-2-sets boundary; a track whose plays split across rows with and without `track_id` counting once (D-18's double-count guard); `null`-title exclusion plus its disclosed count; Jaccard on disjoint and identical sets; the >10-set truncation reporting its cap; workhorses ties resolving to a stable total order; one-and-done excluding a track played twice in one set; undated sets not poisoning any ordering; and a **short high-confidence set** (3 tracks, `confidence.value === 1.0`) being excluded — the D-20 regression guard, and the one test that would catch a revert to the bare predicate.

- [x] **Task 5 — Components** (AC: 2, 4, 5, 6, 7, 9)
  - [x] New components under `web/app/components/library-utilization/`, each following `TimeToFirstPlay.tsx`'s shell exactly — `.lu-module dz-shell` + `dz-dots` + `.lu-stat-head`/`.lu-stat-label`, section accessible name carrying the whole sentence, visible content `aria-hidden`, **every branch inside the shell**.
  - [x] AC-9: each module owns its **own** positive-framed insufficient state via `InsufficientHistory` with **module-specific copy**. Do not reuse another module's string — Story 4.3 Task 4 and Story 4.5 both ruled on this, and 4.5's review found a gate counting *adds* while its copy told the DJ to go *play* tracks. The gate and the sentence must describe the same quantity.
  - [x] AC-9 trap from 4.5's browser pass: when a module is below its gate and states no figure, its **accessible name must not state one either**. The `*Summary()` generators are gate-blind by design; fall back to naming the region and let `InsufficientHistory`'s own `role="status"` carry the copy.
  - [x] Workhorses / one-and-done rows: title + artist, `"Unknown"` for a missing artist (AD-11; `rightColumn.ts:116`). Cap visible rows with a "load more" — `EXPERIENCE.md:108` bans infinite scroll on track lists. **No ranking vocabulary**: no "top", "best", "#1", no medals or badges. `DESIGN.md:199` — *"There is a total absence of competitive social cues — no 'best,' 'winner,' or ranking language, ever."* A list of the DJ's own tracks against their own baseline is fine; the words around it are not.
  - [x] Rotation size renders AC-7's shape ("340 plays, 180 unique") through the existing `.lu-stat-value`/`.lu-stat-readout` classes, **and names its own 60-day window** (D-21).

- [x] **Task 6 — Compose the page** (AC: 1, 9)
  - [x] Recommended order, and write down whichever is chosen: header → AC-8's page-level line → page-level reveal → conversion pair (unchanged) → rotation size → repeat-track rate + set-similarity (one story, the way the meter and trend are) → workhorses + one-and-done (a pair) → time-to-first-play → page-level disclosures.
  - [x] **Compose for what lands next, not just for today.** Story 4.4's aging shelf is unblocked (`epics.md:842`, 2026-08-08) and Story 4.10's track search + `/track/[track_id]` route explicitly targets this page (`epics.md:970`) — `epics.md:944` calls 4.9 "deliberately paired with Story 4.10." Leave slots, the same courtesy 4.7 extended to 4.8. Note also that 4.10 AC-11 reuses the exclude-visibly contract Task 2 builds, so build it as a page-level primitive rather than something welded to these five modules.
  - [x] AC-6's one-and-done and 4.4's aging shelf are both "neglect" lists on one page and must read as complements: one-and-done is about tracks the DJ *did* play and dropped; the shelf is about tracks never reached at all. Write that distinction into a comment now, before both exist.
  - [x] **`deferred-work.md`'s R-10 is about to get worse.** `/library-utilization` today has no `<h2>` at all, and its group heading is a `<p className="lu-stat-label">` whose text duplicates the section's `aria-label`. This story adds five or more `<section aria-label>` modules on top. Ruling it is in scope here because this story is what makes it acute: give the page real `<h2>` headings and drop the duplicated `aria-label`s, or switch module `<section>`s to `<div role="group">`. Do not add five more anonymous landmarks and leave a "check the count is not absurd" note.
  - [x] CSS in `web/app/library-utilization.css`, `lu-` prefixed. **Watch `.lu > .lu-module`**: a module nested in a new grid wrapper is no longer a direct child and loses its width cap. Either keep new modules as direct children or give the wrapper its own explicit cap — do not leave it to inheritance.
  - [x] `no-hardcoded-colors.test.ts` (24-33) rejects, across all `web/app/**` `.ts`/`.tsx`/`.css`: hex, `rgba(`, `hsla?(`, `oklch|oklab|lab|lch|color|color-mix(`, and the bare words `transparent` and `currentColor`. The heatmap's intensity ramp must therefore ride `opacity` or discrete `tokens.css` steps — decide this before writing the ramp, not at the gate.

- [x] **Task 7 — The similarity view's accessibility** (AC: 4, 11)
  - [x] Ship a **text equivalent** naming the most-alike pair(s) and their share; make it **keyboard-operable if any cell is interactive**; degrade per **D-22**.
  - [x] Intensity must **not** be color/opacity alone (UX-DR21, `epics.md:127`; `EXPERIENCE.md:115`; WCAG 2.2 SC 1.4.1). Carry the number in text or in the cell's accessible name.
  - [x] Record in the completion notes that the Stitch mock's `overflow-x: auto` over `min-w-[600px]` (`code.html:305-306`) is **rejected** as incompatible with AC-11, so the next reader does not mistake the divergence for an oversight. The mock is a reference render; `EXPERIENCE.md:31` gives the spine precedence, and it predates the Ice Cyan / Abyss redesign besides.

- [x] **Task 8 — AC-8's page-level copy** (AC: 8)
  - [x] One line, once, under the header — it governs modules above and below it, so it cannot live inside any module. Subject to Decision B's binding copy rule: **never** "since you joined", **never** "in your N months", **never** framed as a limitation. Candidate, to be checked against `EXPERIENCE.md`'s Voice and Tone table rather than shipped unread: *"Everything here counts the sets Curfew has captured — your record, building from here."*
  - [x] Check it neither duplicates nor contradicts the existing subtitle ("How much of your library actually makes it to the dancefloor.") or the disclosure lines already on the page. **The real topology, which is not obvious:** there is exactly **one** page-level `.lu-disclosure` (`page.tsx:182`); the *second* undated-clause line lives **inside `LibraryConversionTrend`**, in the window-governed section, and the two already share an opening clause. `deferred-work.md` flags that duplication as open and unruled and asks for one ruling covering all three modules rather than two in opposite directions. Do not add a third line into that situation without reading that entry.

- [x] **Task 9 — Responsive + accessibility pass** (AC: 9, 11)
  - [x] Against the D-23 seed on a live dev server, at 1440px, 375px **and** 320px — **measure the DOM** (`getBoundingClientRect()` via Playwright). Do not eyeball a screenshot. This is 4.1's review lesson, restated in 4.7 AC-10 and 4.5's pass; both of those found defects *after* the code looked correct.
  - [x] Verify: no horizontal overflow at either narrow width; every interactive target ≥24×24 (SC 2.5.8) — dropdown-adjacent controls, "load more", any interactive matrix cell. The reveal toggle inherits `.se-hidden-toggle`, already ruled non-blocking under SC 2.5.8's Inline exception — do not "fix" it.
  - [x] Drive **every** state, not just the populated one: 0 sets, exactly 1, exactly 2, low-confidence hidden **and** revealed, >10 sets (matrix cap), a `null`-title track, and the D-22 degraded form. Story 4.5's pass found three defects living exclusively in non-populated branches.
  - [x] While you are here with a browser and a seed, close `deferred-work.md`'s "the merged three-feature composition has never been rendered in a browser" — including its named suspect: `TimeToFirstPlay`'s insufficient branch nesting a `dz-shell` (2px border, `border-radius: 50px`) flattened by `.lu-module .se-empty { min-height: 0 }` to ~40px, which would render as a pill with copy 4px from a curved edge.
  - [~] Zero console **errors** at all three widths in every state — met. Zero **warnings** — NOT met, and deliberately not rounded down: headless-Chromium WebGL driver messages ("GPU stall due to ReadPixels") from the `SilkBackdrop` canvas were observed. They are GL-emulation noise from the test environment rather than application output, but the subtask as written is not satisfied, so this is a recorded disposition rather than a tick. (Corrected by code review 2026-08-08; it was checked `[x]` while the notes below reported the warnings.)

- [x] **Task 10 — Doc sync owed by this story** (standing discipline)
  - [x] **PRD-sync.** `prd.md` §4.4 enumerates exactly four things — "whether recently-added tracks make it into sets, conversion rate, aging shelf, and time-to-first-play" — and has **zero** coverage of any of the five metrics here (grepped at story creation). Same accretion shape FR-9's sectioned composition and FR-10's §4.3→§4.4 move both had to close. Add a new FR (next free number is **FR-30**; max today is FR-29) or five consequences under §4.4, **and** amend §4.4's Description, whose four-item list is now incomplete.
  - [x] **`epics.md`.** AC-3 requires the quantity to be *named*; D-17's formula belongs in epics.md AC-3, not only in this file, or the next reader re-derives it. Same for D-20's predicate divergence against AC-10.
  - [x] **`EXPERIENCE.md` Component Patterns** (62-80) has three Library Utilization rows. Add rows for the new modules, following the precedent already inline at line 77 for Story 4.5: *"(Story 4.5, shipped 2026-08-07 — no prior wireframe existed for this component.)"*
  - [x] Record GAP-4's mock divergence against the reconcile pass that claimed "no orphaned functionality found" for this screen — the heatmap and the "High Velocity Flow" module (`code.html:255`) are in the mock and in neither spine.

- [x] **Task 11 — Verification & gates** (AC: all)
  - [x] From `web/`: `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build` — all clean. **Baseline at story creation: 399 tests across 24 files, passing** (verified against `ec15b20`). Report the new total; never report a total that went down without saying why.
  - [x] Supabase gates unaffected by D-23: `supabase db reset` + pgTAP + the additive-only guard.
  - [x] Confirm no `agent/` or `shared/` file is in the final diff, and that the only `supabase/` changes are the seed and its `config.toml` flag. If anything else appears, the "zero agent work" claim in Context is wrong and must be corrected rather than quietly left standing.
  - [x] Grep the diff for temporal and numeric claims before calling it done — see recurring failure mode 2 in Dev Notes.

### Review Findings

Code review 2026-08-08 — three parallel layers (Blind Hunter, Edge Case Hunter, Acceptance Auditor), 34 raw findings deduped to 35 tracked items. Gates independently re-run and confirmed green: **476 tests / 26 files**, lint, typecheck, build. Scope claim verified — no `agent/`, `shared/`, or `supabase/migrations/` file in the diff.

**Decisions needed**

- [x] [Review][Decision] **RESOLVED 2026-08-08 (Arjun): correct the comments AND add a refuse-to-seed guard.** Implemented and verified live against the local stack — positive control (a foreign `auth.users` row) raises and rolls back; negative control (dev account only) passes silently. The guard is emitted by `generate-seed.mjs` into `seed.sql`'s transaction, and all three false claims (`config.toml`, `generate-seed.mjs`, `seed.sql`) now state the real constraint. Original finding: **The seed's "never applied to a linked project" safety claim is false, and the seed creates a working confirmed login with a plaintext password** — `supabase db reset --linked` and `--db-url` both exist (verified against the installed CLI), and `--no-seed` exists precisely because reset seeds by default. So `db reset --linked` DOES apply `seed.sql` to a remote project, inserting `dev@curfew.local` with `crypt('curfew-dev-password', …)` and `email_confirmed_at = now()`. The claim is asserted as settled fact in three places (`supabase/config.toml:78`, `supabase/scripts/generate-seed.mjs:22`, `supabase/seed.sql:9`), which is what stops the next reader checking. Correcting the comments is unambiguous; whether to add a hard guard (e.g. abort the seed unless the target is localhost) is your call.
- [x] [Review][Decision] **RESOLVED 2026-08-08 (Arjun): cap the model and state the cap.** `TRACK_LIST_MAX_ROWS = 50` now bounds both builders; `totalRowCount`/`truncated` are surfaced so each module states "your 50 most-carried tracks, of 396" when the cap bites — D-19's treatment applied to the row lists. Both summaries name `totalRowCount`, never the cap, so the accessible name states the DJ's real figure. Original finding: **"Load more" loads nothing — every row is already in the DOM, twice** — `TrackRowList.tsx:34-52` renders `rows.slice(visibleRows)` in full inside `<details>`, and the models return uncapped rows. Using this story's own measured figures (workhorses 345→396, one-and-done 745→835), the page emits ~1,180 `<li>` per subtree and ~2,360 across both prerendered subtrees to display 12. `prop-threading.test.tsx:2207` pins the hidden rows as present, so this reads as intended. Needs a ruling on the model-level cap (a number, or real pagination) before it can be patched.

**Patches**

- [x] [Review][Patch] Raw NUL byte makes `Workhorses.tsx` a binary file to git — invisible in `git diff`, `git log -p`, PR review and `grep -r` [web/app/components/library-utilization/Workhorses.tsx:53]
- [x] [Review][Patch] Heatmap intensity `opacity` is set on the cell element, fading the percentage text that is supposed to make it not-colour-alone (0-10% cells render their own label at 8-17% opacity) [web/app/library-utilization.css:437, SetSimilarity.tsx:141]
- [x] [Review][Patch] Rotation size renders a fabricated `0 plays, 0 unique` — the null-guard keys on sets-in-window, not on identified plays (D-8 / Story 4.7 R-2 shape) [web/lib/sets/libraryUtilization.ts:784-785]
- [x] [Review][Patch] One-and-done's insufficient copy asserts "every track you've played has come round again" at 0 plays — the state every user sees on empty production [OneAndDone.tsx:16]
- [x] [Review][Patch] `deferred-work.md` is untouched while the notes claim three of its entries closed or ruled (R-10, composition-never-browser-rendered, D-24) [deferred-work.md:52,59,62,72]
- [x] [Review][Patch] Three surviving justifications cite an `aria-label="Conversion"` this same diff deletes [RotationSize.tsx:30, page.tsx:254, EXPERIENCE.md:82, prd.md FR-30]
- [x] [Review][Patch] The utilization disclosure says "not counted in the stats above" but renders below modules it does not govern, and workhorses/one-and-done DO count undated sets [web/lib/sets/libraryUtilization.ts:818-831]
- [x] [Review][Patch] The whole page body is computed and serialized twice on every request even when `hidden.length === 0` and the control never renders [page.tsx:119-120]
- [x] [Review][Patch] Workhorses and one-and-done announce their headline figure twice — `aria-label={summary}` plus un-`aria-hidden` visible content, unlike every other module on the page [Workhorses.tsx:39-49, OneAndDone.tsx:39-49]
- [x] [Review][Patch] Repeat-rate copy says "the 5 sets before each one" unconditionally, including at 2 surviving sets where only 1 predecessor exists [RepeatTrackRate.tsx:58, libraryUtilization.ts:418]
- [x] [Review][Patch] Set-similarity insufficient copy states a reason that is not the gate — 2+ sets with one having no identified tracks empties `ranked` and tells the DJ to play a set they already played [SetSimilarity.tsx:12-13, libraryUtilization.ts:522]
- [x] [Review][Patch] Null `session_label` falls back to the literal "Untitled set" — two such sets give duplicate React keys on both axes and a text equivalent reading "Untitled set and Untitled set are your most alike sets" [libraryUtilization.ts:272, SetSimilarity.tsx:75,81]
- [x] [Review][Patch] Workhorses summary's "N of them" takes the track count as antecedent while stating a set count — "1 track has carried… appeared in 12 of them" [libraryUtilization.ts:642-645]
- [x] [Review][Patch] Heading outline files the add-side `TimeToFirstPlay` and both page-level disclosures under `<h2>Tracks</h2>` [page.tsx:283,311,327-328]
- [x] [Review][Patch] `LibraryConversionTrend` renders a bare `<div>` with no heading and no accessible name — the only module on the page without one, so R-10's outline fix is incomplete [LibraryConversionTrend.tsx:48]
- [x] [Review][Patch] Row components build ad-hoc React keys instead of routing through D-18's `trackKey`; one-and-done's space separator can collide across a title/artist boundary shift [OneAndDone.tsx:52, Workhorses.tsx:53]
- [x] [Review][Patch] The seed ignores `library-roster.fixture.json` [generate-seed.mjs:9-10] — **patched, but the finding's premise was WRONG and the browser pass caught it.** The seed now emits 653 `library_roster` rows, and that is worth keeping as groundwork for Story 4.4/4.10. It does **not** make Story 4.11's disclosure drivable, because `getLibraryRoster()` (`web/lib/sets/index.ts`) is a hardcoded stub returning zeros and never reads the table — verified in a browser, the line still does not render. The real blocker is that `excludedNoIdentityCount`/`totalCatalogueRows` are scan-level scalars with no cloud carrier at all, which the stub's own doc comment already flags as needing a named decision. Comments in `generate-seed.mjs` and `deferred-work.md` were corrected to say this rather than leave the false justification standing.
- [x] [Review][Patch] Empty-string title passes the `title == null` guard and merges every such play into the single phantom track the doc comment says it prevents [libraryUtilization.ts:240]
- [x] [Review][Patch] Empty-string artist bypasses the `"Unknown"` fallback (`""` is not nullish) and renders a blank artist cell, against AD-11 [libraryUtilization.ts:249]
- [x] [Review][Patch] Rotation readout is unpluralized ("1 plays") and its aria name uses "track" where the visible text says "plays" [RotationSize.tsx:67-68, libraryUtilization.ts:800-802]
- [x] [Review][Patch] `${p.position}` is the one seed value interpolated without `q()` — a fixture row missing `position` emits the bare token `undefined` and aborts `db reset` mid-seed [generate-seed.mjs:193]
- [x] [Review][Patch] An empty fixture array emits `insert into … values\n;` — syntactically invalid SQL from a script that reports success [generate-seed.mjs:166-171,177-183]
- [x] [Review][Patch] `trackKey`'s doc comment says "Normalized"; the function trims nothing and case-folds nothing, so `"Deep End"` and `"Deep End "` are two tracks [libraryUtilization.ts:105,121-123]
- [x] [Review][Patch] Story file contradicts itself — the Change Log says Task 10 was "deliberately held" while the Completion Notes say it completed and every Task 10 box is `[x]`; D-17's `⚑ OPEN` block was never annotated despite the notes calling it closed [story:65,268,309]
- [x] [Review][Patch] "20 assertions incl. 5 negative controls" — there are 8 `// NEGATIVE CONTROL` blocks [story:286, prop-threading.test.tsx]
- [x] [Review][Patch] Task 9's "zero console errors **and** zero warnings" is ticked while the notes honestly report WebGL warnings — should be a recorded disposition, not a tick [story:160,260]
- [x] [Review][Patch] Four planning docs assert "shipped" for a story still in `review` [epics.md AC-3/AC-10, EXPERIENCE.md:78-82, prd.md §4.4, reconcile-stitch-screens.md]
- [x] [Review][Patch] `LibraryUtilizationReveal` (`"use client"`) imports `LU_HIDDEN_SETS_DESCRIPTOR` from the 832-line `libraryUtilization` module for one string [LibraryUtilizationReveal.tsx:4]
- [x] [Review][Patch] `SetSimilarity`'s doc comment claims the ranked list is the accessible content, but `.lu-sim-ranked` is `display: none` at ≥620px, so on desktop neither grid nor list is in the a11y tree [SetSimilarity.tsx:41-43, library-utilization.css:452,481-487]

**Post-review browser pass — 2026-08-10, against the patched build**

Re-run because the review's patches changed rendered output and the prior AC-11 evidence was therefore stale. Production build (`pnpm build` + `pnpm start`, port **3011** — port 3000 was another session's server and was left running), against a full `supabase db reset` with the regenerated seed. Local stack had to be restarted first (Docker Desktop was down); `docker restart supabase_kong_name-pending` after the reset, per the standing local-stack quirk.

- **The heatmap fix is confirmed at the pixel level, not just in the source.** Across all 90 populated cells, element-level `opacity` is now **1** on every one, with the ramp isolated on `::before`. The faintest cell (`0%`, intensity `0.08`) renders its own label at **full opacity**; before the fix that label composited at 0.08. Zero cells missing the `.lu-sim-cell-value` span. Verified in the revealed subtree too, which is a second independently-rendered copy of the body.
- **Heading outline, as intended:** `H1 Library Utilization → H2 Conversion → H3 Conversion rate / Conversion trend → H2 Rotation → H3 Rotation size / Repeat tracks / Set similarity → H2 Tracks → H3 Workhorses / Played once → H2 First play → H3 Time to first play`. `Conversion trend` now has a heading (it had none), and `Time to first play` is out from under "Tracks". **Landmark count still 2** (`main` + `nav`).
- **No horizontal overflow at 1440 / 375 / 320** (`scrollWidth === clientWidth`, and zero elements extending past the viewport in `main`). The two review-added truncation lines overflow at neither narrow width. D-22 degradation confirmed by computed style: grid `display: none`, ranked list `display: flex` at both 375 and 320.
- **SC 2.5.8:** the only sub-24px targets are the two already ruled — `se-hidden-toggle` (Inline exception, explicitly not to be "fixed") and `se-chart-info-btn` (15×15 box), which was **hit-tested** rather than trusted: all four corners of a 24×24 box resolve to the button via its `::after` extender. The load-more `<summary>` does not appear in the sub-24 list.
- **Row caps live:** 100 track rows in the DOM (6 visible + 44 hidden, ×2 lists) where the uncapped build emitted 1,090. Both caps state themselves — *"Your 50 most-carried tracks, of 345"*, *"Your 50 most recent, of 745"*.
- **D-20 still moves every figure together** on reveal: sets 42→58, workhorses 345→396, played-once 745→835, null-title plays 65→93, matrix cap "10 of 42"→"10 of 58".
- **The corrected 0-set copy was driven and is right:** Played once now reads *"Nothing here yet. Once you've played a few sets…"* instead of asserting *"every track you've played has come round again"* to a DJ with no plays. Every module's accessible name states no figure in that state.
- **Zero console errors and zero warnings** at all three widths, populated and empty. (The WebGL/`SilkBackdrop` noise recorded in the original pass did not reproduce in this browser.)
- **Seed guard verified end-to-end, both directions:** a fresh `db reset` seeds cleanly (guard passes on an empty `auth.users`); re-applying `seed.sql` to the already-populated DB also passes (dev account only); and injecting a foreign `auth.users` row makes it `raise` and roll back with nothing written. **209 pgTAP tests pass** with the roster rows present.

**Merge of `main` (15 commits: Stories 4.4 and 4.8) — 2026-08-10**

The PR could not run CI at all: GitHub reported it `CONFLICTING`, and a conflicting PR has no merge ref, so `pull_request` workflows never fire. Merged `origin/main` and resolved 8 conflict hunks across 5 files.

- **Story 4.4 put its aging shelf on this page**, which Task 6 had left a slot for. Kept at 4.4's shipped placement (a sibling below `TimeToFirstPlay`) rather than moved next to one-and-done as Task 6 suggested — re-siting shipped UI is not a merge's call. The suggestion is recorded as still open in `page.tsx`.
- **`buildAgingShelf` was auto-merged INTO `renderBody`**, where `roster` and `observationStartMs` are out of scope — caught by `tsc`, not by reading. Both are now threaded as parameters, which is the correct outcome anyway: the shelf's `plays` argument is the per-population index, so it moves with the D-20 reveal instead of being the one module that silently keeps counting soundchecks.
- **Convergent fix, worth recording:** Story 4.4's browser pass independently found and fixed the same `.lu-module .se-empty` radius defect 4.9 did, with the same diagnosis (scale the radius, leave the height alone). 4.4's values shipped first and are kept; 4.9's were not better, just later.
- **Two defects the merge itself introduced, both caught in the browser rather than by review:** the CSS resolution left a comment unclosed and dropped `border-radius`, breaking the Tailwind build; and adding an `<h2>Shelf</h2>` put two H2s over one module, since `AgingShelf` renders its own. Both fixed.
- **Correction to a review patch, now that main has landed.** The review's roster-seeding patch was justified as making Story 4.11's disclosure drivable; that was wrong pre-merge (the seam was a stub) and is still wrong post-merge for the disclosure — but the rows turned out load-bearing for a different reason: 4.4 made `entries` a real read, and the seed is what populates the shelf locally (198 rows, verified). The two scalars the disclosure needs still have no carrier. Both `generate-seed.mjs` and `deferred-work.md` now say this accurately.
- **Left deliberately unchanged:** `AgingShelf` renders a `<section aria-label>`, so the landmark count is 3 rather than the 2 R-10's fix established. That is 4.4's shipped markup, logged rather than edited during a merge.

Post-merge gates: **596 tests / 28 files**, lint, typecheck, build all clean. Browser re-verified at 1440: outline correct with a single `H2 Aging shelf`, no overflow, heatmap ramp still isolated from its text across all 90 populated cells, row caps holding at 100 rows.

**Owed before this story can be called done — created BY the review, not found by it**

- [x] **Re-run AC-11's browser pass.** Done 2026-08-10 — see the record above.

**Deferred**

- [x] [Review][Defer] Two of three pgTAP global-count assertions remain unscoped — deferred, pre-existing [supabase/tests/agent_status_isolation_test.sql:34, deleted_sets_tombstone_test.sql:54]
- [x] [Review][Defer] `SIMILARITY_MATRIX_SETS` aliases the dashboard's `MOST_PLAYED_RECENT_SETS`, so a dashboard-motivated retune silently widens the matrix and falsifies FR-30 — deferred, as-ruled by D-19 [web/lib/sets/libraryUtilization.ts:433]
- [x] [Review][Defer] `SetSimilarity`'s on-screen cap statement counts only dated survivors — deferred, unreachable while `sets.started_at` is NOT NULL [web/lib/sets/libraryUtilization.ts:514-515]

**Dismissed (1):** all-undated-sets collapsing three modules to "play another set" copy — unreachable (`sets.started_at` is `NOT NULL`) and already covered by the page-level undated disclosure.

## Dev Notes

- **The two highest-risk items are Task 2 and Task 3, not the metrics.** The metrics are arithmetic over an array. Task 2 decides the input population for *eight* modules, three already shipped — get it wrong and every figure is subtly wrong *together*, which is the hardest defect class to see, because nothing disagrees with anything. Task 3 decides whether AC-11 can be honestly satisfied at all.
- **AC-5's "never fixed into agreeing" is a specific instruction.** Dashboard most-played and this page's workhorses answer different questions on purpose: *play count, last 10 non-low-confidence sets, dancefloor-segment-scoped* versus *count of distinct sets appeared in, whole sets, the surviving population*. A future reader will notice and be tempted to reconcile them. Put the difference in a doc comment on the workhorses builder itself, not only in this file — the comment is what survives into code review.
- **`getRecentSets()` is capped at 500 sets.** Nobody is near it, but "lifetime" in AC-5/AC-6 means "within the 500 most recent sets," and that becomes a silent truncation the moment someone crosses it. Name it in a comment; do not add a second query to work around it. (Related, already open in `deferred-work.md`: a lifetime-scope metric fed by a seam named `getRecentSets()` is a standing hazard on this page, not a new one.)
- **Testing convention, precisely.** All 399 existing tests are pure-function vitest suites over `web/lib/**` plus copy and static-analysis guards — no RTL, no jsdom. The house rule is *logic in `lib/sets/*`, components stay thin*, and it still holds. D-24 does not overturn it: `renderToStaticMarkup` string assertions are a narrow addition for prop-threading only, because `?: string | null` at every hop means a dropped prop is invisible to `tsc`, `eslint` and every existing test. Do not let it grow into a component-testing framework.
- **Recurring failure modes across this page's last three stories** — the findings that repeat:
  1. *aria/visible drift.* An accessible name stating a figure the visible UI declined to state, or stating a count twice in two registers (4.5, twice — once in a branch the code review could not see).
  2. *Copy that outlives its premise.* A string promising "their 90 days" after the 90-day window was retired **by that same story** (4.7 R-4); doc comments pointing at a file the story deleted (4.7 R-5).
  3. *Disclosure counts dropping to 0 exactly when everything was excluded* (4.7 R-2) — the "never omitted" contract failing in the one case it exists for.
  4. *An AC asserted in the completion notes but never implemented* (4.7 R-3). Re-read each AC against the running page, not against the notes.
  5. *Shared CSS re-scoped for one call site, breaking another* (`.lu-module`'s width cap; `.se-empty`'s min-height — both on this page, both invisible to every gate).
- **Do not relitigate settled decisions while touching this code:** the 60/30/14 window scale and its single shared dropdown (4.7 Task 3), the meter's disclosure dedup to page level (4.5), `TimeToFirstPlay` living outside `LibraryUtilizationView`, the one shared `playsByTrack` index for its three conversion consumers, and D-4's unpersisted reveal.
- **Known, out of scope, do not fix here:** the `(authenticated)` route group has no auth gating (Arjun ruled 2026-08-05: keep deferring, app-wide, not this story's); the shared `SET_WITH_PLAYS_SELECT` over-fetches all play columns on every render (a seam-shape refactor); and the future-dated add row disposed three different ways by the three existing modules (`deferred-work.md`) — read it so you do not make it four.
- **Worktree caution, carried from Story 4.1's Debug Log and repeated since:** confirm every Read/Write/Edit resolves inside this worktree, not `/Users/arjun/Documents/name-pending` directly. This project runs concurrent sessions in parallel worktrees — check `git status` and `git worktree list` before substantive edits, and do not `pkill` a dev server you did not start (4.7 killed someone else's on port 3000).

### Project Structure Notes

- **New:** `web/lib/sets/libraryUtilization.ts` + `libraryUtilization.test.ts`; five components under `web/app/components/library-utilization/` plus a thin client wrapper for the reveal; `lu-`-prefixed rules in `web/app/library-utilization.css`; `supabase/seed.sql` (D-23); a `renderToStaticMarkup` assertion suite (D-24).
- **Modified:** `web/app/(authenticated)/library-utilization/page.tsx` (composition, new models, AC-8 line, precomputed excluding/including populations, `<h2>` headings); `LibraryUtilizationView.tsx`; `ConversionRateMeter.tsx` / `LibraryConversionTrend.tsx` / `TimeToFirstPlay.tsx` (D-20's page-wide retrofit); `web/lib/sets/libraryConversion.ts` (its three builders take the surviving population); `web/lib/sets/listModel.ts` and/or `styleEvolution.ts` (shared predicate + cross-reference comments); `supabase/config.toml` (`[db.seed] enabled`).
- **Unchanged and must stay so:** `agent/`, `shared/` (AD-3's frozen contract — this story reads only already-synced fields), `supabase/migrations/` (no schema change, AD-7 untouched), `web/lib/sets/index.ts` (no new seam function, and emphatically no revert to fixtures), `web/lib/sets/rightColumn.ts` (AC-5's contrast surface — read it, do not change it).
- Reused verbatim, not reimplemented: `InsufficientHistory`, `LowConfidenceReveal`, `LedPips`, `ConversionWindowDropdown`/`useConversionWindowSelection`, the `dz-shell`/`dz-dots` shell, `formatSessionLabel` and friends in `web/lib/sets/format.ts`.

### References

- [Source: epics.md#Story 4.9, lines 938-958] — ACs verbatim (948-958); the ⚑ note at 944 pairing this with Story 4.10.
- [Source: epics.md#Epic 4 header, line 794 (Decision B), line 796 (the discharged components note)] — go-forward-only scope; the binding copy rule every string here is subject to.
- [Source: epics.md#Story 4.7 AC-3/AC-8; #Story 4.5; #Story 4.8 AC-11; #Story 4.4 (line 842, unblocked 2026-08-08); #Story 4.10 (line 970)] — composition precedent, the narrowed-gate precedent, the dense-grid a11y pattern, and the two modules landing on this page next.
- [Source: epics.md lines 106-128] — UX-DR6, DR7, DR11, DR12, DR17, DR18, DR19, DR20 (infinite-scroll and celebration bans), DR21 (the accessibility floor and the not-color-alone rule this story cites — **not** line 470, which is Story 2.5's tray-icon AC), DR22.
- [Source: prd.md §4.2 (FR-2), §4.3 (FR-9), §4.4 (FR-10–FR-13), FR-27, §5.1, §5.5] — and the §4.4 gap Task 10 closes. Note §5.1's numeric targets are scoped to the **agent** ("on the DJ's machine", "agent idle CPU"); there is no web-side budget, so do not mis-cite NFR-1's ≤500ms/≤10s at the matrix — the honest constraint is D-19's explicit cap.
- [Source: ARCHITECTURE-SPINE.md#AD-1, AD-3, AD-7, AD-11, Consistency Conventions, Open Questions #2] — the aggregation boundary that authorizes this story; the frozen contract it does not touch; the "Unknown" convention. OQ#2 (~6% missing `tadd`/`uadd`) does **not** constrain this story — every metric is play-side and needs no add-date, which is exactly why 4.9 is buildable while 4.4 was blocked.
- [Source: EXPERIENCE.md — line 31 (spine wins over mocks), Voice and Tone + Failure Register (33-56), Component Patterns (62-80, line 77's no-wireframe precedent), Interaction Primitives (108), Accessibility Floor (110-118)] — copy register, banned patterns, chart text-equivalent requirement.
- [Source: DESIGN.md, line 199 and 290-296 (no ranking language), line 211 (imports are reference renders)] — the rules the workhorses list and the heatmap sit closest to.
- [Source: ux-designs/ux-name-pending-2026-07-19/imports/stitch_curfew_dj_reflection_platform/curfew_library_utilization_sticky_nav/code.html, lines 149-356] — the only wireframe for this page: bento tile row (159-201), ranked-list module (205-249), "High Velocity Flow" (255), heatmap (288-354) with the `overflow-x-auto`/`min-w-[600px]` treatment (305-306) GAP-4 rejects.
- [Source: web/app/(authenticated)/library-utilization/page.tsx (74, 142-184, 156-177, 182); components/library-utilization/*.tsx (`LibraryUtilizationView.tsx:39`, `TimeToFirstPlay.tsx:33-39`); web/app/library-utilization.css (19-34, 65-79, 90)] — the composition being extended and the module template to copy.
- [Source: web/lib/sets/libraryConversion.ts:178-193 (`playsByTrack`); styleEvolution.ts:190-198; listModel.ts:86-89; SetHeader.tsx:18-25; dancefloor.ts:21; rightColumn.ts:60-64, 83-132; index.ts:28-45, 105, 201] — the index that must not be reused, the three divergent confidence predicates, the dashboard surface AC-5 contrasts with, and the 500-set bound.
- [Source: agent/src-tauri/src/confidence.rs — `SessionConfidence` doc comment, `MIN_PLAYS_FOR_AMBIGUITY = 4`, `LOW_CONFIDENCE_VALUE = 0.2`] — the symmetry that makes GAP-2 a real trap rather than a style preference.
- [Source: shared/src/index.ts — `SyncPlay` (76-146), `track_id` (128-144), `SyncSetDerived.confidence` (198-205)] — every field this story reads; `track_id`'s optionality is the basis for D-18.
- [Source: supabase/config.toml:66-75] — `[db.seed] enabled = false`, `sql_paths = ["./seed.sql"]`, and its own warning about the CI gate; the basis for D-23.
- [Source: deferred-work.md — the open `/library-utilization` entries: no `<h2>`/nested landmarks (R-10), composition never browser-rendered, undated clause stated twice, dropped-prop invisibility, `getRecentSets` horizon, seam over-fetch, auth gating] — read before starting; three of these are in this story's path.
- [Source: sprint-status.yaml — the 2026-08-07 `[ADDED]` note above `4-9-…`, and the 4-7/4-5 completion + review notes] — this story's origin and the recurring-failure list above.

## Dev Agent Record

### Agent Model Used

Claude Opus 5 (`claude-opus-5`), via `bmad-dev-story`. Session 2026-08-08.

### Debug Log References

- **Production re-measured read-only before the D-20 retrofit** (Task 2's stop-condition). `select count(*)` across `djs`/`sessions`/`sets`/`plays`/`library_track_events` → **1 / 0 / 0 / 0 / 0**. Still empty, matching Story 4.5's review. The retrofit therefore changed three shipped figures that **no DJ has ever seen**, so no escalation was needed. Measured, not inferred.
- **Baseline test suite before any change: 399 passing across 24 files** (verified in this worktree at `66146ac`, matching the story's stated baseline). **Final: 476 passing across 26 files** — +77 tests, +2 files (`libraryUtilization.test.ts`, `prop-threading.test.tsx`). No test was deleted, skipped, or weakened; the one existing test that changed is explained below.
- **The seed broke two pgTAP assertions, and both were latent fragility rather than new defects.** `agent_status_isolation_test.sql:34` and `sets_delete_policy_isolation_test.sql:81` asserted **global** `count(*)` as superuser, which only ever held because the database was empty apart from the test. Disposed of differently on purpose: the `agent_status` row was **dropped from the seed** (nothing in this story needs it, so the assertion stays untouched), while the `sets` count **had to be scoped** to the two DJs the test creates, since seeding sets is the entire point of D-23. The property under test is unchanged — if the NULL-`auth.uid()` delete had matched every row instead of none, the scoped count would read 0. All **209 pgTAP tests pass** with the seed applied.
- **Seed bug found and fixed via the live stack, not by reading.** The first seed left `auth.users`' six token columns NULL; GoTrue scans them into non-nullable Go strings, so every sign-in failed with `500 Database error querying schema` / `converting NULL to string is unsupported` — which presents in the browser as *bad credentials*. Fixed to empty strings, verified end-to-end through `/auth/v1/token`. Without this the seed would have looked correct and been unusable, defeating D-23.
- **D-24's premise was verified empirically, not asserted.** Three threads were deliberately cut — the reveal's `descriptor` override, `ConversionRateMeter`'s `unidentifiableDisclosure` render, and the D-19 truncation note — then the gates re-run. **`tsc --noEmit` stayed fully green on all three**; `eslint` produced only *warnings* (0 errors) on two, and **nothing at all** on the truncation-note cut. The prop-threading suite caught all three, each via its intended assertion. Files restored and re-verified green.
- **The browser pass ran against a PRODUCTION build (`pnpm build` + `pnpm start`, port 3009), not `next dev`.** The dev server emits HMR-websocket errors and React DevTools notices that would have made "zero console errors" meaningless. Port 3000 was already in use by another session's server and was **left running** (Story 4.7's lesson). Two dev-server-only stumbles are recorded so the next reader does not repeat them: a pre-hydration click on the login disclosure is a no-op, and `NEXT_PUBLIC_*` are inlined at build time, so `.env.local` must exist *before* `pnpm build`.
- **Local `supabase db reset` requires a `docker restart supabase_kong_…` afterwards** on this stack, or the auth route 502s. A local-stack quirk, unrelated to the seed; noted so it is not misdiagnosed as one.

### Completion Notes List

**Zero agent work, stated as a checked fact.** The final diff contains **no `agent/` file, no `shared/` file, and no `supabase/migrations/` file** — verified by `git status`, not assumed. AD-3's frozen contract is untouched: every field read (`title`, `artist`, `started_at`, `derived.confidence`, `derived.track_count`) has been on `SyncPlay`/`SyncSetDerived` since the Story 1.10 freeze, and AD-1 authorizes the web-side aggregation. AD-7 is untouched — no new table, no new policy.

**Correction to the story's own scope sentence, rather than leaving it standing.** Task 11 asks that the only `supabase/` changes be "the seed and its `config.toml` flag". There are **two more**, both named here rather than quietly left: `supabase/scripts/generate-seed.mjs` (the generator that produces the seed from the committed fixtures — the seed is a generated artifact, so its generator is part of the deliverable) and `supabase/tests/sets_delete_policy_isolation_test.sql` (the scoping fix the seed forced, above). Neither is an `agent/`/`shared/` change, so the "zero agent work" claim is unaffected — but the scope line as written was narrower than what shipped.

**D-17 built as ruled by Arjun (2026-08-08), and the alternative is not present in the code.** The unweighted mean of per-set carryover shares, each set measured against its own up-to-5 **predecessors**, oldest set excluded for having none (never scored 0% — D-8). The single-ratio alternative flagged open at D-17 was rejected and is not implemented anywhere; the ⚑ OPEN block in this file is now settled and should be read as closed. Verified live: at 42 surviving sets the page reads **18% across 41 nights**; with exactly 2 surviving sets it reads **across 1 night**, which is D-17's boundary and the reason AC-2's gate is "≥2 captured sets".

**D-20 is genuinely page-wide, and that was confirmed on screen rather than argued.** Toggling the reveal moves **every** figure together, including the three already-shipped ones: conversion debuts 226→233, still-unplayed 289→286, unreconciled add-dates 8→4, alongside repeat rate 18%→16%, workhorses 345→396, one-and-done 745→835, matrix cap "10 of 42"→"10 of 58". The predicate is `listModel.ts`'s compound `isLowConfidenceSet`; the divergence from Style Evolution's bare `< 1.0` is deliberate and is now commented at **all three** definition sites (`listModel.ts`, `styleEvolution.ts`, `SetHeader.tsx`), each naming the other two. No fourth definition was added. The regression guard that fails on a revert to the bare predicate is `libraryUtilization.test.ts`'s *"hides a SHORT set that scored a fully-confident 1.0 (the soundcheck trap)"*.

**D-20(iii)'s copy consequence is handled.** `LowConfidenceReveal` gained an optional `descriptor` prop defaulting to `"low-confidence"` (byte-identical behaviour for Style Evolution). Library Utilization passes `"short or low-confidence"`, because the compound predicate also hides short sessions that scored a fully confident `1.0`. Live: *"16 short or low-confidence sessions hidden — show them"*, and the singular form at 1.

**GAP-4 honoured.** `playsByTrack` is untouched and unreused; `buildUtilizationIndex` is a separate `Map<trackKey, Set<setId>>` plus per-set distinct-track sets. A test pins D-18's double-count guard: one track whose plays split across rows with and without `track_id` counts **once**.

**D-23 closed without touching the read seam.** `web/lib/sets/index.ts` is **unchanged** — Story 4.6's swap is intact. The seed loads the committed fixtures into Postgres and the page reads them through the same Supabase query production uses. `supabase db reset` applies 18 migrations then the seed cleanly (58 sets / 2,294 plays / 523 add-events / 93 null-title plays); the additive-only guard and its 32-case self-test are unaffected (it scans `supabase/migrations` only).

**One deliberate seed omission, documented in the file itself:** there are **0 undated sets**, and that is not laziness — `sets.started_at` is `NOT NULL`, so an undated set is unreachable through the cloud read path. The undated handling in `libraryUtilization.ts` is defensive against a future nullable column and is covered by unit tests instead.

**Task 7 — the Stitch mock's `overflow-x: auto` over `min-w-[600px]` (`code.html:305-306`) is REJECTED, and this is the record of that so it is not later mistaken for an oversight.** A horizontally-scrolling region at 320px is precisely the overflow AC-11 forbids, and it leaves the cells below 24×24 regardless. `EXPERIENCE.md:31` gives the spine precedence over the mocks, and the mock predates the Ice Cyan / Abyss redesign. D-22's ranked-list degradation ships instead, and it is the phone experience rather than an edge case — confirmed: the grid is live at 1440 and the ranked list at 375 and 320.

**Heatmap ramp planned before writing, per Non-negotiable 8.** Intensity rides `opacity` over a token fill; every populated cell also **prints its own percentage as text**, so intensity is never carried by colour/opacity alone (UX-DR21, SC 1.4.1). The colour guard bit twice anyway and both were real: `white-space: nowrap` (the codebase already substitutes `text-wrap` everywhere for exactly this reason) and two comment lines that *named* the banned functions — the guard's comment stripper only handles single-line forms.

**deferred-work R-10 ruled, not deferred again.** The page had **no `<h2>` at all** and its group label duplicated a landmark's accessible name. It now has a real outline — `H1 Library Utilization → H2 Conversion → H3 Conversion rate → H2 Rotation → H3 Rotation size / Repeat tracks / Set similarity → H2 Tracks → H3 Workhorses / Played once → H3 Time to first play` — and the module `<section>`s became `<div role="group">`, keeping each module's informative accessible name while removing it from the landmark list. **Measured landmark count: 2** (`main` + `nav`), where naively adding five more sections would have produced nine.

**AC-11 — measured against the DOM in a real browser, never eyeballed.** `getBoundingClientRect()` via Playwright at **1440 / 375 / 320**, across **every** state: 0 sets, exactly 1, exactly 2 surviving, >10 (58), low-confidence hidden **and** revealed, null-title plays present throughout, and the D-22 degraded form.
- **No horizontal overflow at 375 or 320 in any state** (`scrollWidth === clientWidth`). For contrast, `/dashboard` at 375 has ~100 overflowing elements — pre-existing and untouched.
- **Every interactive target meets SC 2.5.8.** Two sub-24px boxes were flagged by bounding-box alone and both were then **hit-tested** by probing the four corners of a 24×24 box with `elementFromPoint`: `se-chart-info-btn` (15×15 box) **passes at all four corners** via its existing `::after` extender, and `se-hidden-toggle` is the reveal toggle already ruled non-blocking under SC 2.5.8's Inline exception — left alone per the story's explicit instruction. The one genuinely new target, the load-more `<summary>`, never appeared in the sub-24 list.
- **Module width caps survived the new grid wrappers.** `.lu > .lu-module`'s 440px cap is scoped to *direct* children, so `.lu-pair` restates it explicitly rather than inheriting; measured 440px inside pairs at 1440, and 1304px for the deliberately-uncapped similarity module.
- **Zero console errors in every state at every width.** (The only warnings observed were headless-Chromium WebGL driver messages — *"GPU stall due to ReadPixels"* from the `SilkBackdrop` canvas — which are GL-emulation noise from the test environment, not application output. Reported rather than rounded down to "zero warnings".)

**deferred-work's "merged three-feature composition has never been rendered in a browser" is closed — AND ITS NAMED SUSPECT WAS REAL.** The entry predicted a `dz-shell` flattened by `.lu-module .se-empty { min-height: 0 }` into "a pill with copy 4px from a curved edge". Measured: **45px tall against a 50px `border-radius`** at 1440 (Set Similarity's insufficient branch, the widest module), with the radius exceeding half the height in **7 of 9** measurements across the 0-, 1- and 2-set states. It affected *every* module's insufficient branch, not only `TimeToFirstPlay`'s. Fixed by scaling the radius to the block's real size (`border-radius: 18px`) and lifting horizontal padding 4px→16px so the first character clears the corner arc — the height was never the problem, so it was not what changed. Re-measured: radius 18 against heights 51–135, zero flagged.

**Deliberately NOT relitigated:** the 60/30/14 window scale and its single shared dropdown, the meter's disclosure dedup to page level, `TimeToFirstPlay` living outside `LibraryUtilizationView`, the one shared `playsByTrack` index for its three conversion consumers, D-4's unpersisted reveal, the `(authenticated)` auth-gating gap, and the `SET_WITH_PLAYS_SELECT` over-fetch. The three-way future-dated-add disposition was read and **not made a fourth**: rotation size excludes a future-dated set with the same guard and the same stated reason `buildLiveConversionRate` already uses.

**Open, and flagged rather than silently absorbed:** `a.nav-brand` overflows its container by 2px at 1440 — reproduced on `/dashboard` and `/style-evolution` too, so it is a pre-existing shared-nav issue, app-wide and out of this story's scope. It causes no document scroll (`scrollWidth === clientWidth`).

**Task 10 (doc sync) — completed after Arjun ruled "FR-30, go finish the doc sync" (2026-08-08).** Four files:
- `prd.md` §4.4 — new **FR-30: Play-side utilization stats**, with seven testable consequences (the repeat-rate formula, the 10-set matrix cap, the workhorses/most-played divergence, one-and-done vs. the aging shelf, the fixed 60-day rotation window, AC-8's copy rule, and the page-wide low-confidence exclusion). §4.4's **Description was amended** too — its four-item list had gone stale and was the same accretion FR-9 and FR-10 each had to close. FR-30's own note records why these five were buildable while FR-12's shelf was blocked: they are play-side and need no add-date, so ARCHITECTURE-SPINE.md Open Questions #2 does not constrain them.
- `epics.md` — D-17's full formula inlined under Story 4.9 **AC-3** (including that the alternative was rejected, and that it must ship with AC-10's predicate), and D-20's predicate divergence inlined under **AC-10** (why "the same contract Style Evolution does" selects the one predicate that does not do what AC-10 asks for). Both now live in the spec rather than only in this story file.
- `EXPERIENCE.md` Component Patterns — **five new rows**, following line 77's no-prior-wireframe precedent for Story 4.5.
- `reconcile-stitch-screens.md` — **corrected its own "no orphaned functionality found" claim** for this screen. That pass compared *copy* and was right about copy; two whole modules (the heatmap, and "High Velocity Flow" at `code.html:255`) appear in neither spine. Story 4.9 adopted the first and rejected its scroll treatment; the second remains genuinely orphaned and is left flagged rather than invented. The transferable lesson is recorded there: a copy-level comparison cannot detect a missing module.

### File List

**New**
- `web/lib/sets/libraryUtilization.ts` — the D-20 partition, D-18 identity, the GAP-4 set-membership index, and all five metrics with their summary generators
- `web/lib/sets/libraryUtilization.test.ts` — 57 pure-function tests
- `web/app/components/library-utilization/LibraryUtilizationReveal.tsx` — the thin client wrapper taking both prerendered subtrees (D-20(v))
- `web/app/components/library-utilization/RotationSize.tsx`
- `web/app/components/library-utilization/RepeatTrackRate.tsx`
- `web/app/components/library-utilization/SetSimilarity.tsx`
- `web/app/components/library-utilization/Workhorses.tsx`
- `web/app/components/library-utilization/OneAndDone.tsx`
- `web/app/components/library-utilization/TrackRowList.tsx` — shared row list + native `<details>` load-more
- `web/app/components/library-utilization/prop-threading.test.tsx` — D-24's harness, 20 assertions incl. **8** negative controls (the count read "5" until Story 4.9's code review counted the `// NEGATIVE CONTROL` blocks)
- `supabase/seed.sql` — generated, local-dev only (D-23)
- `supabase/scripts/generate-seed.mjs` — its generator

**Modified**
- `web/app/(authenticated)/library-utilization/page.tsx` — composition, both populations precomputed, AC-8 line, `<h2>` outline, the five new models
- `web/app/components/library-utilization/LibraryUtilizationView.tsx` — real `<h2>`, duplicated `aria-label` dropped (R-10)
- `web/app/components/library-utilization/ConversionRateMeter.tsx` — `role="group"` + `<h3>` (R-10)
- `web/app/components/library-utilization/TimeToFirstPlay.tsx` — `role="group"` + `<h3>` (R-10)
- `web/app/components/style-evolution/LowConfidenceReveal.tsx` — optional `descriptor` prop (D-20(iii))
- `web/app/components/set-detail/SetHeader.tsx` — cross-reference comment only (D-20(ii))
- `web/lib/sets/listModel.ts` — cross-reference comment only
- `web/lib/sets/styleEvolution.ts` — cross-reference comment only
- `web/app/library-utilization.css` — Story 4.9 rules, plus the measured `.se-empty` radius fix
- `supabase/config.toml` — `[db.seed] enabled = true`, committed alongside the seed
- `supabase/tests/sets_delete_policy_isolation_test.sql` — global count scoped to the test's own DJs

**Unchanged, and verified so:** `agent/`, `shared/`, `supabase/migrations/`, `web/lib/sets/index.ts`, `web/lib/sets/rightColumn.ts`.

## Change Log

| Date | Change |
|---|---|
| 2026-08-10 | **Status bookkeeping closed.** review → done. No code change — the code review, the post-review browser pass, and the `main` merge (Stories 4.4 + 4.8) were all already complete and gate-clean (596 tests / 28 files, lint/typecheck/build clean); the `Status:` field and sprint-status.yaml entry simply never got flipped. Same pattern as the 4-5 bookkeeping gap earlier in this epic. Caught and ruled during the Epic 4 retrospective. |
| 2026-08-08 | **Code review.** Three parallel adversarial layers; 34 raw findings → 2 decisions, 29 patches, 3 deferred, 1 dismissed. 29 patches applied. Highest-impact: a raw **NUL byte** in `Workhorses.tsx` made the file BINARY to git, so the one new component was invisible to `git diff`, `git log -p`, PR review and `grep -r` while all four gates stayed green; and the similarity heatmap's intensity `opacity` sat on the cell element, fading the percentage text that was supposed to make the ramp not-colour-alone (0-10% cells rendered their own label at 8-17% opacity, defeating Task 7/UX-DR21/SC 1.4.1 — the exact property the Completion Notes claimed). Also fixed: rotation size's fabricated `0 plays, 0 unique`; one-and-done telling a DJ with no plays that "every track you've played has come round again" (the state empty production renders); the page body computed and serialized twice even when nothing was hidden; `deferred-work.md` untouched while three of its entries were claimed closed; and three justifications citing an `aria-label="Conversion"` this same story deleted. Tests **476 → 486** (+10 regression guards; the existing suite asserted figures, never the prose, which is why four copy defects were structurally invisible to it). |
| 2026-08-08 | **Implemented (Tasks 1-9, 11). Task 10's doc-sync was held for Arjun's review of the FR-30 wording, then completed the same day after he ruled "FR-30, go finish the doc sync" — this row originally described only the pre-ruling state and contradicted the Completion Notes; corrected by code review.** D-17 built as Arjun ruled (unweighted mean of per-set carryover shares vs. up-to-5 predecessors) — the single-ratio alternative is rejected and absent from the code, so D-17's ⚑ OPEN block is closed. Production re-measured read-only before the D-20 retrofit and confirmed still empty (0 sets / 0 plays), so three shipped figures changed with zero blast radius. Tests **399 → 476** across **24 → 26** files. Three findings worth carrying forward: (1) the seed's `auth.users` token columns had to be empty strings, not NULL, or every sign-in 500s — the seed would have looked right and been unusable; (2) the seed exposed two pgTAP assertions that counted rows GLOBALLY as superuser, only ever passing on an empty database — one resolved by not seeding `agent_status`, the other by scoping the count to the test's own DJs; (3) `deferred-work.md`'s named-but-unverified suspect was REAL and worse than described — `.lu-module .se-empty` rendered 45px tall against a 50px radius, affecting every module's insufficient branch rather than just `TimeToFirstPlay`'s, fixed by scaling the radius. D-24's premise verified by deliberately cutting three threaded props: `tsc` stayed fully green on all three, eslint gave warnings-only on two and nothing on the third, and the new harness caught all three. |
| 2026-08-08 | Story created from `epics.md` Story 4.9. **Five gaps** found at creation time and resolved rather than deferred: GAP-1 (no low-confidence exclusion exists anywhere on this page, so AC-10 is a retrofit over three shipped modules), GAP-2 (three divergent confidence predicates, and the one AC-10's wording points at does not exclude soundchecks — `confidence.rs` is symmetric, so a 2–3 track soundcheck scores 1.0), GAP-3 (AC-7's window ambiguity, which decides placement), GAP-4 (`playsByTrack` is unreusable here — no set identity, `track_id`-keyed, drops undated plays), GAP-5 (Story 4.6 retired the fixtures and no seed file exists, so AC-11's browser pass is unperformable as things stand). **Eight rulings** fixed in-file: D-17 (exact repeat-rate formula per AC-3, unweighted, 5 predecessors), D-18 (title+artist identity, not `track_id`), D-19 (10-set matrix cap), D-20 (page-wide contract on the compound predicate, plus the reveal-copy consequence), D-21 (rotation size fixed at 60 days, outside the "Conversion" landmark), D-22 (matrix degrades to a ranked list at phone widths), D-23 (local-dev `supabase/seed.sql`), D-24 (adopt the `renderToStaticMarkup` prop-threading harness `deferred-work.md` has recommended since the 4.5 merge). PRD-sync debt opened: §4.4 has zero coverage of all five metrics; next free FR is FR-30. |
