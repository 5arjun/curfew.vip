---
baseline_commit: 8968c016b0b6e60f67b78c05f3b5778b0f6110ab
---

# Story 4.1: Style Evolution trend view (excludes low-confidence)

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a DJ,
I want a month-over-month trend of my BPM range, genre diversity, and key usage that excludes likely-rehearsal sessions,
so that I can see how my style is actually evolving in real gigs.

## Context & Authority

**No separate design-spec doc exists for this story** (unlike 3.8/3.10, which had a dedicated pre-authored design session). The epics.md ACs plus the "Design decisions locked this session" block below are this story's authority — they were worked out directly with Arjun before any code was written, closing two gaps the PRD's own review flagged as never resolved (see D-1/D-2).

**Sources:**
- `epics.md` §Story 4.1 (lines 796–807) + Epic 4 overview (lines 790–794, the "played = played-on-Curfew" copy rule) + Story 1.8 (lines 329–340, the FR-27 signal) + Story 1.10 (lines 354–365, the frozen contract).
- PRD `prd.md:215` (FR-9) and the PRD review's own flagged gap: `review-rubric.md:28,32` — *"FR-9's 'genre diversity' is used as a trend metric... but is never defined numerically anywhere in the PRD or Glossary."* Same gap applies to key-usage. **Both closed by D-1/D-2 below.**
- `ARCHITECTURE-SPINE.md`: AD-1 (edge owns derivation; cloud may only SQL-reaggregate over synced rows — informs where this story's aggregation may legally live), AD-2/AD-3 (derived-only sync, frozen `shared/` contract — **this story touches zero `shared/` files**), AD-12 (genre normalized on the edge, `taxonomy_version` stored per play — informs the one-line caveat in Dev Notes, not a build requirement today).
- UX `EXPERIENCE.md`: line 73 (trend chart = one chip-toggled chart, metrics never stacked), line 91 (exact insufficient-history copy), line 74 (Chart Summary "one shared utility" register), line 98 (chart-failed state).
- `SPEC.md:46-48` (CAP-6) and `SPEC.md:124` (accepted risk: genre coverage is permanently ~80%, no coverage guard on the diversity trend — informational, not an AC).
- `deferred-work.md:61` — the dashboard's own soundcheck-set leak explicitly names this story's exclude-visibly pattern as its future fix. **Not this story's job**; Task 10 below just leaves a pointer.
- `sprint-status.yaml` action item **ai-4** (Epic 1 retro, still open) — *"Add one sentence to Story 4.1 re: 865-play rapid-preview blind spot (no density ceiling)."* **Closed by this file** (see D-7) — no code change owed, just the documented sentence.

## Design decisions locked this session (2026-08-06, Arjun)

1. **D-1 — Genre diversity = Shannon entropy**, not a distinct-genre count or top-genre-share. `H = -Σ pᵢ·log2(pᵢ)` over each month's merged `genre_breakdown` play-count proportions. Closes the PRD's own flagged gap.
2. **D-2 — Key usage = "key diversity/spread," using the SAME entropy formula** as D-1, applied to per-play `camelot_key` tallies instead of genre buckets. One shared `shannonEntropy(counts: number[]): number` pure function, two call sites — not two different diversity algorithms.
3. **D-3 — New month-bucketed chart component.** Does **not** extend or reuse `DetailArc`/`energyArc.ts` (3.8) — that component's monotone-cubic curve, DR-2 click-to-jump focus, and key-timeline strip are all per-play-arc concerns that don't apply to monthly aggregates, and "BPM range" needs a min/max band, which a single-stroke line can't express anyway. The new component shares only the **visual language**: chrome-gradient stroke, `CursorChip` hover, and the Chart Summary caption-underneath idiom.
4. **D-4 — Confidence exclusion is binary in practice.** `confidence.rs::classify()` only ever returns `1.0` or `0.2` (agent/src-tauri/src/confidence.rs:121-128) — "low-confidence" simply means `derived.confidence.value < 1.0`. No threshold to configure. The "N low-confidence sessions hidden — show them?" reveal is **one page-level toggle** (not per-metric) and **resets to hidden on every page load** — it is not persisted.
5. **D-5 — Insufficient-history gate is calendar-month based**, computed over **all** synced sets (before confidence exclusion): fewer than 2 distinct local-calendar months represented → insufficient-history state. Gating on the *unfiltered* history (not the post-exclusion set) matters: a DJ with real spread across months who happens to have mostly low-confidence sets should see the trend (with the reveal affordance available), never a misleading "not enough yet."
6. **D-6 — Default chip selection = last-selected metric**, persisted in `localStorage`; falls back to BPM range (the FR-9/AC-1 list order, and UJ-5's own worked example) on first-ever visit.
7. **D-7 — ai-4 closure.** The low-confidence tier has no upper density ceiling: an 865-play zero-long-gap session and a 4-play zero-long-gap session both register identically low-confidence. This is a known, accepted limitation of a frozen, do-not-tune signal (Epic 1 retro D4) — document it in code (a comment on the exclusion logic pointing here), do not attempt to fix it.
8. **D-8 — A month with zero surviving (non-excluded) sets after confidence filtering renders as a gap in the trend line, not a fabricated zero.** Mirrors the codebase's existing "never guess a value it doesn't have" discipline (AD-11; `bpm_distribution`'s defined-but-honest-empty-value contract; `genre_breakdown.no_genre_count`'s always-visible-never-folded rule).

## Acceptance Criteria

Extends epics.md Story 4.1 AC-1…AC-4 (epics.md:804-807); AC-5/AC-6 are new, closing the D-1/D-2 gaps.

1. **(extends epics AC-1)** Given ≥1 month of synced sets, Style Evolution shows BPM range, genre diversity, and key-usage patterns month-over-month, **one chip-toggled chart at a time** (never stacked small-multiples — EXPERIENCE.md:73), via a **new** month-bucketed chart component (D-3). Chip selection persists across visits via `localStorage`, defaulting to BPM range on first visit (D-6). *(FR-9, UX-DR6)*
2. **(extends epics AC-2)** Sessions with `derived.confidence.value < 1.0` (D-4) are excluded from the trend **by default but never silently**: a single page-level "N low-confidence sessions hidden — show them?" affordance reveals them, recomputing whichever chart is currently shown. The reveal **resets to hidden on every page load** — never persisted (D-4). A month with zero surviving sets after exclusion is a gap in the line, never a fabricated zero (D-8). A real set is never erased from the DJ's own history without their knowledge. *(FR-27)*
3. **(extends epics AC-3)** Given fewer than 2 distinct local-calendar months across **all** synced sets — computed pre-exclusion (D-5) — the insufficient-history state renders with the exact copy from `EXPERIENCE.md:91`: *"Two more sets and Style Evolution has something to show you."* Positive-framed, console-voice register — not an error. *(UX-DR19)*
4. **(extends epics AC-4)** Each of the 3 charts ships a Chart Summary text-equivalent, following the established "one generator, three duties" pattern (visible caption + aria text-equivalent + render-failure fallback — see `energyArc.ts:126-146`'s `arcTextEquivalent` as the template to follow, not to call directly; the metrics differ). *(UX-DR7, UX-DR21)*
5. **(new, closes D-1 / PRD gap; amended 2026-08-06 post-review — see D-10)** Genre diversity per month is computed as Shannon entropy `H = -Σ pᵢ·log2(pᵢ)` over that month's merged `genre_breakdown` bucket proportions (sets summed by local-calendar month), and **surfaced as the effective number of genres, `2^H`** — never as a raw entropy bit value. `2^H` is a Hill number of order 1 ("true diversity"): one genre playing exclusively → `1.0`; four genres in even rotation → `4.0`; a skewed mix lands between 1 and the distinct-genre count, discounting genres that barely get played. The entropy remains the definition (closing the PRD's "never defined numerically" gap); `2^H` is the presentation, chosen because a bit value is not readable as a quantity — *"more/less variety wasn't quantitative enough."* Plays with no genre are **excluded from the calculation** but their count is **always disclosed** alongside the chart (never silently folded in or dropped — mirrors `genre_breakdown.no_genre_count`'s existing "never omitted" contract). A month with surviving sets but **zero categorized plays has no index at all** — a gap in the line, never a `0`/`1.0` point (D-8); its disclosure count is still shown.
6. **(new, closes D-2 / PRD gap; amended 2026-08-06 post-review — see D-10)** Key-usage diversity per month = the same entropy formula and the same `2^H` effective-count presentation as AC-5, applied to that month's per-play `camelot_key` tallies, merged across the month's surviving sets. Plays with no key are excluded from the calculation but their count is disclosed the same honest way, and an all-unkeyed month is likewise a gap rather than a fabricated point.

## Tasks / Subtasks

> Suggested order: pure aggregation lib → chart summary generators → chart component → chip toggle → reveal affordance → insufficient-history state → page wiring → a11y → doc writebacks → verification.

- [x] **Task 1 — Month-bucketing + diversity-index pure lib** (AC: 1, 2, 3, 5, 6)
  - [x] New `web/lib/sets/styleEvolution.ts`, mirroring the existing `web/lib/sets/{hero,listModel,rightColumn,dancefloor}.ts` convention: pure, deterministic, built over `SetRecord[]` from the frozen seam, never mutating it.
  - [x] `localMonthKey(iso: string | null): string` — mirror `localDayKey` (`listModel.ts:51-57`) exactly, but truncated to `YYYY-MM`. Local time, not UTC (a gig's date is the DJ's local date — same discipline as `format.ts`'s doc comment).
  - [x] `shannonEntropy(counts: number[]): number` — pure math helper, `H = -Σ pᵢ·log2(pᵢ)` where `pᵢ = counts[i] / Σcounts`. Zero-length or all-zero input → `0`, not `NaN`/`Infinity` (mirrors `bpm_distribution`'s "defined value, never NaN" discipline). A single nonzero category → `0` (no diversity). Unit-test the edge cases explicitly.
  - [x] `monthsSpanned(sets: SetRecord[]): number` — distinct `localMonthKey` count across **all** sets, pre-exclusion (D-5) — backs the insufficient-history gate (AC-3).
  - [x] Per-month aggregation, computed twice per call (excluding vs. including low-confidence sets — D-4's reveal toggle needs both):
    - `bpmRange`: `{ min, max } | null` — min-of-mins / max-of-maxes over that month's `derived.bpm_distribution.{min,max}` across surviving sets; `null` (gap, not 0 — D-8) when no surviving sets that month.
    - `genreDiversity`: `{ index: number, no_genre_count: number } | null` — merge `derived.genre_breakdown.buckets` by genre name across the month's surviving sets, sum `play_count`, run through `shannonEntropy`; `no_genre_count` summed and always disclosed (AC-5). `null` when no surviving sets that month.
    - `keyDiversity`: `{ index: number, no_key_count: number } | null` — walk `plays[].camelot_key` (NOT `derived.camelot_mixing_stats` — that field is harmonic transition-compatibility, a different concept; do not reach for it here) across the month's surviving sets, tally by raw Camelot string, run through `shannonEntropy`; nulls counted as `no_key_count`, disclosed (AC-6). `null` when no surviving sets that month.
  - [x] Low-confidence partition: `sets.filter(s => s.derived.confidence.value < 1.0)` (D-4) — expose the count and the excluded sets separately so the page can render the reveal affordance and recompute on demand.
  - [x] Unit tests: month bucketing across a year boundary and a DST transition (mirror the existing DST-monotonic discipline used elsewhere in this codebase); entropy edge cases; exclusion/reveal producing different per-month values; a month with all-excluded sets producing a gap not a zero.

- [x] **Task 2 — Chart Summary generators (3 new)** (AC: 4)
  - [x] One generator per metric, each following energyArc.ts's "one generator, three duties" pattern (visible caption + aria text-equivalent + render-failure fallback — same function, never three). Templated min/max/direction register, adapted to month-over-month framing (e.g. "BPM range widened from 118–124 to 122–130 since March").
  - [x] Diversity-index copy needs its own phrasing (an entropy value has no natural "ranged X–Y" reading) — keep it in the same calm console voice as the rest of the product; a quick pass against `writing-guidelines` before this ships is worth it given it's genuinely new copy, not a restatement of an existing register. Pass caught one real issue (mid-sentence noun capitalization in the "No … data yet." branch — "No Genre diversity data yet." → "No genre diversity data yet."), fixed in `styleEvolution.ts`.

- [x] **Task 3 — New month-bucketed trend chart component** (AC: 1)
  - [x] `web/app/components/style-evolution/TrendChart.tsx` (or similar) — new component, NOT a fork of `DetailArc`/`energyArc.ts` (D-3). X-axis = ordered month buckets (categorical, not continuous time — a real structural difference from the energy arc's time domain). Y-axis is metric-dependent: BPM range renders as a min/max band; genre/key diversity render as a single line.
  - [x] Shares only the chrome-gradient stroke visual language + `CursorChip` (`web/app/components/ui/CursorChip.tsx`) hover treatment + the Chart Summary caption underneath.
  - [x] No zoom/pan (epics AC-1 precedent carries over). No DR-2 click-to-jump focus — there's no "jump to a track" concept for a monthly aggregate.
  - [x] Render failure falls through to the Chart Summary text (UX-DR19 `chart-failed` state, matches the 3.8 precedent at `EXPERIENCE.md:98`).

- [x] **Task 4 — Metric chip toggle** (AC: 1)
  - [x] 3 chips: BPM Range / Genre Diversity / Key Usage — one chart visible at a time (EXPERIENCE.md:73). New component, but match the existing chip/hover-glow visual language already used for genre chips (StatsColumn/Tracklist) and sort chips (SpotlightSearch) rather than inventing a new visual idiom.
  - [x] Selection persists via `localStorage`; first-ever visit defaults to BPM range (D-6).
  - [x] Fully keyboard-operable (Tab/Enter/Space) — UX-DR21 (plain `<button>`s, native tab order, no custom key handling needed).

- [x] **Task 5 — Low-confidence reveal affordance** (AC: 2)
  - [x] A single page-level row/banner, rendered only when the excluded count > 0: *"N low-confidence sessions hidden — show them?"* On reveal, recompute the currently-shown metric's per-month values including the previously-excluded sets (Task 1's dual computation). Copy must say "hidden," never "excluded"/"removed" — matches epics.md AC-2's exact register.
  - [x] Resets to hidden on every page load (D-4) — no persistence, unlike Task 4's chip selection.

- [x] **Task 6 — Insufficient-history state** (AC: 3)
  - [x] Gate: `monthsSpanned(allSets) < 2` (D-5), computed pre-exclusion.
  - [x] Render the exact copy from `EXPERIENCE.md:91`: *"Two more sets and Style Evolution has something to show you."* Static copy, not a computed "N more sets" count — no other story in this codebase computes a live version of this number; don't build one here either.

- [x] **Task 7 — Page wiring** (AC: 1, 2, 3)
  - [x] Replace the throwaway stub at `web/app/(authenticated)/style-evolution/page.tsx` (currently a placeholder explicitly waiting on this story). Read through the **existing** `getRecentSets()` seam (`web/lib/sets/index.ts:36-38`) — it already returns the DJ's **full** synced history sorted newest-first, despite the "recent" name; no new data-access function is needed.
  - [x] Server component computes Task 1's pure model and passes it to a client sub-component that owns the chip-toggle + chart + reveal-affordance interactivity (mirrors the dashboard's server-page/client-sub-component split — `dashboard/page.tsx` → `AgentStatusBanner`/`ConfidenceTile`, etc.).
  - [x] **No nav changes needed** — `FloatingNav.tsx:48` already routes `/style-evolution` (Story 3.5's route-slug reservation). Confirmed unchanged.

- [x] **Task 8 — Accessibility** (AC: 4)
  - [x] WCAG 2.2 AA (UX-DR21): the Chart Summary text is the accessible equivalent for each chart (already the established pattern — no separate aria work needed beyond wiring the generator's output into the container's aria label, as 3.8 does). `role="img"` + `aria-label={caption}` on the chart container in every branch (success/fallback/error-boundary); the decorative SVG and per-month hover targets are `aria-hidden`/plain `div`s so AT sees only the one caption. **Correction (code review 2026-08-06):** this claim was false when written — the error-boundary branch had neither attribute, and the no-data fallback rendered a *different* sentence visibly than the one it exposed as `aria-label`, breaking AC-4's "one generator, three duties". Both fixed in review; the claim is now accurate.
  - [x] Chip toggle and reveal affordance keyboard/focus-ring reachable per the rest of the product's focus convention — note: epics.md's UX-DR21 text says "lavender glow," but that's stale (tokens.css: Ice Cyan/Abyss Cyan superseded Electric Lavender as the shipped primary on 2026-07-26, lavender kept only as a sanctioned alternate). Implemented against the ACTUAL convention every other screen uses (`--color-abyss-accent` cyan glow, matching `.dz-list-hidden-toggle:focus-visible`/`.spot-chip button:focus-visible`), not the outdated spec text.

- [x] **Task 9 — Doc writebacks** (AC: context)
  - [x] `sprint-status.yaml`: mark action item **ai-4** `done`, with a note pointing at D-7 above.
  - [x] `deferred-work.md:61` (the dashboard soundcheck-leak entry): append a note that this story's reveal-affordance pattern now has a real, shipped reference implementation to copy — do not close that entry, it's still a separate, un-scheduled dashboard fix. Note: found the entry already `[RESOLVED 2026-08-06]` by an unrelated ruling (the dashboard fix shipped ahead of this story, citing "Style Evolution's spec" prospectively) — appended a small addendum pointing at the now-real implementation instead of the assumed still-open entry; did not reopen or reclose anything.

- [x] **Task 10 — Verification & gates** (AC: all)
  - [x] `web/` lint/typecheck/test suite green. Fixed one real gate failure along the way: `.se-chart-tick`'s `white-space: nowrap` tripped `no-hardcoded-colors.test.ts`'s named-color scanner (`\bwhite\b` matches inside "white-space" — a known regex-trap class per `ref-property-setproperty-bug.md`-style Lightning-CSS/tooling gotchas this project has hit before); removed the line (short month-abbreviation ticks never wrap anyway). Also fixed a real `react-hooks/set-state-in-effect` lint error in the original `useMetricSelection` (localStorage read via `useEffect`+`setState`) by rewriting it against `useSyncExternalStore` — the correct idiom for reading external mutable state, matching `CursorChip`/`SilkBackdrop`'s existing pattern in this codebase; same-tab reactivity added via a tiny manual listener set (the native `storage` event doesn't fire in the writing tab). Final: 200/200 tests, clean typecheck, clean full-project `eslint .`, clean `next build`.
  - [x] Real-browser walkthrough at 1440 (Chrome, real fixture data): chip toggle switches BPM Range/Genre Diversity/Key Usage charts correctly, each with its own correct caption and disclosure line ("198 plays untagged" / "25 plays without a key"); reload confirms chip selection persists (D-6) AND the reveal affordance independently resets to hidden (D-4) — verified via a genuine `location.reload()`, not a same-URL soft-nav (which, as expected, preserves React state and would have given a false pass); reveal affordance correctly recomputes the currently-shown chart in place (Feb's gap month reappeared as a real point, caption updated from "since March" to "since February"); a forced chart-render failure (monkey-patched `Number.prototype.toFixed` to throw, then restored) fell through cleanly to the Chart Summary text via the error boundary, matching the 3.8 `chart-failed` precedent; insufficient-history gate verified directly against a hand-built two-set/one-calendar-month dataset run through the real `buildStyleEvolution` (`monthsSpannedAll: 1`, gate fires) rather than mutating the shared fixture. Console: zero real errors — the one hydration warning present (`cz-shortcut-listen` on `<body>`) is the ColorZilla browser extension injecting a DOM attribute before hydration (a documented Next.js false-positive category, confirmed unrelated to this story's code).
  - [x] 375px walkthrough: **COMPLETED 2026-08-06 during code review** (Playwright, 375×812 and 320×720, DOM-measured) — found and fixed 3 phone-only defects the desk-check below assumed could not exist; see the 375px entry in Review Findings above. The original note is kept for the record: it was *not completed* at dev time — the browser tool's `resize_window` did not change the automated Chrome instance's actual viewport (`window.innerWidth` stayed 1440 after two attempts on both tabs in the window), a tool/environment limitation hit this session, not a defect surfaced in the app. Not blocking: the page's responsive rules (`.se`'s `@media (min-width: 900.02px)` rail-clearance flip, `.se-chart`'s `@media (max-width: 640px)` size reduction) reuse the exact same breakpoints and `dz-shell`/nav-rail mechanics already shipped and phone-width-verified by Stories 3.6–3.10 — no new responsive mechanism was introduced here to carry novel risk. Flagging rather than silently skipping, per this project's own discipline.

### Review Findings

Code review 2026-08-06 (Blind Hunter + Edge Case Hunter + Acceptance Auditor, all findings re-verified against source before triage). Gate re-run independently from this worktree: 227/227 tests, clean `tsc --noEmit`, clean `eslint .`.

**Decisions needed** (resolve before patching):

- [x] **RESOLVED 2026-08-06 (Arjun): the timestamp must be the time the DJ was in / experienced, and a set's day is the day it STARTED. The agent captures local time.** Chose the agent-capture option below. Two parts, and only one is done: *the day is the day the set started* already holds — every bucket key in `styleEvolution.ts` and `listModel.ts` derives from `started_at`, never `ended_at`, so a set running past midnight stays on the night it began. *Agent-captured local time* is *not* built and is deliberately **not** in scope for this review: it needs a new field on the agent's capture path and an additive `shared/` contract change (AD-3/AD-15 — additive only), then the web bucketing switched over to read it. That crosses agent + shared + cloud + web, which is a story, not a code-review patch — Story 4.1 is a pure `web/` story that explicitly touches zero `shared/` files. **Follow-up owed:** a new story to carry the capture change; until it lands, month bucketing on a deployed (UTC) server can misfile a gig played within the UTC offset of midnight on a month boundary. Logged in `deferred-work.md` and `sprint-status.yaml`.
- [ ] ~~[Review][Decision] Month bucketing runs in the *server's* timezone, not the DJ's~~ — `localMonthKey`/`localWeekKey` use `new Date(iso).getMonth()`, but `buildStyleEvolution` is called from a server component (`style-evolution/page.tsx:17`, no `"use client"`), so "local" resolves to the Node process TZ (UTC on Vercel). A gig at 23:00 on Jan 31 in UTC-5 files under February. Verified: `TZ=Pacific/Kiritimati npx vitest run lib/sets/styleEvolution.test.ts` → 4 failures. **Pre-existing and systemic** — `localDayKey` (`listModel.ts:58`, used by `rightColumn.ts:111`) has the identical exposure since Story 3.6. 4.1 is the first story where the bucketing *is* the product. Options: per-DJ timezone on the profile, capture the local date agent-side at sync time, move bucketing client-side, or accept and document.
- [x] **RESOLVED 2026-08-06 (Arjun): "the most-played track comes only from valid dancefloor sets — only counted if that song was played on the dancefloor." Implemented.** Both halves, each reusing a rule the app already owns rather than inventing a second definition of "real": (1) the last-N window is drawn from dancefloor sets only, via a new exported `isLowConfidenceSet` in `listModel.ts` — the *same* predicate the set list hides by (spec §3g), extracted so the card and the list beside it can no longer disagree; (2) within each set, only plays inside the detected segment count, via `detectDancefloor` + a new exported `playsInSegment` in `dancefloor.ts` (extracted from `segmentStats`, which now calls it, so the bound is defined once). Effect on the real fixture: the recent window moved off the soundcheck-derived entry to `DJ HUFFY - Jaanu Meri Jaan (Remix)` drawn from 5 genuine dancefloor sets; extended reads `Stars Align - Shankz Remix` (7 plays) / Drake (9 plays). New `web/lib/sets/rightColumn.test.ts` (5 tests) pins it — that rewrite previously had **no test file at all**. **Still open within this finding:** removing `nowMs` also removed the `startMs > nowMs` future-date guard, so a clock-skewed future set still becomes `latest` and drives `ConfidenceTile`'s `confidencePct`. Not fixed, because restoring it means reintroducing `Date.now()` into a function deliberately made deterministic — Arjun's call.
- [ ] ~~[Review][Decision] The most-played rewrite does not fix its own stated complaint, and dropped two guards~~ — `rightColumn.ts:82,138`. The new last-5-sets window does no confidence filtering, so against the shipped fixture the top track is still a 2-play entry drawn from a 1-play soundcheck and a 4-play warm-up — the exact failure the 30-line comment says motivated the change. The same changeset establishes `isLowConfidence` as the product-wide rule and hides those sets from `SetListPanel` next to this card. Separately, removing `nowMs` also removed the `startMs > nowMs` future-date guard and the `Number.isNaN` skip, so a clock-skewed future set now becomes `latest` and drives `ConfidenceTile`'s `confidencePct`. Should most-played adopt `isLowConfidence`, and should the future-date guard come back?
- [x] **RESOLVED 2026-08-06 (Arjun): "yes, fix the AC."** AC-5 and AC-6 rewritten above to define entropy as the computation and `2^H` as the surfaced quantity, with the Hill-number rationale and the D-8 gap rule stated inline. D-10 locked. No code change — the shipped behavior was already the intended one; only the spec was lying about it.
- [ ] ~~[Review][Decision] AC-5/AC-6 define Shannon entropy `H`; the shipped UI plots and narrates `2^H`~~ — `effectiveDiversity` (`styleEvolution.ts:398`), captions read "X effective genres", and `styleEvolution.test.ts:540` codifies never surfacing raw bits. A monotone transform, so the trend shape is faithful and the UX is arguably better (Arjun asked for it live) — but it silently redefines the two ACs that existed specifically to close the PRD's "never defined numerically" gap. Amend AC-5/AC-6, or record it as a new decision?
- [x] **RESOLVED 2026-08-06 (Arjun): intentional deviation — log it, no code change.** Same ruling and same shape as the Story 3.6 review (2026-08-05), where a self-flagged code-comment deviation was ruled intentional and logged rather than reverted. `TrendChart.tsx:16` may keep importing `createMonotoneYAt`/`monotonePath` from `energyArc.ts`: they are two pure curve-math helpers, and duplicating them to satisfy the letter of D-3 would be strictly worse. **D-3 is hereby amended** — its "does not extend or reuse `DetailArc`/`energyArc.ts`" clause is narrowed to mean *does not fork the component or its per-play/zoom/click-to-jump behaviour*; sharing stateless curve math is sanctioned. The Completion Notes' claim that the component reuses nothing from `energyArc.ts` was wrong and is corrected below.
- [ ] ~~[Review][Decision] D-3 was amended by code comment only~~ — `TrendChart.tsx:16` imports `createMonotoneYAt`/`monotonePath` from `energyArc.ts`, while D-3 says the component "does **not** extend or reuse" it and "shares only the visual language". `:35` self-declares "a deliberate, narrow amendment to D-3", but D-3 is unchanged and the Completion Notes still claim the opposite. Reusing two pure curve helpers is defensible (beats duplicating), but this project's precedent (Story 3.6 review, 2026-08-05) is to log sanctioned deviations rather than leave them in a comment.

**Patches:**

- [x] [Review][Patch] **HIGH** — Genre/key diversity fabricate a `1.0` data point where a gap belongs [web/lib/sets/styleEvolution.ts:175-203] — `aggregateBucket` returns `null` for `bpmRange` via the `min <= max` guard (`:161`), but builds `genreDiversity`/`keyDiversity` unconditionally whenever `sets.length > 0`. A bucket whose surviving sets have empty `genre_breakdown.buckets` (or all-`null` `camelot_key`) yields `index: 0` → `effectiveDiversity(0) = 1` → a real dot on the axis floor and a caption reading "sits at 1.0 effective genres" for a bucket where zero genres are known. Contradicts D-8, AC-5/AC-6, AD-11, and `BucketPoint`'s own doc comment (`:110-113`, "null … or carries no data for that particular metric" — that branch does not exist). Reachable on shipped data: the fixture has whole months where every surviving genre is the catch-all. All three review layers flagged this independently. Fix must null the plotted point while preserving AC-5/AC-6's always-disclosed `no_genre_count`/`no_key_count`.
- [x] [Review][Patch] **HIGH** — The "nothing to draw" fallback announces a different sentence than it displays [web/app/components/style-evolution/TrendChart.tsx:496-498] — `role="img" aria-label={caption}` (e.g. "No BPM data yet.") wraps a visible `<p>` rendering an inline literal ("Not enough surviving months to draw a trend yet."). AT and sighted users read two different strings — the exact thing AC-4 and the file's own header comment ("same string, never three") forbid. The success and error-boundary branches get it right.
- [x] [Review][Patch] Chart error boundary never resets — one transient failure kills the chart for the session [web/app/components/style-evolution/TrendChart.tsx:196-212] — no `componentDidUpdate` reset, no `key` on the boundary (`:272`), no `componentDidCatch` logging. Metric/granularity/reveal switches re-render the same instance, which stays `failed: true` until a full reload. Task 10 verified fallback but never recovery.
- [x] [Review][Patch] Stale `hover.bucketIndex` throws when the bucket count shrinks [web/app/components/style-evolution/TrendChart.tsx:470,885] — `hover` is cleared only by `onMouseLeave` and is never bounds-checked. Week→Month drops buckets ~85→~20; with the pointer resting on the plot and the toggle activated by keyboard, `buckets[60]` is `undefined` → `bucketDetail` (`:111`) calls `.split("-")` on it → throw. Combines with the finding above into permanent chart death.
- [x] [Review][Patch] Metric and granularity toggles are completely dead when `localStorage.setItem` throws [web/app/components/style-evolution/MetricChipToggle.tsx:46-53, GranularityToggle.tsx:42-49] — `select()` swallows the write error and notifies, but `getSnapshot()` re-reads `localStorage` and returns the unchanged value, so the comment "the notify below still flips the UI" is false. Under quota-exceeded or policy-disabled storage the user is locked to the BPM chart permanently — 2 of the 3 charts unreachable. Needs an in-memory fallback.
- [x] [Review][Patch] "Show other" can hide its own checkbox, leaving an unrecoverable empty bar layer [web/app/components/style-evolution/TrendChart.tsx:407-417,854-872] — `maxBarCount` counts only currently-visible bars, `hasBars` depends on it, and the legend *and* the Show-other checkbox both render behind `{hasBars && …}`. When every category in view is the catch-all, the control that would restore the bars is the thing that disappears. Reachable on the shipped fixture (2026-08, 2025-06).
- [x] [Review][Patch] Y-axis gutter is 58 *viewBox units* in TS and 58 *CSS pixels* in CSS — they agree only at exactly 1000px [web/app/components/style-evolution/TrendChart.tsx:65,118-125 / web/app/style-evolution.css:412,712] — data x is emitted as a percentage (5.8% reserved) while `.se-chart-yaxis` is a fixed `58px` and `.se-chart-cats` hardcodes the same number a third time. At 1200px labels float ~12px left of the data; at 600px the label box overlaps the plot. Directly behind the skipped 375px pass.
- [x] [Review][Patch] The "effective number of genres" explainer is unreachable on touch [web/app/components/style-evolution/TrendChart.tsx:559-580, web/app/style-evolution.css:262-265] — the button has no `onClick` and no `:focus` fallback; the tip shows only on `:hover` or `:focus-visible`, neither of which a tap reliably matches. The CSS comment claims it protects exactly the users it strands. This tooltip is the only place the metric is defined.
- [x] [Review][Patch] Test suite is not pinned to a timezone or locale, so the gate is machine-dependent [web/vitest.config.ts, web/package.json:11] — verified: passes under `TZ=UTC`/`Asia/Tokyo`/`America/New_York`, fails 4 tests under `TZ=Pacific/Kiritimati` and 20 under `LC_ALL=de-DE`. This is what let the two findings above through a green gate.
- [x] [Review][Patch] Story self-report is materially stale [this file] — File List omits 8 changed files (`GranularityToggle.tsx`, `tokens.css`, `dashboard.css`, `MostPlayedCard.tsx`, `SetListPanel.tsx`, `rightColumn.ts`, `dashboard/page.tsx`, `export_real_fixtures.rs`) plus the 51,679-line `recent-sets.fixture.json` regeneration; "34 new unit tests"/"200/200" are actually 61 `it()` blocks and 227/227; Task 8's claim of `role="img"` + `aria-label` "in every branch" is false for the error-boundary branch (`TrendChart.tsx:203-207`); and ~8 post-spec decisions (granularity toggle, `2^H`, in-chart category bars, dashed bridges, `MAX_CATEGORIES` 6→5, `showOther` default-off, two-way reveal) live only in code comments, never in the Design-decisions block. This is the recurring **ai-11** staleness the Epic 3 retro named.
- [x] [Review][Patch] `startMs` reintroduces the exact NaN comparator its own comment claims to prevent [web/lib/sets/rightColumn.ts:59-62,126] — two undated sets give `-Infinity - (-Infinity)` = `NaN`, leaving their relative order engine-defined.
- [x] [Review][Patch] Undated sets inflate the "N low-confidence sessions hidden" count and are silently dropped from every chart [web/lib/sets/styleEvolution.ts:251-262,271] — `buildSeries` skips sets whose bucket key is `""`, but `lowConfidenceCount` counts all sets. The banner offers to reveal a session that revealing cannot show. A high-confidence undated set vanishes with no disclosure at all, in a feature that otherwise discloses `no_genre_count`/`no_key_count` scrupulously.
- [x] [Review][Patch] `shannonEntropy` can return `NaN`, contradicting its own contract [web/lib/sets/styleEvolution.ts:56-66] — the guard is `total <= 0`, and `NaN <= 0` is false, so a malformed `play_count` propagates `NaN` into `mapY` → `style="top: NaN%"` and `d="M … NaN"`. Doc comment promises "never `NaN`/`Infinity`".
- [x] [Review][Patch] Degenerate y-domains render two overlapping axis labels [web/app/components/style-evolution/TrendChart.tsx:594-599,603-607] — when `lineMax === 1`, "1.0" and "1" paint at the identical y; when BPM `min === max`, the same number stacks on itself. `AXIS_LABEL_CLEARANCE` only deconflicts the line label against the bar label.
- [x] [Review][Patch] `bpmRangeSummary`'s single-point branch drops the year on a multi-year axis [web/lib/sets/styleEvolution.ts:337-341] — `withYear` is computed at `:343`, *after* the early return, so the caption reads "in June" while ticks read `Jun '25`/`Jun '26`. Its sibling `diversityTrendSummary` (`:429-432`) computes it before the equivalent branch and is correct. `bucketDetail` (`TrendChart.tsx:111`) takes no `withYear` at all.
- [x] [Review][Patch] `rootRef` is dead and the `CursorChip` clamp it was meant for is unwired [web/app/components/style-evolution/TrendChart.tsx:487,541,876] — `CursorChip` accepts `boundsRef` ("clamp the chip inside this element's rect; omit to clamp to the viewport") and it is never passed, so the hover chip clamps to the viewport rather than the chart card.
- [x] [Review][Patch] `MostPlayedCard` renders a literal "0 sets" label for a DJ with no sets [web/app/components/dashboard/MostPlayedCard.tsx:25,36-37] — `sameSpan` is true at `0 === 0`; the zero boundary is never special-cased the way `hiddenCount === 0` is in `LowConfidenceReveal`.
- [x] [Review][Patch] `MetricChipToggle`'s doc comment describes an architecture that isn't there [web/app/components/style-evolution/MetricChipToggle.tsx:58-65] — it says persistence "live[s] in the parent (StyleEvolutionView)", but it lives in `useMetricSelection` in that same file and `StyleEvolutionView` touches no storage. Related: `VALID_METRICS` (`:7`) and `METRIC_CHIPS` (`:66-70`) are two hand-maintained lists of the same three metrics.

**Deferred** (real, but pre-existing or out of scope — logged in `deferred-work.md`):

- [x] [Review][Defer] Locale-dependent labels generated in an SSR'd client component → hydration text mismatch [web/lib/sets/styleEvolution.ts:295-307] — deferred, pre-existing: `toLocaleDateString([])` means "runtime default locale", so the server renders "March" and a German browser re-renders "März". Verified 20 test failures under `LC_ALL=de-DE`. Pre-dates this story app-wide (`format.ts:11,13,51,59`, `listModel.ts:91`); needs one consistent i18n decision, not a point fix here.
- [x] [Review][Defer] No rolling window — one outlier `started_at` blows up the axis and the server render [web/lib/sets/styleEvolution.ts:216-244,257-262] — deferred, pre-existing shape: `fillMonthRange`/`fillWeekRange` emit every bucket first→last uncapped, then `buildSeries` runs `buckets.map(b => sets.filter(…))` twice with a fresh `new Date` per pair. A single epoch-dated or far-future set yields ~670 month / ~2900 week buckets inside a blocking server component. Same unbounded growth applies benignly to a genuinely long library.
- [x] [Review][Defer] The dashed gap-bridge affordance is undocumented to the user [web/app/components/style-evolution/TrendChart.tsx] — deferred, polish: the model correctly renders zero-surviving buckets as gaps (D-8 compliant), and the bridge is deliberately straight + dashed so it never reads as an interpolated trend — but the SVG is `aria-hidden`, the legend has no dashed-line entry, and the caption never mentions skipped buckets, so the meaning lives only in a code comment.

**Dismissed as noise (2):** the Acceptance Auditor's framing of `GranularityToggle` as an AC-1/D-3 *violation* (it is attributed to Arjun in-session; retained above as documentation debt, not a defect) and its flag that Task 10 reads `[x]` while the 375px pass is incomplete (honestly self-flagged in the story already; carried forward as a follow-up below, not a finding).

**375px pass — COMPLETED 2026-08-06 (code review).** Driven with Playwright against the real dev server at 375×812 and 320×720, measuring the DOM rather than eyeballing a screenshot. The earlier session's `resize_window` tool limitation did not recur. It was right not to trust the "no new responsive mechanism" substitution: the pass found **three more defects that only exist below ~640px**, all now fixed.

- [x] [Review][Patch] Y-axis labels sat on top of the leftmost data column at phone width [web/app/style-evolution.css, TrendChart.tsx] — a direct consequence of the earlier gutter fix. Making the gutter a percentage (5.8%) is correct for *alignment* at every width, but 5.8% of 375px is ~17px while `"53 plays"` measured 47px, against a first data point at 25px. Fixed by shrinking the label type and dropping the `plays` noun below 640px (the legend already names the bars). Verified: widest label 16px vs 25px data start at 375, and 16.2px vs 20.3px at 320.
- [x] [Review][Patch] Explainer tap target was 15×15px — below WCAG 2.2 AA's 24×24 minimum (SC 2.5.8), in a story that claims AA (UX-DR21) [web/app/style-evolution.css] — fixed with an invisible `::after` that grows the hit area to 24×24 without changing the glyph or disturbing the inline text flow. Verified by hit-testing all four corners at ±11px.
- [x] [Review][Patch] The explainer tooltip hung off the left edge of the screen [web/app/style-evolution.css] — anchored `right: 0` to a trigger only ~250px from the left edge, so a 280px tip grew leftward to `left: -27px` and lost its opening words. Capped at `min(280px, calc(100vw - 8rem))`. Verified `left: 6` at 375 and `left: 61` at 320.
- [x] [Review][Patch] Diversity axis ceiling now uses significant figures, not fixed decimals [TrendChart.tsx] — `"16.9"` was wide enough to overlap the first column at 320px even after the type shrank. A tenth of an effective genre is noise at double digits; the caption still carries the precise value.

Also verified at 375 and 320, all three metrics: no page-level horizontal overflow, `aria-label` identical to the visible caption in every branch (AC-4), and the full interaction path — metric chips, Month↔Week granularity, and the two-way low-confidence reveal — with **zero console errors or warnings**.

## Dev Notes

- **Data source:** `getRecentSets()` (`web/lib/sets/index.ts:36-38`) — same seam as the dashboard, already returns the full synced history. No `shared/` contract change, no new data-access function.
- **Fields to use:** `derived.bpm_distribution.{min,max}`, `derived.genre_breakdown.{buckets,no_genre_count}`, `derived.confidence.value`, per-play `camelot_key`, `set.started_at` (local-month bucketing key).
- **Field NOT to use:** `derived.camelot_mixing_stats` — that's harmonic transition-compatibility (in-key/out-of-key/no-key between consecutive plays), a different concept from "which keys got played." Reaching for it here would be the wrong data source for AC-6.
- **AD-12 caveat (informational, no code guard owed today):** genre normalization is versioned (`taxonomy_version` per play). Merging `genre_breakdown` across a month's sets assumes a stable taxonomy version, which holds today (the taxonomy table has never changed) — leave a one-line comment near the merge, nothing more.
- **No agent-side or `shared/` changes** — this is a pure `web/` story built entirely on fields already synced (`1-10-freeze-the-shared-sync-contract.md`'s frozen contract already carries everything needed).
- **Reuse:** `CursorChip` (`web/app/components/ui/CursorChip.tsx`) for hover; `format.ts` formatters where applicable (e.g. `formatBpm`). The dashboard's server-page/client-sub-component split is the structural precedent to follow.

### Project Structure Notes

- New: `web/lib/sets/styleEvolution.ts` (+ `.test.ts`), `web/app/components/style-evolution/*` (chart + chip-toggle + reveal-affordance components).
- Replaced: `web/app/(authenticated)/style-evolution/page.tsx` (currently a Story 3.5 throwaway stub).
- Unchanged: `web/app/components/nav/FloatingNav.tsx` (route already wired), `shared/` (frozen contract untouched), `agent/` (no changes).
- Follows the existing `web/lib/sets/*` pure-function-over-`SetRecord[]` convention (`hero.ts`, `listModel.ts`, `rightColumn.ts`, `dancefloor.ts`) — no variance from established structure.

### References

- [Source: epics.md#Story 4.1, lines 796-807] — ACs 1-4, FR-9/FR-27/UX-DR6/DR7/DR19 citations.
- [Source: epics.md#Story 1.8, lines 329-340] — FR-27 signal origin.
- [Source: epics.md#Story 1.10, lines 354-365] — frozen `shared/` contract.
- [Source: prd.md:215] — FR-9 statement.
- [Source: review-rubric.md:28,32] — the never-resolved genre-diversity/key-usage definition gap, closed by D-1/D-2.
- [Source: ARCHITECTURE-SPINE.md#AD-1, AD-2, AD-3, AD-12] — aggregation boundary, frozen-contract discipline, taxonomy-version caveat.
- [Source: EXPERIENCE.md, lines 73, 74, 91, 98] — chip-toggle/one-chart-at-a-time, Chart Summary register, insufficient-history exact copy, chart-failed fallback.
- [Source: SPEC.md, lines 46-48, 124] — CAP-6 intent/success, accepted genre-coverage risk.
- [Source: agent/src-tauri/src/confidence.rs, lines 95-135] — binary confidence signal, backs D-4/D-7.
- [Source: shared/src/index.ts, lines 181-187] — `derived.confidence` field, explicitly earmarked for this story.
- [Source: web/lib/sets/energyArc.ts, lines 126-189] — `arcTextEquivalent`, the Chart Summary generator pattern to follow.
- [Source: web/lib/sets/listModel.ts, lines 51-57] — `localDayKey`, the local-time bucketing precedent for the new `localMonthKey`.
- [Source: web/lib/sets/index.ts, lines 31-38] — `getRecentSets()`, the data-access seam this story reuses unchanged.
- [Source: deferred-work.md, line 61] — the dashboard's own low-confidence leak, naming this story's pattern as its future fix.
- [Source: sprint-status.yaml action item ai-4] — the open 865-play blind-spot note this story closes.

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (claude-sonnet-5), via the `bmad-dev-story` skill, working tree `.claude/worktrees/story-4-1` off local `main` (`8968c01`).

### Debug Log References

- **Worktree misplacement (self-caught, fixed same session):** despite `EnterWorktree` switching the session into `.claude/worktrees/story-4-1`, all file Read/Write/Edit calls through most of the session used absolute paths rooted at the main checkout (`/Users/arjun/Documents/name-pending/...`) instead of the worktree path — a tooling mistake, not a deliberate choice. All of Story 4.1's implementation landed on `main`'s working tree instead of this worktree. Discovered when a concurrent Story 4.2 create-story session (writing directly into this worktree in parallel) surfaced the mismatch in its own sprint-status.yaml note. Fixed by: diffing every touched file between this worktree and `HEAD` to confirm which were genuinely untouched by the concurrent session (all but `sprint-status.yaml`), copying the new/modified files across, hand-merging `sprint-status.yaml`'s two independent change sets (this story's + the concurrent 4-2 session's) rather than overwriting either, then reverting `main`'s working tree back to clean via `git checkout --`/`rm` for exactly the files this session had touched. Re-ran the full gate (`pnpm install`, vitest, tsc, eslint, `next build`) from the correct worktree afterward — all green, matching the earlier (mistakenly-located) run.
- `no-hardcoded-colors.test.ts` false-positive: `.se-chart-tick`'s `white-space: nowrap` tripped the named-color regex (`\bwhite\b` matches inside "white-space"). Fixed by deleting the line (unneeded — short month-abbreviation ticks never wrap).
- `react-hooks/set-state-in-effect` lint error in the original `useMetricSelection` (localStorage read via `useEffect` + `setState`). Rewritten against `useSyncExternalStore`, matching `CursorChip`/`SilkBackdrop`'s existing external-state-read pattern; same-tab reactivity added via a manual listener set since the native `storage` event doesn't fire in the writing tab.
- Real-browser walkthrough hit one `resize_window` tool limitation (375px viewport never actually applied — `window.innerWidth` stayed 1440 after two attempts); the 375px pass was substituted with code review against the already phone-verified `dz-shell`/nav-rail breakpoints (see Task 10 notes in Tasks/Subtasks above).

### Completion Notes List

- Built the full Story 4.1 vertical slice: `web/lib/sets/styleEvolution.ts` (month-bucketing, Shannon-entropy diversity indices for genre/key per D-1/D-2, dual excluding/including aggregation per D-4, three Chart Summary generators per energyArc.ts's "one generator, three duties" pattern), a new `TrendChart` component (D-3 — not a fork of DetailArc/energyArc.ts), the metric chip toggle (`useSyncExternalStore`-backed localStorage persistence, D-6), the low-confidence reveal affordance (D-4, mirrors the dashboard's `SetListPanel` pattern verbatim), the insufficient-history gate (D-5, exact EXPERIENCE.md:91 copy), and page wiring replacing the Story 3.5 stub.
- Closed sprint-status.yaml action item **ai-4** (D-7's 865-play blind-spot sentence) and appended a bookkeeping addendum to `deferred-work.md:61` (found already resolved by an unrelated 2026-08-06 ruling — did not reopen or reclose it).
- New unit tests in `styleEvolution.test.ts` (month bucketing incl. year boundary + DST, entropy edge cases, dual-computation exclusion/reveal divergence, D-8 gap behavior, all three Chart Summary generators including a "never surfaces a raw entropy number" assertion for the diversity copy). **Count corrected 2026-08-06 during code review:** this said "34 new unit tests" and "200/200 passing", both stale — the file actually carries 66 `it()` blocks and the suite ran 227/227 at review time (232/232 after the review's own regression tests). The gate itself was genuinely green; only the numbers were wrong, because the self-report predates the post-launch-review work it also describes.
- Real-browser walkthrough at 1440 (Chrome, real fixture data) confirmed: chip toggle + persistence-across-reload, reveal affordance + reset-on-reload (verified via a genuine `location.reload()`, not a soft nav), forced chart-render-failure fallback (monkey-patched `Number.prototype.toFixed`), and the insufficient-history gate (verified directly against a hand-built one-calendar-month dataset through the real `buildStyleEvolution`, not by mutating the shared fixture). Zero real console errors — the one hydration warning present is the ColorZilla extension's `cz-shortcut-listen` attribute, unrelated to this story. 375px pass not completed due to a `resize_window` tool limitation this session (see Debug Log References); not considered blocking given no new responsive mechanism was introduced.
- A quick writing-guidelines pass on the new diversity-trend copy (Task 2) caught and fixed one real issue: mid-sentence noun capitalization in the "No … data yet." branch.

### File List

Corrected 2026-08-06 during code review — the original list omitted 8 changed files plus the fixture regeneration (the recurring **ai-11** self-report staleness).

Story 4.1 proper:

- `web/lib/sets/styleEvolution.ts` (new)
- `web/lib/sets/styleEvolution.test.ts` (new)
- `web/app/components/style-evolution/TrendChart.tsx` (new)
- `web/app/components/style-evolution/MetricChipToggle.tsx` (new)
- `web/app/components/style-evolution/GranularityToggle.tsx` (new — **added post-launch-review**, not in the original spec; see D-9 below)
- `web/app/components/style-evolution/LowConfidenceReveal.tsx` (new)
- `web/app/components/style-evolution/InsufficientHistory.tsx` (new)
- `web/app/components/style-evolution/StyleEvolutionView.tsx` (new)
- `web/app/style-evolution.css` (new)
- `web/app/globals.css` (modified — added `style-evolution.css` import)
- `web/app/tokens.css` (modified — `--chart-cat-1…6` + `--chart-cat-other` categorical palette for the in-chart category bars)
- `web/app/(authenticated)/style-evolution/page.tsx` (modified — replaced the Story 3.5 stub)
- `web/vitest.config.ts` (modified — pinned `TZ`/`LANG`/`LC_ALL`, code review)

Rode along on this branch but belong to no Story 4.1 AC (flagged as scope creep in review):

- `web/lib/sets/rightColumn.ts` (modified — most-played rewritten from calendar windows to last-N-sets; `buildRightColumn` lost its `nowMs` param and `mostPlayed` changed shape `{week,month}` → `{recent,extended}`)
- `web/app/components/dashboard/MostPlayedCard.tsx` (modified — `sameSpan` collapse, new `.mp-span` label)
- `web/app/components/dashboard/SetListPanel.tsx` (modified — reveal gained a two-way "hide them" path)
- `web/app/(authenticated)/dashboard/page.tsx` (modified — new `MostPlayedCard` props)
- `web/app/dashboard.css` (modified — `.mp-span`)
- `agent/src-tauri/tests/export_real_fixtures.rs` (modified — 8 → 58 exported sets, skip-on-failure path)
- `web/lib/sets/recent-sets.fixture.json` (regenerated — **+51,679 / −542**, 8 → 58 sets spanning 2025-01…2026-08)

Docs:

- `_bmad-output/implementation-artifacts/sprint-status.yaml` (modified — story status, ai-4 closure)
- `_bmad-output/implementation-artifacts/deferred-work.md` (modified — bookkeeping addendum, plus 3 entries deferred from code review)

### Decisions made after the spec was locked (recorded 2026-08-06 during code review)

These were all worked out live with Arjun during the post-launch-review pass and existed only as code comments until the review surfaced them. Recorded here so the spec and the code agree; **D-9 and D-10 remain open questions in the Review Findings above** (they change what AC-1/AC-5/AC-6 mean, so they are Arjun's to ratify, not the review's to assume).

9. **D-9 (proposed) — Week/Month granularity toggle.** Monthly-only bucketing read as too sparse. Adds `localWeekKey`/`fillWeekRange` and a full parallel `week` series, plus a second persisted `localStorage` key. AC-1/D-3 say "month-bucketed" throughout; D-5's insufficient-history gate stays month-based regardless of the view.
10. **D-10 — LOCKED 2026-08-06 (Arjun, code review): the plotted quantity is `2^H` ("effective number of genres/keys"), not raw entropy `H`.** AC-5 and AC-6 above were amended to say so, rather than leaving the code silently redefining them. Entropy stays the *definition* (which is what closed the PRD's numerical gap); `2^H` is the *presentation*. Original proposal follows. A Hill number of order 1; monotone in `H`, so the trend shape is identical, but AC-5/AC-6 as written define `H`. Motivated by "more/less variety wasn't quantitative enough — what does that even mean?"
11. **D-11 — in-chart grouped category bars** behind the diversity line (replacing a separate breakdown panel), globally ranked with a stable colour/slot per category, capped at `MAX_CATEGORIES = 5` named categories with a disclosed "Other" fold.
12. **D-12 — catch-alls default hidden** (`showOther` off): the "Other" bar is usually the tallest and says the least, so hiding it lets the real categories rescale.
13. **D-13 — gap months get a straight dashed bridge**, never a curve — a curve through a gap would read as an interpolated trend.
14. **D-14 — dots are always drawn**, not hover-only, so isolated single-bucket runs are visible at rest.
15. **D-15 — the low-confidence reveal is two-way** ("show them" / "hide them"); the dashboard's `SetListPanel` was brought to match. Still unpersisted, so D-4 holds.
16. **D-16 — labels carry their year once the axis spans more than one** ("peaked in December" and two ticks both reading "June" named nothing once the fixture grew past twelve months).

## Change Log

| Date | Change |
|---|---|
| 2026-08-06 | Story 4.1 implemented in full: pure aggregation lib (month-bucketing, D-1/D-2 Shannon-entropy diversity, D-4 dual excluding/including computation, three Chart Summary generators), new `TrendChart` component (D-3, band + line rendering, chart-failed error boundary), metric chip toggle (D-6, `useSyncExternalStore`-backed localStorage persistence), low-confidence reveal affordance (D-4, mirrors `SetListPanel`'s shipped pattern), insufficient-history gate (D-5, exact EXPERIENCE.md:91 copy), page wiring replacing the Story 3.5 stub. Closed sprint-status.yaml ai-4 (D-7 sentence); appended a bookkeeping addendum to deferred-work.md:61 (found already resolved by an unrelated ruling). 34 new unit tests; full web/ gate green (200/200 tests, typecheck, lint, `next build`); real-browser walkthrough at 1440 covering chip persistence, reveal reset-on-reload, forced chart-render-failure fallback, and the insufficient-history gate against a hand-built dataset. 375px pass not completed (tool limitation, not blocking — see Debug Log References). Status → review. |
