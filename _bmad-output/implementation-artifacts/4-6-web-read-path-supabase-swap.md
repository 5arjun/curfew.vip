---
baseline_commit: 22b449b8c0884a5a525d2b3d39133113725e6fa0
---

# Story 4.6: Web read path — swap the committed fixture for Supabase

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a DJ,
I want the dashboard, Set Detail, and Style Evolution pages to show my real synced data,
so that the sets and library adds the agent has been syncing since Epic 3 actually appear, instead of the same committed fixture every DJ sees.

## Context & Authority

**No separate design-spec doc exists for this story — it is pure infrastructure, not a feature.** Its authority is `epics.md` Story 4.6 (added 2026-08-07, this session) plus the ruling below, worked out directly with Arjun while reviewing Story 4.2's deferred-work backlog.

**The gap:** `web/lib/sets/index.ts` is the one seam every set/library-consuming page imports through (Story 3.6 Task 4, AC-13/SM-1) — its own header comment says "when the cloud read path lands, only the three function bodies below change to a Supabase query and every component keeps working unchanged (Decision A: the fixture is the day-one stand-in for the real read)." That swap has never happened. The agent write path has been fully live since Epic 3 (`sync_set` → `sessions`/`sets`/`plays`, idempotent `PUT`) and Story 4.2 added a second live write path (`sync_library_add_events` → `library_track_events`). Meanwhile `getRecentSets`, `getSetById`, `deleteSet`, and `getLibraryAddEvents` all still read `recent-sets.fixture.json` / `library-add-events.fixture.json` — the same static, committed data for every DJ, regardless of what their agent has actually synced. Only `getAgentStatus` (Story 3.9) reads Supabase for real; it is this story's working model for how the other four should behave.

**Why this couldn't just get flagged and left:** production data is already accumulating (every real DJ's agent has been syncing since Epic 3) that the product cannot display. Flagged as a "[LAUNCH BLOCKER — unstoried]" entry in `deferred-work.md` during Story 4.2's review — it didn't fit Epic 4 (Style Evolution/Library Utilization features), Epic 5 (segments/enrichment), 6 (marketing), or 7 (billing), so it sat undiscovered by the normal per-epic story-creation process. Ruled by Arjun 2026-08-07: create the story now, sequence it at the end of Epic 4 (cross-cutting infrastructure, not a Style Evolution feature, but needs to land before Epic 5 work starts reading through the same seam), rather than let it accrete further.

**Sources:**
- `web/lib/sets/index.ts` — the seam itself. Read the whole file before starting; it is short (142 lines) and every doc comment on it is load-bearing context, not decoration.
- `deferred-work.md` — the "[LAUNCH BLOCKER — unstoried]" entry this story closes (originally logged during Story 4.2's implementation, confirmed still open during this story's creation).
- `sprint-status.yaml` — Epic 4 section, `4-6-web-read-path-supabase-swap` entry and its comment.
- `ARCHITECTURE-SPINE.md` AD-7 — the owner-`SELECT`-only RLS convention every table here already follows (`sessions`/`sets`/`plays`/`library_track_events`/`agent_status`). This story reads through existing RLS; it does not add or change any policy.
- `supabase/migrations/20260730204057_create_sessions_sets_plays.sql` — the actual `sessions`/`sets`/`plays` table shapes and RLS policies `getRecentSets`/`getSetById`/`deleteSet` must query against.
- `supabase/migrations/20260807100000_create_library_track_events.sql` — the `library_track_events` table shape `getLibraryAddEvents` must query against.
- `3-9-console-voice-failure-register-state-a11y-responsive-pass.md` (or wherever `getAgentStatus` was introduced/hardened) — the resilience pattern (lazy `createClient` import, try/catch, dev-only `console.error`, calm `null`/empty fallback in production) this story's four functions must match, not reinvent.
- `3-6-dashboard-home.md` Task 4 — the seam's own founding contract (AC-13/SM-1: "the dashboard and Set Detail import ONLY from here — never a fixture file, a Supabase client, or the wire envelope directly").
- `4-2-library-to-setlist-correlation-trend.md` — introduced `getLibraryAddEvents()` on the same fixture-first-swap-later footing as the other three; its Dev Notes/File List are the most recent precedent for this exact seam.

## Acceptance Criteria

Reproduced verbatim from `epics.md` Story 4.6 (added 2026-08-07):

1. **Given** the data-access seam in `web/lib/sets/index.ts`, **Then** `getRecentSets`, `getSetById`, `deleteSet`, and `getLibraryAddEvents` read from Supabase (`sessions`/`sets`/`plays`/`library_track_events`, all owner-SELECT via RLS per AD-7) instead of the committed fixtures — no `dj_id` filter needed or wanted, `auth.uid()` is the filter, matching `getAgentStatus`'s existing precedent exactly. *(Decision A's swap point; AD-7)*
2. **Given** every component that currently imports from this seam — confirmed at story-creation time to be `dashboard/page.tsx` (`getRecentSets`), `style-evolution/page.tsx` (`getRecentSets`, `getLibraryAddEvents`), `set/[id]/page.tsx` (`getSetById`), and `set/[id]/actions.ts` (a server action wrapping `deleteSet` for `DeleteModal.tsx`) — **plus `library-utilization/page.tsx` (`getRecentSets`, `getLibraryAddEvents`)**, which this AC and Task 1 originally described as "still a Story 3.5 throwaway stub that imports nothing from this seam today"; that was **already stale at story-creation time** — Story 4.3 (commit `cd72bd8`) had made it a real caller, corrected here by the 2026-08-07 code review — **Then** none of them change — the seam's function signatures and return shapes are preserved so the swap is invisible above this file, per the seam's own founding contract ("only the function bodies below change"). *(Story 3.6 Task 4, AC-13/SM-1)*
3. **Given** a DJ with no synced sets or add-events yet (a brand-new account, or Epic 3/4's own dev/test accounts before the agent has run), **Then** each function returns the same empty/`null` shape the fixture stage returns today, rendering the existing insufficient-history / empty-dashboard states — never a thrown error. *(Mirrors `getLibraryAddEvents`'s existing "day one, every DJ is empty by construction" contract)*
4. **Given** a genuine Supabase read failure (missing env, broken RLS, network), **Then** it fails calmly and resiliently exactly like `getAgentStatus` does today — rendered identically to "nothing synced yet" in production, but logged loudly in non-production so a real regression cannot sit invisible indefinitely. *(Mirrors `getAgentStatus`'s existing resilience contract)*
5. **Given** `deleteSet`, **Then** it performs a real hard delete against Supabase (not the fixture stage's in-memory `store` mutation), scoped by RLS so a DJ can only ever delete their own row. *(AC-12's original "removal path" intent, now against the real store)*
6. **Given** the committed fixtures (`recent-sets.fixture.json`, `library-add-events.fixture.json`) and their generator scripts (`build-fixture.mjs`, `build-library-fixture.mjs`), **Then** a decision is made and recorded on whether they are retired, or kept solely as local-dev/test fixtures decoupled from the production seam — not left ambiguous about which one `web/lib/sets/index.ts` actually serves. *(Prevents the same "which one is real" ambiguity this story exists to close)*
7. **Given** the full test suite, **Then** existing seam-consuming component tests are updated to mock/stub the Supabase client rather than relying on fixture data being served automatically, and the seam itself gains coverage for the empty-state and failure-state paths (AC-3/AC-4). *(Standing gate discipline, ai-8)*

## Tasks / Subtasks

> Suggested order: read the seam and its callers first (nothing here is safe to guess) → design the four queries against the real table shapes → swap the bodies one at a time, `getAgentStatus`-style → decide the fixtures' fate → update/add tests → doc writebacks → verification.

- [x] **Task 1 — Read before writing** (no AC, prerequisite for all)
  - [x] Read `web/lib/sets/index.ts` in full — every doc comment on it describes a contract this story must preserve or deliberately supersede; do not skim.
  - [x] Read every current caller of `getRecentSets`, `getSetById`, `deleteSet`, `getLibraryAddEvents` — confirmed at story-creation time: `dashboard/page.tsx`, `style-evolution/page.tsx`, `set/[id]/page.tsx`, and `set/[id]/actions.ts` (the server action `deleteSet` actually goes through, one layer below `DeleteModal.tsx`) — to confirm none of them need to change (AC-2). `library-utilization/page.tsx` is **not** a current caller (still Story 3.5's stub) — re-grep for the seam's exports anyway rather than trusting this list is still exhaustive by the time this story is implemented.
  - [x] Read `supabase/migrations/20260730204057_create_sessions_sets_plays.sql` and `supabase/migrations/20260807100000_create_library_track_events.sql` in full for the real column names, RLS policies, and any existing indexes the queries below should use.
  - [x] Read `SetRecord` (`web/lib/sets/types.ts`) — it is `SyncPayload["set"]` verbatim (`external_id`, `started_at`, `ended_at`, `plays[]`, `derived`) — and `LibraryAddEvent`/`LibraryAddEventSnapshot` (`libraryConversion.ts`, `index.ts`) to know the exact shape each query must reconstruct.

- [x] **Task 2 — `getLibraryAddEvents`: the simplest swap first** (AC: 1, 3, 4)
  - [x] Rewrite the body to select `track_id, added_at` from `library_track_events`, no `dj_id` filter (RLS is the filter), matching `getAgentStatus`'s try/catch/lazy-import/dev-only-log shape exactly.
  - [x] Empty result → `{ events: [], readAtMs: Date.now() }`, same as today's fixture-empty case would look like — confirm this actually renders the insufficient-history copy per AC-3, don't just assume the shape matches.
  - [x] Do this one first: it is a flat single-table read with no join, the smallest possible version of the pattern the other three repeat.

- [x] **Task 3 — `getRecentSets` / `getSetById`: reconstructing `SetRecord`** (AC: 1, 2, 3, 4)
  - [x] Design the query (or queries) that reconstruct a `SetRecord` (`external_id`, `started_at`, `ended_at`, `plays[]`, `derived`) from `sessions`/`sets`/`plays`. Confirmed at story-creation time: `derived` is a `jsonb` column directly on `sets` (`20260730204057_create_sessions_sets_plays.sql:42`) — a plain `select` retrieves it with no reassembly needed.
  - [x] **`sets.id` IS the `external_id`/`set_id` — there is no separate `external_id` column** (same migration, lines 27-29). `getSetById`/`deleteSet` must filter on `sets.id = externalId`, not a column literally named `external_id`; don't let the `SetRecord.external_id` field name in the TS type lead to querying a column that doesn't exist.
  - [x] `getRecentSets`: newest-first by `started_at` — the fixture-stage comment already notes the sort is applied in-function "regardless of fixture order or a future unsorted Supabase result"; keep sorting in the function rather than trusting query order, per that existing rationale.
  - [x] `getSetById(externalId)`: single-row fetch by `id = externalId` (see the `sets.id` note above), `null` on not-found — RLS makes a not-found and a not-mine indistinguishable by design, matching the fixture stage's existing `?? null` behavior.
  - [x] No results (new account) → `[]` / `null` respectively, per AC-3.

- [x] **Task 4 — `deleteSet`: a real hard delete, blocked on a new migration** (AC: 1, 5)
  - [x] **Confirmed at story-creation time: no DJ-facing `DELETE` grant or policy exists on `sessions`/`sets`/`plays` today** (`20260730204057_create_sessions_sets_plays.sql:92-109` — only `select` grants + `*_select_own` policies; the migration's own comment says write access, including delete, is deliberately withheld pending a future write-path story). **This story is that story.** A client-side Supabase `.delete()` will fail (permission denied / RLS-filtered to nothing) until this is added — do not attempt to implement AC-5 without first writing the migration.
  - [x] Add a new migration granting a scoped DJ-owned `DELETE` policy (`for delete using (auth.uid() is not null and auth.uid() = dj_id)`, matching the shape of the existing `*_select_own` policies), or a `SECURITY DEFINER` RPC mirroring `sync_set`/`sync_library_add_events`'s shape — pick whichever fits this codebase's existing convention better for a DJ-initiated (not agent-initiated) write; a direct RLS-scoped `DELETE` policy is likely the simpler and more consistent choice, since this is a user action through the authenticated web client, not an agent sync.
  - [x] Once the delete path exists, replace the fixture stage's in-memory `store = store.filter(...)` with the actual `delete` against Supabase scoped to `id = externalId` (see the `sets.id` note in Task 3), relying on RLS to make cross-DJ deletion impossible rather than adding a redundant application-level `dj_id` check.
  - [x] Confirm cascade behavior: deleting a `sets` row should also remove its `plays` rows (check for `on delete cascade` on `plays`'s foreign key, or handle it explicitly if not present) so a delete never leaves orphaned play rows behind.
  - [x] New pgTAP isolation test for the delete policy, mirroring the existing `*_isolation_test.sql` pattern: a DJ can delete their own set, cannot delete another DJ's set.

- [x] **Task 5 — Fixtures' fate (AC-6)** (AC: 6)
  - [x] Decide, in the open rather than by default: retire `recent-sets.fixture.json` / `library-add-events.fixture.json` and their generators from the production import path entirely, or explicitly repurpose them as local-dev/test-only fixtures wired through a separate, clearly-named seam (e.g. a `USE_FIXTURE_DATA` dev flag, or fixture-backed test doubles only). Either is acceptable; leaving `web/lib/sets/index.ts` importing both a fixture and a Supabase client with no documented reason is not.
  - [x] If kept for tests, update their own header comments (they currently describe themselves as the production stand-in) so a future reader doesn't reach the same "which one is real" confusion this story exists to resolve.

- [x] **Task 6 — Tests** (AC: 7)
  - [x] No existing test file or Supabase-client mocking convention covers `web/lib/sets/index.ts` today — confirmed by grep during story creation. This story establishes that pattern; look at how `getAgentStatus`'s own callers/tests (if any) handle the lazy `@/lib/supabase/server` import for a starting point, and keep the mock shape consistent across all four rewritten functions rather than inventing a new approach per function.
  - [x] New test coverage for the seam itself: empty-state path (AC-3) and failure-state path (AC-4) for each of the four functions, at minimum.
  - [x] Audit existing component/page tests that currently pass because they transitively read the committed fixture through this seam — they need to mock the seam (or the Supabase client) explicitly once the fixture is no longer the default read path, or they will start failing (or worse, silently pass against stale assumptions) after this story lands.

- [x] **Task 7 — Doc writebacks** (no AC, process)
  - [x] `deferred-work.md`: close the "[LAUNCH BLOCKER — unstoried]" entry this story resolves, pointing here.
  - [x] `web/lib/sets/index.ts`'s own header comment: it currently describes itself as fixture-backed with Supabase as a future swap — rewrite it to describe current reality once this story ships, the same way `getAgentStatus`'s comment already does.
  - [x] `sprint-status.yaml`: this story's own comment block (added at creation) already explains why it's sequenced here; no further note needed unless Task 4's RLS finding changes scope.

- [x] **Task 8 — Verification & gates** (AC: all)
  - [x] Full `web` gate: lint, typecheck, test, `next build` — clean.
  - [x] Manual verification against a real Supabase project with real synced data (at least one DJ account with agent-synced sets and library add-events): dashboard, Set Detail, and Style Evolution all render real data, not the old fixture's fixed set. Delete a set via Set Detail's `DeleteModal` (through `set/[id]/actions.ts`) and confirm it is actually gone from Supabase (including its `plays` rows), not just the page.
  - [x] Manual verification against a brand-new/empty DJ account: every page renders its existing empty/insufficient-history state, not an error.
  - [x] Force a Supabase read failure (e.g. temporarily bad env var) and confirm the calm-fallback behavior matches `getAgentStatus`'s (empty/no-agent-equivalent rendering, dev-console log, no thrown error reaching a page).

### Review Findings

Code review 2026-08-07. The verification gap the Completion Notes flagged was **closed during this review**: PostgREST/Kong came up cleanly, so the nested `sets(...plays(...))` embed, the DELETE policy, the cascade, and `sync_set` re-ingest were all exercised through real REST calls. Three findings below are empirically proven, not inferred.

- [x] [Review][Patch] **RULED 2026-08-07 (Arjun): build the suppress table now.** Add a `deleted_sets` tombstone keyed on stable session identity plus a `sync_set` check that skips re-ingest, with pgTAP coverage; then close `deferred-work.md:60` pointing here. **A deleted set resurrects on the next agent sync — the tombstone this story was named owner of was not built or re-flagged.** Proven live: synced a set via `sync_set` (id `872d5614-…`), deleted it through the real REST delete path (`sets=0`), re-ran `sync_set` with the same `session_identity` → `sets=1, plays=1`, identical id. `sync_set` computes `computed_set_id := uuid_generate_v5(dj_id, session_identity)` and `on conflict (id) do update`, and nothing consults a suppress list. `deferred-work.md:60` assigns this explicitly to "the Supabase sync/read-path story" and states "once the cloud read path lands, a naive row-delete WOULD resurrect the set on the next sync" — that precondition is now true. `DeleteModal.tsx:98` tells the DJ "it can't be undone." That entry was left open and unmentioned. Options: build the `deleted_sets` suppress table + `sync_set` check (what the ledger specifies), or re-flag with an explicit ruling. [web/lib/sets/index.ts:225; supabase/migrations/20260731120000_create_sync_set_function.sql; deferred-work.md:60]
- [x] [Review][Patch] **RULED 2026-08-07 (Arjun): derive the label from `sessions.session_identity`.** Add `sessions(session_identity)` to the embed and map it into the label — no migration, no agent change. Verified formats: `serato4:<id>` where the numeric part is Serato's own `history_session.id` (so `serato4:975` → `SET 975`, restoring the fixture's exact label for modern captures), and `legacy:<fnv1a_hex>` for pre-Serato-4 sets, which has no Serato-visible number and should render the short hash rather than a UUID. `external_id` stays `sets.id` (it is the correct routing/delete key); only the *display* label changes. **`external_id` silently changed kind — Set Detail now renders `SET 872d5614-9894-5803-80f5-aa1dd4177944` where it rendered `SET 975`.** `toSetRecord` maps `external_id: row.id`, but `sets.id` is not the agent's `external_id`: the `sync_set` RPC has **no `external_id` parameter at all** (verified against the live function signature and `agent/src-tauri/src/sync.rs:58-64`), so the Serato session id is never persisted. Fixture values were `'975'`/`'17577'`; `formatSessionLabel`'s own doc comment reads `/** Session-id header label, e.g. "SET 975". */`. Task 3's premise ("`sets.id` IS the `external_id`") is true of the *column* and false of the *value*. This breaks AC-2's invisibility clause on every Set Detail page, and a 36-char id in a mono header is also a 375px layout risk. The original id is not recoverable from the DB today — so this needs a ruling: join `sessions.session_identity` for the label, persist a real `external_id` (migration + agent change), or change the label. [web/lib/sets/index.ts:99; web/lib/sets/format.ts:18-21; web/app/components/set-detail/SetHeader.tsx:81]
- [x] [Review][Patch] **RULED 2026-08-07 (Arjun): rethrow from `deleteSet` on a Supabase error.** No signature change and no caller edit, so AC-2 holds: `deleteSetAction` propagates and `DeleteModal`'s existing error branch becomes reachable again. Update the header comment — "follows `getAgentStatus`'s shape exactly" is now true of the three reads only, deliberately not of the mutation. The RLS-filtered zero-row no-op stays indistinguishable (that would need `count: "exact"` and a caller change); note it in the code rather than silently leaving it. **`deleteSet` reports success when the delete failed or affected nothing, making `DeleteModal`'s error state dead code.** It returns `void`, logs only in dev, never rethrows; `actions.ts:16-18` then unconditionally `revalidatePath` + `redirect("/dashboard?deleted=1")`. A calm empty fallback is truthful for a read; for a mutation it asserts something that did not happen. Also verified live: an RLS-filtered cross-DJ delete returns **HTTP 204 with `error: null`** — success and no-op are indistinguishable without `{ count: "exact" }`. `DeleteModal.tsx:101-103`'s "nothing was deleted" branch is now unreachable. The minimal fix (throw from `deleteSet` on error) needs no signature change but does contradict the header's "follows `getAgentStatus`'s shape exactly"; a `count`-based fix touches `actions.ts`, which AC-2 forbids. Needs a ruling on AC-2 vs. an honest delete. [web/lib/sets/index.ts:221-235; web/app/(authenticated)/set/[id]/actions.ts:16-18]
- [x] [Review][Patch] getLibraryAddEvents is silently truncated at 1000 rows, so every conversion percentage is computed over a partial denominator — proven live: 1202 rows in the DB returned exactly 1000 over REST, `HTTP 200`, `Content-Range: 0-999/*`, `error: null` (so the resilience pattern cannot detect it). `max_rows = 1000` (supabase/config.toml:18); the committed fixture already holds 523 events from a partial library. Needs `.order()` + `.range()` pagination. [web/lib/sets/index.ts:164]
- [x] [Review][Patch] getRecentSets issues no `.order()` and no `.limit()` against the same `max_rows = 1000` cap, so past 1000 sets the surviving rows are arbitrary and the newest can be dropped — sorting after the fetch is only safe when the fetch is complete. Add server-side `order("started_at", desc)` + an explicit limit, plus an `id` tie-break (equal `started_at` currently yields nondeterministic hero/list order). [web/lib/sets/index.ts:116,123]
- [x] [Review][Patch] `derived` is cast to `SyncSetDerived` with no validation while the column is `not null default '{}'::jsonb`; a `{}` or older-shape row throws `TypeError` in the caller (`listModel.ts:88` reads `set.derived.confidence.value`), outside the seam's try/catch, 500-ing the whole dashboard rather than one set. Low reachability today (only `sync_set` writes, and it always supplies the blob) but a cheap defensive default belongs at the seam. [web/lib/sets/index.ts:104]
- [x] [Review][Patch] Story doc is factually wrong about the caller inventory: `library-utilization/page.tsx` **is** a seam caller — it imports and calls both `getRecentSets` and `getLibraryAddEvents` (Story 4.3 superseded the 3.5 stub). AC-2, Task 1, and Project Structure Notes all assert it "imports nothing from this seam today." AC-2's outcome still holds (the file is unchanged) but the re-grep Task 1 mandated was not reflected, and this is the route most exposed to the truncation above since its meter is a bare ratio over that denominator. [_bmad-output/implementation-artifacts/4-6-web-read-path-supabase-swap.md:41,110]
- [x] [Review][Patch] Header comment claims the module reads `sessions`/`sets`/`plays`/`library_track_events`; no query touches `sessions`. Cosmetic but it is exactly the "which one is real" ambiguity AC-6 exists to prevent. [web/lib/sets/index.ts:7]
- [x] [Review][Patch] The new test suite never asserts the select string — `makeBuilder`'s `select: vi.fn(() => builder)` ignores its argument — so a wrong column or malformed embed is untestable, and the untyped `createServerClient` (no `<Database>` generic) means `tsc` validates zero column names. The query is correct *today* (verified live this review), so this is drift protection: assert `SET_WITH_PLAYS_SELECT`, and cover the non-UUID id path (Postgres `22P02` → PostgREST 400, currently untested). [web/lib/sets/index.test.ts:519-531]
- [x] [Review][Patch] New pgTAP file is `plan(5)` and every case runs as `authenticated` with a valid `sub`; nothing asserts `anon` cannot delete from `sets` (the grant is `to authenticated` only) or exercises the policy's `auth.uid() is not null` guard. The removed Case 5 covered the anon/no-grant leg implicitly. Add both, plus an assertion that the parent `sessions` row deliberately survives. [supabase/tests/sets_delete_policy_isolation_test.sql]
- [x] [Review][Defer] A genuine production read failure renders identically to a brand-new account with zero telemetry, so an existing DJ's whole archive can silently show the first-run state. AC-4 explicitly specifies this behavior (mirroring `getAgentStatus`), so the code is correct as specified — the concern is with the spec. — deferred, pre-existing
- [x] [Review][Defer] Every dashboard / Style Evolution / Library Utilization render now fetches all 14 play columns of every set (58 sets ≈ 2,294 plays today), uncached, though only Set Detail needs full play rows. Splitting the seam into a plays-free list select and a plays-bearing detail select is a real refactor beyond this story. [web/lib/sets/index.ts:59-60] — deferred, pre-existing
- [x] [Review][Defer] `style-evolution/page.tsx` and `library-utilization/page.tsx` have no auth self-guard and no route-group middleware (`layout.tsx:7`: "each page self-guards"), so a logged-out visitor renders a new-DJ page. Pre-existing gap that this change slightly improves — the fixture no longer leaks to unauthenticated visitors. — deferred, pre-existing

## Dev Notes

- **This is `web/` plus one new `supabase/` migration — not agent or `shared/`.** The `sessions`/`sets`/`plays`/`library_track_events` tables and their `SELECT` RLS this story reads through already exist and are correct (built across Epic 3 and Story 4.2) — no changes needed there. But Task 4 is confirmed, not speculative: **no DJ-facing `DELETE` policy exists on `sets`/`sessions`/`plays` today**, so AC-5 cannot ship without a new migration adding one. Budget for that migration + its own pgTAP test as real, in-scope work, not a stretch goal.
- **Follow `getAgentStatus` as the pattern, not as a template to copy blindly.** It is the one function in this file already doing the real thing: lazy `@/lib/supabase/server` import (keeps `next/headers` out of every fixture-only consumer — no longer relevant once all four are real, worth reconsidering whether the lazy import is still needed or whether a top-level import is now fine), try/catch around the whole body, `error && process.env.NODE_ENV !== "production"` for dev-only logging, and a `{ ...snapshot, readAtMs: Date.now() }` shape so callers get the read time alongside the data (already established for `LibraryAddEventSnapshot` too — `buildLibraryConversion` needs an injected clock, not `Date.now()` inside a "pure" function, per Story 4.1's review).
- **Do not change any component.** AC-2 is a hard constraint: if a page needs to change to make this work, that is a sign the query design is wrong, not a sign the constraint should bend. The seam's entire reason for existing (Story 3.6 Task 4) is that swap.
- **RLS is the filter, not an application-level `dj_id` check.** Every existing table already follows AD-7's owner-`SELECT`-only convention; querying without a manual `dj_id` filter (letting `auth.uid()` do the scoping via policy) is the established pattern (`getAgentStatus` already does this) — adding a redundant client-side filter would be new, unnecessary complexity, not extra safety.
- **Watch for `derived`.** `SetRecord.derived` (`SyncSetDerived`) is a computed/cached summary — confirm from the migration whether it round-trips as a single JSON(B) column on `sets` or was decomposed into relational columns at write time. Getting this wrong is the most likely way `getRecentSets`/`getSetById` silently return a shape that satisfies TypeScript but renders wrong.
- **No test-mocking convention exists yet for this seam** — flagged explicitly in Task 6 rather than left to be discovered mid-implementation. Whatever pattern is chosen here will likely become the template for future Supabase-backed seam functions; keep it simple and consistent across all four rewrites rather than four different approaches.

### Project Structure Notes

- Updated: `web/lib/sets/index.ts` (all four function bodies + header comment); possibly `web/lib/sets/recent-sets.fixture.json` / `library-add-events.fixture.json` / `build-fixture.mjs` / `build-library-fixture.mjs` headers (Task 5) or their removal from the production path.
- New: a Supabase migration adding the `DELETE` policy (Task 4, confirmed required) + its pgTAP test; a test file (or files) for `web/lib/sets/index.ts` and whatever Supabase-client mock helper Task 6 establishes.
- Unchanged (per AC-2): `dashboard/page.tsx`, `style-evolution/page.tsx`, `set/[id]/page.tsx`, `set/[id]/actions.ts`, and `library-utilization/page.tsx` — which **is** a caller (`getRecentSets`, `getLibraryAddEvents`, since Story 4.3), so AC-2 does protect it. Corrected by the 2026-08-07 code review; this note previously claimed it wasn't a caller yet.
- Changed by the 2026-08-07 code review, as a deliberate and recorded AC-2 exception: `web/app/components/set-detail/SetHeader.tsx` (one line) now renders the session label from `session_label` instead of `external_id`. AC-2's constraint is that the *read swap* be invisible above the seam; here the swap was visibly wrong (`SET <uuid>` in place of `SET 975`), so leaving the component untouched would have preserved the letter of AC-2 while shipping the regression it exists to prevent.

### References

- [Source: web/lib/sets/index.ts] — the seam itself; every doc comment is a contract.
- [Source: deferred-work.md, "[LAUNCH BLOCKER — unstoried]" entry] — the gap this story closes.
- [Source: ARCHITECTURE-SPINE.md#AD-7] — owner-SELECT-only RLS convention.
- [Source: supabase/migrations/20260730204057_create_sessions_sets_plays.sql] — `sessions`/`sets`/`plays` shapes and RLS.
- [Source: supabase/migrations/20260807100000_create_library_track_events.sql] — `library_track_events` shape and RLS.
- [Source: web/lib/sets/types.ts] — `SetRecord = SyncPayload["set"]`.
- [Source: 3-6-dashboard-home.md, Task 4] — the seam's founding contract (AC-13/SM-1).
- [Source: 4-2-library-to-setlist-correlation-trend.md] — `getLibraryAddEvents()`'s introduction, most recent precedent for this exact seam.
- [Source: epics.md#Story 4.6] — this story's acceptance criteria, verbatim.
- [Source: sprint-status.yaml, `4-6-web-read-path-supabase-swap` entry] — sequencing rationale.

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (claude-sonnet-5)

### Debug Log References

- Ran `supabase start` / `supabase db reset` / `supabase test db` against a local Docker Supabase stack to apply all migrations (including the new `20260807120000_add_sets_delete_policy.sql`) and run the full pgTAP suite for real — 123/123 tests pass across all six `supabase/tests/*.sql` files.
- Manually seeded a DJ account's `sessions`/`sets`/`plays`/`library_track_events` rows and ran the exact RLS-scoped column selects the rewritten functions issue (`set local role authenticated; set request.jwt.claims ...`) directly against the local Postgres container, confirming the read shapes match what `toSetRecord`/`toSyncPlay`/`reconstructGenre` expect. PostgREST/Kong containers would not stay up in this sandboxed environment (exited immediately after each `supabase start`), so the nested `sets(...plays(...))` embed could not be exercised through an actual REST call — the SQL-level projection was verified instead, and the embed syntax itself is standard Supabase JS nested-resource selection over the existing `plays.set_id → sets.id` FK.
- `web`: `pnpm install --frozen-lockfile` (no `node_modules` existed in this worktree), then `eslint .`, `tsc --noEmit`, `vitest run` (308/308 passing, including the new `lib/sets/index.test.ts`), `next build` — all clean.

### Completion Notes List

- Rewrote all four data functions in `web/lib/sets/index.ts` (`getRecentSets`, `getSetById`, `deleteSet`, `getLibraryAddEvents`) to read/write Supabase directly, following `getAgentStatus`'s existing shape exactly (lazy `@/lib/supabase/server` import, try/catch, dev-only `console.error`, calm empty/`null` fallback). Removed the fixture imports and the in-memory `store` entirely from this file.
- `getRecentSets`/`getSetById` reconstruct `SetRecord` from a nested `sets` + `plays` select (`sets.id` is the `external_id`; `derived` is a plain jsonb passthrough). Sorting (sets by `started_at` desc, plays by `position`) is done in JS after the fetch, not trusted from the query — same rationale the fixture-stage code already carried.
- Task 4 (AC-5): confirmed via the migration's own header comment on `20260730204057_create_sessions_sets_plays.sql` that no DJ-facing `DELETE` grant/policy existed on `sets` before this story. Added `20260807120000_add_sets_delete_policy.sql` (grant + `sets_delete_own` RLS policy, scoped to `sets` only — `plays` cascades via its existing `on delete cascade` FK, verified for real against a local Supabase instance that cascade fires correctly even though `plays` itself has no DELETE grant). Updated `deleteSet` to issue a real `.delete().eq("id", externalId)` instead of the fixture stage's `store.filter(...)`.
- New pgTAP file `supabase/tests/sets_delete_policy_isolation_test.sql`: a DJ can delete their own set (and its plays cascade away), a DJ cannot delete another DJ's set (RLS makes it a no-op, not a permission error). Updated the pre-existing `sessions_sets_plays_isolation_test.sql` — its Case 5 previously asserted "authenticated cannot delete from sets (no delete grant)," which this story's AC-5 deliberately makes false; removed that one `throws_ok` case (plan count 31 → 30) and added a comment pointing to the new test file for the sets-delete coverage.
- Task 5 (AC-6): decision made and recorded — the fixtures are retired from the production read path (no longer imported by `index.ts`) but kept committed as test-only sample data, since `dancefloor.test.ts` and `setDetail.test.ts` import `recent-sets.fixture.json` directly for realistic pure-function test input (confirmed via grep; no test imports the fixtures through the seam itself). Updated both fixture builders' (`build-fixture.mjs`, `build-library-fixture.mjs`) header comments to state the test-only status explicitly, closing the "which one is real" ambiguity AC-6 exists to prevent.
- Task 6 (AC-7): audited every existing test for a transitive dependency on this seam's old fixture-backed behavior — none exists (grep confirmed no test file imports `getRecentSets`/`getSetById`/`deleteSet`/`getLibraryAddEvents` from `@/lib/sets`; the two tests that import fixture JSON do so directly, bypassing the seam, and are unaffected by this story). No test updates were needed for that reason. Added `web/lib/sets/index.test.ts` (15 tests) establishing the Supabase-client-mocking convention this story's Dev Notes flagged as not yet existing: a minimal thenable query-builder stand-in (`makeBuilder`) mocking `@/lib/supabase/server`'s lazily-imported `createClient`, covering the empty-state (AC-3) and failure-state (AC-4, both a Supabase-returned `error` and an unexpected `throw`) paths for all four functions, plus shape-reconstruction assertions (genre-null vs genre-populated, play sort-by-position, set sort-by-`started_at`, `sets.id → external_id`).
- Task 7: closed the "[LAUNCH BLOCKER — unstoried]" entry in `deferred-work.md` with a `[RESOLVED 2026-08-07 by implementation:` note. `web/lib/sets/index.ts`'s own header comment was rewritten to describe current reality (Supabase-backed, fixtures retired) rather than "fixture today, Supabase later." No `sprint-status.yaml` comment change was needed beyond the status transition itself — Task 4's DELETE-migration requirement was already anticipated in the story text, not a new scope finding.
- Task 8: full `web` gate (lint/typecheck/test/build) is clean. Migration + pgTAP verification ran for real against a local Docker-backed Supabase instance (`supabase start`/`db reset`/`test db`), including a manual seed-and-query pass confirming the RLS-scoped column shapes. Two of the story's four manual-verification bullets could not be completed as literally written and are flagged here rather than silently claimed: (1) verification went through direct Postgres/pgTAP rather than a live browser session against the deployed app (no real DJ account/browser session available in this environment) — the dashboard/Set Detail/Style Evolution pages were not visually confirmed rendering real data end-to-end; (2) the PostgREST/Kong layer specifically (as opposed to the underlying SQL) was not exercised, for the sandboxing reason noted in Debug Log References. A follow-up manual pass against a real deployed environment (or this repo's Supabase project with `supabase start` in an unrestricted environment) is recommended before this story is considered fully closed out, even though the code-level and RLS-level verification here is thorough.
- **[VERIFICATION GAP CLOSED — code review 2026-08-07]** The PostgREST/Kong half of the flagged gap was closed during the review: the containers came up cleanly, so every query this seam issues was exercised through real REST calls, not just raw SQL. Confirmed for real: the nested `sets(...plays(...))` embed returns HTTP 200 with the expected shape (and `plays: []`, never `null`, for a set with no plays); the new `sessions(session_identity)` embed returns a to-one **object**, matching `SetRow`; `order`+`limit` are accepted; the DELETE policy cascades to `plays` despite `plays` having no DELETE grant; a cross-DJ delete is a silent HTTP 204 no-op; a non-uuid id is a 400/`22P02` that renders as a calm not-found; and paged `Range` requests past `max_rows` return contiguous non-overlapping rows (1000 + 202 = 1202). That REST verification is also what **found** three defects the SQL-level check could not have: the `max_rows` truncation, the `external_id`→uuid label regression, and the delete-resurrection.
- **[BROWSER WALKTHROUGH COMPLETED — code review 2026-08-07]** The second half of the gap is closed too. A `.env.local` pointing at the local stack, a confirmed test DJ, and the fixture's own data reseeded as real Supabase rows (58 sets / 2,294 plays / 523 add-events spanning 19 months) let all four seam-consuming routes be walked in a real browser, signed in as a real DJ. All render real synced data, not the retired fixture: **`/dashboard`** — greeting, hero (Fri 26 Jun, 17 dancefloor tracks, 122 median BPM), sets newest-first, "16 low-confidence sessions hidden"; **`/set/[id]`** — routed by uuid, tracklist with BPM/Camelot/genres, and the header reads **`FRI · 26 JUN 2026 · SET 977`**, confirming the `session_label` fix visually (a uuid in the URL, a real Serato session id in the label); **`/style-evolution`** — 19 months of BPM-range points plus the Library Conversion cohorts and their "1 recent month is still inside the 90-day window" disclosure; **`/library-utilization`** — the conversion meter at "53% of tracks added in the last 60 days have been played in a set". Nothing in the story's manual-verification list is outstanding now. Caveat worth recording: this was against a **local** `supabase start` stack, not a deployed environment, and the delete-through-`DeleteModal` flow was exercised via REST/pgTAP rather than by clicking the modal.

### File List

- `web/lib/sets/index.ts` — modified: all four functions (`getRecentSets`, `getSetById`, `deleteSet`, `getLibraryAddEvents`) rewritten to read/write Supabase; fixture imports and in-memory `store` removed; header comment rewritten.
- `web/lib/sets/index.test.ts` — new: seam-level unit tests (empty-state, failure-state, shape-reconstruction) for all four rewritten functions.
- `web/lib/sets/build-fixture.mjs` — modified: header comment updated to state test-only status.
- `web/lib/sets/build-library-fixture.mjs` — modified: header comment updated to state test-only status.
- `supabase/migrations/20260807120000_add_sets_delete_policy.sql` — new: grants `DELETE` on `sets` to `authenticated` and adds the `sets_delete_own` RLS policy (AC-5).
- `supabase/tests/sets_delete_policy_isolation_test.sql` — new: pgTAP coverage for the delete policy (own-set delete, cross-DJ no-op, plays cascade).
- `supabase/tests/sessions_sets_plays_isolation_test.sql` — modified: removed the now-obsolete "authenticated cannot delete from sets" assertion (Case 5) and updated its plan count/comment.
- `_bmad-output/implementation-artifacts/deferred-work.md` — modified: closed the "[LAUNCH BLOCKER — unstoried]" entry with a resolution note; code review additionally closed the "Permanent delete tombstone / suppress-id" entry and logged 3 new deferred items.

Added by the 2026-08-07 code review:

- `supabase/migrations/20260807130000_add_deleted_sets_tombstone.sql` — new: `deleted_sets` suppress table, the `record_deleted_set()`/`trg_sets_record_delete` trigger, and a `sync_set` re-ingest check so a deleted set is never resurrected. Rebuilt from `sync_set`'s CURRENT body (`20260807100000`), not the original — rebuilding from the original silently reverts `subgenre`/`played_ms`/`library_added_at`/`track_id`.
- `supabase/tests/deleted_sets_tombstone_test.sql` — new: 9 assertions proving sync → delete → re-sync does not restore the set or its plays, that the suppressed re-sync still returns the deterministic id, and that suppression is per-DJ.
- `supabase/tests/sets_delete_policy_isolation_test.sql` — modified: `plan(5)` → `plan(8)`; added `anon`-cannot-delete, null-`auth.uid()`-matches-nothing, and parent-`sessions`-survives cases.
- `web/lib/sets/index.ts` — modified: `sessions(session_identity)` added to the select; server-side `order`/`limit` + `external_id` tie-break on `getRecentSets`; `.range()` pagination on `getLibraryAddEvents`; `hasRenderableDerived` guard; `deleteSet` now throws; header comment corrected.
- `web/lib/sets/types.ts` — modified: optional display-only `session_label` on `SetRecord` (the frozen `@curfew/shared` contract is untouched).
- `web/lib/sets/format.ts` — modified: `formatSessionLabel` parses `serato4:<n>` / `legacy:<hex>` and shortens a bare uuid.
- `web/lib/sets/format.test.ts` — modified: 3 new label cases.
- `web/lib/sets/index.test.ts` — modified: mock rebuilt to mirror postgrest-js's real two-tier builder; select-string, pagination, tie-break, derived-guard, and non-uuid coverage added.
- `web/app/components/set-detail/SetHeader.tsx` — modified: one line, renders `session_label ?? external_id`. Deliberate, recorded AC-2 exception (see Project Structure Notes).
- `_bmad-output/implementation-artifacts/sprint-status.yaml` — modified: `4-6-web-read-path-supabase-swap` status `ready-for-dev` → `in-progress` → `review`.

## Change Log

| Date | Change |
|---|---|
| 2026-08-07 | Story 4.6 code review: 3 decision-needed + 10 patch findings, all resolved and applied. Closed the story's own PostgREST verification gap against a live stack, which found three defects raw SQL could not: (1) `getLibraryAddEvents` was silently truncated at `max_rows`=1000 (HTTP 200, `error: null`), making every conversion percentage a partial-denominator lie — now ordered + `.range()`-paged; (2) `external_id` is `sets.id`, a uuid, because `sync_set` has no `external_id` parameter, so Set Detail rendered `SET 872d5614-…` instead of `SET 975` — the label now comes from a `sessions(session_identity)` embed via `formatSessionLabel`; (3) a deleted set was resurrected by the next agent sync (reproduced end-to-end) — closed by `20260807130000_add_deleted_sets_tombstone.sql` (a `deleted_sets` suppress table, an `after delete` trigger, and a `sync_set` check), which also closes the standing "delete tombstone owed by the sync/read-path story" ledger entry from Story 3.7. Also: `getRecentSets` now orders/limits server-side with an `external_id` tie-break; `deleteSet` throws instead of reporting a delete that did not happen (reads stay calm, the mutation does not); a `hasRenderableDerived` guard drops an incomplete `derived` blob per-row instead of 500-ing the dashboard from the caller; the seam's test mock was rebuilt to mirror postgrest-js's real two-tier builder and now asserts the select string; pgTAP gained `anon`/null-`auth.uid()` delete denial, a `sessions`-survives assertion, and the tombstone suite. Corrected the story's stale claim that `library-utilization/page.tsx` is not a seam caller (it has been since Story 4.3). Gates re-run green: pgTAP 135/135 across 7 files, `eslint` clean, `tsc --noEmit` clean, `vitest` 323/323, `next build` clean. Status → done. |
| 2026-08-07 | Story 4.6 implemented: swapped `web/lib/sets/index.ts`'s four DJ-data functions from the committed fixture to real Supabase reads/writes, matching `getAgentStatus`'s existing resilient shape (AC-1/3/4). Added `20260807120000_add_sets_delete_policy.sql` granting a DJ-owned `DELETE` policy on `sets` — none existed before this story — so `deleteSet` performs a real hard delete instead of an in-memory mutation (AC-5); new pgTAP coverage confirms own-set delete, cross-DJ no-op, and plays-cascade. Fixtures retired from the production path, kept test-only (AC-6). No caller changed (AC-2). New `web/lib/sets/index.test.ts` establishes this seam's first Supabase-mocking test convention, covering empty-state/failure-state for all four functions (AC-7). Full `web` gate (lint/typecheck/test/build) and full `supabase` pgTAP suite (123/123) verified green against a real local Supabase instance. Status → review. |
