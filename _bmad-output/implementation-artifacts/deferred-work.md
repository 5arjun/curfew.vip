# Deferred Work

## Triage (2026-08-10, Epic 4 retrospective — ai-15)

Every entry below this section that does **not** already carry an inline `[RESOLVED …]` / `[CLOSED …]` / closed-by-ruling marker, sorted by urgency. Adapted from `pre-launch-services-checklist.md`'s §1–§6 scheme for code-level work. Pointers are `[story / section heading it lives under]` — no line numbers, the heading is enough to find it. Nothing below this section was edited to produce it.

### A. Launch-blocking — would ship visibly wrong data, or a security/privacy hole, to a real user today

- [code review of 4-1 — Arjun's timezone ruling] Sets bucket to the server's UTC, not the DJ's local night — late gigs file on the wrong day/month.
- [post-merge review of #20/#21/#22/#23 — agent-side] Reach high-water mark never shrinks; one drive swap permanently disables absent-marking, so sold/deleted tracks read "owned, never played".
- [code review of 4-5-time-to-first-play] `getRecentSets`' 500-set cap silently reclassifies older debuts as "never played" in a lifetime metric.

**Two entries tagged `[LAUNCH BLOCKER]` / `[PRODUCT — likely launch-affecting]` in the log below are very likely stale — verify before acting, don't re-investigate:** the 4.2-implementation entry flagging "nothing swaps `web/`'s read path from fixture to Supabase" was exactly Story 4.6's whole purpose, and 4.6 is `done`; the path-based-track-identity conversion-rate concern was superseded by 4.3's title+artist identity change (Decision E-2). Neither is re-marked resolved in the log itself — a 2-minute check and a `[RESOLVED]` stamp each.

### B. Pre-launch hardening — close before Epic 6 (first public-facing) or Epic 7 (billing) story-creation

- [code review of 3-5-floating-pill-nav] `(authenticated)` route group still ships with no auth gating at all.
- [code review of 4-6-web-read-path-supabase-swap] Style Evolution and Library Utilization pages have no auth self-guard either.
- [code review of story-3.7] `deleteSetAction` has no ownership check; Server Actions bypass route-group gating.
- [code review of 4-6-web-read-path-supabase-swap] Hosted `auto_expose_new_tables` re-grants every future table to `anon`; nothing local fails.
- [post-merge review of #20/#21/#22/#23 — SQL hardening] No test asserts RLS is enabled on every `public` table; the anon-privilege sweeps have holes.
- [post-merge review of #20/#21/#22/#23 — SQL hardening] `service_role` has no CRUD grants on a fresh replay — detonates on Epic 7's Stripe webhook.
- [code review of 2-3c-phone-on-file-post-signup-prompt] AD-19's column-scoped grant is enforced only by a comment; Epic 7 could re-add a blanket `grant update on djs`.
- [code review of 2-1-supabase-cloud-foundation-isolation-baseline] Additive-only guard has no opinion on function/trigger redefinition.
- [code review of 2-1 …, round 2] Additive-only guard bypassed entirely by `ALTER TYPE … RENAME/DROP` — an Epic 7 enum's exact shape.
- [code review of 2-1 …, round 2] Nothing protects the load-bearing `grant select on public.djs` from a future silent `REVOKE`.
- [code review of 4-6-web-read-path-supabase-swap] Supabase leaked-password (HaveIBeenPwned) protection is disabled — one dashboard toggle.
- [code review of 2-3a-email-identity-path-email-password-passkey] No application-level rate limiting on `signIn`/`signUp` Server Actions.
- [code review of 2-4-auth-ui-components] `rp_id` pinned to the throwaway `.vercel.app` host; every passkey breaks at the `curfew.vip` move.
- [code review of 2-10-agent-secure-token-storage] Refresh token travels through a URL and OS argv, readable by any local process on Windows/Linux.
- [code review of 2-10-agent-secure-token-storage] `curfew-agent://` scheme has no protection against another app squatting it.
- [code review of 1-1-monorepo-scaffold-with-three-workspaces] `csp: null` in the Tauri config the agent inherits while loading local Serato data.
- [code review of 3-4-format-drift-resilience-backfill] Sentry has no fingerprinting and its free-text messages may embed local paths / OS username once a DSN lands.
- [code review of 4-6-web-read-path-supabase-swap] A real production read failure renders identically to a brand-new account, with zero telemetry.
- [post-merge review of #20/#21/#22/#23 — contract and guard gaps] `no-raw-data.test.ts` covers 1 of 3 schemas, so a future `portable_path` ships an AD-2 breach unguarded.
- [3-4-format-drift-resilience-backfill dev-story session] Sync payload never carries `agent_version` despite AD-3 — no fleet-version handling once real installs diverge.
- [code review of 4-11-library-roster-sync] One poison roster batch head-of-line-blocks the whole queue forever; no quarantine set.
- [post-merge review of #20/#21/#22/#23 — agent-side] Unbounded first roster drain blocks the AD-20 heartbeat, so the healthiest agent reads stale.
- [code review of 2-9b-windows-signed-build] Two release workflows race on one GitHub Release; a failed platform publishes a silently incomplete release and update manifest.
- [code review of 2-9c-signed-auto-updater-pipeline] No key-rotation plan for the updater signing keypair if it is ever compromised.
- [code review of 2-3a-email-identity-path-email-password-passkey] Visiting `/login` while already signed in re-shows the form — an Epic 6 entry surface.
- [code review of 5-1-segments-overlay-schema] `segments.dj_id` isn't cross-checked against `set_id`'s real owner; RLS trusts the denormalized column.
- [code review of 5-1-segments-overlay-schema] `grant_matrix_test.sql` has no `authenticated`-scoped INSERT/DELETE sweep for any table.

### C. Real gap, no story owns it yet

- [code review of 4-11-library-roster-sync — Arjun's library-scope ruling] NEEDS A STORY: exclude video loops and untagged rips from the library denominator.
- [code review of 4-11-library-roster-sync — Arjun's single-machine ruling] NEEDS A STORY: one linked machine per DJ, plus the missing unlink path.
- [code review of 4-1-style-evolution-trend-view-excludes-low-confidence] One app-wide i18n decision owed: six sites use runtime-default locale, hydration-mismatching on non-`en` browsers.
- [post-merge review of #20/#21/#22/#23 — agent-side] `scan_identity_coverage`'s scalars have no cloud carrier, so AC-6's disclosure can never render.
- [post-merge review of #20/#21/#22/#23 — three-way future-dated disposition] Ruled count-and-disclose 2026-08-10; the `libraryConversion.ts` implementation is still owed.
- [merging main (4.6 + 4.11 + 4.7) into 4-5-time-to-first-play] Ruled 2026-08-10 that the page note owns `noAddDateCount`; the trend call-site change is owed.
- [3-3-offline-sync-queue manual verification] Windows Serato 4 internal history path is unknown, so Windows DJs may capture nothing.
- [code review of 2-6-serato-folder-auto-detection-first-run-confirm] Windows paths and removable-volume detection are entirely unverified.
- [code review of 2-10-agent-secure-token-storage] Windows/Linux deep-link argv forwarding has zero verification on a shipping target.
- [3-3-offline-sync-queue manual verification] Verified-setup onboarding that ends in a live test capture — deliberately deferred to its own story.
- [Surfaced by 2-11-account-deletion-data-export-manual-runbook] Agent cannot learn its account was deleted; local SQLite purge stays a manual instruction.
- [code review of 1-6-edge-genre-normalization-versioned] Genre fold mis-buckets multi-value tags, punctuation and Unicode variants to "Other"; refinement trigger already passed.
- [code review of 1-6-edge-genre-normalization-versioned] V1 taxonomy omits high-volume electronic buckets it claims to target; needs a real frequency histogram.
- [code review of 1-4-library-join-for-in-library-enrichment] Volume-hosted `database V2` path resolution unproven — a USB library may read wholly off-library.
- [code review of 1-4-library-join-for-in-library-enrichment] Path encoding/case agreement untested; every accented artist could silently read off-library.
- [code review of 1-4-library-join-for-in-library-enrichment] Serato 4+ `in_library` is unconditionally guessed `true`, against the spine's "never guessed"; `asset_id` is a confirmed candidate signal.
- [code review of 4-1-style-evolution-trend-view-excludes-low-confidence] No rolling window or outlier guard: one bad `started_at` yields ~670 buckets in a blocking server render.

### D. Product/copy judgment call awaiting a ruling from Arjun

- [Story 4.10 — track lookup and track detail] D-36's placement clause contradicts its own Critical clause; confirm or reverse the page-level search field.
- [code review of 4-10-track-lookup-and-track-detail] Per-module "no data" copy in `TrackDetail` can't distinguish genuinely-nothing from hidden-by-reveal.
- [Story 4.4 — aging shelf] The all-clear line and the recently-downloaded nudge contradict each other on screen; both are AC-required.
- [code review of 1-8-live-practice-confidence-signal] Confidence tier has no density/rate ceiling; thresholds are explicitly Arjun's call (Open Question #1).
- [implementation of 1-8-live-practice-confidence-signal] The 865-play/27.5-minute previewing session is the concrete input for that same threshold ruling.
- [code review of 5-1-segments-overlay-schema] No decision recorded on whether overlapping or duplicate segments within a set are permitted.

### E. Post-MVP / low priority — efficiency, cleanup, pre-existing a11y, or already measured and declined

- [Story 4.10 — track lookup and track detail] `.se-hidden-toggle` is under the 24x24 WCAG floor; pre-existing, shared with `/style-evolution`.
- [Story 4.10 — track lookup and track detail] D-29's index form measured 3.3x over its bar and was ruled to tuple rows; next lever needs GAP-5.
- [Story 4.10 — track lookup and track detail] `SpotlightSearch`'s placeholder isn't `aria-hidden`, so its text is announced twice.
- [code review of 4-10-track-lookup-and-track-detail] `SetSimilarity`'s server-rendered day labels are locale/timezone-dependent; ruled accept-and-disclose.
- [code review of 4-10-track-lookup-and-track-detail] Search can list one physical track twice under a D-28 identity conflict; zero instances today.
- [Story 4.4 — aging shelf] The row-level prep-crate action is out of MVP; its structural cost is recorded so nobody re-derives it.
- [Story 4.4 — aging shelf] The clamp gives every pre-observation track an identical day count, making that block's sort inert.
- [code review of 4-4-aging-shelf-with-prep-crate-action] `epics.md` and the architecture spine still describe the prep-crate action with no ruling note.
- [post-merge review of #20/#21/#22/#23 — contract and guard gaps] `SyncLibraryRosterBatch`'s TS type describes a body PostgREST rejects.
- [post-merge review of #20/#21/#22/#23 — contract and guard gaps] `shared/package.json`'s `exports` omits the roster schema its own constant points at.
- [post-merge review of #20/#21/#22/#23 — contract and guard gaps] `hasRenderableDerived` guards 3 of 9 dereferenced `derived` sub-objects; currently unreachable.
- [post-merge review of #20/#21/#22/#23 — contract and guard gaps] Nothing validates any payload against any JSON Schema at runtime.
- [post-merge review of #20/#21/#22/#23 — contract and guard gaps] Both `export_real_library.rs` integration tests are `#[ignore]`, so the new export path is uncovered.
- [post-merge review of #20/#21/#22/#23 — SQL hardening] `auth_rls_initplan` on all 10 policies: `auth.uid()` re-evaluated per row.
- [post-merge review of #20/#21/#22/#23 — SQL hardening] Index cleanup, plus the actually-hot `getRecentSets` ordering query is unindexed.
- [post-merge review of #20/#21/#22/#23 — SQL hardening] Orphan `sessions` rows accumulate with no cleanup path; harmless today.
- [post-merge review of #20/#21/#22/#23 — UI, a11y, and efficiency] Two sibling modules expose the same "early read" caveat differently to assistive tech.
- [post-merge review of #20/#21/#22/#23 — UI, a11y, and efficiency] Under-shared work: `styleEvolution.ts`'s O(buckets x sets) pass, six `aggregateBucket` passes, agent double-reads.
- [post-merge review of #20/#21/#22/#23 — process] The undated-add-date disclosure state has never rendered anywhere; the fixture carries zero.
- [code review of 4-6-web-read-path-supabase-swap] Every dashboard / Style Evolution / Library Utilization render fetches all 14 play columns, uncached.
- [code review of 3-6-dashboard-home, chunk 2/2 CSS/config] Several CSS selector blocks are reopened non-adjacently and silently clobber earlier declarations.
- [code review of 3-6-dashboard-home, chunk 2/2 CSS/config] Motion tokens are routinely bypassed with hardcoded durations and easings.
- [code review of 3-6-dashboard-home, chunk 2/2 CSS/config] No z-index scale exists; values are ad hoc across `dashboard.css`.
- [code review of 3-6-dashboard-home, chunk 2/2 CSS/config] `.spot-goo`'s SVG filter has no `@supports` guard or fallback; Safari risk.
- [code review of 3-6-dashboard-home, chunk 2/2 CSS/config] `components.json` registers an unpinned third-party shadcn registry.
- [code review of 3-6-dashboard-home, chunk 2/2 CSS/config] `forced-colors` and `prefers-contrast: more` have no support path.
- [code review of 3-6-dashboard-home, chunk 2/2 UI components] `Silk.jsx`'s `hexToNormalizedRGB` has no format validation; internal tokens only.
- [code review of 3-6-dashboard-home, chunk 2/2 UI components] Aggregate WebGL context budget isn't reconciled against real device ceilings.
- [code review of 3-6-dashboard-home, chunk 2/2 UI components] Incomplete ARIA tablist pattern across three dashboard components; one a11y pass, not three patches.
- [code review of 3-6-dashboard-home, chunk 2/2 UI components] `RightColumn`'s `ResizeObserver` watches the wrong box, so scroll-fade indicators go stale.
- [code review of 3-6-dashboard-home, chunk 2/2 UI components] `SetListPanel`'s sheet geometry is measured once at open, with no resize/rotation handling.
- [code review of 3-6-dashboard-home, chunk 1/2] Camelot mapping's "verified 24/24 against real data" claim has no committed artifact.
- [code review of 3-6-dashboard-home, chunk 1/2] Dancefloor density-floor bias is a known interim gap, superseded by Story 5.2.
- [code review of 3-6-dashboard-home, chunk 1/2] Backfill sweep re-derives all captured serato4 rows on every agent startup.
- [code review of 3-6-dashboard-home, chunk 1/2] `FloatingNav` reintroduces a `:focus-visible` Safari pattern; its breakpoint is comment-synced only.
- [code review of 3-6-dashboard-home, chunk 1/2] `export_real_fixtures.rs` hardcodes real gig dates in source comments.
- [code review of 3-6-dashboard-home, chunk 1/2] `arcDirection` can label a set with a dramatic mid-set arc "steady".
- [code review of 3-5-floating-pill-nav] No `padding-bottom` under the dock on 3.5's stub pages; superseded per-page already.
- [code review of 3-5-floating-pill-nav] No `env(safe-area-inset-bottom)` on those same stub pages; same resolution path.
- [code review of 3-5-floating-pill-nav] The icon-color root cause is patched only on `.floating-nav-link`; other anchors stay exposed.
- [code review of 3-5-floating-pill-nav] `FloatingNav` has zero component-level test coverage beyond one pure helper.
- [code review of 3-9-console-voice-failure-register-state-a11y-responsive-pass] `set_agent_status` surfaces a raw FK violation if the `djs` row is deleted mid-token.
- [code review of 3-7-set-detail-summary-tracklist backend group] `DateAddedIndex::live` re-implements a `/Volumes` scan instead of reusing `DiskSource`.
- [code review of 3-7-set-detail-summary-tracklist backend group] Catalogue loading is synchronous on the watch-loop thread; worth one confirmation.
- [code review of 3-7-set-detail-summary-tracklist backend group] No backfill test covers the played-flag filter against the re-derive sweep.
- [3-4-format-drift-resilience-backfill dev-story session] A terminal capture failure re-fires the error reporter on every poll tick forever.
- [code review of 3-3-offline-sync-queue] No test exercises `sync_loop`'s pass-level `Err` branch end-to-end.
- [3-3-offline-sync-queue manual verification] Open Q: whether Serato 4 ever double-writes a `.session` for a `master.sqlite` set.
- [code review of 2-11-account-deletion-data-export-manual-runbook] `pre-launch-services-checklist.md` §3 holds a documentation-debt row in a provisioning-only section.
- [code review of 2-11-account-deletion-data-export-manual-runbook] "Party 2026-07-20" attributions carry no linked source artifact.
- [code review of 2-10-agent-secure-token-storage] `link-agent/page.tsx`'s `getUser()` is unguarded; matches an already-shipped sibling pattern.
- [code review of 2-9a-macos-signed-build-notarization] No Rust build caching in `release-macos.yml`; cost nit on a rare workflow.
- [code review of story-2-8-set-capture-into-local-sqlite] `recheck_pending_serato4` opens a fresh connection every poll tick regardless of pending work.
- [code review of story-2-8-set-capture-into-local-sqlite] `store::open_at` sets no `busy_timeout`/WAL pragmas — premise ("only one connection") expired once 3.2/3.3 landed.
- [code review of story-2-8-set-capture-into-local-sqlite] An externally-deleted `history_session` row is retried forever with no terminal outcome.
- [code review of story-2-8-set-capture-into-local-sqlite] A `Remove` event on a legacy `.session` refreshes its quiet clock instead of dropping it.
- [code review of story-2-7-local-only-raw-data-boundary] `watch_loop`'s watermark isn't durable across restart, re-surfacing all history as new.
- [code review of 2-6-serato-folder-auto-detection-first-run-confirm] The legacy watch path emits no discoverable "new session" signal; Story 2.8's territory.
- [code review of 2-6-serato-folder-auto-detection-first-run-confirm] `watch_loop`'s connect/reconnect/watermark state machine has no automated coverage.
- [code review of 2-6-serato-folder-auto-detection-first-run-confirm] A new SQLite connection is opened per poll tick instead of held for the loop.
- [code review of 2-6-serato-folder-auto-detection-first-run-confirm] Override-change comparison does no path normalization, so cosmetic differences tear down the watcher.
- [code review of 2-6-serato-folder-auto-detection-first-run-confirm] `Cargo.lock` carries two `windows` crate versions via `sysinfo`.
- [code review of 2-5-agent-shell-tray-ui] `is_visible().unwrap_or(false)` swallows errors in the tray click handler (also filed under 1.1 round 2).
- [code review of 2-5-agent-shell-tray-ui] Concurrent Save clicks race on the settings file; benign last-click-wins.
- [code review of 2-5-agent-shell-tray-ui] HiDPI-aware tray icon lookup was never implemented; Retina shows the upscaled `@1x`.
- [3rd code review of 2-3b-oauth-paths-account-linking-google-apple] A falsy `data.user` after a successful exchange skips Story 2.3c's phone gate.
- [3rd code review of 2-3b-oauth-paths-account-linking-google-apple] Google's OAuth button has no availability gate; local dev-setup rough edge only.
- [code review of 2-3b-oauth-paths-account-linking-google-apple (2026-07-27)] Callback route's `createClient()` sits outside its own try/catch, in both routes.
- [code review of 2-3b-oauth-paths-account-linking-google-apple (2026-07-27)] A reused OAuth code redirects an already-signed-in user to an error page.
- [code review of 2-3a-email-identity-path-email-password-passkey] `experimental.passkey` opt-in is duplicated verbatim across three client constructors.
- [code review of 2-3a-email-identity-path-email-password-passkey] Task 5.1's "prompt after successful signUp" trigger can never fire as written.
- [code review of 1-9-golden-file-regression-harness] Golden fixtures never cover two plays tied on `start_time`.
- [code review of 1-8-live-practice-confidence-signal] No guard against violated `start_time` ordering in `classify`/`count_long_gaps`.
- [implementation of 1-7-core-per-set-stat-engine] `TrackIdentity` collides on (title, artist), and blank/blank collides across unrelated plays.
- [implementation of 1-7-core-per-set-stat-engine] AC-4's ≤10s p95 full-library-pass target has never been honestly benchmarked.
- [code review of 1-6-edge-genre-normalization-versioned] Legacy numeric ID3v1 TCON maps to "Other"; none observed in the real corpus.
- [code review of 1-3b-master-sqlite-play-log-reader] A NULL `history_entry.session_id` would make a row silently unreachable.
- [code review of 1-3b-master-sqlite-play-log-reader] `start_time`'s real range was never independently confirmed, unlike `deck`'s.
- [code review of 1-4-library-join-for-in-library-enrichment] `sane_bpm` has no upper bound, so a corrupted positive BPM passes as a measurement.
- [code review of 1-4-library-join-for-in-library-enrichment] Duplicate-path last-wins tiebreak is still an unconfirmed assumption; local catalogue had no duplicates.
- [code review of 1-4-library-join-for-in-library-enrichment] `serato4::join_session` fails a whole session on one row's type-coercion error.
- [code review of 1-3-clean-room-session-parser] RF-2's trailing-fragment hard failure, refuted on the real 474-file corpus, kept as synthetic coverage.
- [code review of 1-3-clean-room-session-parser] Fixture gotcha: understating an `oent` length yields `Truncated`, not `Desync`.
- [code review of 1-1-monorepo-scaffold-with-three-workspaces] No OS matrix in CI; the agent core is built and tested only on ubuntu.
- [code review round 2 of 1-1-monorepo-scaffold-with-three-workspaces] The full Supabase stack boots in CI just to prove one no-op migration applies.
- [code review of 1-2-parser-validation-spike-against-real-sessions] `ParseError::Truncated` is unreachable dead code in the throwaway spike crate.
- [code review of 1-2-parser-validation-spike-against-real-sessions] Spike `home()` and `to_str().unwrap()` panic instead of failing gracefully.
- [code review of 1-2-parser-validation-spike-against-real-sessions] Spike byte-level resync could theoretically desync on a coincidental UTF-16 match.
- [code review of 1-2-parser-validation-spike-against-real-sessions] `2521.session`'s AD-17 "morning block + gap" framing is imprecise.
- [code review of 1-5-off-library-embedded-tag-fallback-with-visible-unknown] Disabling lofty's ID3v2 compression support can drop a readable FLAC Vorbis block.
- [code review of 1-5-off-library-embedded-tag-fallback-with-visible-unknown] `embedded_tags`' module doc overstates content-sniffed dispatch; extension still seeds it.
- [code review of 1-10-freeze-the-shared-sync-contract] No test round-trips a realistic `SyncPayload` through a real JSON-schema validator.
- [code review of 2-1-supabase-cloud-foundation-isolation-baseline] `handle_new_dj` never backfills a `djs` row for a pre-existing `auth.users` row.
- [code review of 2-1 …, round 2] The additive-only guard's splitter ignores `$$` bodies, so its "statement-aware" docstring overclaims.
- [code review of 2-2-obsidian-design-token-system-web-shell] The hardcoded-color guard can't catch dynamically-constructed color strings.
- [code review of 2-9b-windows-signed-build] `signCommand`'s `%1` is unquoted while `productName` contains a space; verify on the first real build.
- [code review of 2-9b-windows-signed-build] The Azure signing endpoint is still a literal placeholder, blocked on provisioning tracked elsewhere.
- [code review of 2-9c-signed-auto-updater-pipeline] No preflight check that `TAURI_SIGNING_PRIVATE_KEY` exists before the expensive build.
- [code review of 2-9c-signed-auto-updater-pipeline] The update endpoint hardcodes the repo slug with no fallback or mirror.
- [code review of 2-9c-signed-auto-updater-pipeline] `$VERSION` is interpolated unescaped into `sed` and `node -e`; maintainer-reachable only.
- [code review of 3-1-sessions-sets-plays-schema-visibility-content-overlay-split] `plays.genre_normalized`/`taxonomy_version` independence isn't enforced by a CHECK.
- [code review of 3-1-sessions-sets-plays-schema-visibility-content-overlay-split] `plays.position`/`camelot_key` have no format or range validation.
- [code review of 3-1-sessions-sets-plays-schema-visibility-content-overlay-split] `sessions.session_identity` has no non-empty or normalization guard.
- [code review of 3-2-idempotent-set-sync] No pagination or cap on a sync pass; serial blocking HTTP per row.
- [code review of 3-2-idempotent-set-sync] No guard against concurrent or overlapping `sync_pending_sessions` calls.
- [code review of story-3.7] `bpmHistogram` doesn't clamp single-outlier BPM values, stretching the overlay's band.
- [code review of story-3.7] One Escape keypress closes the delete modal and a drill-in veil together.
- [code review of 3-8-energy-arc-chart-summary] No component or DOM tests for `DetailArc`, `TempoSpark`, `AnimateNumber`.
- [code review of 3-8-energy-arc-chart-summary] `AnimateNumber`'s digit roll is asymmetric across a magnitude boundary.
- [code review of 3-8-energy-arc-chart-summary] Backfill has no recovery path if Serato prunes its own history out from under a pending row.
- [code review of 4-1-style-evolution-trend-view-excludes-low-confidence] The dashed gap-bridge affordance is explained nowhere the user can see.
- [code review of 4-11-library-roster-sync] Nondeterministic dedupe tie-break can flip retained title/artist and re-upload a row forever.
- [code review of 4-11-library-roster-sync] AC-5's soft-delete still has no end-to-end coverage of a real removal.
- [code review of 4-8-genre-share-stream-camelot-wheel] `TrendChart`'s error-boundary `resetKey` is reveal-blind; same hole in three components.
- [code review of 4-8-genre-share-stream-camelot-wheel] Locale-dependent server-rendered axis ticks in `GenreShareStream`/`TrendChart`; same seam as the i18n decision above.
- [code review of 4-9-library-utilization-composition-play-side-stats] Two pgTAP global-count assertions stay unscoped and will break an unrelated future story.
- [code review of 4-9-library-utilization-composition-play-side-stats] `SIMILARITY_MATRIX_SETS` aliases a dashboard constant; a retune silently falsifies the PRD.
- [code review of 4-9-library-utilization-composition-play-side-stats] Set similarity's cap statement counts only dated survivors; unreachable while `started_at` is NOT NULL.
- [code review of 5-1-segments-overlay-schema] `first_play_id`/`last_play_id` aren't constrained to belong to the same `set_id` as their segment.
- [code review of 5-1-segments-overlay-schema] Nothing rejects a segment whose first play falls after its last; needs a trigger, owned by 5.2/5.3.
- [code review of 5-1-segments-overlay-schema] `segments_isolation_test.sql` never deletes a `sets` row, so the `set_id` cascade is unproven.
- [code review of 5-1-segments-overlay-schema] `supabase/README.md`'s migration/test tree map has been known-incomplete since Story 3.2.

**Totals:** 175 still open (A: 3, B: 27, C: 17, D: 6, E: 122), 56 already closed inline.

*(Not counted in either figure: the post-merge review's seven "Verified NOT broken" records and its one "not over-engineered" record, which are verification notes rather than deferred findings. The Story 5.1 section was appended by a concurrent session while this triage was being written and is included above.)*

## Deferred from: Story 4.10 — track lookup and track detail (2026-08-10)

### Open — accessibility, pre-existing, one small fix

- **`.se-hidden-toggle` ("show them") is 63.5 x 19.5 CSS px — under WCAG 2.2 AA SC 2.5.8's 24x24 floor.** Measured against the DOM in Story 4.10's browser pass at 1440/375/320: of the 97 interactive targets on `/library-utilization`, this is the **only** one that fails, at every width. It is `LowConfidenceReveal`'s button, shipped by Story 4.1 and shared with `/style-evolution`, so it is pre-existing and not introduced by 4.10 — but Story 4.10's AC-14 says "every target", so it is recorded rather than passed over. **Not fixed here deliberately:** the component is another story's shipped UI on a page 4.10 does not otherwise touch, and re-styling shared shipped controls is the drive-by this epic has already been bitten by (failure mode 5 — shared CSS re-scoped for one call site, breaking another). The fix is two lines (`display: inline-flex; align-items: center; min-height: 24px`) and wants a browser pass on `/style-evolution` too. [web/app/components/style-evolution/LowConfidenceReveal.tsx; web/app/style-evolution.css]

### Open — decision deviated from, needs a ruling to confirm or reverse

- **D-36's placement clause and its own "Critical" clause are not both satisfiable, and the Critical one was followed.** D-36 asks for the search field "inside the 'Tracks' group, above the pair" and, in the same paragraph, names why it cannot be there: the Tracks group lives inside `renderBody`, `renderBody` is called **twice** (excluding/including populations), and "putting query state inside it produces two independent search fields" — the DJ's typed query silently discarded the moment they touch the reveal. **Shipped at page level instead**, as a slot on `LibraryUtilizationReveal` so one boolean governs both the search results and the page body. Verified in the browser pass: typing a query, toggling the reveal, and finding the query still in the field and the counts correctly changed. The page's outline (five `<h2>`s) and landmark count (3) are unchanged, so the rest of D-36 holds. If Arjun prefers the field lower on the page, the constraint to preserve is the single owner of `revealed`, not the placement. [web/app/(authenticated)/library-utilization/page.tsx; web/app/components/library-utilization/LibraryUtilizationReveal.tsx]

- **D-29's `haystack`-in-the-model-layer form measured 3.3x over its own stop-condition; ruled to tuple rows (D-39, Arjun 2026-08-10).** Recorded here because the numbers should not have to be re-measured: at seed scale (1,644 entries) D-29 as written serializes to **498.9 KB** against a ~150 KB bar — content is only 92.5 KB of title+artist, the rest being per-entry JSON key names (~148 KB), the `haystack` duplicating title+artist, and a redundant `key`. Shipped form is tuple rows with the haystack rebuilt once client-side: **157.2 KB raw / 61.8 KB gzipped**. The 157.2 is 4.8% over the nominal bar and that overage is entirely AC-12's second count pair (6.4 KB), which is what lets the reveal be a swap rather than a second read. Alternatives measured and declined: 6-tuple with a sparse override list for the 247 rows that actually differ, 153.2 KB — 4 KB saved for a second indirection on every read. **If the index ever needs to shrink further, the next lever is a server-side search, and GAP-5 says that needs its own ruling** (no index on `plays.title`/`artist` or `library_roster.title`/`artist`, and no trigram/full-text extension anywhere; a GIN index is a migration). [web/lib/sets/trackSearch.ts]

### Open — noted while working, not this story's call

- **`SpotlightSearch`'s placeholder is not `aria-hidden`, so its text is announced alongside the input's own `aria-label`.** Story 4.10's field marks its placeholder `aria-hidden` (Task 3 required it); the dashboard's original does not, so a screen-reader user there hears the region named twice. One attribute, on another story's shipped component. [web/app/components/dashboard/SpotlightSearch.tsx]

## Deferred from: code review of 4-10-track-lookup-and-track-detail (2026-08-10)

D-36's placement deviation and the `.se-hidden-toggle` 24×24 gap were already logged above (dev's own self-disclosure) and are not repeated here.

- **`SetSimilarity`'s axis day labels are server-rendered and locale/timezone-dependent — ruled accept-and-disclose 2026-08-10 (Arjun), not patched.** `buildAxisDayLabels()` (`web/lib/sets/libraryUtilization.ts`) calls `formatDayDate` — `Date.toLocaleDateString()` with no explicit timezone — server-side, the same defect class D-32 eliminated for the clock strip's hour, at day granularity. The code review's other three instances of this class (`TrackDetail.tsx`'s date fields) were patched to render client-side post-hydration; this one could not follow the same fix, because `buildAxisDayLabels()`'s output also feeds `setSimilaritySummary()`, which builds the module's `aria-label` **server-side** — a Server Component's accessible name, fixed at SSR time, unlike `ClockStrip`'s own client-owned `aria-label` or `TrackSearch`'s dates (which only ever render after a keystroke, guaranteed post-hydration). Deferring it would mean converting the whole similarity grid to a Client Component. Same pre-existing class as the entry below (`GenreShareStream`/`TrendChart`'s locale-dependent axis ticks) — this is now a third instance, in a new component, rather than the "must not add a second" this diff's own `TrackSearch.tsx` comment aimed for. **Work owed, if picked up:** either accept a shared, explicit-timezone formatter for all day-granularity server-rendered dates (removing the divergence risk without a client conversion), or fix all three instances together at the same seam, as the `TrendChart`/`GenreShareStream` entry below already recommends for its pair. [web/lib/sets/libraryUtilization.ts:573, web/app/components/library-utilization/SetSimilarity.tsx]

- **Search can list the same physical track twice under a D-28 identity conflict.** `buildTrackSearchIndex` (`web/lib/sets/trackSearch.ts`) dedupes the owned half against the played half by checking `playedIds.has(entry.track_id)`, where `playedIds` is populated from `trackIdByKey`. D-28 fails a `trackKey` group closed to `null` when its plays carry ≥2 distinct non-null `track_id`s — and a group that fails closed contributes no id to `playedIds`, even if the roster still carries a valid, unambiguous `track_id` for that same track. The result: one row from the played half (unlinkable, `trackId: null`) and one from the owned half (linkable), for what is really one track. Currently **0 instances** in the seed and in production per Task 1's own measurement, so this is defensive-only today — matches the posture the story already applies to D-28's other branches (unit-test it rather than treat it as reachable). No test covers this shape. [web/lib/sets/trackSearch.ts]

- **Per-module "no data" copy in `TrackDetail` can't distinguish "genuinely nothing" from "hidden by the confidence reveal."** The ride time, mix neighbours and clock strip modules each render their own empty-state string (e.g. "No play of this has carried a duration yet") computed only from whichever population `LibraryUtilizationReveal` is currently showing. A DJ reading one module in isolation can't tell whether that's a true statement about the whole track or just about the surviving population, distinct from the page-level `LibraryUtilizationReveal` banner that discloses the hidden-set count once, above all modules. Not a new problem this story introduces — mirrors the exact shape Story 4.9's five modules already ship under the same wrapper — recorded here because the story added three more instances of it. [web/app/components/track-detail/TrackDetail.tsx, ClockStrip.tsx]

## Deferred from: Story 4.4 — aging shelf (2026-08-08)

### Open — post-MVP feature, cost already measured (do not re-measure)

- **The row-level "add to prep crate" action (UX-DR12, PRD UJ-6 step 3) is OUT OF MVP, ruled by Arjun 2026-08-08.** Recorded here in full so the next person to pick it up inherits the finding rather than re-deriving it, because the cost is structural and not obvious from the UI:
  1. **There is no cloud→agent command channel anywhere in this system.** The agent only ever pushes. AD-8 plus all three of its named write amendments — AD-20 (status heartbeat), AD-21 (library add-events), AD-22 (roster) — are outbound-only, and nothing pulls instructions down. Building the action means building that channel first: a new architecture decision, not a task inside a UI story.
  2. **It would be the first-ever WRITE to Serato.** Story 2.7 scopes the agent's filesystem capability to reads. The target is a binary `.crate` format, with file-locking hazards while Serato is running — a corrupted crate is a DJ's gig, so this needs its own risk work.
  3. **Two cheaper substitutes were considered and also declined.** A Curfew-side saved list in Supabase (DJ-writable via RLS, no new AD needed) — real work, and it does not put the tracks where the DJ actually digs. An `.m3u8` export — weak regardless, because AD-2 keeps file paths off the wire, so the roster is title/artist only and Serato would have to re-match on import.
  **What shipped instead:** read-only rows with **no substitute affordance** — no dismiss, no star, no "mark as reviewed". That was deliberate: UX-DR12 calls this action "the one place the product nudges toward an action, not just a report", and inventing a lesser affordance would have made the gap look filled while changing nothing about what the DJ can do. Without the action the shelf simply *is* the report. **The loss is real and is recorded as one** in PRD UJ-6's Climax and EXPERIENCE.md's UJ-6, rather than reworded into sounding intended. Restoring it is post-MVP and starts at item (1), not at the UI.

### Open — product judgment, needs one ruling

- **The all-clear copy and the recently-downloaded nudge can contradict each other on screen, and both are AC-required.** In the genuinely-clear state the shelf renders EXPERIENCE.md's "Everything you've bought is getting played." (Story 4.4 AC-5, verbatim by requirement) and, directly beneath it, "N tracks added in the last 30 days haven't been played yet." (AC-6, which is unconditional). The nudge is scoped by its own "in the last 30 days" wording, so it reads as *these are just new* rather than a flat rebuttal — and a 5-day-old unplayed track genuinely is not evidence of neglect. But the two sentences are in tension, and the tension grows with the count. Options if Arjun wants it resolved: suppress the nudge in the all-clear state (violates AC-6 as written), or reword the all-clear to scope itself to the 90-day window (violates AC-5's "verbatim"). Left as-is and flagged rather than silently resolved against an AC. [web/app/components/library-utilization/AgingShelf.tsx]
- **The clamp makes every pre-observation track report the identical day count, so sorting is inert across that whole block.** Observed on the committed fixture: 616 qualifying tracks, and the longest-unplayed page is a wall of "730 days" — because every track added before `djs.created_at` clamps to exactly the observation length. This is the clamp working correctly (Curfew has been watching for 730 days and has not seen you play any of them) and the tie-break keeps the order deterministic, but "sortable by days unplayed" does less for a veteran DJ than FR-12 implies: the sort only separates observed-play rows, which have genuinely varied ages. Worth knowing before anyone reads the shelf as a ranked list. Not a defect and deliberately not "fixed" — any secondary sort key would have to come from data the clamp exists to distrust. [web/lib/sets/agingShelf.ts]

### Resolved by this story

- **`/library-utilization` now has one real `<h2>`** — the aging shelf's, which is the first heading below the page `<h1>` and the first module heading-nav can reach. The open finding below ("no `<h2>` at all, and nests three landmark regions") is **partly** addressed: the other three modules still label themselves with `<p className="lu-stat-label">`, and this story added a fourth `<section>` rather than converting anything to `<div role="group">`, because R-10 is still unruled and changing four components under an unruled finding is separate work. Net: 4 sections, 1 h2, where it was 3 sections and 0.
- **`.lu-module .se-empty` rendered as a pill with its copy on a curved edge — measured and fixed.** The open process finding below named this rule as "the first thing to measure" and unverified. Measured in Story 4.4's browser pass: `.dz-shell`'s 50px radius against a box `min-height: 0` collapses to 66px — the radius exceeded half the height — with the copy 6px inside a 50px corner arc. Scaled to `border-radius: 16px` and `padding: 13px 16px`. Fixes `TimeToFirstPlay`'s insufficient-history state too, which is the same component at the same call site and what the finding was logged against. [web/app/library-utilization.css]

## Deferred from: code review of 4-4-aging-shelf-with-prep-crate-action (2026-08-08)

- **Prep-crate action still described, unannotated, outside Story 4.4's own section.** Task 7 named and synced exactly four docs (PRD FR-12/UJ-6, EXPERIENCE.md's Components row/UJ-6 step 4) plus Story 4.4's own AC list. Outside that scope, `epics.md` lines 112 (UX-DR12 description), 201 and 792 (Epic 4 summary), and 991/1007 (Story 4.11's cross-references) still describe the aging shelf as carrying a "add to prep crate" action with no ruling note attached, and `ARCHITECTURE-SPINE.md:201`'s AD-22 "Prevents" bullet does too — the same "ai-2/ai-6 accretion shape" this story explicitly names as a failure mode to avoid, just in sections outside this story's stated doc-sync scope. [_bmad-output/planning-artifacts/epics.md:112,201,792,991,1007; _bmad-output/planning-artifacts/architecture/architecture-name-pending-2026-07-20/ARCHITECTURE-SPINE.md:201]

## Deferred from: post-merge integration review of PRs #20/#21/#22/#23 (2026-08-08)

Stories 4.5, 4.6, 4.7 and 4.11 merged to `main` inside one hour (merge order: #21 4.6 → #23 4.11 → #20 4.7 → #22 4.5). CI was green on the final commit `67b4401` — lint, typecheck, build, 429 JS/TS tests, `cargo fmt`/`clippy --all-targets`/build/403 Rust tests, additive-only guard, a from-scratch `supabase migration up`, and 209/209 pgTAP. The merges were mechanically clean; the findings below are semantic.

**Two were fixed immediately rather than deferred** (branch `fix/epic-4-merge-integration`), both because they shipped wrong numbers to users: `getLibraryRoster` serving one developer's library counts to every DJ, and the two conversion metrics reading a global-earliest play instead of first-play-at-or-after-add. Both now have regression tests verified to fail against the old code.

### Verified NOT broken (recorded so it isn't re-investigated)

- **Migration filename order vs merge order.** 4.11's `create_library_roster` is stamped `...110000`, earlier than all four of 4.6's migrations, but merged after them — so prod applied them in an order a fresh replay will not reproduce. Proven equivalent by `pg_class.oid` creation-order comparison: hosted built `deleted_sets`(17698) before `library_roster`(17709); a fresh `db reset` built them 18714 before 18734. Table grants, column grants, `relrowsecurity`, all 10 policies (`qual`/`with_check` byte-identical), all 15 indexes and all 6 function ACLs match. No `ON ALL TABLES` / `ALTER DEFAULT PRIVILEGES` anywhere in the four PRs, which is why order is immaterial. **New environments provision correctly.**
- **`grant_matrix_test.sql` double-edit** (#21 then #23) is purely additive — 6-table arrays became 7-table, plan 45→51, arithmetic comment updated, 51/51 green. No contradiction.
- **RLS on both new tables.** Enabled (not just policy-written), SELECT-policy-only with no write grant *or* policy, AD-7 `auth.uid()` shape, `dj_id` derived server-side so a caller-supplied one is ignored. The isolation tests genuinely catch leaks (`results_eq` in both directions; a bare `delete from public.sets` under a `sub`-less JWT).
- **Revoking EXECUTE on `record_deleted_set` did not break the tombstone** — Postgres checks trigger-function EXECUTE at `CREATE TRIGGER`, not fire time. 9/9 green against the revoked state.
- **Roster upsert conflict target** matches the real `unique (dj_id, track_id)`; `added_at`/`is_baseline` correctly omitted from `DO UPDATE` per AC-3.
- **Additive-only guard was strengthened, not weakened** — schema is byte-identical to its frozen baseline; the test change extended coverage from 2 schemas to 3.
- **No dangling refs** to the three components #20 deleted; no feature dropped by the three-way merge on `library-utilization/page.tsx`; one window selection, not two.

### Open — agent-side, no user-visible symptom yet

- **The soft-delete safety gate is a one-way ratchet, and disables AC-5 permanently and silently.** `store::observe_catalogue_reach`'s high-water mark only ever grows, which is correct for a temporarily-unplugged drive but wrong for a permanent change. Retire/sell/reformat a drive, rename a volume, or hit a routine `/Volumes/USB 1` remount collision, and the old root becomes a phantom that never reappears → `complete_reach` is `false` on every subsequent scan → `mark_absent_tracks` never runs again for the life of the install, with no log line at any level. Story 4.4's aging shelf then renders tracks deleted years ago as "owned but never played," with nothing pointing at the cause. Secondary defect in the same function: the root set is persisted newline-joined and parsed with `.lines()`, so a volume name containing `\n` (macOS permits it) splits into two phantom roots with the same permanent effect. Fix: per-root last-seen timestamp/counter with expiry after N complete scans or a wall-clock window, plus JSON-array serialization, plus one info-level log when reach is incomplete. [agent/src-tauri/src/store.rs — `observe_catalogue_reach`; agent/src-tauri/src/capture.rs:366; agent/src-tauri/src/joiner/date_added.rs:196-208]
- **The first roster drain blocks AD-20's heartbeat.** `store::library_roster_pending_sync` has no `LIMIT`, so a first scan of a 20,000-track library materializes all 20,000 rows and fires 100 sequential blocking POSTs before `sync_loop` reaches `beat_status`. The dashboard shows its live-agent signal as stale on exactly the healthiest agent — the failure `beat_status` exists to prevent. Set sync is *not* starved (it runs first and is never gated on the roster) and there is no deadlock; this is latency only. Fix: one line — `LIMIT 10 * ROSTER_BATCH_SIZE` and let the remainder drain on later passes. [agent/src-tauri/src/store.rs — `library_roster_pending_sync`; agent/src-tauri/src/sync.rs — `sync_pending_library_roster`]
- **`scan_identity_coverage` has no caller outside its own unit test.** The agent computes and persists the two identity-coverage scalars AC-6 needs, but nothing ships them: `library_roster` is per-track (wrong shape) and AD-20's heartbeat carries no derived Serato data. This is why `getLibraryRoster` now returns 0 for them. Needs a named decision — an optional extra AD-22 RPC argument, or two `agent_status` columns — not a default. [agent/src-tauri/src/store.rs:162,182; web/lib/sets/index.ts — `getLibraryRoster`]

### Open — the three-way future-dated disposition (picked up from party-mode, needs one ruling)

- **RULED 2026-08-10 (Arjun): count + disclose everywhere, matching `buildTimeToFirstPlay`'s existing behavior.** `buildLiveConversionRate` and `buildLibraryConversion` both need a counter added and folded into a disclosed clause the same shape as `unreconciledDateCount` — no module may silently drop a future-dated row, and none may mislabel it as a pending cohort. Chosen over leaving the inconsistency because it matches the project's established "never silently drop, always disclose" discipline (the same one the undated-row clause was ruled under). **Implementation owed** — this is a ruling, not a code change; touches `libraryConversion.ts`'s `buildLiveConversionRate` (add a counter, drop the silent `continue`) and `buildLibraryConversion` (stop routing future-dated rows into `pendingCohortCount`, route them into the new counter instead), plus whatever disclosure copy surfaces it on each module. Original finding kept below for the implementer.
- **A future-dated add row (`addedMs > nowMs`) is disposed of three different ways by the three modules on `/library-utilization`, and told to the DJ three different things.** This was Boundary's parting observation in the Story 4.5 party-mode session (`_bmad-output/party-mode/memories/installed/.memlog.md`, logged verbatim as *"three modules on that page dispose of the same future-dated row three different ways, only one of those is a bug"*) and recorded there as picked up by nobody. Verified against the merged code:
  1. `buildTimeToFirstPlay` **counts** it as `futureDatedCount`, folded into `unreconciledDateCount` and disclosed as "N tracks have add dates Curfew can't reconcile".
  2. `buildLiveConversionRate` (`libraryConversion.ts:447`) **silently drops** it — `if (addedMs < windowStartMs || addedMs > nowMs) continue;` with no counter, so it leaves the denominator with nothing disclosed.
  3. `buildLibraryConversion` **never tests it at all** — the row buckets into a future month key, and `isCohortComplete` then excludes that bucket and increments `pendingCohortCount`, so the same row is reported as "a recent month still inside the window". That is a different factual claim than "an add date we can't reconcile", and it is the one most likely to be wrong: a clock-skewed row is not a pending cohort and will never complete into a real one.
  4. **[ADDED 2026-08-08]** `buildAgingShelf` (Story 4.4) is the fourth module over this data and deliberately did NOT invent a fourth disposition: it adopts (1)'s — counted as `unreconciledDateCount` and disclosed as "N tracks have dates Curfew can't reconcile — not counted here." It applies the guard to every clock source that can sit in the future, not just `added_at` (a future-dated *play* would otherwise render a negative day count). So the tally is now **3 modules on (1)'s side, 1 each on (2) and (3)** — the ruling is cheaper to apply than when this entry was written, and `buildAgingShelf` names this entry at the guard so the change lands in one place if the ruling goes the other way.
  Needs ONE ruling applied to all four, the same way the merge ruled the undated clause once, rather than four independent choices. Left unfixed deliberately: the post-merge review's remit was defects that ship wrong numbers, and (2)/(3) are unreachable without a genuinely skewed clock or a bad `tadd` — but the disagreement is real and pre-existing, and it is now the only place in this file where three metrics over one array answer the same question differently. [web/lib/sets/libraryConversion.ts:447, `buildLibraryConversion`, `buildTimeToFirstPlay`]

### Open — contract and guard gaps

- **`SyncLibraryRosterBatch` (TS) describes a body PostgREST rejects.** It requires `contract_version` and `agent_version`, but the agent sends only `{"entries":[...]}` and the RPC is `sync_library_roster(entries jsonb)`; PostgREST resolves RPCs by argument name, so the extra top-level keys fail signature matching (PGRST202) rather than being ignored. The JSON Schema correctly reduced `required` to `["entries"]` and `index.test.ts` documents why — the TS type was not brought along. Consequences: a future TS producer typed against this builds a rejected body, and AD-22's write has zero agent-version traceability unlike AD-20's heartbeat. Note the fix window for *requiring* them has closed — additive-only now forbids adding to `required`, so traceability needs an additive second RPC argument. Fix: mark both optional with a comment pointing at argument-name resolution. [shared/src/index.ts — `SyncLibraryRosterBatch`]
- **`no-raw-data.test.ts` reads only `sync-payload.schema.json`** — 1 of 3 schemas, even though this same PR extended `additive-only.test.ts` to all 3. The roster schema is the first wire artifact carrying human-readable `title`/`artist`, so it is precisely where the `path`/`raw`/`blob`/`bytes` deny-list and the `additionalProperties:false` sweep earn their keep. A future `portable_path` on `library_roster_entry` would pass additive-only, never be looked at by this test, and ship a raw filesystem path — the exact AD-2 breach it exists to prevent. Nothing else backs it up: `check-additive-only-migrations.sh` guards SQL migrations, not the shared JSON schemas. Fix: hoist `current` into the same `describe.each` loop `additive-only.test.ts` uses; `isReviewedRawException` compares by object identity against `current.$defs?.genre` so it fails closed for the other two automatically. [shared/src/no-raw-data.test.ts:7-9,141]
- **`shared/package.json` `exports` omits `./schema/sync-library-roster.schema.json`** while `shared/src/index.ts` exports `SYNC_LIBRARY_ROSTER_SCHEMA_PATH` so consumers can locate it. A consumer following that documented pattern gets `ERR_PACKAGE_PATH_NOT_EXPORTED`. Latent — `index.test.ts` resolves via `node:fs` and bypasses the exports map. [shared/package.json]
- **`hasRenderableDerived` guards 3 of the 9 `derived` sub-objects consumers dereference**, and 4.7 added a fourth unguarded dereference (`styleEvolution.ts:309`, `camelot_mixing_stats.compatible_transitions`) outside the seam's try/catch — so a partial blob 500s `/style-evolution` instead of dropping one set, the failure the guard was added to prevent. Currently unreachable (all nine are `required` in the frozen baseline from birth and `sync_set` always writes the blob), so the guard's choice of three is arbitrary rather than wrong. [web/lib/sets/index.ts:122-131]
- **Nothing validates any payload against any JSON Schema at runtime.** `lib.rs:107` embeds only `sync-payload.schema.json` and merely parses it. Rust↔schema divergence is caught by hand-written assertions in `shared/src/index.test.ts` and nothing else.
- **Both `export_real_library.rs` integration tests are `#[ignore]`** (they need `CURFEW_REAL_HOME`/`CURFEW_REAL_MASTER`), and that file is what #23 modified to emit `title`/`artist`. The new export path — including the `title.unwrap_or_default()` whose comment claims it "never actually fires" — has zero automated coverage. Green CI does not cover it.

### Open — SQL hardening, both about future work rather than shipped code

- **`service_role` has no CRUD grants in a fresh replay.** `20260807140000` says "`service_role` is deliberately untouched", and untouched means ungranted. It works on the current hosted project only via the legacy `auto_expose` default ACL (`arwdDxtm`); a newly provisioned project gets the modern default (`Dxtm`), and `service_role` is `rolbypassrls=true` but **not** superuser — RLS bypass is not a GRANT. So every service-key path returns `permission denied` on any new environment while passing on this one. Inert today (nothing uses the service key), detonates when AD-18's Stripe webhook or any admin tooling lands. Fix: one migration granting `select,insert,update,delete` on all `public` tables plus matching default privileges (not `grant all` — leave TRUNCATE out), and a `grant_matrix_test` assertion pinning it.
- **No test asserts RLS is enabled on every `public` table.** The root cause behind all six grant migrations is untouched: the `public` default ACL still grants `arwdDxtm` to `anon`/`authenticated` on hosted, so every future table is born browser-writable and every future function anon-executable — `20260807160000` exists only because of this and will need a sibling forever. The generic sweeps at `grant_matrix_test.sql:85-116` are the right mitigation but have holes: no `relrowsecurity` sweep (a migration that forgets `enable row level security` gets full anon CRUD with **nothing failing**), no generic INSERT/UPDATE/DELETE sweep (those are hardcoded 7-table arrays, so a new table trips only the TRUNCATE sweep and the message misdescribes the problem), `MAINTAIN` (PG17) unswept, and the SECURITY DEFINER sweep filters `prosecdef` so a future `SECURITY INVOKER` function stays anon-executable unswept. Fix: three sweeps — every `public` table has `relrowsecurity`; zero non-SELECT privileges for `anon` across all `public` tables; zero `anon` EXECUTE on any `public` function regardless of `prosecdef`.
- **`auth_rls_initplan` on all 10 policies** — `auth.uid()` re-evaluated per row instead of `(select auth.uid())`. Not a seq-scan cause (the comparison is on an indexed leading column, AD-7 doing its job) but a per-row function call on the 1000-row-page roster/add-event reads. Fix: one migration recreating all 10 policies, done as a single sweep rather than per-story; silences 10 of 16 performance lints.
- **Index cleanup, one migration.** `library_track_events_dj_id_added_at_idx` is redundant — nothing filters or orders by `added_at` server-side (all month-bucketing is in JS), and the unique `(dj_id, track_id)` already serves predicate + sort + paging. `plays_dj_id_idx` is a strict prefix of `plays_dj_id_track_id_idx` (pre-existing). `library_roster_dj_id_absent_at_idx` is premature — nothing selects from `library_roster` yet. Meanwhile the actual hot query is unindexed: `getRecentSets` is `order by started_at desc, id desc limit 500` under RLS `dj_id =`, with only `sets_dj_id_idx (dj_id)` — so it fetches then sorts. `(dj_id, started_at desc, id desc)` would make it an ordered index scan short-circuiting at the LIMIT **and** make `sets_dj_id_idx` droppable.
- **Orphan `sessions` rows accumulate.** A set delete deliberately leaves its session, and the tombstone then makes `sync_set` return before the `sessions` insert, so the anchor is stranded permanently with no cleanup path and no client DELETE grant. Harmless (nothing reads `sessions` directly). Also: `is_baseline`/`added_at` are un-correctable once written (correct per AC-3, but an agent that mis-classifies on first sync can never fix it, and only `service_role` could — which per the finding above currently can't on a fresh project). **And: never `force row level security` on these tables** — every write path is `SECURITY DEFINER` owned by `postgres` with no INSERT/UPDATE policy anywhere, so `relforcerowsecurity=false` is load-bearing.

### Open — UI, a11y, and efficiency

- **`/library-utilization` has no `<h2>` at all, and nests three landmark regions.** `LibraryUtilizationView.tsx:39-41` renders `<section aria-label="Conversion">` whose group heading is a `<p className="lu-stat-label">`, so heading-nav skips it and the text duplicates the section's accessible name; inside it sits `<section aria-label={summary}>` (the meter) and `TimeToFirstPlay.tsx:80` adds a third. #20 gave `/style-evolution` a real `<h2 className="se-section-title">` but the port used a `<p>`. This is R-10 from 4.7's own review (open, unruled) now propagated to a second page. Fix: `<h2 className="lu-stat-label">Conversion</h2>` and drop the section's `aria-label`; switch the two module `<section>`s to `<div role="group">` if R-10 is ruled that way.
  - **[CLOSED 2026-08-08 by Story 4.9: R-10 ruled, both halves applied.]** The page now has a real outline — `H1 Library Utilization → H2 Conversion → H3 Conversion rate / Conversion trend → H2 Rotation → H3 Rotation size / Repeat tracks / Set similarity → H2 Tracks → H3 Workhorses / Played once → H2 First play → H3 Time to first play` — and every module `<section>` became `<div role="group">`, keeping each informative accessible name while removing it from the landmark list. Measured landmark count: **2** (`main` + `nav`), where naively adding Story 4.9's five modules as sections would have produced nine. Two gaps found by this story's own code review and fixed in it rather than reopened: `LibraryConversionTrend` was a bare `<div>` with no heading and no accessible name at all (the one module the outline missed), and `TimeToFirstPlay` — an add-side metric — had been filed under the play-side `<h2>Tracks</h2>` group.
- **Two sibling modules treat the identical "early read" caveat differently for AT** — exposed in `ConversionRateMeter.tsx:92-96`, `aria-hidden` in `TimeToFirstPlay.tsx:121-125` because its `summary` carries it while `liveConversionRateSummary` does not. Individually defensible; a screen-reader user hears the caveat once and not the other time on two cards 200px apart.
- **Efficiency, all under-sharing rather than over-abstraction** (the page-level double play-index pass was fixed with the conversion bug): `styleEvolution.ts:374-377` is O(buckets × sets) — `buckets.map(b => sets.filter(...))` re-derives a calendar key per (bucket, set) pair, ~11,800 derivations on the fixture where a group-by needs 116, scaling to ~156k at 3 years/500 sets; and `aggregateBucket` now makes six sequential passes over the same sets (#20 added three), with the new mixPace loop re-walking exactly the plays the key-diversity loop already walks. Fix: one keyed pass into two `Map`s, and accumulate `played_ms` inside the existing per-play loop. Agent-side: `known_track_ids` is read twice per scan (once in `capture.rs`, once inside `mark_absent_tracks` which calls it itself); `observe_catalogue_reach` writes on every scan even when the root set is unchanged; `record_library_tracks`/`refresh_library_track_tags`/`mark_absent_tracks` each issue one un-transactioned statement per row, acceptable at the measured ~775ms/5,000 but the mass-absence path is exercised by no test at any scale.
- **Not over-engineered, checked and confirmed fine:** #20 genuinely consolidated three window-pickers into one (net −2 components, −1 storage key, −1 scale, with both retired keys `removeItem`'d); `LibraryUtilizationView` exists solely to own the one shared selection and is what keeps `TimeToFirstPlay` a server component. The one residual duplication — `ConversionWindowDropdown` and `GranularityToggle` sharing ~40 lines of `useSyncExternalStore`+localStorage boilerplate — is **not** worth extracting at 2 copies; the project's Dev Notes mandate that shape as house convention and a helper here would sit on the overcomplication line.

### Open — process

- **[RESOLVED 2026-08-08 — Story 4.4]** The composition has now been rendered, as **four** modules rather than three: meter + trend + time-to-first-play + aging shelf + the page note, walked at 1440/375/320 with zero console errors or warnings and zero horizontal overflow at any width. `.lu > .lu-module { max-width: 440px }` **holds** and was verified against list content, not just a stat readout (a title—artist—days row leaves the track column ~330px and truncates with ellipsis rather than pushing the day count out of the row). `.lu-module .se-empty { min-height: 0 }` was the one that did NOT hold — see the Story 4.4 section at the top of this file for the measurement and the fix. Original finding, kept for context: ~~**The merged three-feature composition of `/library-utilization` has never been rendered in a browser.**~~ Story 4.5's own file says the pass is owed (`4-5-time-to-first-play.md:221`). Every measurement on record predates the three-way merge: #20 walked 1440/375/320 with two modules, #22 walked 1440/375 with a different two; nothing has seen meter + trend + time-to-first-play + page note together. The post-merge repair rule `.lu > .lu-module { max-width: 440px }` (added by #22 to restore a cap #20 correctly removed for itself) is itself unverified. First thing to measure: `TimeToFirstPlay`'s insufficient-history branch, which nests a `dz-shell` (2px border, `border-radius: 50px`) inside another and flattens it to ~40px via `.lu-module .se-empty { min-height: 0 }` — the radius exceeds half the height, so it renders as a pill with copy 4px from a curved edge. 4.5 measured that state at 151px *before* those two rules landed.
  - **Also measured independently by Story 4.9 (2026-08-10), which did not know 4.4 had closed it.** Same conclusion, arrived at separately: the entry's named-but-unverified suspect — `.lu-module .se-empty` flattening a `dz-shell` so its 50px radius exceeds half the block's height — was REAL, and affected *every* module's insufficient branch rather than only `TimeToFirstPlay`'s. 4.9 measured 45px against the 50px radius at its worst and flagged it in 7 of 9 measurements across the 0-, 1- and 2-set states. Both stories fixed it the same way (scale the radius, leave the height alone); 4.4's values shipped first and are the ones in the tree. Recorded because two independent passes converging on one diagnosis is evidence the rule was genuinely wrong, not a matter of taste.
- **The undated-add-date disclosure state has never rendered anywhere.** `library-add-events.fixture.json` has 523 events and **zero** undated, so every browser pass and every test ran against data where `noAddDateCount` is 0 — and #21 swapped the read path to Supabase in the same hour, so real data is what ships. This is why the duplicate-clause finding below is invisible today.
- **[RESOLVED 2026-08-10]** `sprint-status.yaml` said `4-5-time-to-first-play: review` though PR #22 is merged to `main` — the only Epic 4 story in a non-terminal state; `4-5-time-to-first-play.md:7` also still read `Status: review`. Both flipped to `done`, with a Change Log / session-log entry pointing at PR #24 as the closing work. `4-11-library-roster-sync.md:225-228`'s four Decision checkboxes (unchecked `[ ]` while the narrative said they were applied) also flipped to `[x]`, each now pointing back at its resolving decision in the numbered list above it. Original finding kept below. Findings 1 and 2 above were the *residuals* those decisions created and did not close — still open, not part of this bookkeeping fix.
- **Nothing in the suite would catch a dropped disclosure prop** (carried forward from the 4.5 merge entry, still true and now more load-bearing): `unidentifiableDisclosure` is `?: string | null` at all three hops, so deleting any one leaves `tsc` and `eslint` fully green while AC-6's line silently stops rendering. `react-dom` is already a dependency; a `renderToStaticMarkup` harness with a negative control was proven to catch it during the 4.5 merge and then deleted rather than committed, because `web/` has carried only pure unit tests and adopting the first component-render test is a convention change deserving its own decision. Recommend making it permanent — the `getLibraryRoster` fix above removes the *false* rendering but not the ability to silently lose the *true* one.
  - **[CLOSED 2026-08-08 by Story 4.9 (D-24): adopted, and the premise was verified rather than asserted.]** `web/app/components/library-utilization/prop-threading.test.tsx` is now committed — 20 assertions including 8 negative controls, `renderToStaticMarkup` string assertions only, **no RTL and no jsdom**, so the house rule (logic in `lib/sets/*`, components stay thin) is not overturned. The premise was tested by deliberately cutting three threaded props: `tsc --noEmit` stayed **fully green on all three**, `eslint` gave warnings-only on two and nothing at all on the third, and the new harness caught all three via its intended assertion. Scope it stays narrow: prop-threading only. **Still open, and sharpened by Story 4.9's code review — with one correction made after merging main.** The disclosure this harness protects **still cannot be driven in a browser**, but not for the reason first recorded. Story 4.9's review seeded `library_roster` (653 rows) believing `getLibraryRoster()` read it; on the pre-merge branch that seam was a stub returning zeros, so the note was corrected to say the rows were inert. Merging main corrected it again: **Story 4.4 made `entries` a real Supabase read**, and the seeded rows now feed the aging shelf — verified in a browser at 198 shelf tracks. What remains genuinely blocked is the other half, exactly as the seam's own doc comment predicted: `excludedNoIdentityCount`/`totalCatalogueRows` are scan-level scalars with **no cloud carrier at all** (`library_roster` is the wrong shape; AD-20's heartbeat carries no derived Serato data), so the agent computes them and nothing ships them, and `unidentifiableTracksDisclosure` gates to `null` no matter what is seeded. Until that transport exists — an extra AD-22 RPC argument or two `agent_status` columns — this prop is protected only by the unit-level harness, never by a browser. (Recorded at length because the claim was wrong in both directions before it was right, each time from reading rather than rendering.)

## Deferred from: code review of 4-5-time-to-first-play (2026-08-07)

- **A lifetime-scope metric is fed by a seam named `getRecentSets()` — a constraint Story 4.6 must honor, not a defect today.** `buildTimeToFirstPlay` is documented as lifetime-to-date with no trailing window (unlike `buildLiveConversionRate`, whose denominator is a trailing window as-of-now), but its play history comes from `getRecentSets()`, which `web/lib/sets/index.ts:37-40` is documented as swapping from the committed fixture to a Supabase query. If 4.6 bounds that query to the last N months, **every debut older than N is silently reclassified "never played"** — inflating the user-visible `neverPlayedCount`, inflating the AC-4 population gate, and biasing the median toward recent, fast-debuting tracks. The function has no window parameter and therefore no way to detect that its input was truncated; it would simply report a wrong number confidently. The fixture is unbounded, so this is invisible until 4.6 lands. Either keep the sets query unbounded for this consumer, or give `buildTimeToFirstPlay` an explicit horizon it can disclose. [web/app/(authenticated)/library-utilization/page.tsx:23; web/lib/sets/index.ts:37-40; web/lib/sets/libraryConversion.ts — `buildTimeToFirstPlay`]
  - **UPDATE (2026-08-07, merging 4.6/4.7/4.11 into this branch): 4.6 has landed and the bound is real, but it is a COUNT, not a horizon.** `getRecentSets` now selects from Supabase `.order("started_at", desc).limit(RECENT_SETS_LIMIT)` with `RECENT_SETS_LIMIT = 500` — so the truncation is "the 500 most recent sets", not "the last N months". Same failure mode as predicted, different axis: past 500 sets, every debut in an older set is invisible to `playsByTrack` and its track is silently reclassified never-played. Not yet reachable on real data (58 sets), so this stays deferred rather than becoming a blocker, and the count is deliberately documented as a guard against PostgREST's invisible 1000-row `max_rows` cap — it is not an arbitrary limit. Resolution options are unchanged; the second (an explicit horizon the model can disclose) now looks better than the first, since "unbounded for this consumer" fights the very truncation guard 4.6 added on purpose. Re-verified at merge time, not assumed. [web/lib/sets/index.ts — `RECENT_SETS_LIMIT`, `getRecentSets`]

## Deferred from: merging main (4.6 + 4.11 + 4.7) into 4-5-time-to-first-play (2026-08-07)

- **RULED 2026-08-10 (Arjun): the page-level note owns `noAddDateCount`; the trend states only its window-dependent clause.** The trend's call site passes `noAddDateCount: 0` so its own `undatedDisclosure` line collapses to the `pendingCohortCount` clause alone, and the page-level note remains the single place that states the undated-tracks fact — matching the "one generator, one canonical statement" pattern Story 4.5 already used to dedupe the meter/time-to-first-play pair. **Implementation owed** — this is a ruling, not a code change; touches the trend's call site in `web/app/(authenticated)/library-utilization/page.tsx` (or wherever `LibraryConversionTrend` is invoked) and should be verified by rendering the real tree the way the original merge was, since a dropped/miscounted prop is invisible to `tsc`/`eslint`. Original finding kept below for the implementer.
- **The undated-add-date clause is now stated twice on Library Utilization whenever `noAddDateCount > 0` — once by the trend, once by the page-level note.** Story 4.5's review hoisted ONE page-level note and stripped the meter's own copy, on the reasoning that `noAddDateCount` is window-independent and identical across modules. That still holds, and the merge re-applied it. But 4.7 moved the Story 4.2 trend onto this page, and the trend renders its own `undatedDisclosure` carrying `pendingCohortCount` — which IS window-dependent, so it legitimately belongs inside the window-governed section and was deliberately left alone. The two lines consequently share an opening clause: the trend says *"2 tracks have no known add date, and 2 recent months are still inside the 60-day window — not counted here."* while the page note says *"2 tracks have no known add date, and 2 tracks have add dates Curfew can't reconcile — not counted here."* Verified by rendering the real tree, not by reading. Invisible on the committed fixture (the count is 0) and on any library whose add-dates all resolve. **Deliberately not fixed in the merge**, because the clean fix — passing `noAddDateCount: 0` at the trend's call site so it states only its window-dependent clause — changes shipped 4.2/AC-7 behaviour and detaches the caveat from the chart it qualifies. That is the same concern already open as Story 4.5 review finding 5 ("the page-level disclosure hoist detached the undated caveat from the meter's own region"), so it should be ruled once, for all three modules, rather than twice in opposite directions. [web/app/components/library-utilization/LibraryConversionTrend.tsx; web/app/(authenticated)/library-utilization/page.tsx — `undatedNote`]
- **Nothing in the suite would catch a dropped disclosure prop.** `unidentifiableDisclosure` is `?: string | null` at all three hops (page → `LibraryUtilizationView` → `ConversionRateMeter`), so deleting any one of them leaves `tsc --noEmit` and `eslint` fully green while the Story 4.11 AC-6 line silently stops rendering — precisely the failure AC-6 exists to prevent. Confirmed during this merge with a throwaway `react-dom/server` `renderToStaticMarkup` harness (7 assertions, including a negative control that removes the prop and asserts the copy disappears); it passed, and was then deleted rather than committed, because `web/` has deliberately carried only pure unit tests and adding the first component-render test is a convention change that deserves its own decision rather than riding along in a merge. `react-dom` is already a dependency, so adopting it needs no new packages. Recommend making it permanent. [web/app/components/library-utilization/LibraryUtilizationView.tsx, ConversionRateMeter.tsx]
  - **[CLOSED 2026-08-08 by Story 4.9 (D-24).]** Duplicate of the "Open — process" entry above; see that one for the adopted harness and the empirical verification of its premise.

## Deferred from: code review of 4-6-web-read-path-supabase-swap (2026-08-07)

- **The hosted Supabase project's ACLs diverge from the migrations, and a local pgTAP suite structurally cannot see it — `20260807140000` fixes the current instance but not the mechanism.** The project runs the legacy `auto_expose_new_tables` behaviour, so every table created in `public` is auto-granted the full privilege set to `anon`/`authenticated` as it is created. `supabase/config.toml` leaves that field commented out (the modern default), so `supabase db reset` produces narrow grants and every isolation test passes against a state the hosted project does not share. `20260807140000_harden_table_and_function_grants.sql` revokes and re-grants the intended matrix, and `grant_matrix_test.sql` pins it — but **the next table added by a future migration will be auto-granted again**, and nothing local will fail. Real follow-up scope: either set `auto_expose_new_tables = false` on the hosted project (the field is removed 2026-10-30 when always-revoked becomes permanent, so this self-resolves eventually), or add a CI step that diffs the hosted grant matrix against `grant_matrix_test.sql`'s expectations. Flagged rather than fixed because it is a project-settings/CI concern, not a schema one. [supabase/config.toml:24, supabase/migrations/20260807140000_harden_table_and_function_grants.sql]
- **Supabase's security advisor also reports leaked-password protection disabled** (HaveIBeenPwned checking on Auth). Unrelated to this story, one dashboard toggle, recorded so it is a decision rather than an oversight. Not touched here because it is an Auth project setting, not code. [Supabase dashboard → Auth → Password security]

- **A genuine production Supabase read failure renders identically to a brand-new account, with zero telemetry.** All four seam functions log only when `NODE_ENV !== "production"`, so an existing DJ's entire archive can silently render the first-run/insufficient-history state while a real regression (expired session, RLS change, network) sits invisible. This is not a deviation — AC-4 explicitly specifies exactly this behavior, mirroring `getAgentStatus`'s established contract, so the code is correct *as specified*. The concern is with the spec: the "logged loudly in non-production" half of AC-4 provides no production signal at all. Real follow-up scope: route seam read failures to a production error sink, and/or let the returned snapshot distinguish empty-because-new from empty-because-failed so a page can render an honest "we couldn't load this" instead of a cold-start state. [web/lib/sets/index.ts:118, :166, :198, :227]
- **Every dashboard / Style Evolution / Library Utilization render fetches all 14 play columns of every set, uncached.** `SET_WITH_PLAYS_SELECT` is shared by `getRecentSets` and `getSetById`, but only Set Detail needs full play rows — the dashboard's list/hero/right-column models need `derived` plus counts. Real data today is 58 sets ≈ 2,294 plays shipped on every navigation to three routes (`cookies()` forces dynamic, no `unstable_cache`). Not a correctness bug; a payload/latency cost that grows linearly with a DJ's history. Real follow-up scope: split the seam into a plays-free list select and a plays-bearing detail select — a deliberate refactor of the seam's public shape, which AC-2 froze for this story. [web/lib/sets/index.ts:59-60]
- **`style-evolution/page.tsx` and `library-utilization/page.tsx` have no auth self-guard, and the `(authenticated)` route group has no gating middleware.** `layout.tsx:7` states the convention explicitly ("no auth-gating middleware/redirect exists yet for this group — each page self-guards"), but only `dashboard/page.tsx` actually calls `supabase.auth.getUser()`. A logged-out visitor renders a fully-formed new-DJ page rather than being redirected. Pre-existing (Story 3.5-era) and *improved* by this story — before the swap those routes served the committed fixture to unauthenticated visitors; now RLS returns zero rows, so nothing leaks. Flagged because the swap makes the missing gate the only thing standing between a logged-out visitor and a real read. [web/app/(authenticated)/layout.tsx:7, style-evolution/page.tsx, library-utilization/page.tsx]

## Deferred from: code review of 3-6-dashboard-home, chunk 2/2 CSS/config (2026-08-05, final sub-chunk)

- **Several CSS selector blocks are reopened non-adjacently and silently override earlier declarations with no cross-reference** (`.dz-hero`, `.dz-shell` ×3, `.dz-card`, `.dz-right-pair`, and the "Compaction pass" section's `.cal-month-row`/`.cal-day`/`.mp-rows` re-declarations, which make the file's *first* margin/size/gap values on those selectors permanently dead). Renders correctly today, but a real maintainability hazard — an edit to the "obvious" first copy of any of these can be silently clobbered. Needs a dedicated CSS consolidation pass across `dashboard.css`, not a rushed point-fix. [web/app/dashboard.css]
- **Motion tokens are defined but routinely bypassed with hardcoded magic numbers** in `.mtl-*` and elsewhere (`0.8s`, `0.4s ease`, the same `cubic-bezier(0.4, 0, 0.2, 1)` copy-pasted 3+ times), contradicting the section's own comment that "all timings/easings are the ref's, via the liquid motion tokens." [web/app/dashboard.css]
- **[RESOLVED 2026-08-06]** Inconsistent focus-ring outer-glow radius: most controls use `6px`, but `.cal-tab`/`.cal-nav button`/`.cal-day`/`.mp-toggle button` used `5px`. All four aligned to the standard `6px` glow.
- **[RESOLVED 2026-08-06]** `.dz-row:focus-visible` reused the exact same soft glow as `:hover`. Now gets the standard `2px accent + 6px glow` outer ring used elsewhere in the file, distinct from hover's inset glow fill.
- **No z-index scale exists** — values are ad hoc across `dashboard.css` (2, 10, 20, 30, 40, 60, 99) with no documented relationship.
- **`.spot-goo`'s `filter: url(#dz-blob)` (SVG goo/blob filter) has no `@supports` guard or fallback**, unlike the file's `backdrop-filter` usages which consistently pair the `-webkit-` prefix (and, for `.dz-shell`, a `prefers-reduced-transparency` fallback). Known Safari rendering/perf risk, needs device testing.
- **`components.json` registers a third-party shadcn component registry** (`@react-bits` → `reactbits.dev`) — a new, unpinned external supply-chain trust boundary. Worth a conscious look before anyone runs `shadcn add @react-bits/...`.
- **[RESOLVED 2026-08-06]** `eslint.config.mjs` blanket-ignored the entire vendored `Silk.jsx`. The file's actual lint failure was a single rule (`react-hooks/immutability`, on the useMemo-cached-uniforms mutation) — scoped to an inline `eslint-disable`/`eslint-enable` pair around just that block; the file is back under normal lint coverage otherwise.
- **[RESOLVED 2026-08-06]** `.dz`'s desktop query (`min-width: 900.02px`) and mobile query (`max-width: 900px`) left a ~0.02px gap. Mobile query changed to `(width < 900.02px)` — the exact complement, matching the pattern already used in globals.css for the rail. Same fix applied to `set-detail.css`'s identical `max-width: 900px` (same bug, sibling file, found while fixing this one).
- **[RESOLVED 2026-08-06]** `.dz-hero-stats` had no `flex-wrap` while `.dz-hero` clips overflow. Added `flex-wrap: wrap` with a `row-gap`/`column-gap` split.
- **`forced-colors: active` mode**: `.cal-day`/`.cal-tab`/`.mp-toggle` selected/active states rely solely on background-color, which forced-colors suppresses; **`prefers-contrast: more`** has no boost path for the text-45/text-22 tokens. Niche a11y enhancements, not regressions.
- **[RESOLVED 2026-08-06]** Minor polish batch: `.dz-actions-date` now truncates with an ellipsis like its sibling fields; `.dz-shell:focus-within`'s glint no longer sits inside the `(hover: hover) and (pointer: fine)` gate (split into its own always-on rule, `:hover` stays gated) so tablet+keyboard users get it; `.cursor-chip`'s opacity+scale transition is now killed under `prefers-reduced-motion`. The other two sub-items in this batch were already resolved by drift since this entry was filed — `.cursor-chip-body` no longer carries a `max-width` at all (just `min-width: 120px`, nothing to clamp), and `.dz` already carries `max-width: var(--container-max)` (1100px).

## Deferred from: code review of 3-6-dashboard-home, chunk 2/2 UI components (2026-08-05)

- **[RULED 2026-08-06: drop it]** AC-3's passive NEW marker (unopened-set indicator) had silently vanished from the redesigned dashboard. Shipped in the original 2026-08-02 dev session (lavender border tint, pulsing dot, "NEW SET DETECTED" text, per-set "seen" state in localStorage); the dashboard-redesign pass wholesale-replaced the row rendering (`SetListPanel.tsx`) and the concept didn't come along. AC-3 and Task 9 in `3-6-dashboard-home.md` are now struck and marked DROPPED; no code change needed (there was nothing left to remove). [web/app/components/dashboard/SetListPanel.tsx, web/lib/sets/listModel.ts, _bmad-output/implementation-artifacts/3-6-dashboard-home.md]
- **[RESOLVED 2026-08-06]** `Greeting.tsx`'s `useSyncExternalStore` subscribe was a permanent no-op — the day-part greeting went stale across a 5am/12pm/6pm boundary on a long-lived tab. Subscribe now schedules a `setTimeout` to the next hour boundary and reschedules itself, so the day-part re-evaluates on its own without waiting for an unrelated re-render. [web/app/components/dashboard/Greeting.tsx]
- **`Silk.jsx`'s `hexToNormalizedRGB` has no format validation** — a malformed hex produces NaN color channels. Currently only fed by internal design tokens, not user input. [web/app/components/Silk.jsx:11-18]
- **Aggregate WebGL context budget isn't reconciled anywhere.** Silk's permanent `@react-three/fiber` canvas plus up to 2 simultaneous LiquidMetal shader instances (hero arrow + expanded-sheet "Enter Set" pill, when a sheet is open over the hero) — the "1-2 instances per page" rule is documented per-component but nothing sums the total against real device WebGL context ceilings (iOS Safari historically low). Needs device testing, not a code fix.
- **Incomplete ARIA tablist pattern across `GlassCalendar`/`MostPlayedCard`/`SpotlightSearch` chips.** `role="tab"`/`aria-selected` on plain buttons with no roving tabindex, arrow-key navigation, or `aria-controls`/tabpanel linkage. `GlassCalendar`'s day buttons also expose selection only via a CSS-only `data-selected` hook (no `aria-pressed`/`aria-current="date"`). Better tackled as one a11y pass across all three than three point-patches. [web/app/components/dashboard/GlassCalendar.tsx, MostPlayedCard.tsx, SpotlightSearch.tsx]
- **`RightColumn.tsx`'s `ResizeObserver` is attached to the scrolling container, which doesn't fire when children grow without the container's own box changing** (OdometerCard count-up, MostPlayedCard AnimatePresence height changes) — scroll-fade indicators (`data-fade-top`/`data-fade-bottom`) can go stale until the next manual scroll. [web/app/components/dashboard/RightColumn.tsx:29]
- **`SetListPanel.tsx`'s expanded sheet geometry is computed once at open-time (`getBoundingClientRect()`), with no resize/rotation handling.** A viewport resize or device rotation while the sheet is open can leave it visually misaligned with its anchor row. [web/app/components/dashboard/SetListPanel.tsx:80-94]
- **[RESOLVED 2026-08-06]** `CursorChip.tsx`'s cursor-follow rAF lerp wasn't gated by `prefers-reduced-motion`. A `matchMedia` ref now flips the lerp factor to `1` (instant snap, no smoothing) under reduced motion instead of the usual `0.15` easing. [web/app/components/ui/CursorChip.tsx]
- **[RESOLVED 2026-08-06]** `MetalButton.tsx` ripple position for keyboard-triggered clicks (Enter/Space) used `e.clientX`/`e.clientY`, browser-dependent for synthetic events. Now detects `e.detail === 0` (the standard keyboard-activation signal) and centers the ripple on the button instead. [web/app/components/dashboard/MetalButton.tsx]

## Deferred from: code review of 3-6-dashboard-home, chunk 1/2 (2026-08-05)

- **Camelot mapping's doc-comment claim ("verified 24/24 against real data, 20k+ rows") isn't backed by a committed script/artifact.** The mapping function itself is unit-tested with correct boundary cases (0, 23, -1, both A/B rings), so this is a reproducibility/documentation gap, not a correctness concern. [agent/src-tauri/src/joiner/serato4.rs:174-175]
- **Dancefloor density-floor tuning is a known interim gap, explicitly commented and superseded by Story 5.2/AR-13.** The density floor's structural bias toward short-track genres (fixed track-count threshold regardless of track length) is real but is the acknowledged reason Story 5.2 (per-DJ-calibrated floors) exists. [web/lib/sets/dancefloor.ts:23]
- **[RESOLVED 2026-08-06]** `MIN_PLAYS_FOR_DETECTION` (dancefloor.ts) and `HERO_MIN_TRACKS` (hero.ts) were two separately-hardcoded `6`s linked only by a comment. `dancefloor.ts` now exports the constant; `hero.ts` imports and re-exports it as `HERO_MIN_TRACKS` instead of redeclaring the literal. [web/lib/sets/dancefloor.ts, web/lib/sets/hero.ts]
- **Backfill sweep re-reads/re-derives all captured serato4 rows on every agent startup, with no "already fully backfilled" skip-flag.** Bounded by the self-terminating unchanged-comparison (only changed rows write) and a ~491-row ceiling, so real but not urgent. [agent/src-tauri/src/backfill.rs:137-213]
- **`FloatingNav.tsx`'s `:focus-visible`-dependent focus handling reintroduces a pattern that was the source of a Safari regression fixed in Story 3.9; `RAIL_QUERY = "900.02px"` is synced to `dashboard.css`'s 900px breakpoint only by comment, no shared token.** Carried to the deferred UI/styling review pass for this story (chunk 2/2), since FloatingNav gets full scrutiny there anyway. [web/app/components/nav/FloatingNav.tsx:20,180]
- **`export_real_fixtures.rs`'s `SETS` constant hardcodes real gig dates/play-counts as source comments.** Privacy/data-hygiene nitpick on an env-gated dev tool, not shipped user-facing behavior. [agent/src-tauri/tests/export_real_fixtures.rs]
- **`arcDirection`'s first-vs-last BPM delta can label a set with a dramatic mid-set arc as "steady."** Copy-quality nuance (the headline undersells a set's actual shape), not incorrect data. [web/lib/sets/describe.ts:61-67]

## Deferred from: code review of 3-5-floating-pill-nav (2026-08-05)

- **`(authenticated)` route group ships with no auth-gating.** Flagged in-code as a known, pre-existing gap and explicitly out of scope for Story 3.5 — this diff is what made `/dashboard`, `/settings`, `/style-evolution`, `/library-utilization` reachable, and by now (current main) these routes render real user data with no login check. **Arjun's ruling, code review 2026-08-05: keep deferring** — same call as when 3.5 shipped it, tracked as a pre-existing, app-wide gap, not any single story's scope to fix. [web/app/(authenticated)/layout.tsx]
- **No `padding-bottom` reserved under the fixed dock on Story 3.5's stub pages.** Already addressed per-page by the real pages that superseded these stubs — `dashboard-inner` (globals.css:731) reserves clearance and Story 3.10's settings.css:540 does too. [web/app/(authenticated)/*/page.tsx]
- **No `env(safe-area-inset-bottom)` handling on Story 3.5's stub pages.** Same resolution path as above — later real pages already handle it.
- **Icon-color bug's root cause (a legacy unlayered `a { color: inherit }` rule beating layered Tailwind utilities) is patched locally to `.floating-nav-link` only.** Future anchors elsewhere in the app remain exposed to the same silent-override bug; fixing it properly is an app-wide Tailwind-migration cleanup, out of this story's scope. [web/app/globals.css]
- **Zero component-level test coverage beyond the pure `isActiveNavItem` helper** — no coverage for divider placement, `aria-current`, or hover/focus behavior. Coverage gap, not a known defect. [web/app/components/nav/FloatingNav.test.ts]

## Deferred from: code review of 3-9-console-voice-failure-register-state-a11y-responsive-pass (2026-08-05)

- **`set_agent_status`'s upsert surfaces a raw Postgres FK-violation error rather than the function's own 42501/22023 error contract if the caller's `djs` row is deleted while their JWT is still valid.** A real but narrow race (account deletion while the desktop agent still holds a cached, unexpired token). Mirrors an already-accepted gap in the sibling `sync_set` RPC this migration deliberately copies — not a regression introduced by this diff's own design choices. The fire-and-forget agent path treats it identically to any other rejected/failed beat (silently retried next pass), so there's no material user-facing consequence either way. [supabase/migrations/20260805120000_create_agent_status.sql:110-114]

## Deferred from: code review of 3-7-set-detail-summary-tracklist backend group (2026-08-04)

- **`DateAddedIndex::live` reimplements its own `/Volumes` directory scan instead of reusing the existing `watcher::detect::SystemDisks`/`DiskSource` abstraction.** Real duplication — this codebase already has a testable, mockable disk-discovery seam (`DiskSource` trait + `SystemDisks`), and `date_added.rs` bypasses it with a raw `std::fs::read_dir("/Volumes")` walk that has no test coverage of its own. Deferred rather than patched because a real fix needs `DiskSource` (today scoped to "find a Serato install") extended to general catalogue discovery — an abstraction-design call, not a mechanical patch. [agent/src-tauri/src/joiner/date_added.rs:88-104]
- **Catalogue loading in `DateAddedIndex::live` is synchronous, on whatever thread runs the watch loop, on the tick a session completes.** The code's own comment acknowledges and deliberately scopes this ("catalogue loading only happens on the rare tick that captures"), and the same loop already does blocking SQLite/file I/O elsewhere, so this is very likely already off the main/UI thread — but this review could not confirm the loop's thread placement from the diff alone. Worth a quick confirmation, not a blocking concern. [agent/src-tauri/src/watcher/mod.rs:488-491]
- **No backfill-level test covers the played-flag filter's interaction with the pre-existing "re-derive already-captured serato4 sets" sweep.** Both new backfill tests target the date-carry-forward guard specifically; nothing exercises a pre-3.7 row (containing un-filtered preview rows) going through a full re-derive → filter → renumber → `mark_for_resync` cycle. Coverage gap, not a known defect. [agent/src-tauri/src/backfill.rs]

## Surfaced by: 3-7-set-detail-summary-tracklist dev-story session (2026-08-03)

- **[RESOLVED 2026-08-07 by code review of 4-6-web-read-path-supabase-swap]** The suppress table this entry specified now exists: `20260807130000_add_deleted_sets_tombstone.sql` adds `public.deleted_sets`, keyed `(dj_id, set_id)` — which *is* the "stable session identity" this entry asked for, since `sets.id = uuid_generate_v5(dj_id, session_identity)` — written by an `after delete on public.sets` trigger (atomic with the delete, covers every delete path, needs no client grant) and checked by `sync_set` as its first act, before any insert. The suppressed re-sync still returns the deterministic set id so the agent marks the session synced and stops retrying forever instead of error-looping over a set the DJ deleted. Found live during Story 4.6's review, not assumed: the resurrection was reproduced end-to-end against a local stack (sync → delete → re-sync restored the set *and* its plays under the identical id), and `supabase/tests/deleted_sets_tombstone_test.sql` now pins the fix, including that suppression is per-DJ. Original entry follows. **Permanent delete tombstone / suppress-id — owed by the Supabase sync/read-path story (spec §3e, AC-33).** Set Detail's delete is never-recoverable by product decision (Arjun 2026-08-03), but in 3.7 (fixture-backed, pre-cloud-read) it only removes the row via the `deleteSet` seam. The agent retains raw and re-derives on every startup sweep, so once the cloud read path lands, a naive row-delete WOULD resurrect the set on the next sync. The sync/read-path story must add a permanent tombstone/suppress-id **keyed on stable session identity** (`session_identity`/`set_id`) that blocks re-ingest forever — server-side (a `deleted_sets` suppress table the `sync_set` RPC checks) or equivalently effective. Until then, delete is honest only against the fixture. [web/lib/sets/index.ts — `deleteSet`; supabase sync_set RPC]
- **[RESOLVED 2026-08-06]** Dashboard carry-back (3.6, spec §3g): exclude low-confidence/no-dancefloor sets from the dashboard — visibly, not silently. **Ruled (Arjun, 2026-08-06): yes, apply Style Evolution's pattern.** `SetRowModel.isLowConfidence` (`listModel.ts`) is true when either `derived.confidence.value < 1.0` (FR-27 signal) or track count is below `HERO_MIN_TRACKS` (the same bar `hero.ts` already uses to keep a soundcheck out of the hero slot) — the compound condition is needed because the concrete example named in this entry (the 17577 soundcheck fixture) has `confidence.value === 1` and is only caught by the track-count half. `SetListPanel.tsx` hides these by default, shows a "N low-confidence sessions hidden — show them" note (no persistence — resets on reload, matching Style Evolution's spec), and reveals-and-keeps-searchable once shown. The calendar cross-link (`SELECT_DAY_EVENT`) auto-reveals if a direct day-jump targets a hidden set, so direct access still works. Browser-verified against the real fixture (2 hidden, "show them" reveals both). [web/lib/sets/hero.ts, web/lib/sets/listModel.ts, web/app/components/dashboard/SetListPanel.tsx, web/app/dashboard.css] **Addendum (Story 4.1, 2026-08-06):** this entry cited "Style Evolution's spec" before Style Evolution existed; the pattern is now actually shipped there too — `LowConfidenceReveal.tsx` + `.se-hidden-note`/`.se-hidden-toggle` (`web/app/style-evolution.css`) are a second, real reference implementation of the identical "N hidden — show them, resets on reload" affordance, for whenever this dashboard entry is picked up. Still not closing this entry — the dashboard fix already shipped and is separately resolved above; this is bookkeeping only.
- **[RULED 2026-08-06: leave it, close the entry]** `LiquidMetalButton` lost its only in-product usage when 3.7 replaced the `/set/[id]` stub (3.6 AC-14's demo placement). The component (+ its `metal-hooks.ts` runtime-token plumbing and globals.css styles) stays in the tree, exercised by nothing user-facing; its real hero placements — login "Initialize Session", subscribe/paywall, marketing hero — belong to their own stories. No placement on Set Detail is natural (the screen's actions are the calm [⋯] overflow and stat drill-ins). Flagged so the displacement is a recorded decision, not silent rot. **Settings considered and DECLINED (2026-08-06, Story 3.10, D-17):** the Profile/Settings screen is not the demo home; the component stays owned by the login/marketing hero stories. *(Amended 2026-08-05 code review: the original rationale said "no WebGL/shader material anywhere on it," which the authorized on-theme redesign has since superseded — Settings now mounts the shared `SilkBackdrop`. The decline still stands, on the narrower true ground: Settings' WebGL is the app-wide ambient backdrop, not a hero-material showcase, and a settings page remains the wrong place to keep `LiquidMetalButton` alive.)* **Reconfirmed (Arjun, 2026-08-06):** stays owned by the future login/marketing-hero stories, no interim demo home, no deletion. Closing this entry — the decision is made; it isn't rot, it's a component waiting on the story that needs it. [web/app/components/ui/liquid-metal-button.tsx]
- ~~**Story 3.8 hooks left ready, deliberately unbuilt (Task 11.2)**~~ **CLOSED — landed in Story 3.8 (2026-08-04):** the arc (and the new key-strip) click-to-jump reuses DR-2 `setFocus` exactly as hooked (point → nearest play position → focus, no second focus path); the Camelot **wheel** was CUT by design ruling D-1 (own future story) and its companion role went to the **key timeline strip**; the same `DetailArc` upgraded in place to full annotated + chart-summary mode — no fork. [web/app/components/set-detail/DetailArc.tsx, model.ts]

## Surfaced by: 3-4-format-drift-resilience-backfill dev-story session (2026-08-01)

- **The cloud sync payload never actually carries `agent_version`, despite AD-3 requiring it.** `sync.rs`'s `SyncSetRequest` (`sync.rs:57-64`) has no `agent_version` field, and no migration ever added the column to `public.sets`/`public.sessions` — a Story 3.1/3.2-era gap, not introduced by this story. AD-3 states this as a hard requirement of the sync contract itself ("the cloud must accept the last N `agent_version`s"), but that requirement is only meaningful once a heterogeneous agent fleet actually exists — with exactly one agent version ever shipped, there is nothing yet for it to protect against. This story's own `agent_version` tagging (Sentry event tags + the new `parse_failures.failed_agent_version` column) is local-only and does not require the wire contract to change, so it was scoped out here rather than folded in. **Arjun's ruling (2026-08-01, dev-story session): defer.** Real follow-up scope when picked up: touches `shared/`'s frozen-additive sync-payload schema + `sync.rs` + a new Supabase migration together — the same care Story 1.10 gave the schema originally, not a one-line fix. [agent/src-tauri/src/sync.rs:57-64, shared/schema/sync-payload.schema.json]
- **The hot-retry storm on a terminal capture failure is unthrottled by this story.** `record_parse_failure`'s `ON CONFLICT` overwrite semantics naturally cap the *ledger* at one row per session (updated repeatedly, not accumulated), but the caller (`recheck_pending_serato4`/`recheck_legacy_quiet_periods`) still leaves a permanently-failing session in its in-memory pending tracker, so `capture_and_store_serato4`/`_legacy` — and therefore `ErrorReporter::report` — re-fires on every subsequent poll tick, forever, until a fix ships. Pre-existing behavior (Story 3.3/3.3b's territory), deliberately left unchanged by this story per its own Dev Notes ruling — flagged here for visibility rather than silently accepted a second time. [agent/src-tauri/src/watcher/mod.rs — `capture_and_store_serato4`/`_legacy` terminal `Err` arms]

## Deferred from: code review of 3-3-offline-sync-queue (2026-08-01)

- **[RESOLVED 2026-08-06]** `has_retryable_backlog` silently treated a DB read error as "nothing pending" via `unwrap_or(false)`, hiding the underlying store error. Still fails open to `false` (tray-state behavior unchanged — a double-failure path stays low-likelihood and this isn't the place to redesign fail-open/closed semantics), but the error is no longer swallowed: an `eprintln!` now logs it, matching this file's existing error-visibility convention. [agent/src-tauri/src/sync_queue.rs]
- **No test exercises `sync_loop`'s pass-level `Err` branch end-to-end.** `handle_pass_outcome` (the `Ok` path) is unit tested, but the combination of a pass-level failure, `has_retryable_backlog`, backoff increase, and the tray-precedence write together (lines ~724-741) has zero test coverage. Coverage gap, not a code defect. [agent/src-tauri/src/sync_queue.rs:724]

## Surfaced by: 3-3-offline-sync-queue manual verification (2026-07-31)

- **[RESOLVED 2026-08-01, Story 3.3b]** **Serato 4 play history is not in the `_Serato_` folder the DJ points at.** Serato 4 writes all play history to the internal `~/Library/Application Support/Serato/Library/master.sqlite` (`history_session` table), independent of where the DJ's library/tracks live — so a DJ pointing at a USB `_Serato_` folder (especially a migrated install with a legacy `database V2` present) is classified **legacy**, watches a non-existent `<_Serato_>/History/Sessions/` path, and **captures nothing with no error surfaced**. The real root cause is the **override precedence**: `resolve_startup` honors a saved override unconditionally and *skips detection entirely* (`mod.rs:80-82`), so the override wins over the OS-default `detect_os_default(home)` → `classify(home)` that *already* resolves the internal path correctly when no override is set. The override mechanism, meant to help, is what breaks Serato 4 capture. **Decided design (design discussion w/ Arjun 2026-07-31 — SUPERSEDES the "redirect to a single source + new never-silent tray state" proposal in the write-up doc): Story 3.3b — watch BOTH sources, dedup at capture.** Rather than resolving to one source and betting we picked right, watch the Serato 4 internal `master.sqlite` and the legacy `History/Sessions` concurrently whenever both catalogues are present — silent-miss becomes *structurally* impossible. Add a capture-time **"Serato 4 wins" dedup guard**: the two formats produce deliberately non-colliding identities (`serato4:{id}` vs `legacy:{fnv1a(path+start_time)}`, asserted never-collide at `capture.rs:473`), so a night landing in both would otherwise surface as **two** dashboard sets — Story 3.2's idempotency key does NOT dedup them. Also in 3.3b: internal `master.sqlite` is always a watched source when a Serato 4 install is present regardless of a library-folder override (fixes the override precedence bug **and** the `open_read_only(root, db_path)` containment coupling the redirect exposes — when history redirects to internal, `root` must swap to the internal container or the read-only open refuses it); Save-time validation rejects a no-history folder synchronously in the confirm UI ("No Serato library found here — point me at your `_Serato_` folder") instead of a silent green. **No new tray state** — runtime staleness stays owned by the existing `DriveNotConnected` (unplug) and Story 3.4 (post-setup format/layout drift); a new state would duplicate one or steal the other's job. [agent/src-tauri/src/watcher/detect.rs:14-90, agent/src-tauri/src/watcher/mod.rs:80-82,730-747, agent/src-tauri/src/capture.rs:92-147]
  - **Open Q (determines whether the dedup guard is load-bearing or belt-and-suspenders):** does Serato 4 ever write a NEW `.session` file for a set it *also* records in `master.sqlite`? One data point (Arjun's machine, 2026-07-31: test set landed in `master.sqlite` only, USB legacy `History/` stayed empty) says **no**; a second migrated install would settle it.
  - **Open Q:** Windows Serato 4 internal history path is unknown/unconfirmed (`SERATO4_HOME_RELPATH` is macOS-only, `detect.rs:14-19`).
- **Verified-setup / onboarding polish — SEPARATE follow-on story, deliberately deferred (Arjun 2026-07-31: "onboarding polish can be done later as long as we know to do it").** First-run onboarding should end in a **live test capture** — DJ plays/ends a ~30s set and watches it land green, or the agent surfaces a count of already-visible historical sets — proving capture works on that exact machine rather than assuming it. This is the **only** thing that closes the residual false-green Save-time validation can't catch (path validates, but new plays land somewhere we're not watching — e.g. a hypothetical Serato 4 external-history mode). Optional gentle "on Serato 4? you're all set" copy, **never a version gate** (a real population runs older/legacy Serato deliberately, and DJs resist updating a working rig; "on latest" also doesn't collapse the migration case — the old files persist on disk regardless of version). Held out of Story 3.3b to keep the agent-side correctness core shippable fast. Spans the confirm/first-run flow (Story 2.6 lineage) + possibly web onboarding. [agent/src-tauri/src/watcher/mod.rs — confirm/first-run flow]
- **Interim unblock for Story 3.3 manual verification (test setup, no code):** repoint the agent's Serato path override to the internal file `/Users/<home>/Library/Application Support/Serato/Library/master.sqlite`, then `supabase stop` → capture a set → tray shows `Queued` → `supabase start` → drains to `Idle`. Lets 3.3's manual end-to-end complete now, ahead of Story 3.3b landing. Full investigation + design discussion in `serato4-history-location-detection-gap-2026-07-31.md`. **[RESOLVED 2026-08-01, Story 3.3b] Superseded — real fix shipped, this interim workaround is no longer needed.** resolution: `sprint-status.yaml` confirms `3-3b-version-agnostic-history-capture: done`; `detect.rs`'s `resolve_watch_plan` now unconditionally probes the internal `master.sqlite` regardless of override (the root-cause fix the entry above called for), and `capture.rs` implements the "Serato 4 wins" dedup guard. Both Open Qs above remain genuinely open (Windows path, and the coincidental-double-write question) but are no longer blocking — 3.3b's fail-open design tolerates either answer.

## Deferred from: code review of 2-11-account-deletion-data-export-manual-runbook (2026-07-30)

- **`pre-launch-services-checklist.md` §3 row miscategorized.** The new "Account deletion + export runbook forward-hooks" row is filed under §3, a section otherwise reserved for external account/credential provisioning gates (Apple certs, Azure keys, OAuth apps, signing keys). This row is actually a documentation-debt tracker (unfilled forward-hook TODOs), not an external provisioning blocker — filing it there muddies what §3 means. Pre-existing taxonomy issue in the checklist, not a defect of this diff's content. [_bmad-output/implementation-artifacts/pre-launch-services-checklist.md:34]
- **"Party 2026-07-20" attributions lack a linked source artifact.** The story's "Resolved before this story was written" section cites several rulings ("Paige's catch," "Winston / Mary," a CCPA-scope ruling) with no `[Source: ...]` citation, unlike every other claim in the Dev Notes. This content predates this diff (untouched by it) — flagging for whoever next touches this section to add a citation if the source transcript/decision record is findable. [_bmad-output/implementation-artifacts/2-11-account-deletion-data-export-manual-runbook.md:19-35]

## Surfaced by: 2-11-account-deletion-data-export-manual-runbook (2026-07-30)

- **The agent has no way to learn its account was deleted, so there is no automatic local-SQLite purge on account deletion.** No sync/auth-check path exists today (Story 3.2/3.3, backlog) and Story 2.10's token-refresh work doesn't add a "was I deleted" check either — deleting a DJ's `auth.users` row cascades `public.djs` cleanly, but a still-running agent on the DJ's machine keeps its local `local.sqlite` (captured session history) indefinitely with no way to know the account is gone. `supabase/ACCOUNT-DELETION-EXPORT-RUNBOOK.md` documents the local purge as a manual instruction relayed to the DJ (quit the agent, delete the file) as an interim measure. Owned by whichever future story builds the sync/auth-check path — likely Story 3.2/3.3 territory. [agent/src-tauri/src/store.rs, supabase/ACCOUNT-DELETION-EXPORT-RUNBOOK.md]

## Deferred from: code review of 2-10-agent-secure-token-storage (2026-07-30)

- **Refresh token travels through a URL and OS process argv.** `web/app/link-agent/link-handoff.tsx`'s `window.location.href = curfew-agent://link?access_token=...&refresh_token=...` carries both tokens; on Windows/Linux this becomes a literal argv entry to the launched/forwarded process (`agent/src-tauri/src/lib.rs`'s `forward_deep_link_from_argv`), visible to other local processes via `ps`/Task Manager. Inherent to the custom-URI-scheme handoff mechanism Arjun already selected specifically to avoid new backend surface (per this story's own "Resolved before this story was written" section), not a defect introduced by this diff's code. [web/app/link-agent/link-handoff.tsx:37, agent/src-tauri/src/lib.rs:152-160]
- **`curfew-agent://` scheme has no protection against another app squatting the same scheme first.** Well-documented risk category of custom-URI-scheme OAuth handoffs (particularly on Windows, where registration can be last-write-wins). Same class as the item above — inherent to the mechanism already chosen over a pairing-code/device-code flow, not fixable without changing the fundamental mechanism. [agent/src-tauri/tauri.conf.json:52-56]
- **Windows/Linux single-instance-argv-forwarding path has zero verification.** `forward_deep_link_from_argv` (the entire reason `tauri-plugin-single-instance` had to be paired with `tauri-plugin-deep-link`, per Task 3's own stated gotcha) was only manually walked through on macOS this session. Windows is a real, already-shipping release target (Story 2.9b). Already disclosed honestly in the dev-story session log; needs a real Windows verification pass before this is fully trusted, mirroring this project's own precedent of filing "Windows paths unverified" items (see Story 2.6's review below). [agent/src-tauri/src/lib.rs:152-160]
- **`web/app/link-agent/page.tsx`'s unguarded `getUser()` call.** No try/catch around `await supabase.auth.getUser()` (a network hiccup surfaces as a raw 500 instead of a graceful `/login` redirect), but this is a verbatim copy of the identical unguarded pattern already shipped and done-reviewed in `web/app/phone-required/page.tsx:14`. Fixing it only here would be inconsistent with the established project convention; if worth hardening, should be done for both call sites together as a separate cross-cutting fix. [web/app/link-agent/page.tsx:12]

## Deferred from: code review of 2-9a-macos-signed-build-notarization (2026-07-29)

- **[RESOLVED 2026-07-30, Story 2.9c]** App version stays hardcoded `0.0.0` across `tauri.conf.json`/`Cargo.toml`/`package.json` with no workflow step syncing it to the pushed `agent-v*.*.*` tag. Every signed release will embed the same internal version regardless of the git tag used to trigger it. Out of this story's AC scope (no AC mentions version stamping); will matter once Story 2.9c's auto-updater needs a real version to compare against. Resolved via a new "Sync app version from git tag" step added to both `release-macos.yml` and `release-windows.yml`, run before their `tauri-action` step — derives the version from `GITHUB_REF_NAME` and writes it into all three files, ephemeral-checkout-only, never committed back. [agent/src-tauri/tauri.conf.json, agent/src-tauri/Cargo.toml, agent/package.json]
- **No Rust build caching (`Swatinem/rust-cache`) in the new `release-macos.yml` workflow**, unlike `ci.yml` which already uses it. Cost/speed nit only — this workflow runs rarely (tag-triggered releases, not every push), so the from-scratch two-architecture compile cost is tolerable for now. [.github/workflows/release-macos.yml]

## Deferred from: code review of story-2-8-set-capture-into-local-sqlite (2026-07-29)

- **`recheck_pending_serato4` opens a fresh read-only connection to the live `master.sqlite` every ~5s poll tick regardless of whether `pending` is empty** (the emptiness check happens only inside the filter, after the connection is already opened) — compounds the "new connection per tick" item already deferred from Story 2.6's review below (`check_for_new_sessions`'s own per-call connection). Inefficiency, not a correctness issue. [agent/src-tauri/src/watcher/mod.rs:440-448]
- **`store::open_at` sets no SQLite pragmas (`busy_timeout`, `journal_mode=WAL`).** Anticipates Story 3.2's eventual second connection to the same `local.sqlite` file (a sync-queue reader alongside this loop's writer) colliding with an immediate `SQLITE_BUSY` rather than a brief wait-and-retry. Not needed while this story's single long-lived connection is the only reader/writer. [agent/src-tauri/src/store.rs:210-216]
- **`recheck_pending_serato4`'s filter treats `session_by_id` returning `Ok(None)` (row externally deleted from `history_session`) identically to "not yet resolved."** Such an id is retried forever every poll tick with no terminal outcome. No AC covers a session's `history_session` row disappearing mid-watch; speculative, no evidence Serato does this. [agent/src-tauri/src/watcher/mod.rs:450-459]
- **A filesystem `Remove` event on a tracked legacy `.session` path is routed through `handle_legacy_session_event` identically to a `Modify` event**, refreshing its quiet-period clock instead of dropping it from tracking — a deleted session's completion is delayed by a full quiet period rather than abandoned immediately. Unlikely DJ workflow (mid-set file deletion); no AC covers it. [agent/src-tauri/src/watcher/mod.rs:342-370]

## Deferred from: code review of story-2-7-local-only-raw-data-boundary (2026-07-29)

- **`watch_loop`'s watermark is not durable across a process restart.** `current_path` starts `None` on every boot, so the very first override read is treated as a "path change" and resets `watermark = 0`, re-surfacing the entire session history as "new" once the agent restarts. Pre-existing behavior, not introduced by this story's diff — surfaced during Story 2.7's code review while reading the new reconnect-vs-path-change comments, which otherwise read as implying watermark durability. [agent/src-tauri/src/watcher/mod.rs:176]

## Deferred from: code review of 2-6-serato-folder-auto-detection-first-run-confirm (2026-07-29)

- **Legacy `.session` watch path produces no discoverable "new session" signal at all.** For Serato 4+, `check_for_new_sessions`/`log_new_session` gives a concrete, structured event; for legacy, the code comment admits "nothing to do here beyond having proven the watcher fires." Pre-existing scope boundary this story documents explicitly — Story 2.8 owns real capture semantics for both formats, not this story. [agent/src-tauri/src/watcher/mod.rs:216-219]
- **`watch_loop`'s connect/disconnect/reconnect/watermark state machine has no automated test coverage.** Every new test in `watcher/mod.rs` exercises only the pure helper functions (`resolve_startup`, `pending_after_override_check`) — the live loop itself is untested. Real risk: this exact function is where this story's own manual walkthrough already found one wiring bug (Save not wired into the watch loop) that no unit test caught. [agent/src-tauri/src/watcher/mod.rs:154]
- **`end_time: Some(-1)` sentinel inconsistency vs. `non_empty`'s None-normalization.** Story 1.3b's Dev Agent Record confirms `-1` is the real "no end time yet" sentinel; `list_sessions_after` normalizes `name` (empty string → `None`) but does no equivalent translation for `end_time`. No current consumer treats `-1` as meaningful — becomes Story 2.8's concern once it consumes `SessionSummary` for real. [agent/src-tauri/src/parser/serato4.rs:116] **[CLOSED 2026-07-29 by Story 2.8:** `capture::serato4_end_time_resolved(end_time: Option<i64>) -> bool` treats any `Some(t) if t >= 0` as resolved and `Some(-1)`/`None` as unresolved — the real consumer this entry anticipated. `list_sessions_after`'s own `end_time` column is left untranslated deliberately; the sentinel interpretation lives at the one call site that needs it.]
- **New SQLite connection opened on every poll tick / notify event instead of held for the loop's lifetime.** `check_for_new_sessions` calls `open_read_only` fresh each time; during an active DJ set with WAL-mode temp-file churn firing multiple `notify` events, this could mean frequent connection open/close cycles. Inefficiency, not a correctness issue. [agent/src-tauri/src/watcher/mod.rs:260-263]
- **No path normalization on override-change comparison.** `override_path != current_path` is a raw string/PathBuf comparison — trailing slash, case (case-insensitive filesystems), or symlink-vs-resolved differences would be treated as a genuine path change, needlessly tearing down and rebuilding the watcher/tray state. Cosmetic churn, not a correctness bug. [agent/src-tauri/src/watcher/mod.rs:170]
- **Windows paths remain entirely unverified.** No Windows dev/CI environment exists to confirm `%USERPROFILE%\Music\_Serato_\` or `sysinfo`'s removable-volume detection on Windows — already openly flagged in the story's own Dev Notes/Completion Notes, restated here for tracking continuity (same class of gap as Story 2.5's tray parity). [agent/src-tauri/src/watcher/detect.rs]
- **`Cargo.lock` now carries two versions of the `windows` crate (0.61.3 and 0.62.2)** as a side effect of adding the `sysinfo` dependency — dependency-graph duplication, not a functional defect. [agent/src-tauri/Cargo.lock]

## Deferred from: code review of 2-5-agent-shell-tray-ui (2026-07-29)

- **`is_visible().unwrap_or(false)` swallows errors in the tray click handler.** Explicitly flagged as a known carried-over issue since Story 1.1's original review round (see the 2026-07-21 entry below); this story's diff heavily edits the surrounding `on_tray_icon_event` closure but doesn't touch this line. Still low likelihood/low impact — a platform visibility-query error is treated as "not visible," which could re-show/refocus an already-visible window with no diagnostic. [agent/src-tauri/src/lib.rs:176]
- **Concurrent Save-button clicks race on the settings file.** `set_serato_path_override` does an unsynchronized read-modify-write (`load()` then `save()`) with no in-flight guard — two rapid clicks (or two IPC calls in flight) can interleave. Largely mitigated once the atomic-write patch (temp-file + rename) from this same review round lands; residual risk is only a benign "last click wins," not real corruption. Revisit only if a real double-submit report surfaces. [agent/src-tauri/src/settings.rs:87-95]
- **HiDPI-aware tray icon lookup was never implemented.** `@2x` retina variants of all four tray icon states were added to the repo (and originally listed in this story's own File List) but `TrayState::icon()` has no runtime scale-factor lookup, so Retina displays render the upscaled `@1x` bitmap. Per Arjun's review-time decision, the unused `@2x` PNGs were deleted this round rather than left as dead code; proper HiDPI selection needs its own investigation (Tauri has no obvious one-line API for scale-aware tray icons) and is left for a future polish pass. [agent/src-tauri/src/tray.rs — `TrayState::icon`]

## Deferred from: code review of 2-4-auth-ui-components (2026-07-29)

- **[RESOLVED 2026-08-06, Story 3.10 — by relocation (D-7), superseding the 2026-07-30 ruling]** The passkey affordance's durable home is now the Settings providers row: "Add a passkey" (reusing `registerPasskey()`, hidden on non-WebAuthn browsers) lives on the screen every DJ can reach at any time, instead of chasing one-time "just signed in, no passkey yet" state through two server-side redirect flows — disproportionate plumbing for a nudge, per the 3.10 design session. The post-signin nudge itself remains email-path-only, accepted. Original entry, for the record: OAuth sign-ins never reach the post-signin "Enable Passkey" nudge. `EnablePasskeyPrompt` is gated on client-side React state that only the email-path's `useActionState` sets; OAuth's `/auth/callback` (and email-confirmation's `/auth/confirm`) both do a full server-side redirect straight to `/` on success, never back through `/login`'s state. Pre-existing since Story 2.3b (FR-29 scoped the nudge to the email path only) — not a Story 2.4 regression — but Story 2.4's AC-5 made OAuth the *prominent, default* sign-in path, so the population that never sees the passkey nudge is now much larger. **Arjun's ruling: extend the nudge to OAuth too** — route `/auth/callback` and `/auth/confirm` back through the same post-signin state so OAuth users see it as well. Not implemented this session (real feature work — two server-side redirect flows plus client state need to carry the "just signed in, no passkey yet" signal through, not a prep-task-sized fix). Owner: unassigned, next story that touches `/login`'s auth flow (candidate: whenever Story 3.10's Profile/Settings work next touches account/auth surfaces) should pick this up. [web/app/login/page.tsx, web/app/auth/callback/route.ts, web/app/auth/confirm/route.ts, story 2-4-auth-ui-components.md]
- **`rp_id` is pinned to the throwaway `name-pending-gamma.vercel.app` host.** WebAuthn credentials bind to the RP ID at registration. When the real `curfew.vip` custom domain goes live (already purchased, explicitly deferred by this story's own Scope Boundaries), every passkey registered against the `.vercel.app` host will stop working and users will have to silently re-register. Not actionable now — the custom domain is its own, larger checklist item — but should be closed out (or at minimum re-verified) as part of that future DNS-wiring work. [supabase Dashboard `[auth.webauthn]` rp_id/rp_origins config; pre-launch-services-checklist.md §4; story 2-4-auth-ui-components.md]

## Deferred from: 3rd code review of 2-3b-oauth-paths-account-linking-google-apple (2026-07-28)

- **`web/app/auth/callback/route.ts`: if `exchangeCodeForSession` succeeds but `data.user` is falsy, `needsPhone()` never runs and the user is redirected to `/` instead of `/phone-required`, skipping Story 2.3c's phone gate.** Not this story's diff to fix — this branch was introduced by Story 2.3c (already reviewed and closed as `done`), and `data.user` being falsy after a successful `exchangeCodeForSession` is not documented Supabase behavior (very low likelihood in practice). [web/app/auth/callback/route.ts:19-28]
- **Google's OAuth button has no client-side availability gate analogous to Apple's `appleSignInAvailable`.** A fresh clone that runs `supabase start` without first creating `supabase/.env` will have Google `enabled = true` in `config.toml` with an unresolved secret, reproducing the same "raw GoTrue error instead of calm copy" failure mode that was worth gating for Apple. Local dev-setup-only rough edge, already covered by `web/README.md`'s setup instructions — not a production risk. [web/app/login/page.tsx]
- **[RESOLVED 2026-07-28, Story 2.4]** Disabled "Sign in with Apple" button's only explanation was a `title` attribute — inconsistently exposed by screen readers, unavailable on touch. `AppleSignInButton.tsx` now sets `aria-disabled` plus folds the reason into the button's own visible/accessible label text ("Sign in with Apple (coming soon)") — confirmed via the browser's accessibility tree. [web/app/components/auth/AppleSignInButton.tsx]

## Deferred from: 2nd code review of 2-3b-oauth-paths-account-linking-google-apple (2026-07-28)

- **[RESOLVED 2026-07-28, Story 2.4]** Passkey sign-in is broken against the prod Supabase project. Root cause: Story 2.3a's `[auth.passkey]`/`[auth.webauthn]` config existed only in local `supabase/config.toml` (`rp_id = "localhost"`), never provisioned on prod. Resolved via Story 2.4's Task 3: prod Supabase Dashboard now has passkey/WebAuthn enabled with `rp_id`/`rp_origins` set to the real deploy (`name-pending-gamma.vercel.app`, from Task 3.1's new Vercel deploy). Human-verified end-to-end by Arjun — real register + real sign-in, both confirmed working against prod. See `pre-launch-services-checklist.md` §4 and the story file's Dev Agent Record. [supabase Dashboard `[auth.passkey]`/`[auth.webauthn]`, prod Supabase Dashboard, story 2-4-auth-ui-components.md]

## Deferred from: code review of 2-3d-production-email-delivery (2026-07-28)

- **[RESOLVED 2026-07-28] Free-tier prod Supabase project auto-pauses after 7 days idle, with no monitoring/keep-alive plan.** `supabase/PROVISIONING.md` and `pre-launch-services-checklist.md` §3 both document the pause risk as a known fact, but this diff put live production auth/email on that same free-tier project without adding any monitoring, alerting, or keep-alive mechanism. Arjun's ruling: add a keep-alive ping rather than upgrade tier pre-launch. Resolved via `.github/workflows/supabase-keepalive.yml`, a new scheduled Action (every 3 days, well inside the 7-day pause window) hitting the prod project's `/auth/v1/health` endpoint. Requires a new `SUPABASE_PROD_PUBLISHABLE_KEY` repo secret (prod project's anon/publishable key) to be added in GitHub repo settings — not yet done, Arjun's own action, tracked in `pre-launch-services-checklist.md` §3. [supabase/PROVISIONING.md, pre-launch-services-checklist.md §3, .github/workflows/supabase-keepalive.yml]
- **[RESOLVED — already done, entry was stale]** Domain purchase (`curfew.vip`) renewal/expiration status. Confirmed by Arjun during this review: renewal date is 2027-07-27, $16/yr via Cloudflare. Checked 2026-08-06: this was already written back into `pre-launch-services-checklist.md`'s domain row (dated 2026-07-28, same day as this entry) — the ledger entry just never got closed. (Note: this is the domain registrar's renewal, unrelated to product billing — Curfew itself has no free-trial tier, subscription-only.) [pre-launch-services-checklist.md:16]

## Deferred from: code review of 2-3c-phone-on-file-post-signup-prompt (2026-07-27)

- **[RESOLVED 2026-08-06, Story 3.10 — the ruled middleware gate shipped (D-9, epics.md ⚑ RULED 2026-08-05).]** `updateSession` (web/lib/supabase/middleware.ts) now runs a cookie-marked lazy phone check on the `(authenticated)` routes + `/link-agent`: no `curfew_phone_on_file` cookie → one `djs.phone` read (via `needsPhone`, fail-open) → non-null sets the session cookie, null redirects to `/phone-required`. One DB read per session, not per request; the spoofable cookie is acceptable because AR-10 is a contactability invariant, not a security boundary (JWT-claim auth hook noted as the airtight upgrade path). This closes the bypass paths named below — plain `signIn()`, passkey sign-in, and abandon-`/phone-required`-and-return. Original entry: `web/app/login/actions.ts`'s `signIn()` always returns `"signed-in"` with no phone check. This is the story's own documented "accepted residual limitation" for a returning DJ (its Scope resolution section explicitly waives re-gating on plain `signIn`). Separately, `signUp()`'s `data.session === null ? "check-email" : "signed-in"` branch would also skip the phone gate entirely if `enable_confirmations` were ever turned off in `supabase/config.toml` — dead under today's config (confirmations are on), not an active bug, but the same class of gap and wasn't explicitly named in the story's Scope boundaries. Not actionable within this story per its own "do not add a third enforcement layer speculatively" ruling. **Enforcement owner assigned 2026-07-28: Story 3.10 (Profile/Settings)** — the invariant's strength ("prompted once, best-effort" vs. AR-10/FR-29's literal "guaranteed on file") needs a conscious ruling rather than the current accident; pinned as a Decision-needed note in `epics.md` Story 3.10, which already manages phone-on-file and is the natural home for hard enforcement (required/non-clearable field + a middleware or `phone_on_file`-JWT-claim gate). Awaiting Arjun's confirm on best-effort-through-Epic-2 vs. a middleware gate now. [web/app/login/actions.ts:49-53,69, epics.md Story 3.10]
- **AD-19's column-scoped-grant requirement is enforced only by a comment in the new migration file, not structurally.** This story's migration correctly satisfies it today (`grant update (phone)`, not a blanket grant), but nothing in the schema or CI stops a future Epic 7 migration from re-introducing a blanket `grant update on public.djs`. Worth a schema-level guard (or a CI check alongside the existing additive-only migration check) whenever Epic 7's billing columns actually land — not this story's to fix. [supabase/migrations/20260727192439_add_djs_phone_column.sql]

## Deferred from: code review of 2-3b-oauth-paths-account-linking-google-apple (2026-07-27)

- **Callback route's `createClient()` call sits outside its own try/catch.** If required Supabase env vars are ever unset at runtime, this throws an unhandled exception instead of degrading to the calm failure redirect. Pre-existing: `confirm/route.ts` (2.3a, done) has the exact same gap, and this diff faithfully mirrors that established pattern per the story's own instruction to match `confirm/route.ts`'s exact shape. Not introduced by this diff; the fix belongs to both routes at once. [web/app/auth/callback/route.ts:21, web/app/auth/confirm/route.ts:21]
- **A reused/expired OAuth `code` (e.g. browser back-button after a successful sign-in) fails `exchangeCodeForSession` and redirects an already-signed-in user to `/login?error=confirmation-failed`, despite their session cookie from the earlier successful exchange still being valid.** A new trigger path for the existing "visiting `/login` while already signed in re-shows the form instead of redirecting away" entry below — not a new class of bug. [web/app/auth/callback/route.ts:19-28]
- **[RESOLVED 2026-07-28, Story 2.4]** `oauthError` wasn't cleared when the user toggled Login/Signup mode or submitted the email/password form. Fixed alongside the sibling entry below (`key={mode}` remount + explicit `oauthError`/`passkeyError` clearing on every handler and mode switch) — human-verified live: a real wrong-password error against local Supabase was cleared by switching Login→Signup. [web/app/login/page.tsx]

## Deferred from: code review of 2-3a-email-identity-path-email-password-passkey (2026-07-27)

- **Visiting `/login` while already signed in re-shows the login/signup form instead of redirecting away.** `page.tsx` has no existing-session check on mount — `state.status` starts at `"idle"` regardless of a valid session cookie. Low impact today since the post-auth redirect target (`/`) is still Story 2.2's placeholder scaffold page; a real routing pattern for "already authenticated" belongs with Epic 3's real Dashboard, not this story. [web/app/login/page.tsx:44]
- **[RESOLVED 2026-07-28, Story 2.4]** Toggling Login/Signup mode didn't reset the other mode's stale `useActionState` error. Fixed via a new `AuthForm` subcomponent keyed on `mode` (`key={mode}`) — forces a fresh `useActionState` instance, and therefore fresh `INITIAL_AUTH_STATE`, on every mode switch, since `useActionState` has no manual reset API. Human-verified live. [web/app/login/page.tsx]
- **[RESOLVED 2026-07-28, Story 2.4]** `inputStyle` hardcoded `fontSize: "16px"` instead of a `tokens.css` custom property. Superseded by the new `GhostInput` component, which uses the existing `text-mono-data` utility class instead of any inline literal — the correct fix within this codebase's actual typography convention (no `.text-*` class in this repo sources font-size from a custom property; none ever have). [web/app/components/auth/GhostInput.tsx]
- **`experimental.passkey` opt-in is duplicated verbatim across three client constructors** (`client.ts`, `server.ts`, `middleware.ts`) instead of centralized in one place. Supabase's own docs warn the passkey beta API "may change without notice" — a future change requires remembering to touch all three call sites. No functional impact today. [web/lib/supabase/client.ts, web/lib/supabase/server.ts, web/lib/supabase/middleware.ts]
- **No application-level rate limiting on `signIn`/`signUp` Server Actions** — relies entirely on Supabase's own `[auth.rate_limit]` config in `supabase/config.toml`, which this story never touches, inspects, or verifies. A larger decision than this story owns; worth a dedicated look before this auth surface is production-facing. [web/app/login/actions.ts]
- **Task 5.1's "prompt after successful signUp" trigger never fires as written.** Task 1.1's confirmation gate means `signUp()` always returns a null session (`status: "check-email"`), so `EnablePasskeyPrompt` only ever renders after a later `signIn`, not immediately post-signup as Task 5.1 describes. AC-2 still holds (the prompt remains gated behind an existing session either way) — this is an internal Task-level contradiction the implementation didn't reconcile or flag, not an AC violation. [web/app/login/actions.ts:49-53, web/app/login/page.tsx:68-69]

## Deferred from: code review of 1-9-golden-file-regression-harness (2026-07-25)

- **Golden `.session` fixtures never cover two plays tied on `start_time`.** `sort_by_start_time` (parser/session.rs) relies on `sort_by_key`'s stable-sort guarantee to keep tied-timestamp plays in file order, but neither the new golden suite nor any existing inline test pins this with a fixture that actually has two plays sharing a `start_time`. Low severity — a regression would require someone to deliberately swap to an unstable sort — but worth a fixture (golden or inline) next time this file is touched. [agent/src-tauri/tests/golden_session.rs]

## Deferred from: code review of 1-8-live-practice-confidence-signal (2026-07-25)

- **No guard against a violated `start_time`-sorted-order invariant in `classify`/`count_long_gaps`.** `count_long_gaps` walks consecutive pairs of known `start_time`s with `saturating_sub`, so if `plays` were ever passed out of chronological order (violating `enrich_session`'s guarantee, which this module trusts rather than re-verifies), a reversed pair silently reads as a 0-second gap instead of surfacing corrupted/out-of-order input — the same class of implicit, unenforced cross-module ordering invariant already deferred for `stats::enrich_session` below. There is no live caller of `classify` yet (mirrors that same precedent), so this is not an active bug; revisit once a real caller shape is known (same trigger condition as the `enrich_session` entry). [agent/src-tauri/src/confidence.rs — `count_long_gaps`]
- **Dense/continuous tier can fire off a low ratio of known-timestamps to total plays, and has no density/rate ceiling — both reproduced on real data (see the 865-play/27.5-min session entry below).** A session with a large `track_count` but very few known `start_time`s has its entire tier decided by that small sample; separately, a very-high-density/short-duration session (rapid library previewing) scores the same `LOW_CONFIDENCE_VALUE` as an ordinary dense set, with no upper bound to catch it. **Deferred: already logged (see entry below), thresholds are Arjun's call (Open Questions #1), not a code guess (AD-11 discipline).** [agent/src-tauri/src/confidence.rs — `classify`]

## Deferred from: implementation of 1-8-live-practice-confidence-signal (2026-07-25)

- **[REAL DATA FOUND 2026-07-25] `classify`'s thresholds were eyeballed against all 489 real sessions in `master.sqlite` (Open Questions #1's suggested validation) via a throwaway `cargo run --example` script, not committed.** Distribution: 174 sessions (35.6%) scored `LOW_CONFIDENCE_VALUE` (dense/continuous, the PRD's named ambiguous case), 315 (64.4%) scored `1.0`. No panics across any real session, including a 0-track edge (none observed) and the largest real session (865 plays). The heuristic behaves as spec'd throughout.
  - **A real edge case worth flagging for Open Question #1's eventual threshold review**: session 124 has 865 `history_entry` rows spanning only ~27.5 real-world minutes (`start_time` min/max delta), max consecutive gap 233s — well under `LONG_GAP_THRESHOLD_SEC` — so it scores the same `LOW_CONFIDENCE_VALUE = 0.2` as an ordinary 20-50-track dense set. Inspecting the raw rows: track titles change every ~2 seconds on average, all with `history_entry.played = 1` (confirming PRD FR-27's own research note that Serato's "Played" flag doesn't distinguish this from a real play) — the shape looks like rapid library previewing/cueing, not audible track changes during a set. This isn't a code defect (the heuristic faithfully implements the story's spec'd tiering, which has no upper bound on track count or session density), but it means very-high-track-count/short-duration sessions currently share a tier with ordinary ambiguous sets rather than being distinguishable from them. No fix applied — thresholds are explicitly Arjun's call (Open Questions #1), and inventing an upper-bound rule now would be guessing at a product decision (AD-11). Worth considering when thresholds are next revisited: a track-count-per-minute density ceiling, or excluding rows where consecutive plays are implausibly close together. [agent/src-tauri/src/confidence.rs — `classify`, `MIN_PLAYS_FOR_AMBIGUITY`/`LONG_GAP_THRESHOLD_SEC`]

## Deferred from: code review of 1-7-core-per-set-stat-engine (2026-07-24)

- **`enrich_session`'s pairing API rests on an implicit, untested cross-module invariant.** `pub fn enrich_session(pairs: &[(Play, JoinedMetadata)])` requires the caller to have already zipped each `Play` with its `JoinedMetadata` by position. For Serato 4+, this is only safe because `parser::serato4::read_session` and `joiner::serato4::join_session` happen to share an identical `ORDER BY start_time ASC, id ASC` clause — two independently-maintained SQL strings kept in lockstep by convention, not by any shared constant, assertion, or test. There is no live caller of `enrich_session` yet (confirmed: `enrich_session` is referenced only from tests and doc comments in the current codebase), so this is not an active bug — deferred until Story 1.10 or Epic 2 wires the real `parser -> joiner -> stats` pipeline and a real caller shape is known; worth an explicit alignment check (shared correlation id, or an assertion) at that point. **[Story 1.10, 2026-07-25] Still open** — Story 1.10 froze the `shared/` wire shape that consumes `EnrichedPlay`'s output but explicitly did not build the payload-construction glue that would call `enrich_session` for real (out of scope — see that story's Scope Boundaries); this entry's trigger condition (a real caller) is Epic 2 Story 2.8 / Epic 3 Story 3.2's job. **[CLOSED 2026-07-29 by Story 2.8:** `capture::build_serato4` is the first real caller, and closes the alignment risk this entry asked for with an explicit test (`capture::tests::tied_start_time_rows_correlate_correctly_by_id_not_start_time`) using two/three plays sharing one `start_time`, each carrying a distinguishing field, proving the positional zip pairs correctly rather than merely asserting equal `Vec` lengths — plus a defensive `CaptureError::Correlation` row-count check on the zip itself. `read_session`'s/`join_session`'s `ORDER BY` clauses remain two independently-maintained strings, not a shared constant — the test is the alignment check, not a structural guarantee.] [agent/src-tauri/src/stats/mod.rs — `enrich_session`]

## Deferred from: implementation of 1-7-core-per-set-stat-engine (2026-07-24)

- **`TrackIdentity` collides on `(title, artist)` for every Serato 4+ play — a known, accepted limitation, not fixed.** `parser::serato4::read_session` always leaves `Play.path: None` (confirmed at `parser/serato4.rs:72`), so `stats::TrackIdentity::for_play` falls back to a `(title, artist)` tuple identity for every Serato-4+-sourced play — the normal case for that format, not an edge case. Two different untagged remixes sharing the same title+artist would count as one track in `most_played_tracks`. This also covers the narrower **blank/blank case**: a play with no path AND no title AND no artist produces `TrackIdentity::TitleArtist(None, None)`, which collides with every other such play in the set — nothing distinguishes two unrelated fully-untagged plays, so a run of them can surface as one inflated "track" in `most_played_tracks` rather than the doc comment's "(likely singleton)" framing. Fixing either case would require the same library-join path-resolution expansion Story 1.4 already explicitly deferred (following `location_id`/`asset_id` into a further join) — out of scope here by the same reasoning. No action needed unless a future story revisits Story 1.4's carve-out. [agent/src-tauri/src/stats/mod.rs — `TrackIdentity`]
- **AC-4's ≤10s p95 full-library-pass target (and "agent idle CPU ≈ 0"/"no UI-perceptible lag") could not be honestly benchmarked by this story.** Only the ≤500ms single-set half of AC-4 is testable in isolation today (see `stats::tests::per_set_stat_computation_stays_within_regression_guard_bound`, a 100ms regression-guard bound over 1,000 synthetic plays — deliberately tighter than the unconfirmed 500ms product number, which remains `[ASSUMPTION]`-status per PRD §5.1/§12). The full-pipeline half spans `watcher → parser → joiner → genre → stats` plus real file I/O across a ~5,000-track library; no story has wired those stages together end-to-end yet. Natural home for that benchmark: Epic 2's capture story (2.8, "Set capture into local sqlite") or Story 1.9's golden-file regression harness — whichever first exercises the full pipeline against real data. [_bmad-output/planning-artifacts/epics.md — Story 1.9, Story 2.8]

## Deferred from: code review of 1-6-edge-genre-normalization-versioned (2026-07-23)

- **Exact-equality lookup on a `trim().to_lowercase()` key silently degrades recognized genres to the `"Other"` bucket for several real-world input shapes.** `bucket_for` matches the folded key against alias strings by `aliases.contains(&key)` (exact `&str` equality), and the fold only trims *surrounding* whitespace + lowercases. So all of the following miss a bucket they arguably belong to and fall through to `DEFAULT_BUCKET`: (a) **internal whitespace** — `"deep  house"` (double space), `"deep\thouse"`, non-breaking space; (b) **un-listed punctuation/separator variants** — `"drum&bass"` / `"drumnbass"` (only `"drum & bass"`/`"drum and bass"` are listed), `"r & b"` (only `"r&b"`), `"funk / soul"` (only `"funk & soul"`); (c) **typographic punctuation** — a curly apostrophe `"jackin’ house"` vs. ASCII `"jackin' house"`, en/em dash vs. ASCII hyphen; (d) **non-ASCII / Unicode folding** — `to_lowercase()` is full-Unicode (`"HİP-HOP"` → `i`+combining dot ≠ `"hip-hop"`), and no NFC/NFD normalization is applied, so decomposed input misses; (e) **multi-value tags** — `"House / Techno"`, `"House, Techno"`, `"Deep House; Tech House"` are treated as one lookup key, match nothing, and collapse to `"Other"`, discarding two in-taxonomy genres. **Deliberately not fixed now:** nothing consumes `genre::normalize` yet (verified — only `genre.rs` and a doc reference in `joiner/mod.rs`), and the worst case is a mis-bucket to `"Other"`, never a *wrong* bucket or a dropped play (`raw` is preserved verbatim, so any of these can be re-normalized later). Refine the fold (collapse internal whitespace, canonicalize separators, consider NFC) against a **real Serato genre-string fixture** when Story 1.7 first consumes `normalize`, and bump `TAXONOMY_VERSION` if the matching rules change. [agent/src-tauri/src/genre.rs — `normalize` (`raw.trim().to_lowercase()`), `bucket_for` (`aliases.contains(&key)`)]
- **V1 taxonomy content has gaps for its own stated electronic/DJ target.** The table self-describes as "drafted for electronic/DJ libraries," but omits high-volume Beatport/Rekordbox categories that silently become `"Other"`: `"melodic house"` (only `"melodic techno"` is present), `"organic house"`, `"big room"`, `"future rave"`, `"microhouse"`, `"grime"`, `"footwork"`, `"hard groove"`. Not a defect — this is exactly the OQ2 *content* decision the spec explicitly carved out from the *mechanism* this story freezes ("cheap to refine later; bump `TAXONOMY_VERSION` when you do"). Log for the product-content refinement pass: expand buckets/aliases against a real genre-tag frequency histogram from a live library, then bump `TAXONOMY_VERSION`. [agent/src-tauri/src/genre.rs — `TAXONOMY`]
- **Legacy ID3v1 numeric TCON `"(17)"`-form maps to `"Other"` even though index 17 == "Rock", an in-taxonomy bucket.** Story 1.5 stores legacy numeric TCONs raw and uninterpreted, handing interpretation to this story; V1 deliberately chose OQ3 **option (b)** (treat `"(17)"`/`"17"`/`"(17)Rock"` as ordinary unrecognized raw → default bucket), keeping the table purely string-keyed and avoiding importing the 192-entry ID3v1 table for a case a Serato electronic library rarely carries. The outcome is deterministic and explicitly tested. Revisit **option (a)** — decode the fixed 0–191 ID3v1 genre-number table so `"(17)" → Rock`-family bucket — only if real libraries actually surface numeric TCONs (Story 1.9 golden-file corpus is the natural place to check), and bump `TAXONOMY_VERSION` if adopted. [agent/src-tauri/src/genre.rs — `normalize`, `legacy_numeric_tcon_forms_map_to_default_bucket`]
  - **[REAL DATA FOUND 2026-07-25, Story 1.9 — none observed]** Incidental to the duplicate-path scan (see the Story 1.4 review section above), every genre string among the local `database V2`'s 930 track records was checked for a `"(<digit>"`-shaped prefix. **Zero** numeric-TCON-shaped genre strings found. No evidence to act on from this corpus; still an open question for whichever future corpus (or the unavailable USB library) next surfaces one.

## Deferred from: code review of 1-3b-master-sqlite-play-log-reader (2026-07-23)

- **Connection-sharing contract between `parser::serato4::read_session` and `joiner::serato4::join_session` has no integration test.** Both the module doc and the story's Dev Notes describe a caller opening one `Connection` and passing it to both functions, but no test anywhere (in this diff or in `joiner/serato4.rs`) actually exercises that pattern end-to-end. Not required by any AC/task for this story; worth a follow-up test whenever a real caller (the watcher/capture story) wires the two together. [agent/src-tauri/src/parser/serato4.rs — `read_session`; agent/src-tauri/src/joiner/serato4.rs — `join_session`] **[CLOSED 2026-07-29 by Story 2.8:** `capture::build_serato4` is the real caller this entry anticipated — opens exactly one `Connection` via `joiner::serato4::open_read_only` and passes it to both `parser::read_session` and `joiner::serato4::join_session`, exercised end-to-end by `capture::tests::build_serato4_correlates_tied_start_time_rows_correctly` and the manual real-data walkthrough (5 real historical sessions, non-panicking, plausible stats).]
- **`history_entry.session_id` being SQL `NULL` would silently make a row unreachable.** `WHERE session_id = ?1` never matches under SQL three-valued logic regardless of the `session_id` passed, so such a row would look identical to "no such session" (already the tested zero-rows case) rather than surfacing as a distinct condition. No evidence this column can be `NULL` in production — the six columns confirmed `NOT NULL` this story don't include `session_id` itself — so this is speculative, logged per AD-11 rather than fixed. [agent/src-tauri/src/parser/serato4.rs — `read_session`]
- **`start_time`'s real range was never independently confirmed the way `deck`'s was.** Real data confirmed `start_time` is `NOT NULL` and defaults to "the current time" when unset — but nobody checked whether a stored value can ever be negative or exceed `u32::MAX`. `read_session` already degrades an out-of-range value to `Play.start_time: None` without panicking (see `out_of_range_start_time_is_none_not_a_panic`), but the SQL `ORDER BY start_time ASC, id ASC` still sorts on the raw, un-degraded value — so such a row's position in the returned `Vec` would be driven by a value no longer visible on the `Play` it produced. No evidence this occurs; not fixed speculatively. [agent/src-tauri/src/parser/serato4.rs — `read_session`]

## Deferred from: code review of 1-4-library-join-for-in-library-enrichment (2026-07-22)

- **Flag for Story 1.9 (golden-file suite) — volume-hosted `database V2` path resolution is unproven against real data.** The legacy join resolves a play by trying the absolute path, then stripping one leading `/` (Story 1.2 findings §5/D4). Confirmed correct for a root-hosted library; **never observed succeeding on a USB/volume-hosted one** — the spike's USB session resolved 0 of 116 plays for an unrelated reason (tracks were never in Serato's library), so the convention was never exercised end-to-end. If a volume-hosted catalogue stores paths relative to its own volume (`Theo Indian/track.wav` rather than `Volumes/ARJUN SSD/Theo Indian/track.wav`), those tracks silently read as off-library. Deliberately not "fixed" — a third strip-the-library-root step would be guessing at a schema, which AD-11 forbids. Resolution: read one USB `database V2`'s stored path strings during 1.9's fixture work, then confirm or extend with a real fixture behind it. [agent/src-tauri/src/joiner/legacy.rs — `LegacyLibrary::get`]
  - **[REAL DATA FOUND 2026-07-25, Story 1.9 — SSD unavailable this run, root-hosted case re-confirmed instead]** `/Volumes/ARJUN SSD` was not mounted during this story's implementation, so the volume-hosted check itself still could not run (per this story's Open Question #3, logging as unavailable rather than blocking). As a substitute data point, the **local (root-hosted)** `database V2` (930 track records, via a throwaway `cargo run --example`, not committed) stores every path with **no leading slash** (`"Users/arjun/Downloads/.../track.mp3"`), while the matching real `.session` file (`2521.session`, 151/151 plays with a path) stores the **absolute** form (`"/Users/arjun/Downloads/.../track.mp3"`) — i.e. exactly one leading `/` of difference, confirming the existing "strip one leading `/`" convention is correct for root-hosted libraries. This does not touch the volume-hosted question (a USB path's on-disk convention could still differ, e.g. relative-to-volume-root) — still open, re-check when the SSD is next available.
- **Flag for Story 1.9 — path *encoding* mismatch is untested in either direction, and Open Question #6 does not cover it.** The catalogue is keyed by `HashMap<PathBuf, _>`, i.e. byte-exact equality, while APFS is case-insensitive and macOS stores filenames decomposed (NFD). If `.session` and `database V2` disagree on Unicode normalization or case for the same file, every accented artist in the library (`Café`, `Beyoncé`, `Björk`, `Sigur Rós` — a large share of a real dance library) silently reads as off-library, which is indistinguishable from the app working correctly. **No evidence either way; do not add a normalization layer speculatively.** Same inspection, same file, same ten minutes as the entry above: while a real `database V2` is open for 1.9, compare its stored path bytes against the `.session` bytes for one accented track. [agent/src-tauri/src/joiner/legacy.rs — `LegacyLibrary::get`, `tracks: HashMap<PathBuf, LibraryTrack>`]
  - **[REAL DATA FOUND 2026-07-25, Story 1.9 — inconclusive, SSD unavailable this run]** The USB library this entry is really aimed at (the one likelier to carry accented artist names at volume-mount scale) was not mounted. The local `database V2`'s 930 paths were scanned for any non-ASCII byte as a substitute — one was found (an emoji, `🥰`, in a downloaded-playlist folder name), which is not the accented-Latin-letter/NFC-NFD case this entry is about, so it provides no evidence either way on normalization or case-folding agreement. Still open; re-check with the USB library when mounted.
- **Spine deviation — Serato 4+ `in_library` is guessed, against the Consistency Conventions' "never guessed".** `joiner::serato4::join_session` sets `in_library: true` unconditionally for every `history_entry` row. No column in the explored schema distinguishes "played from an indexed library track" from "played straight off disk", and unlike the legacy path there is no lookup that could miss, so there is no honest way to compute it today. Defensible on the merits and documented at the function, but it contradicts an explicit spine rule (ARCHITECTURE-SPINE.md Consistency Conventions: the `in_library` flag is "never omitted, never guessed") and that record belongs here, not only in an implementation-artifact story file. **Does not affect AC-4 routing** — a `NULL` column still reports `None` regardless of the flag — only the flag's own display semantics (the in-library/off-library glossary distinction, PRD §3). **[REAL DATA FOUND 2026-07-23, Story 1.3b, still not implemented]** A real `master.sqlite` (`~/Library/Application Support/Serato/Library/master.sqlite`) was inspected read-only during this story. `history_entry` does carry both `location_id` (FK → `location.id`, which has a `path` column) and `asset_id` (FK → `asset.id`, the fuller per-track catalogue record) — real, confirmed columns, not the "unexplored" FKs earlier entries described. Measured against one DJ's real history (23,254 `history_entry` rows across 489 sessions): `asset_id` resolves (`> 0`) for only 1,075 rows (~4.6%); every row with a resolved `asset_id` also has a resolved `location_id` (no exceptions), and `location_id` alone resolves for 1,129 rows (~4.9%) — so `asset_id > 0` is the stricter of the two candidate signals and a plausible `in_library` replacement. **Still not wired in this story** — Story 1.4's explicit carve-out keeps `location_id`/`asset_id` out of scope regardless of whether they're confirmed to exist, since consuming them means a second query/join this story didn't spec. Left for whoever next revisits this default: the ~4.6% figure is far lower than intuition suggests "in-library" should be for an actively-used install, which may mean either the signal is right and this DJ's Serato-4-generation profile genuinely has little indexed overlap with what got played (plausible — this DJ's primary, heavily-used install is the legacy `.session`/`database V2` path inspected separately, and this `master.sqlite` may reflect a much newer, lightly-used profile), or `asset_id` populates on a lag (e.g. only after an explicit "analyze" pass) rather than at play time — both are worth checking against a second real profile before trusting the number as a general rate. [agent/src-tauri/src/joiner/serato4.rs — `join_session`]
- **[CONFIRMED 2026-07-23 by Story 1.3b, real data] `history_entry.deck` values are plain text digits.** Real distribution from the same file: `"1"` (14,600 rows), `"2"` (8,447), `"3"` (104), `"4"` (103) — no letters, no unparseable values observed. Confirms `parser::serato4::read_session`'s `.and_then(|d| d.parse().ok())` approach is sound against real data, and that a 4-deck controller setup is real and already exercised (not just theoretical). [agent/src-tauri/src/parser/serato4.rs — `read_session`]
- **[CONFIRMED 2026-07-23 by Story 1.3b, real data] `history_entry.end_time` defaults to `-1`, not `NULL`, when unset.** 464 of 23,254 rows have `end_time = -1`; the remaining 22,790 have a positive value; zero rows have SQL `NULL`. Informational only — this story still does not derive `duration_sec` from it (Open Question #1 stands), but whoever eventually validates `end_time`'s meaning should know `-1` is the real "unset" sentinel, not `NULL`, and must be excluded from any future `end_time - start_time` computation. [agent/src-tauri/src/parser/serato4.rs — `read_session`]
- **[CONFIRMED 2026-07-23 by Story 1.3b] Serato 4+ `join_session` — `genre`/`key` redundant, `bpm` still needed.** Story 1.3b's `parser::serato4::read_session` now reads `genre`/`key` directly from `history_entry` onto `Play`, exactly as this entry predicted — a caller building a Serato 4+ `Play` no longer needs `join_session`'s `genre`/`key`. `bpm` cannot be folded in the same way: `Play` has no `bpm` field by design (BPM is scoped to the library join, never the play-log, for both formats — see Story 1.3b Task 1). `join_session` was not modified or removed; it is still needed for `bpm` alone. [agent/src-tauri/src/joiner/serato4.rs — `join_session`; agent/src-tauri/src/parser/serato4.rs — `read_session`]
- **`sane_bpm` has no upper bound.** Accepts any finite positive value, so a corrupted-but-positive BPM (e.g. a mis-decoded `"999999.00"`) passes through as a real measurement into what will become Story 1.7's tempo averages. Not required by any AC; the correct threshold is a product call (DJs do spin 200-300 BPM genres) better made once Story 1.7 actually consumes this value rather than picked arbitrarily now. [agent/src-tauri/src/joiner/mod.rs — `sane_bpm`]
- **Duplicate-path last-wins tiebreak is an unconfirmed assumption.** `LegacyLibrary::from_database_bytes` keeps the last-processed record when two carry the same path, on the belief that file order reflects the catalogue's own append order (so a re-analysed track's newer BPM wins). Unlike the volume-hosted-path and Unicode-encoding entries above, this assumption has no Story 1.2 findings citation behind it. Confirm against a real catalogue with a genuinely re-analysed duplicate during Story 1.9's fixture work, alongside the other two. [agent/src-tauri/src/joiner/legacy.rs — `duplicate_paths_resolve_to_the_last_record`]
  - **[REAL DATA FOUND 2026-07-25, Story 1.9 — inconclusive]** Scanned the local `database V2` (930 track records via `triseratops::library::database::parse` in a throwaway `cargo run --example`, not committed): **zero** duplicate paths — all 930 records resolve to 930 distinct paths, so this DJ's local catalogue has no re-analysed track to observe the tiebreak against. The larger USB catalogue (4,972 tracks per this story's own Dev Notes, more likely to contain a genuine re-analysis) was unavailable this run (SSD not mounted). Still an unconfirmed assumption; the fixture built in Task 3 encodes the *documented* last-wins behavior without real-data backing, exactly as this entry originally called for absent a confirmed real duplicate.
- **`serato4::join_session` fails the whole session on one row's type-coercion error.** `row.get::<_, Option<f64>>` etc. propagate a column type-coercion failure as `Err` via `?`, so `.collect()` fails the *entire session's* join rather than degrading just that one field to `None` — asymmetric with the legacy path's per-field grace (a gap in one column there costs only that field). Low real-world likelihood (requires a non-conforming SQLite column type) and arguably consistent with this file's own precedent that schema-shape anomalies are hard errors (`missing_table_is_an_error_not_an_empty_set`) — worth revisiting with real `master.sqlite` data during Story 1.3b rather than patching speculatively now. **[REVISITED 2026-07-23, Story 1.3b code review] Same shape confirmed in the new sibling function.** `parser::serato4::read_session`'s `rows.collect()` has the identical `Result`-short-circuit behavior — a single row's `Option<i64>`/`Option<String>` coercion failure would abort the whole session's *play-log* read, not just its own metadata join. Real `master.sqlite` data (inspected this same story) did not surface a case that would trigger this — every row read cleanly — so there is still no real-world evidence to patch against, only the same structural risk now present in two functions instead of one. Left deferred for the same reason as the original entry: patching the failure mode (e.g. per-row `Result` degrading to a partial `Play`) is a design change, not obviously "the fix," and should wait for a real trigger. [agent/src-tauri/src/joiner/serato4.rs — `join_session`; agent/src-tauri/src/parser/serato4.rs — `read_session`]

## Deferred from: story creation of 1-3-clean-room-session-parser (2026-07-22)

- **[RESOLVED 2026-07-22] `master.sqlite` play-log scope gap** — Story 1.2's findings (§6) recommend prioritizing `master.sqlite` as the live play-log source (it holds this DJ's entire history, no binary decoding needed), but no epics.md story owned reading play-log data directly from `master.sqlite`'s `history_session`/`history_entry` tables; Story 1.4 as scoped treats `master.sqlite` only as a metadata-join library. Resolved by spinning a new sibling story **1.3b — `master.sqlite` play-log reader** (not folded into 1.4, to keep play-log-source and metadata-join concerns separate); Story 2.6/2.8 ACs updated to select the source per DJ install generation. [_bmad-output/planning-artifacts/epics.md — Story 1.3b, Story 2.6 AC-5, Story 2.8 AC-1]

## Deferred from: code review of 1-3-clean-room-session-parser (2026-07-22)

- **Flag for Story 1.9 (golden-file suite) — RF-2's trailing-fragment hard failure is unverified against real files.** RF-2 makes `parse()` return `Err(Desync)` on a trailing fragment too short to hold a record header. If real `.session` files carry trailing padding, this converts what may be benign padding into a hard parse failure. `parse_partial()` still returns the plays already decoded (loud, not lossy), but this needs checking against the real 474-file corpus once 1.9 lands. [agent/src-tauri/src/parser/session.rs — `is_plausible_tag`/trailing-fragment check]
  - **[REAL DATA FOUND 2026-07-25, Story 1.9 — REFUTED as a live concern]** Ran `parser::parse_session_file` (the strict path) over all 474 real `.session` files in `~/Music/_Serato_/History/Sessions/` via a throwaway `cargo run --example`, not committed. Result: **474/474 parsed `Ok`, zero `Desync`, zero `Truncated`.** RF-2's hard-failure path has never fired on this DJ's real corpus — no benign trailing-padding shape exists to model a fixture on. Task 2's desync/truncated fixtures are therefore built as synthetic worst-case coverage only (proving the guard still fires correctly if a file *is* malformed), not as a reproduction of an observed real occurrence.
- **Flag for Story 1.9 — fixture-construction gotcha found while testing RF-2.** Understating an `oent`'s declared length yields `Truncated` (the inner `adat` bound catches it first), not `Desync` — to exercise the tag-plausibility/desync check, a fixture must understate a top-level *header* record's length instead. Worth documenting explicitly when 1.9's real-corpus fixtures are built, so a "desync" golden fixture isn't accidentally written as a `Truncated` one. [agent/src-tauri/src/parser/session.rs test module]

## Deferred from: code review of 1-1-monorepo-scaffold-with-three-workspaces (2026-07-21)

- **`csp: null` in Tauri config** — the webview Content-Security-Policy is disabled in the baseline every later story inherits. The agent will later load/parse local Serato data; a `null` CSP tends never to get tightened. Set a restrictive CSP before the agent loads untrusted/local file content. [agent/src-tauri/tauri.conf.json:20]
- **No OS matrix in CI** — the Rust agent core is only built and tested on `ubuntu-latest`. Cross-platform (macOS/Windows) compile breakage is unguarded. Deferred because signed cross-platform bundling is explicitly Epic 2 (AR-14) scope. [.github/workflows/ci.yml:48]
- **[RESOLVED 2026-07-25, Story 1.10] Shallow TS↔schema parity check** — the parity test (Rust + vitest) asserted only the AR-15 enums and `contract_version`, not full property sets / required keys. Structural drift between the TS type and JSON schema could pass both guards. Closed by Story 1.10's contract freeze: `shared/src/index.test.ts` now also asserts the full top-level `SyncPayload`/`set`/`SyncPlay`/`SyncSetDerived`/`confidence` required arrays and property sets, and a new `shared/src/additive-only.test.ts` recursively diffs the current schema against a checked-in frozen-baseline snapshot (`shared/schema/sync-payload.schema.frozen-baseline.json`), catching any future removed/renamed/re-typed property or required entry at any nesting level. The Rust-side `parses_shared_sync_contract_schema` test (`agent/src-tauri/src/lib.rs`) was widened the same way. [agent/src-tauri/src/lib.rs; shared/src/index.test.ts; shared/src/additive-only.test.ts]

## Deferred from: code review round 2 of 1-1-monorepo-scaffold-with-three-workspaces (2026-07-21)

- **`window.is_visible().unwrap_or(false)` swallows errors** — the tray click handler treats a platform visibility-query error as "not visible," which could re-show/refocus an already-visible window with no diagnostic. Low likelihood, low impact. [agent/src-tauri/src/lib.rs]
- **Heavy full Supabase stack in CI for a no-op migration** — the `supabase` CI job boots Postgres, Studio, Auth, Storage, Realtime, Edge Runtime, Analytics, vector, etc. just to prove one no-op migration applies. Mirrors what the story's Dev Notes prescribe, so it's an accepted tradeoff today, not a code defect — worth a lighter-weight CI investigation later. [.github/workflows/ci.yml]
- **[RESOLVED 2026-07-30, Epic 2 retrospective]** `sync_payload_schema_path()` used a compile-time build-machine path — resolved the shared schema via `env!("CARGO_MANIFEST_DIR")`, which wouldn't exist on a bundled, installed end-user agent. Flagged in the Epic 1 retro as a functional blocker with DoD assigned to "first build/bundle story (2.9a/b)" — those stories turned out to be pure CI-signing-pipeline work with no `src/` touch, so the assignment never actually landed on a story that could close it, and it carried unfixed through all of Epic 2. Caught again at the Epic 2 retrospective (previous-retro follow-through check) before Story 3.2 became its first real runtime caller. Fixed by embedding the schema at compile time via `include_str!` instead of a runtime path — `sync_payload_schema_path()`/`SYNC_PAYLOAD_SCHEMA_RELPATH`/`SchemaLoadError::Read` removed, `load_sync_payload_schema()` now parses the embedded `SYNC_PAYLOAD_SCHEMA_JSON` constant directly, no filesystem read at runtime at all. Full four-command cargo gate + workspace-wide pnpm lint/typecheck/build/test re-verified green after the change. [agent/src-tauri/src/lib.rs]

## Deferred from: code review of 1-2-parser-validation-spike-against-real-sessions (2026-07-22)

- **`ParseError::Truncated` is unreachable dead code; malformed field-length payloads and truncated trailing records are silently dropped rather than logged as discrepancies** — in tension with the module's own "logged discrepancy, not a crash" doc comment. No occurrence observed in the real 474-file corpus; crate is explicitly throwaway (AC-4) and not extended by Story 1.3. [agent/spike-1-2-parser-validation/src/legacy_session.rs]
- **`home()`'s `.expect("HOME not set")` and `serato4_path()`'s `db_path.to_str().unwrap()` panic instead of failing gracefully** — story's own Dev Notes explicitly mark this as not a hard requirement for throwaway spike code. [agent/spike-1-2-parser-validation/src/main.rs]
- **Byte-level resync (`i += 1` until the next literal `"oent"`) could theoretically desync on a coincidental match inside a UTF-16BE text payload** — never observed against the real corpus; would show up as a track-order/name mismatch in the (now-passing) ground-truth cross-validation. [agent/spike-1-2-parser-validation/src/legacy_session.rs:104]
- **`2521.session`'s framing as matching AD-17's "morning block + gap + evening block" pattern is imprecise** — the 144-minute gap sits near the start of the session, not after a distinct morning block. Interpretive/documentation nuance; doesn't affect fixture validity. [_bmad-output/implementation-artifacts/1-2-parser-validation-spike-against-real-sessions.md]

## Deferred from: implementation of 1-6-edge-genre-normalization-versioned (2026-07-23)

- **[RESOLVED 2026-07-23] The `agent/src-tauri` cargo gate is now green.** Story 1.6 was *implemented* on a Windows box with **no Rust toolchain** (`cargo`/`rustc`/`rustup` absent; `settings.local.json` shows the project was previously worked from macOS at `/Users/arjun/...`), so the four mandated commands could not be run there and the code was completed by careful hand-review. The gate was subsequently **run on macOS** (commit `e2feb27`): the only fix needed was a `cargo fmt` reflow of the hand-formatted `const TAXONOMY` table and two test assertions to rustfmt's canonical layout — exactly the predicted fmt nit, no logic change. Build, clippy (`--all-targets -D warnings`), and all 11 inline tests pass. Verification gap closed. [agent/src-tauri/src/genre.rs]

## Deferred from: code review of 1-5-off-library-embedded-tag-fallback-with-visible-unknown (2026-07-23)

- **Disabling `lofty`'s `id3v2_compression_support` default feature can silently drop an otherwise-readable FLAC Vorbis-comment block.** A FLAC file carrying a legacy prepended ID3v2 block with a *compressed* frame makes `lofty::flac::FlacFile::read_from` fail outright (verified: with the feature off, `handle_compression` unconditionally returns `Err`, and `flac::read::read_from` propagates it via `?` before it ever reaches the Vorbis-comment block that this module actually wants) — losing genre/key/BPM that were perfectly readable. The `Cargo.toml` comment's rationale ("all ID3 reads go through `id3` above, not lofty") is incomplete: lofty's FLAC reader still parses embedded ID3v2 internally, just to skip past it. Requires a legacy prepended+compressed ID3v2 block specifically — low real-world likelihood, and re-enabling the feature is a deliberate dependency-weight tradeoff the story already made consciously, not an oversight. Revisit if a real FLAC file from the DJ's library ever turns up with this shape. [agent/src-tauri/Cargo.toml:36]
- **Module doc overstates "content, not extension" dispatch.** `agent/src-tauri/src/joiner/embedded_tags.rs` claims dispatch is purely content-sniffed, but `lofty::probe::Probe::open` seeds `file_type` from the path's extension first, and `guess_file_type()`'s content sniff only overwrites it on success (`f_ty = f_ty.or(self.f_ty)`) — an inconclusive sniff silently falls back to the extension-based guess. The diff's own dispatch test uses an extension-less file specifically, which sidesteps this gap rather than proving the doc's general claim. Low practical impact: any resulting mismatch still degrades to the module's normal `None` outcome, never a wrong value. [agent/src-tauri/src/joiner/embedded_tags.rs:10-11, 91-100]

## Deferred from: implementation of 4-2-library-to-setlist-correlation-trend (2026-08-07)

- **[PRODUCT — likely launch-affecting] Path-based track identity (D-2) makes the SAME song on two drives two different tracks, which structurally deflates the conversion rate for any DJ who keeps a laptop library and a gig USB.** `track_id` is `fnv1a_hex` of the volume-root-relative path, so `Users/arjun/Music/x.mp3` and `A Indian/x.mp3` are unrelated identities. Measured on Arjun's own real data while building the 4.2 fixture: of 2,294 in-library plays, only **179** carry an identity present in the boot-drive `database V2` — **2,115 resolve from another drive**. The consequence is direct: every boot-drive copy of a track he actually plays off the USB counts as a permanent non-conversion, while the USB copy carries the play. On the exported boot catalogue that produced whole 0% cohorts (2025-04, 2025-05, 2025-08, 2025-10) for months where he demonstrably played music. This is inherent to D-2 as specified — not a defect in the implementation, and not fixable in the fixture (mounting every drive at export time fixes the *fixture*, not the product). Also affects Story 4.3's FR-11 meter, FR-12's aging shelf (a duplicate would sit on the shelf forever), and FR-13. Options, all product calls: (a) identity by normalized title+artist instead of path; (b) keep path identity on the wire but union duplicates at cohort time in `libraryConversion.ts`; (c) resolve duplicates cloud-side. Needs Arjun's ruling before 4.3. [agent/src-tauri/src/capture.rs — `track_id`; web/lib/sets/libraryConversion.ts] **[CLOSED 2026-08-07 by Story 4.3, Decision E-2:** option (a) shipped — `track_id` is now `fnv1a_hex` over normalized (trim + lowercase, matching `genre::normalize`'s existing fold) title+artist, computed by the new `capture::track_id_from_title_artist(title: Option<&str>, artist: Option<&str>) -> Option<String>`, replacing the path-hashing `track_id`. Both fields must resolve or there is no identity (`None`, AD-11 — never a fabricated partial hash); `LibraryTrack` gained `title`/`artist` fields parsed from `database V2`'s already-available `Field::SongTitle`/`Field::Artist`. Re-ran the real exports this entry's own numbers came from: `library-add-events.fixture.json` and `recent-sets.fixture.json` regenerated against Arjun's actual library and Serato history. Measured effect (boot-drive catalogue only — no USB mounted this session, so the fix's full cross-drive magnitude is likely larger than this): join rate (add-events that were ever played) went from **20% (143/714)** under the old path identity to **45% (237/523)** under the new one — join rate roughly doubled on the reachable subset. Real, disclosed cost: **473 of 2,294 real plays (~21%) now carry no `track_id`** — mostly missing artist tags (380), some missing title (28) or both (65) — versus 100% coverage under the old (collision-prone) path scheme; accepted as the correctness/coverage trade the AD-11 "require both fields" design deliberately makes. `ARCHITECTURE-SPINE.md` AD-21's identity clause updated per its own pre-clearance (no re-ratification needed). Full re-mount-and-re-export for the complete cross-drive picture is still owed whenever the USB is next connected — not blocking, the mechanism itself is verified correct.]

- **[LAUNCH BLOCKER — unstoried] Nothing in the sprint plan swaps `web/`'s read path from the committed fixture to Supabase.** `getRecentSets`, `getSetById`, `deleteSet` and now `getLibraryAddEvents` all read `recent-sets.fixture.json` / `library-add-events.fixture.json`; only `getAgentStatus` reads Supabase for real. The WRITE path is complete and shipping (agent → `sync_set` / `sync_library_add_events` → `sessions`/`sets`/`plays`/`library_track_events`), so data will accumulate in production that the product cannot display. Decision A named the fixture as "the day-one stand-in for the real read" (web/lib/sets/index.ts's own header), but no story anywhere owns the swap: Epic 4 is Style Evolution/Library Utilization, Epic 5 is segments/enrichment, Epic 6 is marketing, Epic 7 is billing. Confirmed by reading the full `development_status` list. Needs a story before launch — the seam is deliberately one file, so it is likely small, but "small and unowned" is how it stays undone. [web/lib/sets/index.ts] **[CLOSED 2026-08-07 by story creation:** Arjun ruled to create the story now rather than let it accrete further. Added as Story 4.6 (`epics.md`, `sprint-status.yaml`) — sequenced at the end of Epic 4 since it's cross-cutting infrastructure, not a Style Evolution feature, but needs to land before Epic 5 reads through the same seam. Story file: `4-6-web-read-path-supabase-swap.md`, status `ready-for-dev`. This entry is closed by the story's existence, not by implementation — the swap itself is still outstanding.] **[RESOLVED 2026-08-07 by implementation:** all four functions (`getRecentSets`, `getSetById`, `deleteSet`, `getLibraryAddEvents`) now read/write Supabase directly (`sessions`/`sets`/`plays`/`library_track_events`, owner-RLS per AD-7), matching `getAgentStatus`'s existing pattern. `deleteSet` required a new migration (`20260807120000_add_sets_delete_policy.sql`) granting a DJ-owned `DELETE` policy on `sets`, since none existed before this story. The committed fixtures are retired from the production read path and kept only as test-only sample data (see the fixture builders' own updated headers). Story 4.6, status `review`.]

- **The Style Evolution chart's explainer tap target is 24x24 in SIZE but only ~1/4 of it is actually hit-testable — Story 4.1's WCAG 2.2 AA (SC 2.5.8) fix is defeated by stacking, not by sizing.** 4.1's review grew `.se-chart-info-btn` from 15x15 via an invisible `::after` and hit-tested it at the time; the computed `::after` box does measure exactly 24x24 today. But at 375px, `document.elementFromPoint` at its four corners returns the button for only the top-left one — `.se-chart-head` covers the top-right corner and `.se-chart-hit` (the hover-column row) covers both bottom corners, so the enlarged region is inert everywhere except where the visible 15px icon already was. Reproduced identically on the **Genre Diversity** chip (pure 4.1 code, untouched by 4.2) and on 4.2's new Library Conversion chip, which is why this is filed as a 4.1 regression rather than a 4.2 defect: 4.2 reuses that markup verbatim and introduced neither the `::after` nor the overlapping layers. Not fixed in 4.2 — it is 4.1's component, the fix is a z-index/stacking change to shipped code that 4.2's ACs do not cover, and touching it silently under a different story is exactly the drive-by-edit pattern the Epic 3 retro flagged. Likely fix: raise the `::after`'s stacking above `.se-chart-hit`/`.se-chart-head`, then re-verify by corner hit-test (not by bounding box — the bounding box was already right and still is). [web/app/components/style-evolution/TrendChart.tsx; web/app/styles/style-evolution.css — `.se-chart-info-btn::after`] **[CLOSED 2026-08-07:** root cause was CSS stacking, not sizing — `.se-chart-plot` (`position: relative`, later in document order) and its descendants (`.se-chart-hit`, also positioned) are promoted into the same z-index:auto paint tier as `.se-chart-info-btn`'s positioned subtree; a positioned z-index:auto descendant of a *later* tree sibling paints on top regardless of visual position, independent of `.se-chart-head` itself (which is unpositioned and never competed). Fix: `.se-chart-info` (`style-evolution.css`) now sets `z-index: 20`, promoting the button + its `::after` hit area + the tip into their own stacking context above `.se-chart-plot`'s content — the same value already used by `.se-chart-info-tip` for "float above everything in the chart." Re-verified by `document.elementFromPoint` at all four corners of the 24×24 box at 375px on both the **Genre Diversity** chip and 4.2's **Library Conversion** chip — all four now resolve to the button on both.]

- **A one-track cohort plots with the same visual weight as a 256-track cohort.** On the committed fixture, `2026-04` is a single added track that happened to be played, so it plots at a confident-looking 100%, immediately beside `2025-04`'s 47/256 = 18%. Nothing in the chart distinguishes them, and the caption's from/to sentence reads the endpoints — which on this data means it compares a 6-track cohort to a 1-track cohort. This is the same failure shape Story 4.1's review hit with most-played ("at 5 sets the recent window crowned a 2-play track"), and the same answer would work (a minimum cohort size below which the point is suppressed or de-emphasised) — but choosing that threshold is a product call, not an implementation detail, so 4.2 deliberately did not invent one. Worth resolving before Story 4.3's conversion-rate meter, which reads the same cohorts. [web/lib/sets/libraryConversion.ts — `buildLibraryConversion`/`libraryConversionSummary`] **[CLOSED 2026-08-07:** Arjun's ruling — de-emphasize, don't drop; keep the line running through the point; disclose the exact count on hover. `libraryConversion.ts` adds `LOW_CONFIDENCE_COHORT_SIZE = 5` (a default, not derived — flagged for revisiting before 4.3) and `isLowConfidenceCohort(added)`. `TrendChart.tsx`'s per-bucket dot for the library metric renders dimmer (`.se-chart-dot--low-confidence`, opacity 0.55, no glow) with a small amber `.se-chart-dot-flag` badge (`--color-spark`, the existing "look at this specific value" accent) when its cohort is under threshold; the line itself is untouched, so it still runs through the point. The hover chip (`libraryHoverDetail`) appends `— only N track(s) added, low confidence` when applicable. Verified live at 375px: the April 2026 1-track cohort and one other sub-threshold month both flag; hover text confirmed; `tsc`/`eslint`/vitest (38/38, 3 new cases) all clean. Story 4.3's conversion-rate meter should reuse `isLowConfidenceCohort`/`LOW_CONFIDENCE_COHORT_SIZE` rather than re-deriving a threshold.]

## Deferred from: implementation of 1-10-freeze-the-shared-sync-contract (2026-07-25)

- **`stats::EnrichedPlay` does not carry `JoinedMetadata.in_library`, but the frozen `shared/` wire shape requires it on every `SyncPlay`.** The Consistency Conventions table requires `in_library` to travel with every play's "Unknown" data, and Story 1.10's `SyncPlay.in_library: boolean` field is `required`, sourced from `JoinedMetadata.in_library` directly — not from `EnrichedPlay`, whose field list (`title, artist, path, start_time, bpm, genre, camelot`) never included it. This story deliberately did not touch `stats::EnrichedPlay`/`enrich`/`enrich_session` (out of Story 1.10's Scope Boundaries — no `agent/`-side payload-construction glue exists yet). Whoever writes the real payload-construction function (Epic 2 Story 2.8 or Epic 3 Story 3.2, whichever wires `stats -> local store -> sync-queue` first) needs to read both `EnrichedPlay` and its paired `JoinedMetadata.in_library` — by position, per pair — to build a real `SyncPlay`, which also means that glue inherits the pairing-order invariant already flagged above for `enrich_session`. [agent/src-tauri/src/stats/mod.rs — `EnrichedPlay`; shared/src/index.ts — `SyncPlay.in_library`] **[CLOSED 2026-07-29 by Story 2.8:** this is that payload-construction glue's first draft (local, not the wire payload — Story 3.2 still builds the actual `SyncPayload`). `capture.rs`'s `assemble` helper reads `in_library` off each pair's `JoinedMetadata` at the same index `enrich_session` produced the matching `EnrichedPlay` at, and `store::CapturedPlay.in_library: bool` carries it through to the local row. The pairing-order invariant this entry inherited from the `enrich_session` entry above is closed the same way (see that entry).]
- **Raw file `path` deliberately excluded from the v1 frozen wire shape (privacy).** `EnrichedPlay.path` is a local filesystem path; Task 2 excludes it from `SyncPlay` entirely rather than sending it to the cloud. Additive-only means a purpose-built (possibly hashed/opaque) per-track identity field can be added later without reopening the freeze — flagged as Open Question #1 in the story for whichever future Epic 4 story implements FR-10 (library-to-setlist correlation). Not a defect; a recorded product decision. [shared/src/index.ts — `SyncPlay`] **[CLOSED 2026-08-07 by Story 4.2:** that future story arrived. `SyncPlay.track_id` (optional, additive per AD-15) carries `fnv1a_hex` of the volume-root-relative path — the "purpose-built (possibly hashed/opaque) per-track identity field" this entry predicted, added without reopening the freeze. The raw path is still excluded, so the privacy decision recorded here stands unchanged. Story 4.2 also added the missing half FR-10 needed but this entry did not anticipate: a *denominator*. `SyncPlay` only ever describes tracks that were PLAYED, so a track added and never played was invisible; the new `SyncLibraryAddEvent` batch + `library_track_events` table (proposed AD-21) close that. Also unblocks Story 4.3's FR-11, which has the identical denominator problem.]

## Deferred from: code review of 1-10-freeze-the-shared-sync-contract (2026-07-25)

- **No test round-trips a realistic `SyncPayload` through real JSON-schema validation (e.g. `ajv`).** AC-2 says a payload "validate[s] against the same schema via contract tests," but every new/widened test in this story (`shared/src/index.test.ts`, `shared/src/additive-only.test.ts`) checks property-name/`required`-array membership only — none actually invokes a JSON-schema validator against an example instance. `shared/` currently has zero schema-validator dependency. Deferred because adding one is a bigger decision than a review-time patch, not because the gap isn't real. [shared/src/index.test.ts; shared/src/additive-only.test.ts]

## Deferred from: implementation of 2-1-supabase-cloud-foundation-isolation-baseline (2026-07-25)

- **[CONFIRMED 2026-07-25, real local Postgres] RLS alone is not enough — `authenticated`/`anon` need an explicit base `GRANT SELECT` or every query on an RLS-enabled table fails closed with "permission denied," not an RLS-filtered result.** Discovered by manually exercising the `djs` table against local Postgres before writing the pgTAP suite: with `alter table djs enable row level security` + a `for select` policy but no `grant`, both `set role authenticated` and `set role anon` errored with `permission denied for table djs` instead of returning the row-filtered/empty result AC-3 and Task 4's test matrix require. Fixed in this story's migration with `grant select on public.djs to authenticated, anon;` (no insert/update/delete grant — matches the read-only-via-RLS, write-only-via-trigger design). Confirmed the fix is load-bearing by removing the grant and re-running the pgTAP suite: 3 of 6 tests failed with the same permission error, restoring it brought all 6 back to green. **Not a Supabase-CLI-generated default** — a table created by a migration (running as the `postgres` role) gets no default ACL for `anon`/`authenticated` on that table; only `supabase_admin`-owned relations do. **Whoever builds the next DJ-owned table (Story 3.1's `sessions`/`sets`/`plays`) needs the same explicit `grant select` line** — RLS policies alone will silently fail closed otherwise, and depending on test order/coverage that could read as "isolation works" when it's actually "nobody except postgres/service_role can read anything." [supabase/migrations/20260726012050_create_djs_table.sql]
- **[CONFIRMED 2026-07-25, installed CLI 2.109.1] pgTAP convention resolved per the story's Open Question #3.** `supabase test new --template pgtap <name>` scaffolds `supabase/tests/<name>_test.sql` (a `BEGIN; SELECT plan(N); ... SELECT * FROM finish(); ROLLBACK;` skeleton); `supabase test db <path>` (file or directory) runs it via a `pg_prove`-backed container against the local database. `pgtap` (extension, default version 1.3.3) is not pre-installed — the test file must `create extension if not exists pgtap with schema extensions;` itself before `plan()`. No CI wiring existed yet; this story adds `supabase test db supabase/tests` as a new step in `.github/workflows/ci.yml`'s `supabase` job. [supabase/tests/djs_isolation_test.sql; .github/workflows/ci.yml]

## Deferred from: code review of 2-1-supabase-cloud-foundation-isolation-baseline (2026-07-25)

- **The `handle_new_dj` trigger only fires on new `auth.users` inserts (`AFTER INSERT`); it never retroactively backfills a `djs` row for a pre-existing `auth.users` row.** Not an issue for this story's actual deployment path — `PROVISIONING.md`'s runbook pushes this migration to a brand-new prod project with no pre-existing users — but the trigger pattern itself has no backfill story if reused against a table that already has data (e.g. a future bulk/admin-API user import). Whoever reuses this idempotent-creation-trigger pattern on a table with pre-existing rows needs a one-time backfill `insert ... select ... on conflict do nothing` alongside it. [supabase/migrations/20260726012050_create_djs_table.sql]
- **The additive-only guard (`check-additive-only-migrations.sh`) only recognizes column/table DROP, RENAME, and TYPE-change DDL — it has no opinion on function or trigger redefinition.** A future migration that does `create or replace function public.handle_new_dj() ...` with materially different (or broken) behavior would sail through the guard untouched, since the guard's scope was Task 5's five named DDL patterns, not a general schema-safety net. Real gap, but matches the story's own explicit scoping — not a bug in what was built, a candidate for a future guard-coverage decision if trigger/function changes to DJ-isolation-critical objects become a real risk. [supabase/scripts/check-additive-only-migrations.sh]

## Deferred from: code review of 2-1-supabase-cloud-foundation-isolation-baseline, round 2 (2026-07-26)

- **The additive-only guard has no opinion on custom `ALTER TYPE ... RENAME/DROP` (e.g. a future `subscription_status` enum) — verified live to bypass entirely.** `alter type public.subscription_status rename to subscription_status_old;` exits 0 clean, since every RENAME/DROP pattern in the guard requires the literal word `table`. Out of Task 5's five named DDL forms, same class as the existing function/trigger-redefinition deferral above — fold in if/when Epic 7 introduces an enum-typed billing column. [supabase/scripts/check-additive-only-migrations.sh:33-39]
- **The guard's docstring claims scanning is fully "statement-aware," but the statement splitter has no awareness of `$$...$$` dollar-quoted function bodies** — it splits purely on counting literal `;` characters. The one function body in this diff (`handle_new_dj`) still scans correctly since it has no internal semicolons before its own body ends, so this isn't a proven bypass today, but it's unproven for a more complex future function/trigger body with multiple internal statements. Soften the docstring's claim or add explicit test coverage the next time a migration's function/trigger body grows more complex. [supabase/scripts/check-additive-only-migrations.sh:9-20]
- **No rule protects the load-bearing `grant select on public.djs to authenticated, anon;` from a future silent `revoke`** — REVOKE isn't one of the five forbidden patterns at all. Given how much narrative weight the Debug Log and this file's own 2026-07-25 entry place on this exact grant being easy to omit or remove without noticing, it's a real blind spot — but GRANT/REVOKE was never in Task 5's scope, so this is a scope-expansion candidate, not a bug in what was built. [supabase/scripts/check-additive-only-migrations.sh:33-39]

## Deferred from: code review of 2-2-obsidian-design-token-system-web-shell (2026-07-26)

- **`no-hardcoded-colors.test.ts`'s guard can't catch dynamically-constructed color strings (template-literal interpolation or string concatenation, e.g. `` `#${x}` `` or `"#" + "fff"`).** The guard is a per-line regex scan (`COLOR_LITERAL_PATTERN`), which only matches literal hex/`rgb()`/`hsl()` tokens already present in source text — a computed color string assembled at runtime would need AST-based detection to catch, a materially different (and heavier) implementation than the current static-analysis approach. No current instance in `web/app` triggers this; revisit only if a real dynamically-built color string shows up in a future story. [web/app/no-hardcoded-colors.test.ts]

## Deferred from: code review of 2-9b-windows-signed-build (2026-07-30)

- **`signCommand`'s `%1` substitution is unquoted while `productName` ("Curfew Agent") contains a space.** A space in the bundled installer's path could, in principle, cause the substituted argument to split — but this matches Tauri's own documented example verbatim (confirmed via a live fetch of the v2 docs during review), so it's likely a non-issue at the vendor-tooling level, not something this diff introduced uniquely. Unverified in practice — no real signed Windows build has run yet (no Azure secrets exist). Whoever runs the first real `release-windows.yml` execution once Azure Artifact Signing is provisioned should confirm the signing step actually receives the correct single-file path. [agent/src-tauri/tauri.windows-release.conf.json:4]
- **`release-windows.yml` and `release-macos.yml` are two fully independent workflows, both triggered by the same `agent-v*.*.*` tag, both targeting the same GitHub Release, with no job ordering between them.** This relies entirely on `tauri-action`'s documented behavior of appending to an existing release by tag rather than erroring — an assumption this story's own Task 1 states explicitly, but one that had never been exercised with two sibling release workflows before 2.9b (2.9a shipped alone). No step reconciles that both platform assets actually landed before the release is considered complete — if one platform's signing step fails after the other already published, the release goes live silently incomplete. Verify both risks on the first real dual-tag release; consider a shared "ensure release exists" job or a post-publish asset-reconciliation check if either surfaces as a real problem. **[ESCALATED 2026-07-30, Story 2.9c review]** Both workflows now also upload updater artifacts (`latest.json`/`.sig`) to the same shared Release via the same race-prone path — the risk now covers the update manifest in addition to the platform installers. Same verdict: verify on the first real dual-tag release. [.github/workflows/release-windows.yml, .github/workflows/release-macos.yml]
- **The signing endpoint ships as a literal `<AZURE_ARTIFACT_SIGNING_ENDPOINT>` placeholder**, which technically contradicts this story's own Dev Notes text ("the endpoint must be a literal ... value ... not left as a placeholder") — but the root cause is that the Azure Artifact Signing account doesn't exist yet, so there is no real regional endpoint to bake in. Already tracked as this story's own Open Question #2; blocked entirely on Arjun provisioning the Azure account. [agent/src-tauri/tauri.windows-release.conf.json:4]

## Deferred from: code review of 2-9c-signed-auto-updater-pipeline (2026-07-30)

- **No preflight check that `TAURI_SIGNING_PRIVATE_KEY` is actually set before the expensive build starts.** Consistent with the same already-accepted gap for `APPLE_*`/`AZURE_*` secrets in the same two workflows (2.9a/2.9b never added preflight checks either); the workflow fails deep in the signing step instead, which is the documented, expected state until Arjun provisions the secret. [.github/workflows/release-macos.yml, .github/workflows/release-windows.yml]
- **The update endpoint hardcodes the `5arjun/curfew.vip` repo slug with no fallback/mirror endpoint.** An architecture-level tradeoff of the static-JSON-on-GitHub-Releases pattern chosen at the architecture-spine level, not a regression introduced by this diff. [agent/src-tauri/tauri.conf.json:50]
- **No key-rotation plan exists for the updater signing keypair** (no dual-key transition period, no versioned pubkey list) if `TAURI_SIGNING_PRIVATE_KEY` is ever rotated or compromised. An operational/process gap, candidate for `pre-launch-services-checklist.md`, not a code-patch item. [agent/src-tauri/tauri.conf.json]
- **`$VERSION` is interpolated unescaped into the `sed` replacement string and the `node -e` argv** in the tag-to-version-sync step (both workflows) — only reachable via a maintainer-crafted malformed tag (e.g. containing `&`, `/`, or `"`), not attacker-controlled since only the repo owner pushes release tags. [.github/workflows/release-macos.yml:74-90, .github/workflows/release-windows.yml (mirrored)]

## Deferred from: code review of 3-1-sessions-sets-plays-schema-visibility-content-overlay-split (2026-07-30)

- **[RESOLVED 2026-08-06]** No index on `sets.dj_id` / `plays.dj_id` despite every RLS policy filtering directly on it. Added via a new additive migration (`20260806100000_add_sets_plays_dj_id_index.sql`), passes the additive-only migration guard. [supabase/migrations/20260730204057_create_sessions_sets_plays.sql:38-39,57-58]
- **`plays.genre_normalized`/`taxonomy_version` independence isn't enforced by a CHECK.** AD-12 frames `taxonomy_version` as existing so a normalized genre can be recomputed consistently, but the schema allows one to be set while the other is null with zero enforcement. Not spec-required for this story; a CHECK is additive-safe to add later. [supabase/migrations/20260730204057_create_sessions_sets_plays.sql:64-66]
- **`plays.position`/`camelot_key` have no format/range validation.** `position` can be inserted as `0` or negative despite implied 1-based ordering (only `unique(set_id, position)` is enforced), and `camelot_key` accepts arbitrary text instead of a constrained notation. Not spec-required; additive to add later. [supabase/migrations/20260730204057_create_sessions_sets_plays.sql:59,67]
- **`sessions.session_identity` has no non-empty or normalization guard.** Described as the intrinsic pre-hash string `sessions.id` is derived from; whitespace or case variance could silently produce two different `session_identity` values for what an upstream hash function intends as the same session, with zero DB-level guard. Not spec-required; additive to add later. [supabase/migrations/20260730204057_create_sessions_sets_plays.sql:22]

## Deferred from: code review of 3-2-idempotent-set-sync (2026-07-31)

- **No pagination/batching/cap on a sync pass; serial blocking HTTP calls per row.** `rows_pending_sync` has no `LIMIT` and `sync_pending_sessions` processes every eligible row serially, one blocking HTTP call (up to the 15s timeout) at a time, with no cancellation. Not reachable today — nothing calls `sync_pending_sessions` yet (deliberately, per this story's own scope). Tied to Story 3.3's ownership of the retry/backoff/queue wiring design; batch size and cadence should be decided together with that wiring. [agent/src-tauri/src/sync.rs:231]
- **[RESOLVED 2026-08-01, Story 3.3]** **`SetIdMismatch` has no circuit breaker.** Framed in the error variant's own doc comment as "should never happen," but nothing stops a deterministic mismatch (e.g. a byte-encoding drift between Rust's `Uuid::new_v5` and Postgres's `uuid_generate_v5`) from retrying forever once wired into a retry loop, silently re-attempting the same doomed call on every pass. Story 3.3 explicitly owns retry/backoff design per this story's Dev Notes. [agent/src-tauri/src/sync.rs] resolution: Story 3.3's circuit breaker is built (`sync_queue.rs`, explicitly commented as "Story 3.3's circuit breaker (Task 1)") and covers this exact case.
- **No guard against concurrent/overlapping `sync_pending_sessions` calls** (e.g. a multi-machine DJ syncing from two agents at once). Idempotent upserts mean this is a redundancy/efficiency concern (duplicate/racing network requests) rather than data corruption — Postgres's `on conflict` handling keeps the end state correct either way. Only matters once this is wired into a live/periodic trigger, which is Story 3.3's territory. [agent/src-tauri/src/sync.rs]

## Deferred from: code review of story-3.7 (2026-08-04)

- **No authorization/ownership check in `deleteSetAction`.** Server Actions are reachable independent of route-group layout gating, so anyone who knows/guesses an `external_id` could delete it. Same pre-existing gap already flagged in this story's own Dev Notes ("No auth-gating redirect exists yet on the `(authenticated)` group") and in Story 3.5's Dev Agent Record — not introduced by 3.7, not actionable without the auth-gating work landing first. [web/app/(authenticated)/set/[id]/actions.ts], [web/app/(authenticated)/layout.tsx]
- **`bpmHistogram` doesn't clamp or bucket single-outlier BPM values**, which can produce a long, mostly-empty band range in the BPM overlay. Polish/enhancement, not a spec violation. [web/lib/sets/setDetail.ts:168-183]
- ~~**Arc silently falls back to the whole-night domain when the detected dancefloor segment doesn't overlap any BPM-carrying play**~~ **CLOSED — fixed in Story 3.8 (2026-08-04, ruling D-4):** the band now projects from the segment's *time bounds* independent of play overlap, and a dancefloor window with <2 plottable points renders the in-scope chart-summary fallback ("No tempo data in the dancefloor window.") instead of silently drawing the whole night under a "Dancefloor" scope line. Covered by `heroArc.test.ts` + verified in-browser on a synthetic no-BPM-window set. [web/lib/sets/heroArc.ts, web/app/components/set-detail/DetailArc.tsx]
- **A single Escape keypress can close the delete-confirmation modal AND a stats drill-in veil simultaneously** if both happen to be open (independent `document`-level keydown listeners, no shared modal stack/priority). Low stakes, rare compound state — nothing destructive happens and few users will hit both open at once. [web/app/components/set-detail/DeleteModal.tsx:25-27], [web/app/components/set-detail/Overlays.tsx:60-66]

## Deferred from: code review of 3-4-format-drift-resilience-backfill (2026-08-05)

- **Sentry "panic" feature enabled + no event fingerprinting, both dormant until a real DSN is provisioned.** `capture_message` sends free-text `e.to_string()` with no `fingerprint`, which will likely fragment near-identical parse errors into many distinct Sentry issues instead of clustering under one alert; those same messages may embed local filesystem paths (potential OS-username PII) that `send_default_pii = false` does not redact; and the "panic" feature installs a global panic hook with no discussion of whether it changes this tray app's existing crash behavior. Not reachable today — `SentryReporter`/`init()` both no-op while `SENTRY_DSN` is empty — worth a fresh look once Arjun actually provisions a DSN. [agent/src-tauri/Cargo.toml (sentry feature list), agent/src-tauri/src/error_reporting.rs]

## Deferred from: code review of 3-8-energy-arc-chart-summary (2026-08-05)

- **No automated component/DOM tests for the new interactive surfaces.** `DetailArc.tsx` (hover math, click mapping, viewBox morph, error boundary), `TempoSpark.tsx`, and `AnimateNumber.tsx`'s digit-roll state machine ship with zero React/DOM tests — only pure-function tests exist in `energyArc.test.ts`/`heroArc.test.ts`/`setDetail.test.ts`. Verification currently rests solely on the manual real-browser walkthrough narrated in the story's Debug Log, which already missed one bug (the mobile stranded-chip issue) before being caught live. Test-coverage scope decision, not blocking. [web/app/components/set-detail/DetailArc.tsx, web/app/components/set-detail/TempoSpark.tsx, web/app/components/ui/AnimateNumber.tsx]
- **`AnimateNumber`'s digit-roll is asymmetric across a magnitude boundary.** Slots are keyed by distance-from-right, so a newly-appearing leading digit (e.g., a percentage crossing 9%→10%) mounts/unmounts with no entrance/exit animation — only the shared low-order digits get the intended slide/blur treatment. Low-severity animation glitch, deferred to the motion polish pass per this project's build-first/polish-at-end convention. [web/app/components/ui/AnimateNumber.tsx]
- **Backfill has no recovery path if Serato itself prunes/vacuums its own `master.sqlite` history, or a Legacy `.session` file is deleted.** `parse_failures.raw_ref` is a pointer into external, agent-unowned state, not retained raw bytes — Arjun's ruling on this story's review was to fix the Legacy-path staleness inconsistency (re-resolve against the current `WatchPlan` on reprocess) but explicitly not to build full raw-byte retention. If Serato ever prunes history out from under a pending `parse_failures` row, that session becomes permanently unrecoverable with no path to resolution. Revisit if this is ever observed in practice. [agent/src-tauri/src/backfill.rs, agent/src-tauri/src/store.rs (parse_failures schema)]

## Deferred from: code review of 4-1-style-evolution-trend-view-excludes-low-confidence (2026-08-06)

- **Locale-dependent date labels are generated inside an SSR'd client component, producing a hydration text mismatch for every non-`en` browser.** `toLocaleDateString([])` means "runtime default locale", so the server renders "March" with Node's locale and the browser re-renders "März"/"mars"/etc. — a React hydration mismatch on every axis tick, caption, and hover chip. Verified: `LC_ALL=de-DE npx vitest run lib/sets/styleEvolution.test.ts` → 20 failures. **Pre-existing app-wide**, not introduced by 4.1: `format.ts:11,13,51,59` and `listModel.ts:91` use the identical `[]`-locale idiom and pre-date this story. Fixing only Style Evolution would make the app internally inconsistent — this needs one deliberate i18n decision (pin `en-US` everywhere, or thread a real locale through) applied across all six sites at once. [web/lib/sets/styleEvolution.ts:295-307, web/lib/sets/format.ts, web/lib/sets/listModel.ts:91, web/app/components/style-evolution/TrendChart.tsx:89-116]
- **Style Evolution has no rolling window, so one outlier `started_at` blows up the axis and the server render.** `fillMonthRange`/`fillWeekRange` emit every bucket from first to last with no cap, and `buildSeries` then runs `buckets.map(b => sets.filter(...))` twice, constructing a fresh `Date` for every (bucket × set) pair. A single epoch-defaulted (`1970-01-01`) or far-future set alongside current data yields ~670 month / ~2900 week buckets and millions of `Date` constructions inside a blocking server component, with bar groups collapsed to sub-pixel width. The same unbounded growth applies benignly to a genuinely long library. Needs either a rolling window or an outlier guard — a product decision about how much history the trend should show, so not a mechanical fix. [web/lib/sets/styleEvolution.ts:216-244,257-262]
- **The dashed gap-bridge affordance is explained nowhere the user can see it.** The model correctly renders a bucket with zero surviving sets as a genuine gap (D-8 compliant), and the bridge across it is deliberately straight + dashed rather than curved so it never reads as an interpolated trend — a good decision. But the SVG is `aria-hidden`, the legend has no dashed-line entry, and the Chart Summary caption never mentions that buckets were skipped, so the distinction between "a month with no gigs" and "a month whose sets were all hidden" is conveyed only in a code comment. Polish item for the motion/design pass, per this project's build-first/polish-at-end convention. [web/app/components/style-evolution/TrendChart.tsx]

## Deferred from: code review of 4-1 — Arjun's timezone ruling (2026-08-06)

- **[NEEDS A STORY] Agent must capture the DJ's local time; web must bucket against it.** Ruling (Arjun, 2026-08-06 code review): *"it should be the time that the DJ is in / experienced. The day should be the day the set started. Yes — have the agent capture local time."* Half of this already holds: every bucket key derives from `started_at`, never `ended_at`, so a set running past midnight correctly stays on the night it began (`styleEvolution.ts` `localMonthKey`/`localWeekKey`, `listModel.ts` `localDayKey`). The other half is **not built**: those helpers call `new Date(iso).getMonth()`, which resolves to the *process* timezone, and `buildStyleEvolution`/`buildRightColumn` both run in server components — UTC on Vercel. A gig at 23:00 local on Jan 31 in UTC-5 is filed under February. Verified: `TZ=Pacific/Kiritimati npx vitest run lib/sets/styleEvolution.test.ts` → 4 failures (the suite is now TZ-pinned, so this no longer hides, but pinning the tests did not fix the app). Work owed, in order: (1) agent captures the local date — or a UTC offset — at set-capture time; (2) additive-only `shared/` contract field (AD-3/AD-15 — the contract is frozen, so additive is the only legal shape, and AD-20/the 4-2 `library_track_events` work are the precedents for a named, scoped amendment); (3) sync + cloud carry it; (4) web bucketing reads the captured local date instead of re-deriving it. This spans agent + shared + cloud + web, so it is a story, not a patch — Story 4.1 is a pure `web/` story that touches zero `shared/` files by design. Affects the dashboard calendar (`localDayKey`, shipped since 3.6) as much as Style Evolution. [web/lib/sets/styleEvolution.ts:25-48, web/lib/sets/listModel.ts:58, web/lib/sets/rightColumn.ts, agent capture path, shared/src/index.ts]

## Deferred from: code review of 4-11-library-roster-sync (2026-08-07)

- **One poison batch head-of-line-blocks the entire roster queue indefinitely.** `sync_pending_library_roster` returns `Err` on the first failing batch, and `RetryClass` classifies a 4xx `Rejected` as Permanent. Any batch the RPC rejects outright — e.g. one entry whose `added_at` fails the `::bigint` cast, which raises `22P02` and aborts the whole call — leaves every row with `roster_synced_at NULL`, so the identical 200-entry batch is re-POSTed on every drain pass forever and every subsequent batch is starved. Unlike the set-sync path there is no `permanently_skipped` quarantine set; the drain only logs. Inherited from AD-21's add-event drain (same shape), but materially worse here because the roster's first drain is the entire library rather than a handful of go-forward rows. [agent/src-tauri/src/sync.rs:668-671, agent/src-tauri/src/sync_queue.rs drain_library_roster]
- **Nondeterministic dedupe tie-break can cause perpetual re-sync churn.** `DateAddedIndex::all_tracks()` returns `seen.into_iter()` over a `std::collections::HashMap`, whose iteration order is randomly seeded per process. When a cross-drive duplicate has equal `added_at` (or both `None`), `dedupe_by_identity`'s `should_replace` is false either way, so the winner is whichever entry the map yielded first. If the two entries differ only in raw casing/whitespace (identical normalized form, so same `track_id`), the retained title/artist flips between runs, `refresh_library_track_tags` sees a real difference, resets `roster_synced_at`, and the row re-uploads forever with a bumped `updated_at`. `dedupe_by_identity`'s doc comment dismisses cosmetic differences as harmless — true for the old insert-only path, no longer true for a current-state roster. Narrow trigger conjunction, hence deferred rather than patched. [agent/src-tauri/src/capture.rs:410-418, agent/src-tauri/src/joiner/date_added.rs:217]
- **[PARTLY RESOLVED 2026-08-08 — Story 4.4] AC-5's soft-delete had no web-side or end-to-end coverage.** `build-library-roster-fixture.mjs:52` sets `absent_at: null` for all 653 entries by design (a single catalogue snapshot cannot honestly simulate a removal), and `libraryRoster.test.ts` type-checked an absent entry literal without exercising any behaviour over it. Story 4.4 closed the **web** half at both layers it now has: `getLibraryRoster` filters `.is("absent_at", null)` server-side with a test asserting `is` and not `eq` (`eq` renders `absent_at=eq.null`, a literal string comparison matching nothing), and `agingShelf.test.ts` exercises the pure model's own exclusion — an absent track is dropped from the rows, from `qualifyingCount`, from `recentlyDownloadedCount`, from `unknownAddDateCount` and from `presentTrackCount`, and stays excluded even when it has observed plays. A roster whose entries are ALL absent now reports the day-one shape rather than an all-clear about tracks the DJ no longer owns. **Still open:** end-to-end coverage of a real removal (agent marks absent → RPC → shelf drops the row) exists nowhere, and the fixture still cannot simulate one. [web/lib/sets/index.test.ts, web/lib/sets/agingShelf.test.ts, web/lib/sets/build-library-roster-fixture.mjs:52]

## Deferred from: code review of 4-11-library-roster-sync (2026-08-07) — Arjun's library-scope ruling

- **[NEEDS A STORY] Library scope: separate the DJ's real library from everything Serato indexed.** Ruling (Arjun, 2026-08-07 code review): *"make sure we fix this in the followup story."* Measured directly from Arjun's real `~/Music/_Serato_/database V2` during this review (930 `otrk` records): **271 of 930 rows carry no `tart` (artist) field at all** — the parser is correct, the field is genuinely absent from the file. Composition, by extension: `.mp3` 874 total / 216 no-artist; `.m4a` 24/24 (100%); `.mp4` 14/14 (100%); `.wav` 12/11; `.mov` 6/6 (100%). Sampled titles show what they are — `yt5s.com - RANGISARI (Lyrical) _ JugJugg Jeeyo…`, `LOOP LAD (1)` (a `.mov`), `Mujhe Shaadi Karogi` — i.e. untagged YouTube rips, video loops, and untagged desi tracks, where Serato falls back to the filename for `tsng` and writes no artist. 112 of the 271 have `" - "` in the title, suggesting the artist is embedded in the filename.

  **Why this is the real fix rather than the disclosure Story 4.11 shipped.** These files are not record-pool material and are not tracks Arjun would ever play to a dancefloor — they should not be in the library-utilization denominator at all. The conversion rate is currently measured against 930 files that include 20 video loops. Fixing the denominator is strictly better than caveating it: the disclosure apologises for a number that is itself wrong.

  **Why the dancefloor toggle does NOT solve this** (checked during review): `detectDancefloor(plays: SyncPlay[])` (`web/lib/sets/dancefloor.ts:70`) segments a session from **plays**. Library-utilization's denominator is the *library*, including never-played tracks — a track with no play can never be dancefloor-classified. The toggle can filter the numerator; the denominator stays whole-library, and all 272 exclusions live on the denominator side. The toggle DOES retire this disclosure on play-derived surfaces (style evolution, set detail), where exclusion is ~0 and `unidentifiableTracksDisclosure` already auto-suppresses below its 5% bar — no work needed there.

  **Work owed:** (1) decide the scope rule — exclude non-audio extensions (`.mp4`/`.mov`, 20 rows) at minimum; consider a broader "is this a DJ-library track" signal; (2) apply it to the roster scan, the conversion denominator, and the disclosure count consistently; (3) revisit whether the disclosure line is still needed once the denominator is honest. Also worth a separate look: **every `.m4a` (24/24) lacks an artist** — a 100% rate is a tagging/import quirk, not natural variation. [agent/src-tauri/src/capture.rs scan_library_adds, web/lib/sets/libraryConversion.ts, web/lib/sets/libraryRoster.ts]

## Deferred from: code review of 4-11-library-roster-sync (2026-08-07) — Arjun's single-machine ruling

- **[NEEDS A STORY, POST-MVP] Enforce one linked machine per DJ account.** Ruling (Arjun, 2026-08-07 code review): *"let's make sure we note to make it so only 1 machine is allowed. doesn't have to be part of the mvp."* Today the constraint runs the wrong way: `agent/src-tauri/src/auth/store.rs:17` guarantees "there is only one DJ per agent" (one account per install), but **nothing prevents one DJ from linking several installs**. Each machine keeps its own refresh token in its own keyring, Supabase issues independent per-device sessions, and no code revokes a prior device. There is also no unlink/sign-out path at all (`auth/store.rs:43` — no AC ever asked for one).

  **Why it matters more after Story 4.11.** Two concurrent installs already shared `agent_status` (`dj_id uuid primary key`, AD-20), but that is benign — a status row, last writer wins, nothing lost. `library_roster` (AD-22) is destructive under the same conditions: each install marks the other's tracks `absent_at` and clears its own, so the roster oscillates and never converges, and Story 4.4's aging shelf reads the result. Same single-install assumption the architecture always had; materially sharper consequence.

  **Work owed:** (1) product decision on the enforcement shape — refuse the link, or revoke-and-replace the previous device on a new link (revoke-and-replace is the friendlier default and also supplies the missing unlink path); (2) surface it in the link-agent flow (`web/app/link-agent/`) with calm copy per the Failure Register; (3) an unlink/sign-out path, which is owed independently of this; (4) until it lands, AD-22 carries an explicit single-install assumption (added in this review). Spans AD-20 and AD-22 together — installation identity is the alternative design if multi-machine is ever wanted instead. [agent/src-tauri/src/auth/store.rs, web/app/link-agent/, supabase/migrations AD-20 + AD-22 tables]

## Deferred from: code review of 4-8-genre-share-stream-camelot-wheel (2026-08-08)

- **`TrendChart`'s own error-boundary `resetKey` is reveal-blind.** `resetKey={`${metric}:${granularity}:${conversionWindow}:${buckets.length}`}` (`TrendChart.tsx:393`) omits `revealed`, and `buckets` is `BucketSeries.buckets` — documented at `styleEvolution.ts:161` as "the union across ALL sets (pre-exclusion)", so its length is byte-identical for the `excluding` and `including` partitions. A chart that throws while rendering one partition therefore keeps its caption-only fallback when the DJ toggles the low-confidence reveal, even though the new props draw fine. This is exactly the failure the boundary's own comment (`TrendChart.tsx:247-250`) says the key exists to prevent: "every later metric/granularity/reveal switch re-rendered the same already-failed instance." Pre-existing since Story 4.1; Story 4.8 reproduced the identical hole in `GenreShareStream.tsx:122` (patched under that story's Review Findings) and a narrower variant in `CamelotWheel.tsx:99`. Worth fixing all three at the same seam rather than one at a time. [web/app/components/style-evolution/TrendChart.tsx:393]

- **Locale-dependent, server-rendered axis tick labels are a latent hydration mismatch on any non-`en` browser.** `bucketTick` calls `new Date(y, m - 1, 1).toLocaleDateString([], { month: "long" })` (`GenreShareStream.tsx:88-93`, and the same idiom pre-exists in `TrendChart`). The empty locale array resolves to the *runtime's* default: Node's ICU default on the server, the user's browser locale on the client. These components have no client-only guard, so a DJ with a `fr-FR` browser gets `June` from the server and `juin` from the client — a React hydration mismatch on every tick, in both the stream and the trend chart. Story 4.8's G-8 discipline covers float/trig divergence and was applied correctly; it does not cover locale divergence, and the walkthrough ran on an `en-US` browser where the mismatch cannot appear. Pre-existing house pattern, faithfully duplicated into the new component rather than introduced by it. Fix shape: format month names from an explicit in-repo table (or pin the locale), which also removes the timezone-adjacent `new Date(y, m-1, 1)` local-midnight construction. [web/app/components/style-evolution/GenreShareStream.tsx:88-93, web/app/components/style-evolution/TrendChart.tsx]

## Deferred from: code review of 4-9-library-utilization-composition-play-side-stats (2026-08-08)

- **Two of three pgTAP global-count assertions remain unscoped.** Story 4.9 scoped `sets_delete_policy_isolation_test.sql:80-91` to the test's own DJs after the D-23 seed broke it, but `agent_status_isolation_test.sql:34` and `deleted_sets_tombstone_test.sql:54` still assert superuser `count(*)` globally. They pass today only because the seed deliberately touches neither table (`generate-seed.mjs:69-76`). CI runs `supabase start` (which seeds a fresh volume) before `supabase test db` (`.github/workflows/ci.yml:114,120`), so the next story that seeds a heartbeat row or a tombstone fails an RLS isolation test that has nothing to do with its change. The class of fragility 4.9 identified was only partly retired. [supabase/tests/agent_status_isolation_test.sql:34, supabase/tests/deleted_sets_tombstone_test.sql:54]

- **`SIMILARITY_MATRIX_SETS` is an alias of the dashboard's `MOST_PLAYED_RECENT_SETS`.** This is as D-19 ruled (reuse the existing "recent" scale rather than invent a second one on the same product), so it is deferred rather than patched — but `rightColumn.ts:78-83` records that this constant has already been retuned once (5→10, 2026-08-06) for reasons entirely about the most-played card's pool size. D-19, `epics.md` AC-4 and FR-30 all pin "the 10 most recent". A future dashboard-motivated retune to 15 silently widens the similarity matrix and falsifies the PRD; it surfaces as a failure in `libraryUtilization.test.ts`'s truncation test, whose name and location give no hint the cause is a dashboard constant. Worth a comment at the `rightColumn.ts` definition site naming the second consumer, if not a decoupling. [web/lib/sets/libraryUtilization.ts:433, web/lib/sets/rightColumn.ts:78-83]

- **Set similarity's on-screen cap statement counts only dated survivors.** `buildSetSimilarity` sets `survivingSetCount: index.dated.length` and `truncated: index.dated.length > recent.length`, so undated survivors are absent from "your 10 most recent sets, of N" with no disclosure — inconsistent with the undated-disclosure discipline the same file applies everywhere else. Unreachable today because `sets.started_at` is `NOT NULL` (the seed header documents this, and 4.9's undated handling is explicitly defensive against a future nullable column). Becomes live the moment that column is made nullable. [web/lib/sets/libraryUtilization.ts:514-515]

## Deferred from: code review of 5-1-segments-overlay-schema (2026-08-10)

- **`first_play_id`/`last_play_id` aren't constrained to belong to the same `set_id` as the `segments` row.** A segment could reference `plays` rows from an entirely different set — nothing at the DB layer ties the two FKs together. Pre-existing pattern, not a novel gap: Story 3.1's own migration explicitly documents the identical "known gap, not this story's to close" for `sets.dj_id`/`plays.dj_id` never being checked against their parent row's `dj_id` (`20260730204057_create_sessions_sets_plays.sql`'s trailing comment), and rules that the write-path story must derive, never trust, these values (AD-19's pattern). Story 5.2 (algorithm-written suggestions) and Story 5.3 (DJ drag/keyboard editing) own closing this when they design the actual write path. [supabase/migrations/20260810153813_create_segments.sql:19-29]

- **`dj_id` isn't validated against the `dj_id` implied by `set_id`.** A `segments` row could claim a different owner than its own `set_id` actually belongs to; RLS (`segments_select_own`) trusts the denormalized `dj_id` column directly with no cross-check. Same precedent and same owner as the finding above.

- **No ordering constraint that `first_play_id`'s play precedes `last_play_id`'s play.** A segment's boundaries could be inserted chronologically reversed and nothing rejects it. Not fixable with a plain CHECK (it needs a cross-row join to `plays.position`, which requires a trigger) — belongs with the write-path story (5.2/5.3) that will already be computing positions to write these FKs in the first place. [supabase/migrations/20260810153813_create_segments.sql:25-26]

- **`segments_isolation_test.sql` never independently deletes a `sets` row to prove cascade via `set_id`.** Only the `dj_id`/`auth.users` cascade and the `plays`-FK cascade (via `first_play_id`) are exercised; a typo'd or mis-pointed `set_id` FK would pass every test in the suite. Pre-existing gap, not introduced by this story: `sessions_sets_plays_isolation_test.sql` has the identical hole for `sets`/`sessions`. [supabase/tests/segments_isolation_test.sql]

- **`grant_matrix_test.sql` has no hardcoded `authenticated`-scoped INSERT/DELETE sweep for any table.** `segments` was correctly added to the `anon` INSERT-denied and `anon` DELETE-denied arrays (the two that exist), but there is no equivalent `authenticated` array to add it to — that coverage exists only via `segments_isolation_test.sql`'s own Case 5. Pre-existing structural gap across all 8 tables in the file, not introduced by this story. [supabase/tests/grant_matrix_test.sql]

- **`supabase/README.md`'s migration/test tree map has been known-incomplete since Story 3.2.** It reads as a directory listing but is missing migrations/tests from several intervening stories; this story adds its own two entries correctly without closing the pre-existing gap. Not introduced by this diff. [supabase/README.md]

- **No decision recorded on whether overlapping/duplicate segments within a set are permitted.** Two segments could cover an identical (or overlapping) `plays` range with nothing to prevent it. Explicitly out of this story's scope per its own Scope Boundaries — Story 5.3 owns the segment editor and the write UX that will need to rule on this. [supabase/migrations/20260810153813_create_segments.sql]
