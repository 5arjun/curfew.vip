// Temporary stub (Story 3.5 Task 5.2) — throwaway, exists only so FloatingNav
// can be manually verified end-to-end in a running dev server. Story 3.10
// builds the real Profile/Settings page and supersedes this file entirely.
export default function SettingsPage() {
  return <h1 className="text-headline-md p-8">Profile/Settings — placeholder, Story 3.10 builds the real page</h1>;
}
